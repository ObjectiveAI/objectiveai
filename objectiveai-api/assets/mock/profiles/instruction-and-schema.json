{
  "description": "Two mock agents with different weights.",
  "agents": [
    {
      "upstream": "mock",
      "output_mode": "instruction"
    },
    {
      "upstream": "mock",
      "output_mode": "json_schema"
    }
  ],
  "weights": [
    0.7,
    0.3
  ]
}
