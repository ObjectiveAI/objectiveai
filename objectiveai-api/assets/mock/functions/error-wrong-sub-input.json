{
  "type": "alpha.scalar.branch.function",
  "description": "Branch that passes incorrect field names to its sub-function.",
  "input_schema": {
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      }
    },
    "required": [
      "text"
    ]
  },
  "tasks": [
    {
      "type": "alpha.scalar.function",
      "remote": "mock",
      "name": "binary-classifier",
      "input": {
        "$starlark": "{'wrong_field': input['text']}"
      }
    }
  ]
}
