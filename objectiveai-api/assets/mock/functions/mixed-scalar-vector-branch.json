{
  "type": "alpha.vector.branch.function",
  "description": "Combines text classification scores with item ranking into a single vector output.",
  "input_schema": {
    "context": {
      "type": "object",
      "properties": {
        "query": {
          "type": "string"
        }
      },
      "required": [
        "query"
      ]
    },
    "items": {
      "type": "string"
    }
  },
  "tasks": [
    {
      "type": "alpha.scalar.function",
      "remote": "mock",
      "name": "binary-classifier",
      "input": {
        "$starlark": "{'text': input['context']['query']}"
      }
    },
    {
      "type": "alpha.scalar.function",
      "remote": "mock",
      "name": "email-importance",
      "input": {
        "$starlark": "{'subject': input['context']['query'], 'body': input['context']['query']}"
      }
    },
    {
      "type": "alpha.vector.function",
      "remote": "mock",
      "name": "item-ranker",
      "input": {
        "items": {
          "$starlark": "input['items']"
        }
      }
    }
  ]
}
