{
  "type": "alpha.vector.leaf.function",
  "description": "Single-task vector function that pipes `input['messages']` and `input['responses']` straight into one `vector.completion` task. Used by the CLI vector-completion snapshot test to exercise swarm count expansion + content-hash inclusion of `client_objectiveai_mcp.tools` at scale with single-pass JsonSchema vote extraction. Paired with the `twenty-agents-json-schema-10x-tools-profile` Auto profile.",
  "input_schema": {
    "messages": {
      "type": "array",
      "items": {
        "type": "object"
      }
    },
    "responses": {
      "type": "string"
    }
  },
  "tasks": [
    {
      "type": "vector.completion",
      "messages": {
        "$starlark": "input['messages']"
      },
      "responses": {
        "$starlark": "input['responses']"
      }
    }
  ]
}
