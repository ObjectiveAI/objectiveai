{
  "type": "scalar.function",
  "description": "Two placeholder tasks that always output fixed 0.5 scores, useful as a baseline.",
  "input_schema": {
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      }
    },
    "required": [
      "text"
    ]
  },
  "tasks": [
    {
      "type": "placeholder.scalar.function",
      "input_schema": {
        "type": "object",
        "properties": {
          "text": {
            "type": "string"
          }
        },
        "required": [
          "text"
        ]
      },
      "input": {
        "$starlark": "{'text': input['text']}"
      },
      "output": {
        "$special": "output"
      }
    },
    {
      "type": "placeholder.scalar.function",
      "input_schema": {
        "type": "object",
        "properties": {
          "text": {
            "type": "string"
          }
        },
        "required": [
          "text"
        ]
      },
      "input": {
        "$starlark": "{'text': input['text']}"
      },
      "output": {
        "$special": "output"
      }
    }
  ]
}
