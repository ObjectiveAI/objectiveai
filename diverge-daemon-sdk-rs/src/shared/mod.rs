//! What more than one endpoint speaks of.
//!
//! [`provider`] is what the daemon knows about a provider an agent
//! runs on, which the log names and which anything else that points
//! at a provider will name the same way.

pub mod provider;
