//! Empty library target for `objectiveai-cli-tests`.
//!
//! The crate exists only to host the integration suite under `tests/`,
//! which drives the installed `objectiveai` cli shim end-to-end against
//! the shared `.objectiveai` test root. No library code ships here.
