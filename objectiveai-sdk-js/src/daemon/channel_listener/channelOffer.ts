// THIS FILE IS AUTO-GENERATED. DO NOT EDIT.
// Generated by scripts/install-zod.cjs from objectiveai-json-schema/.
import { z } from "zod";
import { JsonValueSchema } from "../../jsonValue";

export const DaemonChannelListenerChannelOfferSchema = z.object({
  agent_full_id: z.string().nullable().meta({ omitempty: true }).optional(),
  agent_id: z.string().nullable().meta({ omitempty: true }).optional(),
  agent_instance_hierarchy: z.string().nullable().meta({ omitempty: true }).optional(),
  agent_remote: z.string().nullable().meta({ omitempty: true }).optional(),
  channel_id: z.string().describe("The daemon-minted channel id — the accept + log routing key."),
  details: JsonValueSchema.describe("Arbitrary offer payload, opaque to the daemon."),
  key: z.string().describe("Caller-chosen discriminator (e.g. `\"browser.login\"`) — how a\nuser surface decides whether/how to accept the offer."),
  message: z.string().describe("Human-readable offer message, opaque to the daemon."),
  plugin_name: z.string().nullable().meta({ omitempty: true }).optional(),
  plugin_owner: z.string().nullable().meta({ omitempty: true }).optional(),
  plugin_version: z.string().nullable().meta({ omitempty: true }).optional(),
  response_id: z.string().nullable().meta({ omitempty: true }).optional(),
  response_ids: z.string().nullable().meta({ omitempty: true }).optional(),
  task: z.boolean().default(false).describe("Fired by the task scheduler (daemon-authored; see the struct\ndocs). Always present on the wire; `default` only tolerates\nframes from older producers."),
}).describe("One channel OFFER, as broadcast to every connected channel stream.\nCarries no secret — the publisher's `S_pub` is returned to the\npublisher's command, and the owner's `S_owner` is returned by the\naccept POST.").meta({ title: "daemon.channel_listener.ChannelOffer" });
export type DaemonChannelListenerChannelOffer = z.infer<typeof DaemonChannelListenerChannelOfferSchema>;
