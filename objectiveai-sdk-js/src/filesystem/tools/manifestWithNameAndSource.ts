// THIS FILE IS AUTO-GENERATED. DO NOT EDIT.
// Generated by scripts/install-zod.cjs from objectiveai-json-schema/.
import { z } from "zod";

export const FilesystemToolsManifestWithNameAndSourceSchema = z.object({
  description: z.string().describe("One-line description of what the tool does. Surfaced to\nagents that consume the tool."),
  exec: z.string().describe("Filename of the executable to invoke, resolved relative to\n`<base_dir>/tools/`. The author is responsible for including\nany platform-specific extension (`.exe`, `.sh`, `.bat`, …) —\nthe SDK doesn't synthesise one."),
  name: z.string().describe("The tool's identifier — the filename it lives under in the\ntools directory (e.g. `hello` for `~/.objectiveai/tools/hello.json`)."),
  owner: z.string().describe("GitHub-style owner (user or org) of the tool's source repo.\nFree-form; tools have no installer, so this is purely\nauthor-supplied metadata — nothing overrides it the way the\nplugin installer overrides the plugin manifest's owner."),
  source: z.string().describe("Where this manifest came from — typically an absolute\nfilesystem path. Free-form string; the host just displays it."),
  version: z.string().describe("Version string. Free-form; the host displays whatever's here\n(semver convention recommended but not enforced)."),
}).describe("A [`Manifest`] enriched with the tool's identifying `name` and the\n`source` it was loaded from. Same shape and ordering convention\nas `filesystem::plugins::ManifestWithNameAndSource`.").meta({ title: "filesystem.tools.ManifestWithNameAndSource" });
export type FilesystemToolsManifestWithNameAndSource = z.infer<typeof FilesystemToolsManifestWithNameAndSourceSchema>;
