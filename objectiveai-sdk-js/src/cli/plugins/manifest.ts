// THIS FILE IS AUTO-GENERATED. DO NOT EDIT.
// Generated by scripts/install-zod.cjs from objectiveai-json-schema/.
import { z } from "zod";
import { CliPluginsViewerTabSchema } from "./viewerTab";

export const CliPluginsManifestSchema = z.object({
  containerfile: z.string().describe("Repo-relative path (forward slashes) to the Containerfile /\nDockerfile the plugin's image builds from — the repo checkout\nis the build context."),
  description: z.string().nullable().meta({ omitempty: true }).optional(),
  icon: z.string().nullable().describe("The identity icon, shown beside the identity in the tab strip.").meta({ omitempty: true }).optional(),
  port: z.number().int().min(0).max(65535).describe("The port the plugin's MCP server listens on inside the\ncontainer — published to a random loopback host port at\ncreate. Never 0."),
  tabs: z.array(CliPluginsViewerTabSchema).nullable().describe("The plugin's tabs, in declaration order (strip order). A\n[`ViewerTab::Channel`] entry (`channel_key`) is a CHANNEL\nHANDLER: it never opens at boot — it opens when an offer with\nthat key is accepted, with the full offer as its arguments,\ntitled by the offer key. A [`ViewerTab::Tab`] entry (`title`)\nis a regular tab, opened at viewer boot. Duplicates (same\n`module` among regular tabs, same `channel_key` among\nhandlers) — entries after the first are ignored.").meta({ omitempty: true }).optional(),
  viewer: z.string().nullable().describe("Repo-relative path (forward slashes) to the viewer extension's\nJS ROOT — the folder holding its `package.json`; `icon` and\ntab `module` paths resolve against it. ABSENT = the plugin\nhas no viewer extension (`icon`/`tabs` are ignored).").meta({ omitempty: true }).optional(),
}).describe("A plugin's `objectiveai.json`, whole — ONE schema for the ONE\nfile. The container half (`containerfile`, `port`) is what the\nlaboratory host builds and runs; the viewer half (`viewer`,\n`icon`, `tabs`) is what the viewer surfaces. `icon` and tab\n`module` paths are relative to the declared `viewer` root\n(authored as if the CWD were inside it).").meta({ title: "cli.plugins.Manifest" });
export type CliPluginsManifest = z.infer<typeof CliPluginsManifestSchema>;
