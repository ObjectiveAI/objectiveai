// THIS FILE IS AUTO-GENERATED. DO NOT EDIT.
// Generated by scripts/install-zod.cjs from objectiveai-json-schema/.
import { z } from "zod";
import { CliWebsocketAgentsInstancesListenerAgentRecordSchema } from "./agentRecord";
import { CliWebsocketAgentsInstancesListenerConversationRowSchema } from "./conversationRow";

export const CliWebsocketAgentsInstancesListenerAgentInstanceEventSchema = z.union([z.object({
  row: CliWebsocketAgentsInstancesListenerConversationRowSchema,
  type: z.literal("row"),
}).describe("One conversation row (snapshot replay or live). Replaces any\nprior row with the same identity.").meta({"variantTitle":"Row"}), z.object({
  type: z.literal("live"),
}).describe("The snapshot is complete; every following `Row` is live.").meta({"variantTitle":"Live"}), z.object({
  agent: CliWebsocketAgentsInstancesListenerAgentRecordSchema,
  type: z.literal("agent"),
}).describe("This agent's list record — the same shape\n`/agents/instances/list` tracks (lock-driven `active` flag,\nbound tags, counters). A FULL-VALUE upsert: sent once on\nconnect with the current state, then re-sent whenever it\nchanges (activation; deactivation — on stream end OR holder\nkill; a tag apply/move/removal). Replaces any prior value;\nstructurally independent of the conversation rows.").meta({"variantTitle":"Agent"})]).describe("One event on the `/agents/instances/{*aih}` stream. Two structurally\nindependent concerns ride it:\n\n- **The conversation**: the DB snapshot replays as `Row` events (in\n  `objectiveai.messages.\"index\"` order), one `Live` marks the\n  snapshot complete, then live `Row` events follow as the\n  conversation occurs.\n- **The agent's status**: `Agent` carries this agent's list record\n  (active flag + tags + counters), once at connect and on every\n  change.\n\nExtensibility contract: future variants WILL be added (e.g.\nclient-message delivery notifications) — consumers must skip events\nthey cannot parse.").meta({ title: "cli.websocket_agents_instances_listener.AgentInstanceEvent" });
export type CliWebsocketAgentsInstancesListenerAgentInstanceEvent = z.infer<typeof CliWebsocketAgentsInstancesListenerAgentInstanceEventSchema>;
