// THIS FILE IS AUTO-GENERATED. DO NOT EDIT.
// Generated by scripts/install-zod.cjs from objectiveai-json-schema/.
import { z } from "zod";
import { JsonValueSchema } from "../../jsonValue";
import { CliWebsocketAgentsInstancesListenerAgentRecordSchema } from "./agentRecord";
import { CliWebsocketAgentsInstancesListenerAssistantResponsePartSchema } from "./assistantResponsePart";
import { CliWebsocketAgentsInstancesListenerClientNotificationPartSchema } from "./clientNotificationPart";
import { CliWebsocketAgentsInstancesListenerRequestMessageUserPartSchema } from "./requestMessageUserPart";
import { CliWebsocketAgentsInstancesListenerToolResponsePartSchema } from "./toolResponsePart";
import { CliWebsocketAgentsInstancesListenerVectorRequestChoicePartSchema } from "./vectorRequestChoicePart";

export const CliWebsocketAgentsInstancesListenerAgentInstanceEventSchema = z.union([z.object({
  agent_instance_hierarchy: z.string(),
  part: CliWebsocketAgentsInstancesListenerRequestMessageUserPartSchema,
  response_id: z.string(),
  row_index: z.number().int().min(-9223372036854776000).max(9223372036854776000),
  row_sub_index: z.number().int().min(-9223372036854776000).max(9223372036854776000).nullable().meta({ omitempty: true }).optional(),
  type: z.literal("request_message_user_part"),
}).describe("One part of a `RequestMessageUser` block. `row_index` = message\nindex, `row_sub_index` = part index.").meta({"variantTitle":"RequestMessageUserPart"}), z.object({
  agent_instance_hierarchy: z.string(),
  part: CliWebsocketAgentsInstancesListenerAssistantResponsePartSchema,
  response_id: z.string(),
  row_index: z.number().int().min(-9223372036854776000).max(9223372036854776000),
  row_sub_index: z.number().int().min(-9223372036854776000).max(9223372036854776000).nullable().meta({ omitempty: true }).optional(),
  type: z.literal("request_message_assistant_part"),
}).describe("One part of a `RequestMessageAssistant` block.").meta({"variantTitle":"RequestMessageAssistantPart"}), z.object({
  agent_instance_hierarchy: z.string(),
  part: CliWebsocketAgentsInstancesListenerToolResponsePartSchema,
  response_id: z.string(),
  row_index: z.number().int().min(-9223372036854776000).max(9223372036854776000),
  row_sub_index: z.number().int().min(-9223372036854776000).max(9223372036854776000).nullable().meta({ omitempty: true }).optional(),
  tool_call_id: z.string().describe("The wire tool-call id the block answers (block boundary)."),
  type: z.literal("request_message_tool_part"),
}).describe("One part of a `RequestMessageTool` block.").meta({"variantTitle":"RequestMessageToolPart"}), z.object({
  agent_instance_hierarchy: z.string(),
  choice_index: z.number().int().min(-9223372036854776000).max(9223372036854776000).describe("The choice's index within the request (`row_index`)."),
  key: z.string().describe("This agent's inline voting key for the choice."),
  part: CliWebsocketAgentsInstancesListenerVectorRequestChoicePartSchema,
  part_index: z.number().int().min(-9223372036854776000).max(9223372036854776000).describe("The part's index within the choice (`row_sub_index`)."),
  response_id: z.string(),
  type: z.literal("vector_request_choice_part"),
}).describe("One part of one choice of a `VectorRequestChoices` block.").meta({"variantTitle":"VectorRequestChoicePart"}), z.object({
  agent_instance_hierarchy: z.string(),
  response_id: z.string(),
  type: z.literal("vector_response_vote"),
  vote: z.array(z.number().min(-3.4028234663852886e+38).max(3.4028234663852886e+38)),
}).describe("A complete `VectorResponseVote` block (single-row).").meta({"variantTitle":"VectorResponseVote"}), z.object({
  agent_instance_hierarchy: z.string(),
  key: z.string().nullable().describe("Idempotency token, if the row was enqueued with `--key`.").meta({ omitempty: true }).optional(),
  message_queue_id: z.number().int().min(-9223372036854776000).max(9223372036854776000).describe("`message_queue.id` of the consumed parent queue row —\none block per parent row."),
  part: CliWebsocketAgentsInstancesListenerClientNotificationPartSchema,
  queued_at: z.string().describe("`message_queue.enqueued_at` of the parent row."),
  response_id: z.string(),
  row_index: z.number().int().min(-9223372036854776000).max(9223372036854776000),
  sender_agent_instance_hierarchy: z.string().describe("AIH of the enqueuer (block boundary, with\n`message_queue_id`)."),
  type: z.literal("client_notification_part"),
}).describe("One part of a `ClientNotification` block. `row_index` = the\nconsumed `message_queue_contents.id`.").meta({"variantTitle":"ClientNotificationPart"}), z.object({
  agent_instance_hierarchy: z.string(),
  part: CliWebsocketAgentsInstancesListenerAssistantResponsePartSchema,
  response_id: z.string(),
  row_index: z.number().int().min(-9223372036854776000).max(9223372036854776000),
  row_sub_index: z.number().int().min(-9223372036854776000).max(9223372036854776000).nullable().meta({ omitempty: true }).optional(),
  type: z.literal("assistant_response_part"),
}).describe("One part of an `AssistantResponse` block.").meta({"variantTitle":"AssistantResponsePart"}), z.object({
  agent_instance_hierarchy: z.string(),
  part: CliWebsocketAgentsInstancesListenerToolResponsePartSchema,
  response_id: z.string(),
  row_index: z.number().int().min(-9223372036854776000).max(9223372036854776000),
  row_sub_index: z.number().int().min(-9223372036854776000).max(9223372036854776000).nullable().meta({ omitempty: true }).optional(),
  tool_call_id: z.string().describe("The wire tool-call id the block answers (block boundary)."),
  type: z.literal("tool_response_part"),
}).describe("One part of a `ToolResponse` block.").meta({"variantTitle":"ToolResponsePart"}), z.object({
  agent_instance_hierarchy: z.string(),
  delivered_at: z.string(),
  error: JsonValueSchema,
  response_id: z.string().nullable().meta({ omitempty: true }).optional(),
  type: z.literal("error"),
}).describe("A complete `Error` block. Errors are IMMUTABLE and single-shot\n— never updated, so they carry no replace-at identity;\nconsumers dedupe the snapshot/live seam by value equality.").meta({"variantTitle":"Error"}), z.object({
  type: z.literal("live"),
}).describe("The snapshot is complete; every following event is live.").meta({"variantTitle":"Live"}), z.object({
  agent: CliWebsocketAgentsInstancesListenerAgentRecordSchema,
  type: z.literal("agent"),
}).describe("This agent's list record — the same shape\n`/agents/instances/list` tracks (lock-driven `active` flag,\nbound tags, counters). A FULL-VALUE upsert: sent once on\nconnect with the current state, then re-sent whenever it\nchanges. Structurally independent of the conversation events.").meta({"variantTitle":"Agent"})]).describe("One event on the `/agents/instances/{*aih}` stream. The\nconversation events carry ONE mirrored part each, addressed by\ntheir block's boundary fields (exactly the fields `ResponseItem`\nkeeps at block level) plus the row's DB identity — `row_index` /\n`row_sub_index` are `objectiveai.messages`' values, an OPAQUE\nreplace-at + ordering key: a re-sent identity replaces the prior\npart, both within the snapshot/live seam and across streaming\nupdates of one part. Single-row blocks (`vote` / `error`) ship\ncomplete.\n\nThe DB snapshot replays first (in `objectiveai.messages.\"index\"`\norder), one `Live` marks the snapshot complete, then live events\nfollow as the conversation occurs. `Agent` events (the status\nrecord) are structurally independent and can arrive at any time.\n\nExtensibility contract: future variants WILL be added — consumers\nmust skip events they cannot parse.").meta({ title: "cli.websocket_agents_instances_listener.AgentInstanceEvent" });
export type CliWebsocketAgentsInstancesListenerAgentInstanceEvent = z.infer<typeof CliWebsocketAgentsInstancesListenerAgentInstanceEventSchema>;
