// THIS FILE IS AUTO-GENERATED. DO NOT EDIT.
// Generated by scripts/install-zod.cjs from objectiveai-json-schema/.
import { z } from "zod";
import { JsonValueSchema } from "../../jsonValue";
import { CliCommandAgentArgumentsSchema } from "../command/agentArguments";

export const CliChannelListenerChannelOfferSchema = z.object({
  agent_arguments: CliCommandAgentArgumentsSchema.describe("The originating caller's agent identity, from its scope."),
  channel_id: z.string().describe("The daemon-minted channel id — the accept + log routing key."),
  details: JsonValueSchema.describe("Arbitrary offer payload, opaque to the daemon."),
  key: z.string().describe("Caller-chosen discriminator (e.g. `\"browser.login\"`) — how a\nuser surface decides whether/how to accept the offer."),
  plugin_owner: z.string().nullable().describe("The PLUGIN that originated the offer — daemon-authored\n(unspoofable; stamped by `plugins run`), absent when the\ncaller wasn't a plugin.").meta({ omitempty: true }).optional(),
  plugin_repository: z.string().nullable().meta({ omitempty: true }).optional(),
  plugin_version: z.string().nullable().meta({ omitempty: true }).optional(),
}).describe("One channel OFFER, as broadcast to every connected channel stream.\nCarries no secret — the publisher's `S_pub` is returned to the\npublisher's command, and the owner's `S_owner` is delivered only\nover the accepting connection's stream.").meta({ title: "cli.channel_listener.ChannelOffer" });
export type CliChannelListenerChannelOffer = z.infer<typeof CliChannelListenerChannelOfferSchema>;
