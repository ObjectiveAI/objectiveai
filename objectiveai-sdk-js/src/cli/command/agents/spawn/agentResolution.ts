// THIS FILE IS AUTO-GENERATED. DO NOT EDIT.
// Generated by scripts/install-zod.cjs from objectiveai-json-schema/.
import { z } from "zod";
import { CliCommandAgentsSpawnAgentSpecSchema } from "./agentSpec";

export const CliCommandAgentsSpawnAgentResolutionSchema = z.union([z.object({
  agent_spec: CliCommandAgentsSpawnAgentSpecSchema,
  by: z.literal("direct"),
}).meta({"variantTitle":"Direct"}), z.object({
  agent_tag: z.string(),
  by: z.literal("tag"),
}).meta({"variantTitle":"Tag"})]).describe("Discriminated agent-resolution mode for `agents spawn`.\n\n- `Direct` — `--agent <ref>` / `--agent-inline <json>` paths\n  produce an `AgentSpec` directly. No tag is involved; the\n  conduit constructs without an `agent_tag`.\n- `Tag` — `--agent-tag <name>` references an existing GROUPED\n  tag (BOUND tags are rejected — there's already a live AIH for\n  them, use `agents message` instead). The CLI handler\n  resolves the tag → `tag_groups` row, takes the row's\n  `agent_spec` to spawn, and threads the tag name into the\n  conduit so the spawn's `agent_instance_hierarchy` flips the\n  whole group to BOUND on the first conduit read.").meta({ title: "cli.command.agents.spawn.AgentResolution" });
export type CliCommandAgentsSpawnAgentResolution = z.infer<typeof CliCommandAgentsSpawnAgentResolutionSchema>;
