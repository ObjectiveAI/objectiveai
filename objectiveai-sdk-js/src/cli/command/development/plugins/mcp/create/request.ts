// THIS FILE IS AUTO-GENERATED. DO NOT EDIT.
// Generated by scripts/install-zod.cjs from objectiveai-json-schema/.
import { z } from "zod";
import { CliCommandDevelopmentPluginsMcpCreatePathSchema } from "./path";

export const CliCommandDevelopmentPluginsMcpCreateRequestSchema = z.object({
  jq: z.string().nullable().describe("jq filter applied to the JSON output. Ignored when `python`\nis also set — python overrides jq.").optional(),
  max_tokens: z.number().int().min(0).max(18446744073709552000).nullable().describe("Response token budget, `>= 1` (`0` is rejected at parse\ntime — omit entirely for unlimited). Forward-compatible\nenvelope data — no leaf enforces it yet.").meta({ omitempty: true }).optional(),
  name: z.string().describe("Repository segment, likewise lowercased."),
  owner: z.string().describe("GitHub `<owner>` segment, lowercased on arrival — the\ndeclaration layer lowercases it too, so the registration and an\nagent's `plugin` entry agree."),
  path: z.string().describe("ABSOLUTE path to the plugin's source directory: the one holding\n`objectiveai.json`. Absolute because the laboratory host\nresolves it, and its working directory is not yours."),
  path_type: CliCommandDevelopmentPluginsMcpCreatePathSchema,
  python: z.string().nullable().describe("Python transform applied to the JSON output. Overrides `jq`\nwhen both are provided.").optional(),
  timeout_seconds: z.number().int().min(0).max(18446744073709552000).nullable().describe("Wall-clock execution cap, in whole seconds. Parsed from\n`--timeout` (humantime: `30s`, `5m`, `1h30m`), `> 0`\nenforced at parse time. `db query` threads it to postgres\nwhen set; omit for uncapped.").meta({ omitempty: true }).optional(),
  version: z.string().describe("Plugin version — matched byte-for-byte against what an agent\ndeclares. It is the git tag everywhere else, and stays\ncase-sensitive here even though nothing is fetched."),
}).meta({ title: "cli.command.development.plugins.mcp.create.Request" });
export type CliCommandDevelopmentPluginsMcpCreateRequest = z.infer<typeof CliCommandDevelopmentPluginsMcpCreateRequestSchema>;
