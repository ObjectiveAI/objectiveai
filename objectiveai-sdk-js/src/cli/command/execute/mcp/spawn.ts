// THIS FILE IS AUTO-GENERATED. DO NOT EDIT.
// Generated by scripts/install-command-execute.cjs from objectiveai-sdk-rs/src/cli/command/.
import { z } from "zod";
import { CliStream } from "../../cliStream";
import { type CommandExecutor } from "../../executor";
import { type CliCommandMcpSpawnRequest } from "../../mcp/spawn/request";
import { type CliCommandMcpSpawnRequestSchemaRequest } from "../../mcp/spawn/request_schema/request";
import { CliCommandMcpSpawnResponseSchema, type CliCommandMcpSpawnResponse } from "../../mcp/spawn/response";
import { type CliCommandMcpSpawnResponseSchemaRequest } from "../../mcp/spawn/response_schema/request";
import { CliErrorSchema, type CliError } from "../../../error";
import { JsonValueSchema, type JsonValue } from "../../../../jsonValue";

/** `mcp spawn execute` — unary; first stream item, rest discarded. */
export async function mcpSpawnExecute(executor: CommandExecutor, request: Omit<CliCommandMcpSpawnRequest, "path_type">): Promise<CliError | CliCommandMcpSpawnResponse> {
  const stream = new CliStream(executor.execute({ ...request, jq: undefined, python: undefined, path_type: "mcp/spawn" }), z.union([CliErrorSchema, CliCommandMcpSpawnResponseSchema]));
  const first = await stream.first();
  if (first === undefined) {
    throw new Error("mcp spawn: cli produced no output before the end marker");
  }
  return first;
}

/** `mcp spawn execute_transform` — unary; first stream item, rest discarded. */
export async function mcpSpawnExecuteTransform(executor: CommandExecutor, request: Omit<CliCommandMcpSpawnRequest, "path_type">, transform: { jq: string } | { python: string }): Promise<CliError | JsonValue> {
  const stream = new CliStream(executor.execute({ ...request, jq: undefined, python: undefined, ...transform, path_type: "mcp/spawn" }), z.union([CliErrorSchema, JsonValueSchema]));
  const first = await stream.first();
  if (first === undefined) {
    throw new Error("mcp spawn: cli produced no output before the end marker");
  }
  return first;
}

/** `mcp spawn request_schema execute` — unary; first stream item, rest discarded. */
export async function mcpSpawnRequestSchemaExecute(executor: CommandExecutor, request: Omit<CliCommandMcpSpawnRequestSchemaRequest, "path_type">): Promise<CliError | JsonValue> {
  const stream = new CliStream(executor.execute({ ...request, jq: undefined, python: undefined, path_type: "mcp/spawn/request_schema" }), z.union([CliErrorSchema, JsonValueSchema]));
  const first = await stream.first();
  if (first === undefined) {
    throw new Error("mcp spawn request_schema: cli produced no output before the end marker");
  }
  return first;
}

/** `mcp spawn request_schema execute_transform` — unary; first stream item, rest discarded. */
export async function mcpSpawnRequestSchemaExecuteTransform(executor: CommandExecutor, request: Omit<CliCommandMcpSpawnRequestSchemaRequest, "path_type">, transform: { jq: string } | { python: string }): Promise<CliError | JsonValue> {
  const stream = new CliStream(executor.execute({ ...request, jq: undefined, python: undefined, ...transform, path_type: "mcp/spawn/request_schema" }), z.union([CliErrorSchema, JsonValueSchema]));
  const first = await stream.first();
  if (first === undefined) {
    throw new Error("mcp spawn request_schema: cli produced no output before the end marker");
  }
  return first;
}

/** `mcp spawn response_schema execute` — unary; first stream item, rest discarded. */
export async function mcpSpawnResponseSchemaExecute(executor: CommandExecutor, request: Omit<CliCommandMcpSpawnResponseSchemaRequest, "path_type">): Promise<CliError | JsonValue> {
  const stream = new CliStream(executor.execute({ ...request, jq: undefined, python: undefined, path_type: "mcp/spawn/response_schema" }), z.union([CliErrorSchema, JsonValueSchema]));
  const first = await stream.first();
  if (first === undefined) {
    throw new Error("mcp spawn response_schema: cli produced no output before the end marker");
  }
  return first;
}

/** `mcp spawn response_schema execute_transform` — unary; first stream item, rest discarded. */
export async function mcpSpawnResponseSchemaExecuteTransform(executor: CommandExecutor, request: Omit<CliCommandMcpSpawnResponseSchemaRequest, "path_type">, transform: { jq: string } | { python: string }): Promise<CliError | JsonValue> {
  const stream = new CliStream(executor.execute({ ...request, jq: undefined, python: undefined, ...transform, path_type: "mcp/spawn/response_schema" }), z.union([CliErrorSchema, JsonValueSchema]));
  const first = await stream.first();
  if (first === undefined) {
    throw new Error("mcp spawn response_schema: cli produced no output before the end marker");
  }
  return first;
}
