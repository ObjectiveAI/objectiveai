// THIS FILE IS AUTO-GENERATED. DO NOT EDIT.
// Generated by scripts/install-command-execute.cjs from objectiveai-sdk-rs/src/cli/command/.
import { z } from "zod";
import { CliStream } from "../../cliStream";
import { type CommandExecutor } from "../../executor";
import { type CliCommandViewerSpawnRequest } from "../../viewer/spawn/request";
import { type CliCommandViewerSpawnRequestSchemaRequest } from "../../viewer/spawn/request_schema/request";
import { CliCommandViewerSpawnResponseSchema, type CliCommandViewerSpawnResponse } from "../../viewer/spawn/response";
import { type CliCommandViewerSpawnResponseSchemaRequest } from "../../viewer/spawn/response_schema/request";
import { CliErrorSchema, type CliError } from "../../../error";
import { JsonValueSchema, type JsonValue } from "../../../../jsonValue";

/** `viewer spawn execute` — unary; first stream item, rest discarded. */
export async function viewerSpawnExecute(executor: CommandExecutor, request: Omit<CliCommandViewerSpawnRequest, "path_type">): Promise<CliError | CliCommandViewerSpawnResponse> {
  const stream = new CliStream(executor.execute({ ...request, jq: undefined, python: undefined, path_type: "viewer/spawn" }), z.union([CliErrorSchema, CliCommandViewerSpawnResponseSchema]));
  const first = await stream.first();
  if (first === undefined) {
    throw new Error("viewer spawn: cli produced no output before the end marker");
  }
  return first;
}

/** `viewer spawn execute_transform` — unary; first stream item, rest discarded. */
export async function viewerSpawnExecuteTransform(executor: CommandExecutor, request: Omit<CliCommandViewerSpawnRequest, "path_type">, transform: { jq: string } | { python: string }): Promise<CliError | JsonValue> {
  const stream = new CliStream(executor.execute({ ...request, jq: undefined, python: undefined, ...transform, path_type: "viewer/spawn" }), z.union([CliErrorSchema, JsonValueSchema]));
  const first = await stream.first();
  if (first === undefined) {
    throw new Error("viewer spawn: cli produced no output before the end marker");
  }
  return first;
}

/** `viewer spawn request_schema execute` — unary; first stream item, rest discarded. */
export async function viewerSpawnRequestSchemaExecute(executor: CommandExecutor, request: Omit<CliCommandViewerSpawnRequestSchemaRequest, "path_type">): Promise<CliError | JsonValue> {
  const stream = new CliStream(executor.execute({ ...request, jq: undefined, python: undefined, path_type: "viewer/spawn/request_schema" }), z.union([CliErrorSchema, JsonValueSchema]));
  const first = await stream.first();
  if (first === undefined) {
    throw new Error("viewer spawn request_schema: cli produced no output before the end marker");
  }
  return first;
}

/** `viewer spawn request_schema execute_transform` — unary; first stream item, rest discarded. */
export async function viewerSpawnRequestSchemaExecuteTransform(executor: CommandExecutor, request: Omit<CliCommandViewerSpawnRequestSchemaRequest, "path_type">, transform: { jq: string } | { python: string }): Promise<CliError | JsonValue> {
  const stream = new CliStream(executor.execute({ ...request, jq: undefined, python: undefined, ...transform, path_type: "viewer/spawn/request_schema" }), z.union([CliErrorSchema, JsonValueSchema]));
  const first = await stream.first();
  if (first === undefined) {
    throw new Error("viewer spawn request_schema: cli produced no output before the end marker");
  }
  return first;
}

/** `viewer spawn response_schema execute` — unary; first stream item, rest discarded. */
export async function viewerSpawnResponseSchemaExecute(executor: CommandExecutor, request: Omit<CliCommandViewerSpawnResponseSchemaRequest, "path_type">): Promise<CliError | JsonValue> {
  const stream = new CliStream(executor.execute({ ...request, jq: undefined, python: undefined, path_type: "viewer/spawn/response_schema" }), z.union([CliErrorSchema, JsonValueSchema]));
  const first = await stream.first();
  if (first === undefined) {
    throw new Error("viewer spawn response_schema: cli produced no output before the end marker");
  }
  return first;
}

/** `viewer spawn response_schema execute_transform` — unary; first stream item, rest discarded. */
export async function viewerSpawnResponseSchemaExecuteTransform(executor: CommandExecutor, request: Omit<CliCommandViewerSpawnResponseSchemaRequest, "path_type">, transform: { jq: string } | { python: string }): Promise<CliError | JsonValue> {
  const stream = new CliStream(executor.execute({ ...request, jq: undefined, python: undefined, ...transform, path_type: "viewer/spawn/response_schema" }), z.union([CliErrorSchema, JsonValueSchema]));
  const first = await stream.first();
  if (first === undefined) {
    throw new Error("viewer spawn response_schema: cli produced no output before the end marker");
  }
  return first;
}
