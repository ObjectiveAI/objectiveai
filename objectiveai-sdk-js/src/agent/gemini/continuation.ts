// THIS FILE IS AUTO-GENERATED. DO NOT EDIT.
// Generated by scripts/install-zod.cjs from objectiveai-json-schema/.
import { z } from "zod";
import { AgentGeminiMessageSchema } from "./message";
import { AgentGeminiUpstreamSchema } from "./upstream";

export const AgentGeminiContinuationSchema = z.object({
  agent_instance_hierarchy: z.string().describe("Full slash-separated lineage of the agent this continuation\nbelongs to (e.g. `A/B/agtcpl-<uuid>-<created>`). Minted on the\nagent's first spawn and preserved verbatim across every\ncontinuation round so the agent's identity stays stable\nregardless of who resumes the conversation."),
  mcp_sessions: z.record(z.string(), z.string()),
  messages: z.array(AgentGeminiMessageSchema).describe("Full prior conversation in the runner's message shape. The\nGemini runner is stateless (no server-side session resume), so\nthe API persists the entire history here and replays it — prior\nhistory followed by the next turn's messages — on every\ncontinuation round. Empty on a fresh conversation.").meta({ omitempty: true }),
  session_id: z.string(),
  upstream: AgentGeminiUpstreamSchema,
}).meta({ title: "agent.gemini.Continuation" });
export type AgentGeminiContinuation = z.infer<typeof AgentGeminiContinuationSchema>;
