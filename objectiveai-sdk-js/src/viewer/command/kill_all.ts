// THIS FILE IS AUTO-GENERATED. DO NOT EDIT.
// Generated by scripts/install-viewer-execute.cjs from objectiveai-sdk-rs/src/cli/command/.
import { z } from "zod";
import { type CliCommandKillAllRequest } from "../../cli/command/kill_all/request";
import { type CliCommandKillAllRequestSchemaRequest } from "../../cli/command/kill_all/request_schema/request";
import { CliCommandKillAllResponseSchema, type CliCommandKillAllResponse } from "../../cli/command/kill_all/response";
import { type CliCommandKillAllResponseSchemaRequest } from "../../cli/command/kill_all/response_schema/request";
import { CliErrorSchema, type CliError } from "../../cli/error";
import { JsonValueSchema, type JsonValue } from "../../jsonValue";
import { CliStream } from "../cliStream";
import { invokeCliRequest } from "../invoke";

/** `kill_all execute` — unary; first stream item, rest discarded. */
export async function killAllExecute(request: Omit<CliCommandKillAllRequest, "path_type">): Promise<CliError | CliCommandKillAllResponse> {
  const stream = new CliStream(invokeCliRequest({ ...request, jq: undefined, python: undefined, path_type: "kill-all" }), z.union([CliErrorSchema, CliCommandKillAllResponseSchema]));
  const first = await stream.first();
  if (first === undefined) {
    throw new Error("kill_all: cli produced no output before the end marker");
  }
  return first;
}

/** `kill_all execute_transform` — unary; first stream item, rest discarded. */
export async function killAllExecuteTransform(request: Omit<CliCommandKillAllRequest, "path_type">, transform: { jq: string } | { python: string }): Promise<CliError | JsonValue> {
  const stream = new CliStream(invokeCliRequest({ ...request, jq: undefined, python: undefined, ...transform, path_type: "kill-all" }), z.union([CliErrorSchema, JsonValueSchema]));
  const first = await stream.first();
  if (first === undefined) {
    throw new Error("kill_all: cli produced no output before the end marker");
  }
  return first;
}

/** `kill_all request_schema execute` — unary; first stream item, rest discarded. */
export async function killAllRequestSchemaExecute(request: Omit<CliCommandKillAllRequestSchemaRequest, "path_type">): Promise<CliError | JsonValue> {
  const stream = new CliStream(invokeCliRequest({ ...request, jq: undefined, python: undefined, path_type: "kill-all/request_schema" }), z.union([CliErrorSchema, JsonValueSchema]));
  const first = await stream.first();
  if (first === undefined) {
    throw new Error("kill_all request_schema: cli produced no output before the end marker");
  }
  return first;
}

/** `kill_all request_schema execute_transform` — unary; first stream item, rest discarded. */
export async function killAllRequestSchemaExecuteTransform(request: Omit<CliCommandKillAllRequestSchemaRequest, "path_type">, transform: { jq: string } | { python: string }): Promise<CliError | JsonValue> {
  const stream = new CliStream(invokeCliRequest({ ...request, jq: undefined, python: undefined, ...transform, path_type: "kill-all/request_schema" }), z.union([CliErrorSchema, JsonValueSchema]));
  const first = await stream.first();
  if (first === undefined) {
    throw new Error("kill_all request_schema: cli produced no output before the end marker");
  }
  return first;
}

/** `kill_all response_schema execute` — unary; first stream item, rest discarded. */
export async function killAllResponseSchemaExecute(request: Omit<CliCommandKillAllResponseSchemaRequest, "path_type">): Promise<CliError | JsonValue> {
  const stream = new CliStream(invokeCliRequest({ ...request, jq: undefined, python: undefined, path_type: "kill-all/response_schema" }), z.union([CliErrorSchema, JsonValueSchema]));
  const first = await stream.first();
  if (first === undefined) {
    throw new Error("kill_all response_schema: cli produced no output before the end marker");
  }
  return first;
}

/** `kill_all response_schema execute_transform` — unary; first stream item, rest discarded. */
export async function killAllResponseSchemaExecuteTransform(request: Omit<CliCommandKillAllResponseSchemaRequest, "path_type">, transform: { jq: string } | { python: string }): Promise<CliError | JsonValue> {
  const stream = new CliStream(invokeCliRequest({ ...request, jq: undefined, python: undefined, ...transform, path_type: "kill-all/response_schema" }), z.union([CliErrorSchema, JsonValueSchema]));
  const first = await stream.first();
  if (first === undefined) {
    throw new Error("kill_all response_schema: cli produced no output before the end marker");
  }
  return first;
}
