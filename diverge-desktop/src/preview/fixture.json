{
  "note": "Generated by `cargo test -p diverge-desktop export_preview_fixture`. Do not edit.",
  "contract_pin": "e508aa8a70b9ee46c848614ce72eee3b9c060526",
  "actions": [
    {
      "name": "agents_list",
      "does": "List every agent"
    },
    {
      "name": "agents_create",
      "does": "Make an agent from an image, settings, limits and mounts"
    },
    {
      "name": "agents_delete",
      "does": "Remove an agent (the daemon refuses while it is running)"
    },
    {
      "name": "agents_message",
      "does": "Send an agent a message; resolves when delivered"
    },
    {
      "name": "agents_message_take_back",
      "does": "Take back a message that has not been delivered yet"
    },
    {
      "name": "logs_open",
      "does": "Read an agent's log — filtered, reshaped by jq, perhaps watched"
    },
    {
      "name": "scope_close",
      "does": "Stop watching something"
    },
    {
      "name": "files_tree_open",
      "does": "Watch a folder on the daemon's machine"
    },
    {
      "name": "files_read",
      "does": "Open a file on the daemon's machine"
    },
    {
      "name": "files_write",
      "does": "Save a file on the daemon's machine"
    },
    {
      "name": "machines_list",
      "does": "List the machines the daemon can run on"
    },
    {
      "name": "machines_add",
      "does": "Add a machine: one you dial, or one that dials you"
    },
    {
      "name": "machines_remove",
      "does": "Remove a machine"
    },
    {
      "name": "views_list",
      "does": "List saved Views"
    },
    {
      "name": "views_save",
      "does": "Save a View (a saved log request)"
    },
    {
      "name": "views_delete",
      "does": "Delete a saved View"
    },
    {
      "name": "catalog_images",
      "does": "The six agent images and their settings"
    },
    {
      "name": "catalog_check",
      "does": "Whether settings are ones an image accepts"
    },
    {
      "name": "tabs_snapshot",
      "does": "What is open"
    },
    {
      "name": "tabs_open",
      "does": "Open (or focus) a tab"
    },
    {
      "name": "tabs_close",
      "does": "Close a tab"
    },
    {
      "name": "tabs_focus",
      "does": "Focus a tab"
    },
    {
      "name": "actions_list",
      "does": "This list"
    },
    {
      "name": "app_info",
      "does": "Whether the daemon is the stand-in, and the contract pin"
    }
  ],
  "catalog": [
    {
      "key": "cc",
      "image_name": "diverge-agentic-loop-cc",
      "digest": "unbuilt",
      "schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "title": "Agent",
        "description": "An agent running against Claude Code.\n\nFar fewer knobs than OpenRouter's, and not because anything is\nmissing: Claude Code is an agent harness in its own right, so\nthe sampling decisions OpenRouter exposes are made inside it.\nWhat it does expose is its tools: [`tools`](Self::tools) is the\nexact set of built-ins the model is given, every switch stated,\npassed to Claude Code as `--tools`; [`mcp_tools`](Self::mcp_tools)\nis the other kind, the tool containers the agent asks the caller\nto run for it.",
        "type": "object",
        "properties": {
          "model": {
            "description": "The model to run.",
            "type": "string"
          },
          "tools": {
            "description": "The switchable built-in tools — each absent or `false` is\noff, and the whole structure absent is every tool off. The\nMCP tools and `Skill` are on regardless; see [`Tools`].",
            "$ref": "#/$defs/Tools",
            "default": {}
          },
          "thinking": {
            "description": "Whether to think before answering.",
            "type": [
              "boolean",
              "null"
            ]
          },
          "effort": {
            "description": "How much effort to spend thinking. Meaningful only with\n[`thinking`](Self::thinking) on.",
            "anyOf": [
              {
                "$ref": "#/$defs/Effort"
              },
              {
                "type": "null"
              }
            ]
          },
          "mcp_tools": {
            "description": "The tool containers this agent depends on, each one the caller\nruns and serves to it as an MCP server, in the form the\nprovider's wire defines. Passed back whole as the registration's\nanswer, which is how the caller learns of them. Absent is none.",
            "type": "array",
            "items": {
              "$ref": "#/$defs/Tool"
            }
          }
        },
        "required": [
          "model"
        ],
        "$defs": {
          "Tools": {
            "description": "The built-in tools Claude Code is given, every one stated.\n\nClaude Code's `--tools` flag takes the exact set of built-ins the\nmodel may call, and this is that set: a switch per tool, each an\n`Option<bool>` where ABSENT IS OFF — the same as `false` — so\nnothing is on by omission and a request names only what it\nturns on. The harness renders the `true` switches as the flag's\nlist, in this order, after the always-on names.\n\nAlways on, no switch: the `mcp__*` tools — the caller's own\ntools, served through the proxy — are not built-ins and are\npresent whatever `--tools` says: the tool channel is what the\nprotocol is built around. Beside them the harness always lists\nthe four MCP resource built-ins (`ListMcpResources`,\n`ReadMcpResource`, `ReadMcpResourceDir`, `RefreshMcpTools` — the\ncaller's resources, through the same proxy), `Skill` (skills are\nthe directories mounted under `~/.claude/skills`; the tool is\noffered whether or not any are, and a call names a skill that is\nnot there fails as \"unknown skill\"), and `ToolSearch`, by which\nthe model loads deferred tools' schemas.\n\nAlways off, no switch: `LSP` (nothing in the image installs a\nlanguage server). Nor does a switch exist for what cannot work\nin the container, which the harness never lists:\n`AskUserQuestion`, `EnterPlanMode`,\n`ExitPlanMode`, `ProposeGoal` and `ProposeSkills` (a call waits\non a user who is not there); `CronCreate`, `CronDelete`,\n`CronList` and `ScheduleWakeup` (the container does not outlive\nthe run); `Artifact`, `Projects`, `ClaudeDesign`, `RemoteTrigger`,\n`PushNotification`, `SendFeedback`, `ReadNotifications`,\n`ShowOnboardingRolePicker` and `EndConversation` (claude.ai\naccount and host features). Nor for the legacy names the binary\nstill answers to (`MultiEdit`, `BashOutput`, `KillShell`): they\nare the old spellings of switches here.\n\nWhether the pinned build honors every name here under `-p` with\nstream-json input is a live-run check, not a promise of this\ntype; the names are as the runtime spells them.",
            "type": "object",
            "properties": {
              "bash": {
                "description": "`Bash`: runs a command, with an optional timeout and\noptionally in the background.",
                "type": [
                  "boolean",
                  "null"
                ]
              },
              "read": {
                "description": "`Read`: reads a file by absolute path, with `offset` and `limit`\nfor large files and `pages` for PDFs.",
                "type": [
                  "boolean",
                  "null"
                ]
              },
              "write": {
                "description": "`Write`: writes a whole file, overwriting.",
                "type": [
                  "boolean",
                  "null"
                ]
              },
              "edit": {
                "description": "`Edit`: exact-string replacement in a file.",
                "type": [
                  "boolean",
                  "null"
                ]
              },
              "notebook_edit": {
                "description": "`NotebookEdit`: replaces, inserts or deletes one Jupyter cell.",
                "type": [
                  "boolean",
                  "null"
                ]
              },
              "glob": {
                "description": "`Glob`: file pattern matching.",
                "type": [
                  "boolean",
                  "null"
                ]
              },
              "grep": {
                "description": "`Grep`: ripgrep over the workspace (ripgrep ships with the\nbinary).",
                "type": [
                  "boolean",
                  "null"
                ]
              },
              "task": {
                "description": "`Task` (also spelled `Agent`): launches a sub-agent. Its chunks\nreach the run's stream attributed by `parent_tool_call_id`.\nLocal sub-agents only: `isolation: remote` needs a cloud\nenvironment the container has no account for.",
                "type": [
                  "boolean",
                  "null"
                ]
              },
              "send_message": {
                "description": "`SendMessage`: messages a running named agent.",
                "type": [
                  "boolean",
                  "null"
                ]
              },
              "list_agents": {
                "description": "`ListAgents`: lists addressable agents.",
                "type": [
                  "boolean",
                  "null"
                ]
              },
              "task_output": {
                "description": "`TaskOutput`: reads the output of a background task, blocking\nor not.",
                "type": [
                  "boolean",
                  "null"
                ]
              },
              "task_stop": {
                "description": "`TaskStop`: stops a background task by id.",
                "type": [
                  "boolean",
                  "null"
                ]
              },
              "monitor": {
                "description": "`Monitor`: watches a command's stdout lines or a WebSocket's\nframes as events.",
                "type": [
                  "boolean",
                  "null"
                ]
              },
              "workflow": {
                "description": "`Workflow`: runs a deterministic multi-agent orchestration\nscript.",
                "type": [
                  "boolean",
                  "null"
                ]
              },
              "todo_write": {
                "description": "`TodoWrite`: the session's structured todo list.",
                "type": [
                  "boolean",
                  "null"
                ]
              },
              "task_create": {
                "description": "`TaskCreate`: creates an entry on the session's task board.",
                "type": [
                  "boolean",
                  "null"
                ]
              },
              "task_get": {
                "description": "`TaskGet`: reads one task board entry.",
                "type": [
                  "boolean",
                  "null"
                ]
              },
              "task_update": {
                "description": "`TaskUpdate`: updates one task board entry.",
                "type": [
                  "boolean",
                  "null"
                ]
              },
              "task_list": {
                "description": "`TaskList`: lists the task board.",
                "type": [
                  "boolean",
                  "null"
                ]
              },
              "report_findings": {
                "description": "`ReportFindings`: reports code-review findings as a typed list.",
                "type": [
                  "boolean",
                  "null"
                ]
              },
              "repl": {
                "description": "`REPL`: runs JavaScript with persistent state and top-level\nawait.",
                "type": [
                  "boolean",
                  "null"
                ]
              },
              "web_fetch": {
                "description": "`WebFetch`: fetches a URL and runs a prompt over the content.\nNeeds outbound network from the container.",
                "type": [
                  "boolean",
                  "null"
                ]
              },
              "web_search": {
                "description": "`WebSearch`: searches the web, with domain allow and block\nlists. Needs outbound network from the container.",
                "type": [
                  "boolean",
                  "null"
                ]
              },
              "enter_worktree": {
                "description": "`EnterWorktree`: creates or switches into a git worktree. Needs\nthe workspace to be a git repository.",
                "type": [
                  "boolean",
                  "null"
                ]
              },
              "exit_worktree": {
                "description": "`ExitWorktree`: leaves the worktree, keeping or removing it.",
                "type": [
                  "boolean",
                  "null"
                ]
              },
              "sleep": {
                "description": "`Sleep`: waits.",
                "type": [
                  "boolean",
                  "null"
                ]
              }
            }
          },
          "Effort": {
            "description": "How much effort the model should spend before answering.",
            "oneOf": [
              {
                "description": "Least.",
                "type": "string",
                "const": "low"
              },
              {
                "description": "Balanced.",
                "type": "string",
                "const": "medium"
              },
              {
                "description": "More.",
                "type": "string",
                "const": "high"
              },
              {
                "description": "Above high, below max.",
                "type": "string",
                "const": "xhigh"
              },
              {
                "description": "As much as the model will spend.",
                "type": "string",
                "const": "max"
              }
            ]
          },
          "Tool": {
            "description": "One tool container the program depends on: everything the caller\nneeds to run it that the image can know, and nothing the caller\nalone can know.\n\nA [`Container`](crate::shared::containers::request::Container) is\nan image, two limits, mounts and arguments. The first four are\nhere, member for member; the mounts are not, because a volume is\nnamed by a `volume_name` in the caller's own listing and a FUSE\nmount is something the caller serves live, and a tool that named\neither would be dictating the caller's storage.",
            "type": "object",
            "properties": {
              "name": {
                "description": "What the program calls it: the caller's handle for the tool\ncontainer, and the label the caller uses if it prefixes the\ntool's tools when it merges lists. Unique in the list. The\nprogram never finds a server by it — it calls tools by whatever\nnames the caller's merged list shows, and knows which image\nserves each by the image under `_meta`, see\n[`shared::mcp`](crate::shared::mcp).",
                "type": "string"
              },
              "image": {
                "description": "The image: a name and a digest, the pair a\n`containers::tools::run` request names.",
                "$ref": "#/$defs/Image"
              },
              "memory": {
                "description": "The memory the tool container needs, in bytes.",
                "type": "integer",
                "format": "uint64",
                "minimum": 0
              },
              "disk": {
                "description": "The bytes the tool container may write, in bytes.",
                "type": "integer",
                "format": "uint64",
                "minimum": 0
              },
              "arguments": {
                "description": "The arguments the tool container is registered with — the\n`arguments` of the run request that makes it, verbatim."
              }
            },
            "required": [
              "name",
              "image",
              "memory",
              "disk",
              "arguments"
            ]
          },
          "Image": {
            "description": "The image, by its repository path and its manifest digest — the\npair [`images::check`](crate::endpoints::images::check::client::request::Frame)\nasks about, so a check that came back available names an image a\nrun can ask for, with nothing to translate between them.\n\n# The digest is the image\n\nA digest cannot be repointed at different content, so an image\nmeans the same bytes every time, wherever they come from. That is\nthe whole of what the wire asserts: the provider runs the image\nthe digest names and no other.\n\n# Who supplies the bytes is the provider's\n\nNot on the wire. A provider may hold the image already, pull it\nfrom a registry it uses, or take it from the caller — asking on\nthe run scope whether the caller holds it and, when it does, for\nits manifest and blobs, see [`oci`](crate::shared::containers::oci).\nWhich of those, in what order, and which registries, is the\nprovider's policy, and a caller learns only whether the run\nhappened. A caller need not hold what it names; one that does\nanswers so when asked.",
            "type": "object",
            "properties": {
              "name": {
                "description": "The repository path — `library/nginx`, `myorg/myimage`. No\nhost: where the bytes come from is not the caller's to say.\n\nIt lands in a reference by concatenation wherever a provider\npulls from, so a provider refuses one that is not a repository\npath and normalizes nothing.",
                "type": "string"
              },
              "digest": {
                "description": "The manifest digest, `<algorithm>:<hex>`.\n\nWhat actually identifies the image: a runtime hashes what it\npulls, so a source serving other bytes under it fails before\nanything runs.",
                "type": "string"
              }
            },
            "required": [
              "name",
              "digest"
            ]
          }
        }
      }
    },
    {
      "key": "codex",
      "image_name": "diverge-agentic-loop-codex",
      "digest": "unbuilt",
      "schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "title": "Agent",
        "description": "An agent running against Codex.\n\nFew knobs, and not because anything is missing: Codex decides its\nown sampling, its own tools and its own loop, and what it exposes\nto a caller is the model, how hard it reasons and how it speaks,\nwhether it may search the web, and where it sends its requests.\nHow it logs in is not the agent's to say: the login is a mount or\nthe vault's (see [the module](super)). Each field is one config\nkey or flag, named in its\ndoc; every `Option` absent leaves that key unset, so Codex applies\nits own default for the model — except [`web_search`](Self::web_search),\nwhere absent is off, because nothing is on by omission.",
        "type": "object",
        "properties": {
          "model": {
            "description": "The model to run: `model` (`--model`), in the endpoint's own\nnaming.",
            "type": "string"
          },
          "effort": {
            "description": "How much effort the model spends reasoning:\n`model_reasoning_effort`. See [`Effort`].",
            "anyOf": [
              {
                "$ref": "#/$defs/Effort"
              },
              {
                "type": "null"
              }
            ]
          },
          "reasoning_summary": {
            "description": "How much of its reasoning the model says out loud:\n`model_reasoning_summary`. What the stream's reasoning chunks\ncarry. See [`Summary`].",
            "anyOf": [
              {
                "$ref": "#/$defs/Summary"
              },
              {
                "type": "null"
              }
            ]
          },
          "verbosity": {
            "description": "How long the model's answers run: `model_verbosity`. See\n[`Verbosity`].",
            "anyOf": [
              {
                "$ref": "#/$defs/Verbosity"
              },
              {
                "type": "null"
              }
            ]
          },
          "web_search": {
            "description": "Whether, and how, the model may search the web: `web_search`.\nA capability of Codex itself rather than a tool of the\ncaller's — Codex performs the search and never calls out.\nABSENT IS `disabled`. See [`WebSearch`].",
            "anyOf": [
              {
                "$ref": "#/$defs/WebSearch"
              },
              {
                "type": "null"
              }
            ]
          },
          "provider": {
            "description": "Where requests go instead of OpenAI: a `model_providers` entry\nthe harness writes and selects, its key the `OPENAI_API_KEY`\nthe run found. Absent is OpenAI's own endpoint. See\n[`Provider`].",
            "anyOf": [
              {
                "$ref": "#/$defs/Provider"
              },
              {
                "type": "null"
              }
            ]
          },
          "mcp_tools": {
            "description": "The tool containers this agent depends on, each one the caller\nruns and serves to it as an MCP server, in the form the\nprovider's wire defines. Passed back whole as the registration's\nanswer, which is how the caller learns of them. Absent is none.",
            "type": "array",
            "items": {
              "$ref": "#/$defs/Tool"
            }
          }
        },
        "required": [
          "model"
        ],
        "$defs": {
          "Effort": {
            "description": "How much effort the model spends before answering: Codex's\n`model_reasoning_effort`, its five rungs as Codex spells them.\nThere is no `max`: `xhigh` is the top, and a model that has fewer\nrungs takes the nearest it has.",
            "oneOf": [
              {
                "description": "Least.",
                "type": "string",
                "const": "minimal"
              },
              {
                "description": "Little.",
                "type": "string",
                "const": "low"
              },
              {
                "description": "Balanced.",
                "type": "string",
                "const": "medium"
              },
              {
                "description": "More.",
                "type": "string",
                "const": "high"
              },
              {
                "description": "As much as the model will spend.",
                "type": "string",
                "const": "xhigh"
              }
            ]
          },
          "Summary": {
            "description": "How much of its reasoning the model says out loud: Codex's\n`model_reasoning_summary`. What the run's reasoning chunks carry —\nCodex streams the summary, never the raw reasoning, so this is\nthe whole of what a caller can see of the model thinking.",
            "oneOf": [
              {
                "description": "The model decides.",
                "type": "string",
                "const": "auto"
              },
              {
                "description": "Short.",
                "type": "string",
                "const": "concise"
              },
              {
                "description": "Long.",
                "type": "string",
                "const": "detailed"
              },
              {
                "description": "No summary: the reasoning chunks stay empty.",
                "type": "string",
                "const": "none"
              }
            ]
          },
          "Verbosity": {
            "description": "How long the model's answers run: Codex's `model_verbosity`,\nhonored by the models that take it (the reference names the GPT-5\nfamily on the Responses API) and unset otherwise.",
            "oneOf": [
              {
                "description": "Terse.",
                "type": "string",
                "const": "low"
              },
              {
                "description": "Balanced.",
                "type": "string",
                "const": "medium"
              },
              {
                "description": "Expansive.",
                "type": "string",
                "const": "high"
              }
            ]
          },
          "WebSearch": {
            "description": "Whether, and how, the model may search the web: Codex's\n`web_search`, its four values as Codex spells them. Codex performs\nthe search itself — it is a capability of the upstream, not a tool\nthe caller serves — so a search never appears on the stream as\none of the caller's tool calls. The harness writes `disabled` when\nthe agent says nothing.",
            "oneOf": [
              {
                "description": "No search at all.",
                "type": "string",
                "const": "disabled"
              },
              {
                "description": "Search over a cached index.",
                "type": "string",
                "const": "cached"
              },
              {
                "description": "Search over the live index.",
                "type": "string",
                "const": "indexed"
              },
              {
                "description": "Live fetches of the pages themselves.",
                "type": "string",
                "const": "live"
              }
            ]
          },
          "Provider": {
            "description": "An endpoint other than OpenAI's: one `model_providers` entry the\nharness writes as `model_providers.diverge` and selects with\n`model_provider = \"diverge\"`. The entry's `env_key` is\n`OPENAI_API_KEY`, the vault's, and its `wire_api` is `responses`,\nthe one value the configuration reference names — so the endpoint\nmust speak the Responses API, which the Diverge relay and every\nupstream this protocol routes do.\n\nAbsent, Codex's own `openai` provider is used, with whatever\nlogin the run found. Present, the entry is keyed by the\n`OPENAI_API_KEY` the run found — a ChatGPT login speaks only to\nOpenAI's own backend, and a provider beside one fails as itself,\nin Codex's words.",
            "type": "object",
            "properties": {
              "base_url": {
                "description": "The base URL, `/v1` included: `model_providers.diverge.base_url`.",
                "type": "string"
              }
            },
            "required": [
              "base_url"
            ]
          },
          "Tool": {
            "description": "One tool container the program depends on: everything the caller\nneeds to run it that the image can know, and nothing the caller\nalone can know.\n\nA [`Container`](crate::shared::containers::request::Container) is\nan image, two limits, mounts and arguments. The first four are\nhere, member for member; the mounts are not, because a volume is\nnamed by a `volume_name` in the caller's own listing and a FUSE\nmount is something the caller serves live, and a tool that named\neither would be dictating the caller's storage.",
            "type": "object",
            "properties": {
              "name": {
                "description": "What the program calls it: the caller's handle for the tool\ncontainer, and the label the caller uses if it prefixes the\ntool's tools when it merges lists. Unique in the list. The\nprogram never finds a server by it — it calls tools by whatever\nnames the caller's merged list shows, and knows which image\nserves each by the image under `_meta`, see\n[`shared::mcp`](crate::shared::mcp).",
                "type": "string"
              },
              "image": {
                "description": "The image: a name and a digest, the pair a\n`containers::tools::run` request names.",
                "$ref": "#/$defs/Image"
              },
              "memory": {
                "description": "The memory the tool container needs, in bytes.",
                "type": "integer",
                "format": "uint64",
                "minimum": 0
              },
              "disk": {
                "description": "The bytes the tool container may write, in bytes.",
                "type": "integer",
                "format": "uint64",
                "minimum": 0
              },
              "arguments": {
                "description": "The arguments the tool container is registered with — the\n`arguments` of the run request that makes it, verbatim."
              }
            },
            "required": [
              "name",
              "image",
              "memory",
              "disk",
              "arguments"
            ]
          },
          "Image": {
            "description": "The image, by its repository path and its manifest digest — the\npair [`images::check`](crate::endpoints::images::check::client::request::Frame)\nasks about, so a check that came back available names an image a\nrun can ask for, with nothing to translate between them.\n\n# The digest is the image\n\nA digest cannot be repointed at different content, so an image\nmeans the same bytes every time, wherever they come from. That is\nthe whole of what the wire asserts: the provider runs the image\nthe digest names and no other.\n\n# Who supplies the bytes is the provider's\n\nNot on the wire. A provider may hold the image already, pull it\nfrom a registry it uses, or take it from the caller — asking on\nthe run scope whether the caller holds it and, when it does, for\nits manifest and blobs, see [`oci`](crate::shared::containers::oci).\nWhich of those, in what order, and which registries, is the\nprovider's policy, and a caller learns only whether the run\nhappened. A caller need not hold what it names; one that does\nanswers so when asked.",
            "type": "object",
            "properties": {
              "name": {
                "description": "The repository path — `library/nginx`, `myorg/myimage`. No\nhost: where the bytes come from is not the caller's to say.\n\nIt lands in a reference by concatenation wherever a provider\npulls from, so a provider refuses one that is not a repository\npath and normalizes nothing.",
                "type": "string"
              },
              "digest": {
                "description": "The manifest digest, `<algorithm>:<hex>`.\n\nWhat actually identifies the image: a runtime hashes what it\npulls, so a source serving other bytes under it fails before\nanything runs.",
                "type": "string"
              }
            },
            "required": [
              "name",
              "digest"
            ]
          }
        }
      }
    },
    {
      "key": "hermes",
      "image_name": "diverge-agentic-loop-hermes",
      "digest": "unbuilt",
      "schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "title": "Agent",
        "description": "An agent running against Hermes (Nous Research's agent harness).\n\nEvery field here is HONORABLE: it names a mechanism the container\nactually has — a `config.yaml` key its harness writes before\nHermes starts, or a per-request field on the run it drives — and\nnothing else made the cut. What Hermes cannot be told through its\nwire is not here: no skill preloading (skills arrive as mounts\nunder `/root/.hermes/external-skills/`, one directory per skill\nholding its `SKILL.md`, and Hermes indexes them itself — that\npath, not its own `skills/`, which it writes into), no\ntemperature (Hermes has no\nmain-agent sampling key), no thinking-token budget (the\n[`effort`](Self::effort) ladder is the whole reasoning\nvocabulary).",
        "type": "object",
        "properties": {
          "provider": {
            "description": "The inference source, credentials and all — Hermes is\nmulti-provider by design, this picks which, and the pick\ncarries its own auth as arguments. See [`Provider`].",
            "$ref": "#/$defs/Provider"
          },
          "model": {
            "description": "The model to run, in the provider's own naming.",
            "type": "string"
          },
          "system_prompt": {
            "description": "The system prompt, applied per request.",
            "type": [
              "string",
              "null"
            ]
          },
          "effort": {
            "description": "Reasoning effort. Absent = the provider's own default;\n[`none`](Effort::None) = explicitly disabled — Hermes's own\ndisable sentinel, which is why absence and `none` are\ndifferent facts. See [`Effort`].",
            "anyOf": [
              {
                "$ref": "#/$defs/Effort"
              },
              {
                "type": "null"
              }
            ]
          },
          "toolsets": {
            "description": "Hermes's LOCAL capabilities, one switch each: an\n`Option<bool>` where a tool takes nothing, that tool's own\nargument structure where it does — absent is off either way,\nand the whole structure absent is everything off. (The\ncaller's MCP tools ride beside these regardless, and the tool\ncontainers the agent asks for are\n[`mcp_tools`](Self::mcp_tools).) See [`Toolsets`].",
            "$ref": "#/$defs/Toolsets",
            "default": {}
          },
          "mcp_tools": {
            "description": "The tool containers this agent depends on, each one the caller\nruns and serves to it as an MCP server, in the form the\nprovider's wire defines. Passed back whole as the registration's\nanswer, which is how the caller learns of them. Absent is none.",
            "type": "array",
            "items": {
              "$ref": "#/$defs/Tool"
            }
          }
        },
        "required": [
          "provider",
          "model"
        ],
        "$defs": {
          "Provider": {
            "description": "The inference source, credentials and all.\n\nUntagged, discriminated by each variant's own `provider` constant\n— the same discipline the [`Agent`](super::super::super::Agent)\nunion uses for `upstream`. One variant per provider because the\nargument sets genuinely differ: most are a single key, but\nbedrock is AWS credentials, vertex is a service-account document,\nazure-foundry and custom carry their endpoints — and each key\napplies through a different mechanism the variant's own docs\nspell out.\n\nCLOSED at the pin, deliberately: user plugins can extend Hermes's\nregistry, but the container runs stock Hermes, so the bundled set\nis the whole vocabulary — minus `copilot-acp`, whose auth is not\nHermes's to hold (see [the module](super)). Rotating-OAuth\nproviders carry their state as RESOURCES rather than plain\nvalues; the module doc says what that means.\n\nHermes's own `auto` — provider detection from whatever\ncredentials happen to be present — is deliberately NOT here, and\nunder auth-as-arguments it is not even coherent: the request\ncarries exactly one provider's credentials, so there is nothing\nto detect.",
            "anyOf": [
              {
                "description": "See [`actual::Provider`](super::actual::Provider).",
                "$ref": "#/$defs/Provider2"
              },
              {
                "description": "See [`ai_gateway::Provider`](super::ai_gateway::Provider).",
                "$ref": "#/$defs/Provider3"
              },
              {
                "description": "See [`alibaba::Provider`](super::alibaba::Provider).",
                "$ref": "#/$defs/Provider4"
              },
              {
                "description": "See [`alibaba_coding_plan::Provider`](super::alibaba_coding_plan::Provider).",
                "$ref": "#/$defs/Provider5"
              },
              {
                "description": "See [`anthropic::Provider`](super::anthropic::Provider).",
                "$ref": "#/$defs/Provider6"
              },
              {
                "description": "See [`arcee::Provider`](super::arcee::Provider).",
                "$ref": "#/$defs/Provider7"
              },
              {
                "description": "See [`azure_foundry::Provider`](super::azure_foundry::Provider).",
                "$ref": "#/$defs/Provider8"
              },
              {
                "description": "See [`bedrock::Provider`](super::bedrock::Provider).",
                "$ref": "#/$defs/Provider9"
              },
              {
                "description": "See [`commandcode::Provider`](super::commandcode::Provider).",
                "$ref": "#/$defs/Provider10"
              },
              {
                "description": "See [`commandcode_anthropic::Provider`](super::commandcode_anthropic::Provider).",
                "$ref": "#/$defs/Provider11"
              },
              {
                "description": "See [`copilot::Provider`](super::copilot::Provider).",
                "$ref": "#/$defs/Provider12"
              },
              {
                "description": "See [`custom::Provider`](super::custom::Provider).",
                "$ref": "#/$defs/Provider13"
              },
              {
                "description": "See [`deepinfra::Provider`](super::deepinfra::Provider).",
                "$ref": "#/$defs/Provider14"
              },
              {
                "description": "See [`deepseek::Provider`](super::deepseek::Provider).",
                "$ref": "#/$defs/Provider15"
              },
              {
                "description": "See [`fireworks::Provider`](super::fireworks::Provider).",
                "$ref": "#/$defs/Provider16"
              },
              {
                "description": "See [`gemini::Provider`](super::gemini::Provider).",
                "$ref": "#/$defs/Provider17"
              },
              {
                "description": "See [`gmi::Provider`](super::gmi::Provider).",
                "$ref": "#/$defs/Provider18"
              },
              {
                "description": "See [`huggingface::Provider`](super::huggingface::Provider).",
                "$ref": "#/$defs/Provider19"
              },
              {
                "description": "See [`kilocode::Provider`](super::kilocode::Provider).",
                "$ref": "#/$defs/Provider20"
              },
              {
                "description": "See [`kimi_coding::Provider`](super::kimi_coding::Provider).",
                "$ref": "#/$defs/Provider21"
              },
              {
                "description": "See [`kimi_coding_cn::Provider`](super::kimi_coding_cn::Provider).",
                "$ref": "#/$defs/Provider22"
              },
              {
                "description": "See [`meta_ai::Provider`](super::meta_ai::Provider).",
                "$ref": "#/$defs/Provider23"
              },
              {
                "description": "See [`minimax::Provider`](super::minimax::Provider).",
                "$ref": "#/$defs/Provider24"
              },
              {
                "description": "See [`minimax_cn::Provider`](super::minimax_cn::Provider).",
                "$ref": "#/$defs/Provider25"
              },
              {
                "description": "See [`minimax_oauth::Provider`](super::minimax_oauth::Provider).",
                "$ref": "#/$defs/Provider26"
              },
              {
                "description": "See [`nebius_token_factory::Provider`](super::nebius_token_factory::Provider).",
                "$ref": "#/$defs/Provider27"
              },
              {
                "description": "See [`nous::Provider`](super::nous::Provider).",
                "$ref": "#/$defs/Provider28"
              },
              {
                "description": "See [`novita::Provider`](super::novita::Provider).",
                "$ref": "#/$defs/Provider29"
              },
              {
                "description": "See [`nvidia::Provider`](super::nvidia::Provider).",
                "$ref": "#/$defs/Provider30"
              },
              {
                "description": "See [`ollama_cloud::Provider`](super::ollama_cloud::Provider).",
                "$ref": "#/$defs/Provider31"
              },
              {
                "description": "See [`openai_codex::Provider`](super::openai_codex::Provider).",
                "$ref": "#/$defs/Provider32"
              },
              {
                "description": "See [`opencode_free::Provider`](super::opencode_free::Provider).",
                "$ref": "#/$defs/Provider33"
              },
              {
                "description": "See [`opencode_go::Provider`](super::opencode_go::Provider).",
                "$ref": "#/$defs/Provider34"
              },
              {
                "description": "See [`opencode_zen::Provider`](super::opencode_zen::Provider).",
                "$ref": "#/$defs/Provider35"
              },
              {
                "description": "See [`openrouter::Provider`](super::openrouter::Provider).",
                "$ref": "#/$defs/Provider36"
              },
              {
                "description": "See [`qwen_oauth::Provider`](super::qwen_oauth::Provider).",
                "$ref": "#/$defs/Provider37"
              },
              {
                "description": "See [`router::Provider`](super::router::Provider).",
                "$ref": "#/$defs/Provider38"
              },
              {
                "description": "See [`stepfun::Provider`](super::stepfun::Provider).",
                "$ref": "#/$defs/Provider39"
              },
              {
                "description": "See [`upstage::Provider`](super::upstage::Provider).",
                "$ref": "#/$defs/Provider40"
              },
              {
                "description": "See [`vertex::Provider`](super::vertex::Provider).",
                "$ref": "#/$defs/Provider41"
              },
              {
                "description": "See [`xai::Provider`](super::xai::Provider).",
                "$ref": "#/$defs/Provider42"
              },
              {
                "description": "See [`xiaomi::Provider`](super::xiaomi::Provider).",
                "$ref": "#/$defs/Provider43"
              },
              {
                "description": "See [`zai::Provider`](super::zai::Provider).",
                "$ref": "#/$defs/Provider44"
              }
            ]
          },
          "Provider2": {
            "description": "Actual Computer.\n\nAPPLICATION: the harness sets `ACTUAL_API_KEY` to\n[`api_key`](Self::api_key) in the gateway's process environment\nbefore Hermes starts.",
            "type": "object",
            "properties": {
              "provider": {
                "description": "The discriminator. Always `actual`.",
                "$ref": "#/$defs/Actual"
              },
              "api_key": {
                "description": "The API key, applied as `ACTUAL_API_KEY`.",
                "type": "string"
              }
            },
            "required": [
              "provider",
              "api_key"
            ]
          },
          "Actual": {
            "description": "[`Provider`]'s discriminator.\n\nOne variant, and the reason the [union](super::Provider) can be\nuntagged: no other provider's arguments can produce this value.",
            "type": "string",
            "enum": [
              "actual"
            ]
          },
          "Provider3": {
            "description": "Vercel AI Gateway.\n\nAPPLICATION: the harness sets `AI_GATEWAY_API_KEY` to\n[`api_key`](Self::api_key) in the gateway's process environment\nbefore Hermes starts.",
            "type": "object",
            "properties": {
              "provider": {
                "description": "The discriminator. Always `ai-gateway`.",
                "$ref": "#/$defs/AiGateway"
              },
              "api_key": {
                "description": "The API key, applied as `AI_GATEWAY_API_KEY`.",
                "type": "string"
              }
            },
            "required": [
              "provider",
              "api_key"
            ]
          },
          "AiGateway": {
            "description": "[`Provider`]'s discriminator.\n\nOne variant, and the reason the [union](super::Provider) can be\nuntagged: no other provider's arguments can produce this value.",
            "type": "string",
            "enum": [
              "ai-gateway"
            ]
          },
          "Provider4": {
            "description": "Alibaba Cloud (DashScope).\n\nAPPLICATION: the harness sets `DASHSCOPE_API_KEY` to\n[`api_key`](Self::api_key) in the gateway's process environment\nbefore Hermes starts.",
            "type": "object",
            "properties": {
              "provider": {
                "description": "The discriminator. Always `alibaba`.",
                "$ref": "#/$defs/Alibaba"
              },
              "api_key": {
                "description": "The API key, applied as `DASHSCOPE_API_KEY`.",
                "type": "string"
              }
            },
            "required": [
              "provider",
              "api_key"
            ]
          },
          "Alibaba": {
            "description": "[`Provider`]'s discriminator.\n\nOne variant, and the reason the [union](super::Provider) can be\nuntagged: no other provider's arguments can produce this value.",
            "type": "string",
            "enum": [
              "alibaba"
            ]
          },
          "Provider5": {
            "description": "Alibaba Cloud's dedicated coding tier.\n\nAPPLICATION: the harness sets `ALIBABA_CODING_PLAN_API_KEY` to\n[`api_key`](Self::api_key) in the gateway's process environment\nbefore Hermes starts.\n(`DASHSCOPE_API_KEY` is an accepted fallback in Hermes; the\nspecific var is the one set.)",
            "type": "object",
            "properties": {
              "provider": {
                "description": "The discriminator. Always `alibaba-coding-plan`.",
                "$ref": "#/$defs/AlibabaCodingPlan"
              },
              "api_key": {
                "description": "The API key, applied as `ALIBABA_CODING_PLAN_API_KEY`.",
                "type": "string"
              }
            },
            "required": [
              "provider",
              "api_key"
            ]
          },
          "AlibabaCodingPlan": {
            "description": "[`Provider`]'s discriminator.\n\nOne variant, and the reason the [union](super::Provider) can be\nuntagged: no other provider's arguments can produce this value.",
            "type": "string",
            "enum": [
              "alibaba-coding-plan"
            ]
          },
          "Provider6": {
            "description": "Anthropic, over the Messages API.\n\nAPPLICATION: the harness sets `ANTHROPIC_API_KEY` to\n[`api_key`](Self::api_key) in the gateway's process environment\nbefore Hermes starts.",
            "type": "object",
            "properties": {
              "provider": {
                "description": "The discriminator. Always `anthropic`.",
                "$ref": "#/$defs/Anthropic"
              },
              "api_key": {
                "description": "The API key, applied as `ANTHROPIC_API_KEY`.",
                "type": "string"
              }
            },
            "required": [
              "provider",
              "api_key"
            ]
          },
          "Anthropic": {
            "description": "[`Provider`]'s discriminator.\n\nOne variant, and the reason the [union](super::Provider) can be\nuntagged: no other provider's arguments can produce this value.",
            "type": "string",
            "enum": [
              "anthropic"
            ]
          },
          "Provider7": {
            "description": "Arcee AI.\n\nAPPLICATION: the harness sets `ARCEEAI_API_KEY` to\n[`api_key`](Self::api_key) in the gateway's process environment\nbefore Hermes starts.",
            "type": "object",
            "properties": {
              "provider": {
                "description": "The discriminator. Always `arcee`.",
                "$ref": "#/$defs/Arcee"
              },
              "api_key": {
                "description": "The API key, applied as `ARCEEAI_API_KEY`.",
                "type": "string"
              }
            },
            "required": [
              "provider",
              "api_key"
            ]
          },
          "Arcee": {
            "description": "[`Provider`]'s discriminator.\n\nOne variant, and the reason the [union](super::Provider) can be\nuntagged: no other provider's arguments can produce this value.",
            "type": "string",
            "enum": [
              "arcee"
            ]
          },
          "Provider8": {
            "description": "Microsoft Azure Foundry — a caller-supplied deployment, so the\nendpoint is as much the credential as the key is.\n\nAPPLICATION: the harness sets `AZURE_FOUNDRY_API_KEY` to\n[`api_key`](Self::api_key) and `AZURE_FOUNDRY_BASE_URL` to\n[`base_url`](Self::base_url) in the gateway's process environment\nbefore Hermes starts. Both are required: Hermes raises a hard\nauth error on a missing base URL. The Entra-ID mode is\ndeliberately absent — ambient Azure identity is not an argument.",
            "type": "object",
            "properties": {
              "provider": {
                "description": "The discriminator. Always `azure-foundry`.",
                "$ref": "#/$defs/AzureFoundry"
              },
              "api_key": {
                "description": "The API key, applied as `AZURE_FOUNDRY_API_KEY`.",
                "type": "string"
              },
              "base_url": {
                "description": "The deployment's endpoint, applied as\n`AZURE_FOUNDRY_BASE_URL`.",
                "type": "string"
              }
            },
            "required": [
              "provider",
              "api_key",
              "base_url"
            ]
          },
          "AzureFoundry": {
            "description": "[`Provider`]'s discriminator.\n\nOne variant, and the reason the [union](super::Provider) can be\nuntagged: no other provider's arguments can produce this value.",
            "type": "string",
            "enum": [
              "azure-foundry"
            ]
          },
          "Provider9": {
            "description": "AWS Bedrock — AWS credentials, not a provider key.\n\nAPPLICATION: the harness sets the AWS SDK's own env vars in the\ngateway's process environment before Hermes starts — see\n[`Auth`] for which — plus `AWS_REGION` to\n[`region`](Self::region) when present. These must be REAL\nprocess env: the botocore chain underneath never reads Hermes's\ndotenv.",
            "type": "object",
            "properties": {
              "provider": {
                "description": "The discriminator. Always `bedrock`.",
                "$ref": "#/$defs/Bedrock"
              },
              "auth": {
                "description": "The credential itself. See [`Auth`].",
                "$ref": "#/$defs/Auth"
              },
              "region": {
                "description": "The region, applied as `AWS_REGION`; absent = Hermes's own\ndefault (`us-east-1`).",
                "type": [
                  "string",
                  "null"
                ]
              }
            },
            "required": [
              "provider",
              "auth"
            ]
          },
          "Bedrock": {
            "description": "[`Provider`]'s discriminator.\n\nOne variant, and the reason the [union](super::Provider) can be\nuntagged: no other provider's arguments can produce this value.",
            "type": "string",
            "enum": [
              "bedrock"
            ]
          },
          "Auth": {
            "description": "One of AWS's two static credential shapes. Untagged — the field\nnames are disjoint, so the shape is its own discriminator.\n\nThe rest of the botocore chain (profiles, container/IMDS roles,\nweb identity) is deliberately absent: those are facts about a\nmachine, and a request has no machine.",
            "anyOf": [
              {
                "description": "A Bedrock API key, applied as `AWS_BEARER_TOKEN_BEDROCK` —\nthe first credential the chain checks.",
                "type": "object",
                "properties": {
                  "bearer_token": {
                    "description": "The key.",
                    "type": "string"
                  }
                },
                "required": [
                  "bearer_token"
                ]
              },
              {
                "description": "Static SigV4 credentials, applied as `AWS_ACCESS_KEY_ID`,\n`AWS_SECRET_ACCESS_KEY` and, when present,\n`AWS_SESSION_TOKEN`.",
                "type": "object",
                "properties": {
                  "access_key_id": {
                    "description": "The access key id.",
                    "type": "string"
                  },
                  "secret_access_key": {
                    "description": "The secret access key.",
                    "type": "string"
                  },
                  "session_token": {
                    "description": "The session token, for temporary credentials.",
                    "type": [
                      "string",
                      "null"
                    ]
                  }
                },
                "required": [
                  "access_key_id",
                  "secret_access_key"
                ]
              }
            ]
          },
          "Provider10": {
            "description": "CommandCode.\n\nAPPLICATION: the harness sets `COMMANDCODE_API_KEY` to\n[`api_key`](Self::api_key) in the gateway's process environment\nbefore Hermes starts.",
            "type": "object",
            "properties": {
              "provider": {
                "description": "The discriminator. Always `commandcode`.",
                "$ref": "#/$defs/Commandcode"
              },
              "api_key": {
                "description": "The API key, applied as `COMMANDCODE_API_KEY`.",
                "type": "string"
              }
            },
            "required": [
              "provider",
              "api_key"
            ]
          },
          "Commandcode": {
            "description": "[`Provider`]'s discriminator.\n\nOne variant, and the reason the [union](super::Provider) can be\nuntagged: no other provider's arguments can produce this value.",
            "type": "string",
            "enum": [
              "commandcode"
            ]
          },
          "Provider11": {
            "description": "CommandCode's Anthropic-dialect route.\n\nAPPLICATION: the harness sets `COMMANDCODE_API_KEY` to\n[`api_key`](Self::api_key) in the gateway's process environment\nbefore Hermes starts.\n(The same key as [`commandcode`](super::commandcode::Provider)\n— one credential, two wires.)",
            "type": "object",
            "properties": {
              "provider": {
                "description": "The discriminator. Always `commandcode-anthropic`.",
                "$ref": "#/$defs/CommandcodeAnthropic"
              },
              "api_key": {
                "description": "The API key, applied as `COMMANDCODE_API_KEY`.",
                "type": "string"
              }
            },
            "required": [
              "provider",
              "api_key"
            ]
          },
          "CommandcodeAnthropic": {
            "description": "[`Provider`]'s discriminator.\n\nOne variant, and the reason the [union](super::Provider) can be\nuntagged: no other provider's arguments can produce this value.",
            "type": "string",
            "enum": [
              "commandcode-anthropic"
            ]
          },
          "Provider12": {
            "description": "GitHub Copilot (token exchange).\n\nAPPLICATION: the harness sets `COPILOT_GITHUB_TOKEN` to\n[`github_token`](Self::github_token) in the gateway's process\nenvironment before Hermes starts; Hermes performs the\nGitHub-token → Copilot-api-token exchange on its own, and setting\nthe var also suppresses its `gh auth token` fallback.",
            "type": "object",
            "properties": {
              "provider": {
                "description": "The discriminator. Always `copilot`.",
                "$ref": "#/$defs/Copilot"
              },
              "github_token": {
                "description": "The GitHub token the exchange starts from, applied as\n`COPILOT_GITHUB_TOKEN`. Must be an OAuth or fine-grained\ntoken (`gho_`/`ghu_`/`github_pat_`) — Hermes rejects classic\n`ghp_` PATs.",
                "type": "string"
              }
            },
            "required": [
              "provider",
              "github_token"
            ]
          },
          "Copilot": {
            "description": "[`Provider`]'s discriminator.\n\nOne variant, and the reason the [union](super::Provider) can be\nuntagged: no other provider's arguments can produce this value.",
            "type": "string",
            "enum": [
              "copilot"
            ]
          },
          "Provider13": {
            "description": "Any OpenAI-compatible endpoint the caller names — the local\nruntimes' home (ollama, vllm, llama.cpp all alias onto it in\nHermes itself).\n\nAPPLICATION: the harness sets `CUSTOM_BASE_URL` to\n[`base_url`](Self::base_url) in the gateway's process environment\nbefore Hermes starts, and writes the same URL as `model.base_url`\nin the config it already owns, beside `model.api_key` when\n[`api_key`](Self::api_key) is present — Hermes reads that key only\nnext to a configured URL. With no key, Hermes supplies its own\n`no-key-required` placeholder — the keyless case is the endpoint's\nbusiness, not a protocol gap.",
            "type": "object",
            "properties": {
              "provider": {
                "description": "The discriminator. Always `custom`.",
                "$ref": "#/$defs/Custom"
              },
              "base_url": {
                "description": "The endpoint, applied as `CUSTOM_BASE_URL` and `model.base_url`.",
                "type": "string"
              },
              "api_key": {
                "description": "The API key the endpoint expects, if it expects one; applied\nas `model.api_key`.",
                "type": [
                  "string",
                  "null"
                ]
              }
            },
            "required": [
              "provider",
              "base_url"
            ]
          },
          "Custom": {
            "description": "[`Provider`]'s discriminator.\n\nOne variant, and the reason the [union](super::Provider) can be\nuntagged: no other provider's arguments can produce this value.",
            "type": "string",
            "enum": [
              "custom"
            ]
          },
          "Provider14": {
            "description": "DeepInfra.\n\nAPPLICATION: the harness sets `DEEPINFRA_API_KEY` to\n[`api_key`](Self::api_key) in the gateway's process environment\nbefore Hermes starts.",
            "type": "object",
            "properties": {
              "provider": {
                "description": "The discriminator. Always `deepinfra`.",
                "$ref": "#/$defs/Deepinfra"
              },
              "api_key": {
                "description": "The API key, applied as `DEEPINFRA_API_KEY`.",
                "type": "string"
              }
            },
            "required": [
              "provider",
              "api_key"
            ]
          },
          "Deepinfra": {
            "description": "[`Provider`]'s discriminator.\n\nOne variant, and the reason the [union](super::Provider) can be\nuntagged: no other provider's arguments can produce this value.",
            "type": "string",
            "enum": [
              "deepinfra"
            ]
          },
          "Provider15": {
            "description": "DeepSeek.\n\nAPPLICATION: the harness sets `DEEPSEEK_API_KEY` to\n[`api_key`](Self::api_key) in the gateway's process environment\nbefore Hermes starts.",
            "type": "object",
            "properties": {
              "provider": {
                "description": "The discriminator. Always `deepseek`.",
                "$ref": "#/$defs/Deepseek"
              },
              "api_key": {
                "description": "The API key, applied as `DEEPSEEK_API_KEY`.",
                "type": "string"
              }
            },
            "required": [
              "provider",
              "api_key"
            ]
          },
          "Deepseek": {
            "description": "[`Provider`]'s discriminator.\n\nOne variant, and the reason the [union](super::Provider) can be\nuntagged: no other provider's arguments can produce this value.",
            "type": "string",
            "enum": [
              "deepseek"
            ]
          },
          "Provider16": {
            "description": "Fireworks AI.\n\nAPPLICATION: the harness sets `FIREWORKS_API_KEY` to\n[`api_key`](Self::api_key) in the gateway's process environment\nbefore Hermes starts.",
            "type": "object",
            "properties": {
              "provider": {
                "description": "The discriminator. Always `fireworks`.",
                "$ref": "#/$defs/Fireworks"
              },
              "api_key": {
                "description": "The API key, applied as `FIREWORKS_API_KEY`.",
                "type": "string"
              }
            },
            "required": [
              "provider",
              "api_key"
            ]
          },
          "Fireworks": {
            "description": "[`Provider`]'s discriminator.\n\nOne variant, and the reason the [union](super::Provider) can be\nuntagged: no other provider's arguments can produce this value.",
            "type": "string",
            "enum": [
              "fireworks"
            ]
          },
          "Provider17": {
            "description": "Google Gemini (AI Studio).\n\nAPPLICATION: the harness sets `GEMINI_API_KEY` to\n[`api_key`](Self::api_key) in the gateway's process environment\nbefore Hermes starts.\nThe harness must NOT set `GOOGLE_API_KEY`, which Hermes\nchecks first.",
            "type": "object",
            "properties": {
              "provider": {
                "description": "The discriminator. Always `gemini`.",
                "$ref": "#/$defs/Gemini"
              },
              "api_key": {
                "description": "The API key, applied as `GEMINI_API_KEY`.",
                "type": "string"
              }
            },
            "required": [
              "provider",
              "api_key"
            ]
          },
          "Gemini": {
            "description": "[`Provider`]'s discriminator.\n\nOne variant, and the reason the [union](super::Provider) can be\nuntagged: no other provider's arguments can produce this value.",
            "type": "string",
            "enum": [
              "gemini"
            ]
          },
          "Provider18": {
            "description": "GMI Cloud.\n\nAPPLICATION: the harness sets `GMI_API_KEY` to\n[`api_key`](Self::api_key) in the gateway's process environment\nbefore Hermes starts.",
            "type": "object",
            "properties": {
              "provider": {
                "description": "The discriminator. Always `gmi`.",
                "$ref": "#/$defs/Gmi"
              },
              "api_key": {
                "description": "The API key, applied as `GMI_API_KEY`.",
                "type": "string"
              }
            },
            "required": [
              "provider",
              "api_key"
            ]
          },
          "Gmi": {
            "description": "[`Provider`]'s discriminator.\n\nOne variant, and the reason the [union](super::Provider) can be\nuntagged: no other provider's arguments can produce this value.",
            "type": "string",
            "enum": [
              "gmi"
            ]
          },
          "Provider19": {
            "description": "HuggingFace's inference router.\n\nAPPLICATION: the harness sets `HF_TOKEN` to\n[`api_key`](Self::api_key) in the gateway's process environment\nbefore Hermes starts.",
            "type": "object",
            "properties": {
              "provider": {
                "description": "The discriminator. Always `huggingface`.",
                "$ref": "#/$defs/Huggingface"
              },
              "api_key": {
                "description": "The API key, applied as `HF_TOKEN`.",
                "type": "string"
              }
            },
            "required": [
              "provider",
              "api_key"
            ]
          },
          "Huggingface": {
            "description": "[`Provider`]'s discriminator.\n\nOne variant, and the reason the [union](super::Provider) can be\nuntagged: no other provider's arguments can produce this value.",
            "type": "string",
            "enum": [
              "huggingface"
            ]
          },
          "Provider20": {
            "description": "Kilo Code's gateway.\n\nAPPLICATION: the harness sets `KILOCODE_API_KEY` to\n[`api_key`](Self::api_key) in the gateway's process environment\nbefore Hermes starts.",
            "type": "object",
            "properties": {
              "provider": {
                "description": "The discriminator. Always `kilocode`.",
                "$ref": "#/$defs/Kilocode"
              },
              "api_key": {
                "description": "The API key, applied as `KILOCODE_API_KEY`.",
                "type": "string"
              }
            },
            "required": [
              "provider",
              "api_key"
            ]
          },
          "Kilocode": {
            "description": "[`Provider`]'s discriminator.\n\nOne variant, and the reason the [union](super::Provider) can be\nuntagged: no other provider's arguments can produce this value.",
            "type": "string",
            "enum": [
              "kilocode"
            ]
          },
          "Provider21": {
            "description": "Moonshot's Kimi coding tier.\n\nAPPLICATION: the harness sets `KIMI_API_KEY` to\n[`api_key`](Self::api_key) in the gateway's process environment\nbefore Hermes starts.",
            "type": "object",
            "properties": {
              "provider": {
                "description": "The discriminator. Always `kimi-coding`.",
                "$ref": "#/$defs/KimiCoding"
              },
              "api_key": {
                "description": "The API key, applied as `KIMI_API_KEY`.",
                "type": "string"
              }
            },
            "required": [
              "provider",
              "api_key"
            ]
          },
          "KimiCoding": {
            "description": "[`Provider`]'s discriminator.\n\nOne variant, and the reason the [union](super::Provider) can be\nuntagged: no other provider's arguments can produce this value.",
            "type": "string",
            "enum": [
              "kimi-coding"
            ]
          },
          "Provider22": {
            "description": "Moonshot's Kimi coding tier, China endpoint.\n\nAPPLICATION: the harness sets `KIMI_CN_API_KEY` to\n[`api_key`](Self::api_key) in the gateway's process environment\nbefore Hermes starts.",
            "type": "object",
            "properties": {
              "provider": {
                "description": "The discriminator. Always `kimi-coding-cn`.",
                "$ref": "#/$defs/KimiCodingCn"
              },
              "api_key": {
                "description": "The API key, applied as `KIMI_CN_API_KEY`.",
                "type": "string"
              }
            },
            "required": [
              "provider",
              "api_key"
            ]
          },
          "KimiCodingCn": {
            "description": "[`Provider`]'s discriminator.\n\nOne variant, and the reason the [union](super::Provider) can be\nuntagged: no other provider's arguments can produce this value.",
            "type": "string",
            "enum": [
              "kimi-coding-cn"
            ]
          },
          "Provider23": {
            "description": "Meta's Model API (Muse Spark).\n\nAPPLICATION: the harness sets `MODEL_API_KEY` to\n[`api_key`](Self::api_key) in the gateway's process environment\nbefore Hermes starts.",
            "type": "object",
            "properties": {
              "provider": {
                "description": "The discriminator. Always `meta-ai`.",
                "$ref": "#/$defs/MetaAi"
              },
              "api_key": {
                "description": "The API key, applied as `MODEL_API_KEY`.",
                "type": "string"
              }
            },
            "required": [
              "provider",
              "api_key"
            ]
          },
          "MetaAi": {
            "description": "[`Provider`]'s discriminator.\n\nOne variant, and the reason the [union](super::Provider) can be\nuntagged: no other provider's arguments can produce this value.",
            "type": "string",
            "enum": [
              "meta-ai"
            ]
          },
          "Provider24": {
            "description": "MiniMax.\n\nAPPLICATION: the harness sets `MINIMAX_API_KEY` to\n[`api_key`](Self::api_key) in the gateway's process environment\nbefore Hermes starts.",
            "type": "object",
            "properties": {
              "provider": {
                "description": "The discriminator. Always `minimax`.",
                "$ref": "#/$defs/Minimax"
              },
              "api_key": {
                "description": "The API key, applied as `MINIMAX_API_KEY`.",
                "type": "string"
              }
            },
            "required": [
              "provider",
              "api_key"
            ]
          },
          "Minimax": {
            "description": "[`Provider`]'s discriminator.\n\nOne variant, and the reason the [union](super::Provider) can be\nuntagged: no other provider's arguments can produce this value.",
            "type": "string",
            "enum": [
              "minimax"
            ]
          },
          "Provider25": {
            "description": "MiniMax, China endpoint.\n\nAPPLICATION: the harness sets `MINIMAX_CN_API_KEY` to\n[`api_key`](Self::api_key) in the gateway's process environment\nbefore Hermes starts.",
            "type": "object",
            "properties": {
              "provider": {
                "description": "The discriminator. Always `minimax-cn`.",
                "$ref": "#/$defs/MinimaxCn"
              },
              "api_key": {
                "description": "The API key, applied as `MINIMAX_CN_API_KEY`.",
                "type": "string"
              }
            },
            "required": [
              "provider",
              "api_key"
            ]
          },
          "MinimaxCn": {
            "description": "[`Provider`]'s discriminator.\n\nOne variant, and the reason the [union](super::Provider) can be\nuntagged: no other provider's arguments can produce this value.",
            "type": "string",
            "enum": [
              "minimax-cn"
            ]
          },
          "Provider26": {
            "description": "MiniMax over OAuth. Single-use rotating refresh tokens, so the\ncredential is a RESOURCE: the caller provides its current state,\nthe run rotates it, and the rotated state is surfaced back to\nthe caller instead of silently burning their login. (Keyed\nMiniMax is [`minimax`](super::minimax::Provider) /\n[`minimax_cn`](super::minimax_cn::Provider).)\n\nAPPLICATION: the document lives in the vault under\n[`MINIMAX_OAUTH`](diverge_provider_sdk::shared::containers::vault::keys::MINIMAX_OAUTH). The harness locks that\nkey for the run, reads the document, and writes it as the\n`providers.minimax-oauth` entry of `$HERMES_HOME/auth.json`\nbefore Hermes starts — provider selection rides the config the\nharness already owns, never the file's `active_provider`. On\ntoken refresh Hermes rewrites the entry in place; at the run's\nend the rewritten document is set back under the key, and the\nkey unlocked. Nothing about it is an argument: choosing this\nprovider is the whole ask.",
            "type": "object",
            "properties": {
              "provider": {
                "description": "The discriminator. Always `minimax-oauth`.",
                "$ref": "#/$defs/MinimaxOauth"
              }
            },
            "required": [
              "provider"
            ]
          },
          "MinimaxOauth": {
            "description": "[`Provider`]'s discriminator.\n\nOne variant, and the reason the [union](super::Provider) can be\nuntagged: no other provider's arguments can produce this value.",
            "type": "string",
            "enum": [
              "minimax-oauth"
            ]
          },
          "Provider27": {
            "description": "Nebius Token Factory.\n\nAPPLICATION: the harness sets `NEBIUS_API_KEY` to\n[`api_key`](Self::api_key) in the gateway's process environment\nbefore Hermes starts.",
            "type": "object",
            "properties": {
              "provider": {
                "description": "The discriminator. Always `nebius-token-factory`.",
                "$ref": "#/$defs/NebiusTokenFactory"
              },
              "api_key": {
                "description": "The API key, applied as `NEBIUS_API_KEY`.",
                "type": "string"
              }
            },
            "required": [
              "provider",
              "api_key"
            ]
          },
          "NebiusTokenFactory": {
            "description": "[`Provider`]'s discriminator.\n\nOne variant, and the reason the [union](super::Provider) can be\nuntagged: no other provider's arguments can produce this value.",
            "type": "string",
            "enum": [
              "nebius-token-factory"
            ]
          },
          "Provider28": {
            "description": "Nous Research — the Portal, Hermes's home team. OAuth with\nsingle-use rotating refresh tokens, so the credential is a\nRESOURCE: the caller provides its current state, the run rotates\nit, and the rotated state is surfaced back to the caller instead\nof silently burning their login.\n\nAPPLICATION: the document lives in the vault under\n[`NOUS_OAUTH`](diverge_provider_sdk::shared::containers::vault::keys::NOUS_OAUTH). The harness locks that key\nfor the run, reads the document, and writes it as the\n`providers.nous` entry of `$HERMES_HOME/auth.json` before Hermes\nstarts — provider selection rides the config the harness already\nowns, never the file's `active_provider`, which Hermes treats as\na last-resort fallback. On token refresh Hermes rewrites the\nentry in place; at the run's end the rewritten document is set\nback under the key, and the key unlocked. Nothing about it is an\nargument: choosing this provider is the whole ask.",
            "type": "object",
            "properties": {
              "provider": {
                "description": "The discriminator. Always `nous`.",
                "$ref": "#/$defs/Nous"
              }
            },
            "required": [
              "provider"
            ]
          },
          "Nous": {
            "description": "[`Provider`]'s discriminator.\n\nOne variant, and the reason the [union](super::Provider) can be\nuntagged: no other provider's arguments can produce this value.",
            "type": "string",
            "enum": [
              "nous"
            ]
          },
          "Provider29": {
            "description": "NovitaAI.\n\nAPPLICATION: the harness sets `NOVITA_API_KEY` to\n[`api_key`](Self::api_key) in the gateway's process environment\nbefore Hermes starts.",
            "type": "object",
            "properties": {
              "provider": {
                "description": "The discriminator. Always `novita`.",
                "$ref": "#/$defs/Novita"
              },
              "api_key": {
                "description": "The API key, applied as `NOVITA_API_KEY`.",
                "type": "string"
              }
            },
            "required": [
              "provider",
              "api_key"
            ]
          },
          "Novita": {
            "description": "[`Provider`]'s discriminator.\n\nOne variant, and the reason the [union](super::Provider) can be\nuntagged: no other provider's arguments can produce this value.",
            "type": "string",
            "enum": [
              "novita"
            ]
          },
          "Provider30": {
            "description": "NVIDIA NIM.\n\nAPPLICATION: the harness sets `NVIDIA_API_KEY` to\n[`api_key`](Self::api_key) in the gateway's process environment\nbefore Hermes starts.",
            "type": "object",
            "properties": {
              "provider": {
                "description": "The discriminator. Always `nvidia`.",
                "$ref": "#/$defs/Nvidia"
              },
              "api_key": {
                "description": "The API key, applied as `NVIDIA_API_KEY`.",
                "type": "string"
              }
            },
            "required": [
              "provider",
              "api_key"
            ]
          },
          "Nvidia": {
            "description": "[`Provider`]'s discriminator.\n\nOne variant, and the reason the [union](super::Provider) can be\nuntagged: no other provider's arguments can produce this value.",
            "type": "string",
            "enum": [
              "nvidia"
            ]
          },
          "Provider31": {
            "description": "Ollama Cloud (the hosted service, not a local ollama — that is\n[`custom`](super::custom::Provider)).\n\nAPPLICATION: the harness sets `OLLAMA_API_KEY` to\n[`api_key`](Self::api_key) in the gateway's process environment\nbefore Hermes starts.",
            "type": "object",
            "properties": {
              "provider": {
                "description": "The discriminator. Always `ollama-cloud`.",
                "$ref": "#/$defs/OllamaCloud"
              },
              "api_key": {
                "description": "The API key, applied as `OLLAMA_API_KEY`.",
                "type": "string"
              }
            },
            "required": [
              "provider",
              "api_key"
            ]
          },
          "OllamaCloud": {
            "description": "[`Provider`]'s discriminator.\n\nOne variant, and the reason the [union](super::Provider) can be\nuntagged: no other provider's arguments can produce this value.",
            "type": "string",
            "enum": [
              "ollama-cloud"
            ]
          },
          "Provider32": {
            "description": "The ChatGPT Codex backend, over external OAuth. Single-use\nrotating refresh tokens, so the credential is a RESOURCE: the\ncaller provides its current state, the run rotates it, and the\nrotated state is surfaced back to the caller instead of silently\nburning their login.\n\nAPPLICATION: the document lives in the vault under\n[`OPENAI_CODEX_OAUTH`](diverge_provider_sdk::shared::containers::vault::keys::OPENAI_CODEX_OAUTH). The harness\nlocks that key for the run, reads the document, and writes it as\nthe `providers.openai-codex` entry of `$HERMES_HOME/auth.json`\nbefore Hermes starts — provider selection rides the config the\nharness already owns, never the file's `active_provider`. On\ntoken refresh Hermes rewrites the entry in place; at the run's\nend the rewritten document is set back under the key, and the\nkey unlocked. Nothing about it is an argument: choosing this\nprovider is the whole ask.",
            "type": "object",
            "properties": {
              "provider": {
                "description": "The discriminator. Always `openai-codex`.",
                "$ref": "#/$defs/OpenaiCodex"
              }
            },
            "required": [
              "provider"
            ]
          },
          "OpenaiCodex": {
            "description": "[`Provider`]'s discriminator.\n\nOne variant, and the reason the [union](super::Provider) can be\nuntagged: no other provider's arguments can produce this value.",
            "type": "string",
            "enum": [
              "openai-codex"
            ]
          },
          "Provider33": {
            "description": "OpenCode's free tier — keyless by design.\n\nAPPLICATION: nothing. Hermes short-circuits this provider to an\nanonymous placeholder before any credential check, so the marker\nis the whole argument.",
            "type": "object",
            "properties": {
              "provider": {
                "description": "The discriminator. Always `opencode-free`.",
                "$ref": "#/$defs/OpencodeFree"
              }
            },
            "required": [
              "provider"
            ]
          },
          "OpencodeFree": {
            "description": "[`Provider`]'s discriminator.\n\nOne variant, and the reason the [union](super::Provider) can be\nuntagged: no other provider's arguments can produce this value.",
            "type": "string",
            "enum": [
              "opencode-free"
            ]
          },
          "Provider34": {
            "description": "OpenCode Go.\n\nAPPLICATION: the harness sets `OPENCODE_GO_API_KEY` to\n[`api_key`](Self::api_key) in the gateway's process environment\nbefore Hermes starts.",
            "type": "object",
            "properties": {
              "provider": {
                "description": "The discriminator. Always `opencode-go`.",
                "$ref": "#/$defs/OpencodeGo"
              },
              "api_key": {
                "description": "The API key, applied as `OPENCODE_GO_API_KEY`.",
                "type": "string"
              }
            },
            "required": [
              "provider",
              "api_key"
            ]
          },
          "OpencodeGo": {
            "description": "[`Provider`]'s discriminator.\n\nOne variant, and the reason the [union](super::Provider) can be\nuntagged: no other provider's arguments can produce this value.",
            "type": "string",
            "enum": [
              "opencode-go"
            ]
          },
          "Provider35": {
            "description": "OpenCode Zen.\n\nAPPLICATION: the harness sets `OPENCODE_ZEN_API_KEY` to\n[`api_key`](Self::api_key) in the gateway's process environment\nbefore Hermes starts.",
            "type": "object",
            "properties": {
              "provider": {
                "description": "The discriminator. Always `opencode-zen`.",
                "$ref": "#/$defs/OpencodeZen"
              },
              "api_key": {
                "description": "The API key, applied as `OPENCODE_ZEN_API_KEY`.",
                "type": "string"
              }
            },
            "required": [
              "provider",
              "api_key"
            ]
          },
          "OpencodeZen": {
            "description": "[`Provider`]'s discriminator.\n\nOne variant, and the reason the [union](super::Provider) can be\nuntagged: no other provider's arguments can produce this value.",
            "type": "string",
            "enum": [
              "opencode-zen"
            ]
          },
          "Provider36": {
            "description": "OpenRouter.\n\nAPPLICATION: the harness sets `OPENROUTER_API_KEY` to\n[`api_key`](Self::api_key) in the gateway's process environment\nbefore Hermes starts.\nHermes discards keys that do not start with `sk-or-`.",
            "type": "object",
            "properties": {
              "provider": {
                "description": "The discriminator. Always `openrouter`.",
                "$ref": "#/$defs/Openrouter"
              },
              "api_key": {
                "description": "The API key, applied as `OPENROUTER_API_KEY`.",
                "type": "string"
              }
            },
            "required": [
              "provider",
              "api_key"
            ]
          },
          "Openrouter": {
            "description": "[`Provider`]'s discriminator.\n\nOne variant, and the reason the [union](super::Provider) can be\nuntagged: no other provider's arguments can produce this value.",
            "type": "string",
            "enum": [
              "openrouter"
            ]
          },
          "Provider37": {
            "description": "Qwen's portal, over OAuth. Single-use rotating refresh tokens,\nso the credential is a RESOURCE: the caller provides its current\nstate, the run rotates it, and the rotated state is surfaced\nback to the caller instead of silently burning their login.\n\nAPPLICATION: unlike the other OAuth providers this state is not\nHermes's own — it is the Qwen CLI's token file. The document\nlives in the vault under [`QWEN_OAUTH`](diverge_provider_sdk::shared::containers::vault::keys::QWEN_OAUTH); the\nharness locks that key for the run, reads the document, and\nwrites it to `~/.qwen/oauth_creds.json` at the REAL home\ndirectory (Hermes hardcodes that path; it ignores\n`$HERMES_HOME`). On token refresh Hermes rewrites the creds file;\nat the run's end the rewritten document is set back under the\nkey, and the key unlocked. Nothing about it is an argument:\nchoosing this provider is the whole ask.",
            "type": "object",
            "properties": {
              "provider": {
                "description": "The discriminator. Always `qwen-oauth`.",
                "$ref": "#/$defs/QwenOauth"
              }
            },
            "required": [
              "provider"
            ]
          },
          "QwenOauth": {
            "description": "[`Provider`]'s discriminator.\n\nOne variant, and the reason the [union](super::Provider) can be\nuntagged: no other provider's arguments can produce this value.",
            "type": "string",
            "enum": [
              "qwen-oauth"
            ]
          },
          "Provider38": {
            "description": "Ramp Router.\n\nAPPLICATION: the harness sets `RAMP_ROUTER_API_KEY` to\n[`api_key`](Self::api_key) in the gateway's process environment\nbefore Hermes starts.",
            "type": "object",
            "properties": {
              "provider": {
                "description": "The discriminator. Always `router`.",
                "$ref": "#/$defs/Router"
              },
              "api_key": {
                "description": "The API key, applied as `RAMP_ROUTER_API_KEY`.",
                "type": "string"
              }
            },
            "required": [
              "provider",
              "api_key"
            ]
          },
          "Router": {
            "description": "[`Provider`]'s discriminator.\n\nOne variant, and the reason the [union](super::Provider) can be\nuntagged: no other provider's arguments can produce this value.",
            "type": "string",
            "enum": [
              "router"
            ]
          },
          "Provider39": {
            "description": "StepFun's step plan.\n\nAPPLICATION: the harness sets `STEPFUN_API_KEY` to\n[`api_key`](Self::api_key) in the gateway's process environment\nbefore Hermes starts.",
            "type": "object",
            "properties": {
              "provider": {
                "description": "The discriminator. Always `stepfun`.",
                "$ref": "#/$defs/Stepfun"
              },
              "api_key": {
                "description": "The API key, applied as `STEPFUN_API_KEY`.",
                "type": "string"
              }
            },
            "required": [
              "provider",
              "api_key"
            ]
          },
          "Stepfun": {
            "description": "[`Provider`]'s discriminator.\n\nOne variant, and the reason the [union](super::Provider) can be\nuntagged: no other provider's arguments can produce this value.",
            "type": "string",
            "enum": [
              "stepfun"
            ]
          },
          "Provider40": {
            "description": "Upstage Solar.\n\nAPPLICATION: the harness sets `UPSTAGE_API_KEY` to\n[`api_key`](Self::api_key) in the gateway's process environment\nbefore Hermes starts.",
            "type": "object",
            "properties": {
              "provider": {
                "description": "The discriminator. Always `upstage`.",
                "$ref": "#/$defs/Upstage"
              },
              "api_key": {
                "description": "The API key, applied as `UPSTAGE_API_KEY`.",
                "type": "string"
              }
            },
            "required": [
              "provider",
              "api_key"
            ]
          },
          "Upstage": {
            "description": "[`Provider`]'s discriminator.\n\nOne variant, and the reason the [union](super::Provider) can be\nuntagged: no other provider's arguments can produce this value.",
            "type": "string",
            "enum": [
              "upstage"
            ]
          },
          "Provider41": {
            "description": "GCP Vertex AI — a service account, not an API key.\n\nAPPLICATION: the harness writes\n[`service_account_json`](Self::service_account_json) to a file\nand sets `VERTEX_CREDENTIALS_PATH` to it in the gateway's process\nenvironment before Hermes starts; access tokens are minted from\nit per call. `VERTEX_PROJECT_ID` and `VERTEX_REGION` are set from\nthe optional fields when present. Ambient application-default\ncredentials are deliberately absent — those are facts about a\nmachine, and a request has no machine.",
            "type": "object",
            "properties": {
              "provider": {
                "description": "The discriminator. Always `vertex`.",
                "$ref": "#/$defs/Vertex"
              },
              "service_account_json": {
                "description": "The service-account key document, verbatim JSON.",
                "type": "string"
              },
              "project_id": {
                "description": "The project, applied as `VERTEX_PROJECT_ID`; absent = the\nproject named inside the service-account document.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "region": {
                "description": "The region, applied as `VERTEX_REGION`; absent = Hermes's own\ndefault (`global`).",
                "type": [
                  "string",
                  "null"
                ]
              }
            },
            "required": [
              "provider",
              "service_account_json"
            ]
          },
          "Vertex": {
            "description": "[`Provider`]'s discriminator.\n\nOne variant, and the reason the [union](super::Provider) can be\nuntagged: no other provider's arguments can produce this value.",
            "type": "string",
            "enum": [
              "vertex"
            ]
          },
          "Provider42": {
            "description": "xAI.\n\nAPPLICATION: the harness sets `XAI_API_KEY` to\n[`api_key`](Self::api_key) in the gateway's process environment\nbefore Hermes starts.",
            "type": "object",
            "properties": {
              "provider": {
                "description": "The discriminator. Always `xai`.",
                "$ref": "#/$defs/Xai"
              },
              "api_key": {
                "description": "The API key, applied as `XAI_API_KEY`.",
                "type": "string"
              }
            },
            "required": [
              "provider",
              "api_key"
            ]
          },
          "Xai": {
            "description": "[`Provider`]'s discriminator.\n\nOne variant, and the reason the [union](super::Provider) can be\nuntagged: no other provider's arguments can produce this value.",
            "type": "string",
            "enum": [
              "xai"
            ]
          },
          "Provider43": {
            "description": "Xiaomi MiMo.\n\nAPPLICATION: the harness sets `XIAOMI_API_KEY` to\n[`api_key`](Self::api_key) in the gateway's process environment\nbefore Hermes starts.",
            "type": "object",
            "properties": {
              "provider": {
                "description": "The discriminator. Always `xiaomi`.",
                "$ref": "#/$defs/Xiaomi"
              },
              "api_key": {
                "description": "The API key, applied as `XIAOMI_API_KEY`.",
                "type": "string"
              }
            },
            "required": [
              "provider",
              "api_key"
            ]
          },
          "Xiaomi": {
            "description": "[`Provider`]'s discriminator.\n\nOne variant, and the reason the [union](super::Provider) can be\nuntagged: no other provider's arguments can produce this value.",
            "type": "string",
            "enum": [
              "xiaomi"
            ]
          },
          "Provider44": {
            "description": "Z.AI (GLM / Zhipu).\n\nAPPLICATION: the harness sets `GLM_API_KEY` to\n[`api_key`](Self::api_key) in the gateway's process environment\nbefore Hermes starts, and `GLM_BASE_URL` to\n[`base_url`](Self::base_url) when present.\n\nThe optional endpoint is the one such override in the vocabulary,\nand it earns its place: without a pinned base URL, Hermes's first\nuse fires a live probe across four candidate endpoints and caches\nthe winner to disk — a network round-trip at start, guessing at a\nfact the caller knows (which of Z.AI's global and CN surfaces\ntheir key belongs to).",
            "type": "object",
            "properties": {
              "provider": {
                "description": "The discriminator. Always `zai`.",
                "$ref": "#/$defs/Zai"
              },
              "api_key": {
                "description": "The API key, applied as `GLM_API_KEY`.",
                "type": "string"
              },
              "base_url": {
                "description": "The endpoint the key belongs to, applied as `GLM_BASE_URL`;\nabsent = Hermes's own endpoint probe.",
                "type": [
                  "string",
                  "null"
                ]
              }
            },
            "required": [
              "provider",
              "api_key"
            ]
          },
          "Zai": {
            "description": "[`Provider`]'s discriminator.\n\nOne variant, and the reason the [union](super::Provider) can be\nuntagged: no other provider's arguments can produce this value.",
            "type": "string",
            "enum": [
              "zai"
            ]
          },
          "Effort": {
            "description": "Hermes's reasoning ladder, whole:\n`none < minimal < low < medium < high < xhigh < max < ultra`.\n\nDeliberately NOT the five-tier `Effort` the claude_code and\ncodex agents share: a different upstream owns a different\nvocabulary — [`none`](Self::None) and [`ultra`](Self::Ultra)\nexist only here, `none` being Hermes's explicit\nreasoning-disabled sentinel (which is why the agent's field is\nan `Option`: absent means the provider's default, `none` means\noff, and those are different facts). Hermes clamps to each\nwire's supported subset itself, always to the nearest WEAKER\nlevel — a caller never has to know which rungs a given model\nexposes.",
            "oneOf": [
              {
                "description": "Reasoning explicitly disabled.",
                "type": "string",
                "const": "none"
              },
              {
                "description": "Least.",
                "type": "string",
                "const": "minimal"
              },
              {
                "description": "Less.",
                "type": "string",
                "const": "low"
              },
              {
                "description": "Balanced.",
                "type": "string",
                "const": "medium"
              },
              {
                "description": "More.",
                "type": "string",
                "const": "high"
              },
              {
                "description": "Above high, below max.",
                "type": "string",
                "const": "xhigh"
              },
              {
                "description": "As much as the model will spend.",
                "type": "string",
                "const": "max"
              },
              {
                "description": "Above even max, where a wire supports it.",
                "type": "string",
                "const": "ultra"
              }
            ]
          },
          "Toolsets": {
            "description": "Per-toolset switches over Hermes's configurable checklist — the\nlist its own configuration wizard manages, closed at the pin,\nand narrowed to what a REQUEST can actually make work.\n\nTwo field shapes, by whether the toolset takes arguments. A\ntoolset with nothing to configure is an `Option<bool>` where\nABSENT IS OFF — the same as `false` — so nothing is on by\nomission and Hermes's own API-server defaults are never\nconsulted. A toolset with arguments is an `Option` of its own\nstructure — absent is off, present is the switch thrown on WITH\nthat tool's credentials and endpoint facts, each documented in\nits own file with the mechanism the harness applies. Enabling a\ntool without its prerequisites is not a protocol error — the\ntools simply fail as themselves when used.\n\nThese are the agent's LOCAL capabilities: what Hermes itself can\ndo beside the conversation. The caller's MCP tools are not in\nthis vocabulary and never need to be — each configured MCP\nserver becomes its own toolset in Hermes automatically, and the\ncontainer always wires the caller's in. The SKILLS toolset is\nnot here either, on purpose: whether the agent can browse skills\nfollows from whether the request mounted any, not from a switch\nthat could contradict that fact.\n\nWhat is deliberately ABSENT, and why, is [the module](super)'s\nto say.",
            "type": "object",
            "properties": {
              "web": {
                "description": "Web search and page extraction. See [`web`](super::web).",
                "anyOf": [
                  {
                    "$ref": "#/$defs/Toolset"
                  },
                  {
                    "type": "null"
                  }
                ]
              },
              "browser": {
                "description": "Browser automation. See [`browser`](super::browser).",
                "anyOf": [
                  {
                    "$ref": "#/$defs/Toolset2"
                  },
                  {
                    "type": "null"
                  }
                ]
              },
              "terminal": {
                "description": "Terminal and process control. Nothing to configure — the\ncontainer is the sandbox, and the local backend reads no\ncredentials.",
                "type": [
                  "boolean",
                  "null"
                ]
              },
              "file": {
                "description": "File operations: read, write, patch, search. Nothing to\nconfigure.",
                "type": [
                  "boolean",
                  "null"
                ]
              },
              "code_execution": {
                "description": "Code execution. Nothing of its own to configure — scripts\ncall other tools over RPC, and each called tool's own\narguments apply transitively.",
                "type": [
                  "boolean",
                  "null"
                ]
              },
              "vision": {
                "description": "Image analysis. Nothing to configure — analysis rides the\nrun's own inference provider.",
                "type": [
                  "boolean",
                  "null"
                ]
              },
              "video": {
                "description": "Video analysis. Nothing to configure — it rides the run's\nown inference provider, exactly as vision does.",
                "type": [
                  "boolean",
                  "null"
                ]
              },
              "image_gen": {
                "description": "Image generation. See [`image_gen`](super::image_gen).",
                "anyOf": [
                  {
                    "$ref": "#/$defs/Toolset3"
                  },
                  {
                    "type": "null"
                  }
                ]
              },
              "video_gen": {
                "description": "Video generation. See [`video_gen`](super::video_gen).",
                "anyOf": [
                  {
                    "$ref": "#/$defs/Toolset4"
                  },
                  {
                    "type": "null"
                  }
                ]
              },
              "x_search": {
                "description": "X (Twitter) search. See [`x_search`](super::x_search).",
                "anyOf": [
                  {
                    "$ref": "#/$defs/Toolset5"
                  },
                  {
                    "type": "null"
                  }
                ]
              },
              "tts": {
                "description": "Text-to-speech. See [`tts`](super::tts).",
                "anyOf": [
                  {
                    "$ref": "#/$defs/Toolset6"
                  },
                  {
                    "type": "null"
                  }
                ]
              },
              "todo": {
                "description": "Task planning (todo). Nothing to configure — the store is\nin-memory and the run's own.",
                "type": [
                  "boolean",
                  "null"
                ]
              },
              "session_search": {
                "description": "Searching past conversations. Nothing to configure — the\nlocal session database, credential-free. The database rides\nthe continuation whole, so the searchable past is the\nlineage's, not just this run's.",
                "type": [
                  "boolean",
                  "null"
                ]
              },
              "memory": {
                "description": "Hermes's own memory: the two capped files, `MEMORY.md` (the\nagent's notes) and `USER.md` (the user profile), injected into\nthe system prompt at session start and edited by the `memory`\ntool. Nothing to configure — and the files ride the\ncontinuation, so what the tool writes is what the next run\nstarts with. (External memory-provider plugins are a separate\nconfig switch the harness leaves off.)",
                "type": [
                  "boolean",
                  "null"
                ]
              },
              "homeassistant": {
                "description": "Home Assistant control. See\n[`homeassistant`](super::homeassistant).",
                "anyOf": [
                  {
                    "$ref": "#/$defs/Toolset7"
                  },
                  {
                    "type": "null"
                  }
                ]
              },
              "spotify": {
                "description": "Spotify control. See [`spotify`](super::spotify).",
                "anyOf": [
                  {
                    "$ref": "#/$defs/Toolset8"
                  },
                  {
                    "type": "null"
                  }
                ]
              }
            }
          },
          "Toolset": {
            "description": "Web search and page extraction, with its arguments. The field\nbeing absent from [`Toolsets`](super::Toolsets) is Hermes's own\ndefault for this toolset; present is the switch thrown on.\n\nTwo roles, two slots: [`search`](Self::search) is what answers\nqueries, [`extract`](Self::extract) is what turns a page into\ntext — different tools, different backends, independently\nkeyed. Each slot names ONE backend and carries its credential;\na slot left absent runs on Hermes's keyless free-tier ring,\nwhich needs nothing — the keys buy quality and quota, not\nexistence. Every combination is coherent, the empty object\nincluded.",
            "type": "object",
            "properties": {
              "search": {
                "description": "The search backend, one of [`Search`]'s; absent = the\nkeyless ring.",
                "anyOf": [
                  {
                    "$ref": "#/$defs/Search"
                  },
                  {
                    "type": "null"
                  }
                ]
              },
              "extract": {
                "description": "The extraction backend, one of [`Extract`]'s; absent = the\nkeyless ring.",
                "anyOf": [
                  {
                    "$ref": "#/$defs/Extract"
                  },
                  {
                    "type": "null"
                  }
                ]
              }
            }
          },
          "Search": {
            "description": "One search backend, named by the credential it takes. Untagged —\nevery variant carries exactly one distinctly-named field, so the\nfield name is the discriminator and no marker is needed.\n\nOne backend, deliberately: Hermes can pool several keys as\nfallbacks for each other, but a vocabulary where extra keys\nquietly change behavior is the environment-dependent meaning\nthis module keeps rejecting. The caller names the backend; the\nkeyless ring remains the fallback story. The harness applies\nthe variant's env var in the gateway's process environment and\npins `web.search_backend` to the named provider in the config\nit owns.",
            "anyOf": [
              {
                "description": "Tavily, applied as `TAVILY_API_KEY`.",
                "type": "object",
                "properties": {
                  "tavily_api_key": {
                    "description": "The key.",
                    "type": "string"
                  }
                },
                "required": [
                  "tavily_api_key"
                ]
              },
              {
                "description": "Exa, applied as `EXA_API_KEY`.",
                "type": "object",
                "properties": {
                  "exa_api_key": {
                    "description": "The key.",
                    "type": "string"
                  }
                },
                "required": [
                  "exa_api_key"
                ]
              },
              {
                "description": "Parallel, applied as `PARALLEL_API_KEY`.",
                "type": "object",
                "properties": {
                  "parallel_api_key": {
                    "description": "The key.",
                    "type": "string"
                  }
                },
                "required": [
                  "parallel_api_key"
                ]
              },
              {
                "description": "Keenable, applied as `KEENABLE_API_KEY`.",
                "type": "object",
                "properties": {
                  "keenable_api_key": {
                    "description": "The key.",
                    "type": "string"
                  }
                },
                "required": [
                  "keenable_api_key"
                ]
              },
              {
                "description": "Brave, applied as `BRAVE_SEARCH_API_KEY`.",
                "type": "object",
                "properties": {
                  "brave_search_api_key": {
                    "description": "The key.",
                    "type": "string"
                  }
                },
                "required": [
                  "brave_search_api_key"
                ]
              },
              {
                "description": "A self-hosted SearXNG instance, applied as `SEARXNG_URL` —\nthe one backend whose credential is an address.",
                "type": "object",
                "properties": {
                  "searxng_url": {
                    "description": "The instance.",
                    "type": "string"
                  }
                },
                "required": [
                  "searxng_url"
                ]
              }
            ]
          },
          "Extract": {
            "description": "One extraction backend, named by the credential it takes —\n[`Search`]'s discipline exactly, single-variant today because\nHermes has one keyed extractor at this pin. The harness applies\nthe env var and pins `web.extract_backend`.",
            "anyOf": [
              {
                "description": "Firecrawl, applied as `FIRECRAWL_API_KEY`.",
                "type": "object",
                "properties": {
                  "firecrawl_api_key": {
                    "description": "The key.",
                    "type": "string"
                  }
                },
                "required": [
                  "firecrawl_api_key"
                ]
              }
            ]
          },
          "Toolset2": {
            "description": "Browser automation, with its arguments. The field being absent\nfrom [`Toolsets`](super::Toolsets) is Hermes's own default for\nthis toolset; present is the switch thrown on.\n\nThe one slot is optional because the default backend is the\ncontainer's own headless Chromium, which needs nothing — the\nslot points the toolset at a browser somewhere else instead,\nand the empty object is the local browser, coherently.",
            "type": "object",
            "properties": {
              "remote": {
                "description": "The remote backend, one of [`Remote`]'s; absent = the local\nheadless Chromium.",
                "anyOf": [
                  {
                    "$ref": "#/$defs/Remote"
                  },
                  {
                    "type": "null"
                  }
                ]
              }
            }
          },
          "Remote": {
            "description": "One remote browser backend, named by the credentials it takes.\nUntagged — the variants' field sets are disjoint, so the fields\nare the discriminator and no marker is needed. One slot for\nboth because they are the same role: Browserbase IS a CDP\nprovider, and a request naming a raw endpoint and a managed one\nwould be contradicting itself.",
            "anyOf": [
              {
                "description": "Any browser reachable over CDP, applied as\n`BROWSER_CDP_URL`.",
                "type": "object",
                "properties": {
                  "cdp_url": {
                    "description": "The endpoint.",
                    "type": "string"
                  }
                },
                "required": [
                  "cdp_url"
                ]
              },
              {
                "description": "Browserbase — the pair is one credential, and the type now\nsays so. Applied as `BROWSERBASE_API_KEY` and\n`BROWSERBASE_PROJECT_ID`.",
                "type": "object",
                "properties": {
                  "browserbase_api_key": {
                    "description": "The key.",
                    "type": "string"
                  },
                  "browserbase_project_id": {
                    "description": "The project.",
                    "type": "string"
                  }
                },
                "required": [
                  "browserbase_api_key",
                  "browserbase_project_id"
                ]
              }
            ]
          },
          "Toolset3": {
            "description": "Image generation over FAL, the built-in path, with its\narguments. The field being absent from\n[`Toolsets`](super::Toolsets) is Hermes's own default for this\ntoolset; present is the switch thrown on. Hermes's other image\nproviders (openai, krea, deepinfra, xai) are deliberately\nabsent at this pin — one keyed path is enough until asked for\nmore.\n\nThe key is REQUIRED: generation has no keyless path, so a\nswitch thrown on without one could only mean a dead toolset.",
            "type": "object",
            "properties": {
              "fal_key": {
                "description": "The FAL key, applied as `FAL_KEY` in the gateway's process\nenvironment. [`video_gen`](super::video_gen) names the same\nvariable; a request supplying both MUST agree with itself.",
                "type": "string"
              }
            },
            "required": [
              "fal_key"
            ]
          },
          "Toolset4": {
            "description": "Video generation over FAL, the built-in path, with its\narguments — the same deliberate one-provider scope as\n[`image_gen`](super::image_gen). The field being absent from\n[`Toolsets`](super::Toolsets) is Hermes's own default for this\ntoolset; present is the switch thrown on.\n\nThe key is REQUIRED: generation has no keyless path, so a\nswitch thrown on without one could only mean a dead toolset.",
            "type": "object",
            "properties": {
              "fal_key": {
                "description": "The FAL key, applied as `FAL_KEY` in the gateway's process\nenvironment. [`image_gen`](super::image_gen) names the same\nvariable; a request supplying both MUST agree with itself.",
                "type": "string"
              }
            },
            "required": [
              "fal_key"
            ]
          },
          "Toolset5": {
            "description": "X (Twitter) search over the xAI API, with its arguments. The\nfield being absent from [`Toolsets`](super::Toolsets) is\nHermes's own default for this toolset (off); present is the\nswitch thrown on.\n\nThe key path ONLY, deliberately: Hermes's own dispatch prefers\nthe key, and the xAI subscription OAuth bearer answers this\ntool in a degraded no-citation mode — the rotating path is a\nworse product here, not a missing feature.",
            "type": "object",
            "properties": {
              "api_key": {
                "description": "The xAI API key, applied as `XAI_API_KEY` in the gateway's\nprocess environment.",
                "type": "string"
              }
            },
            "required": [
              "api_key"
            ]
          },
          "Toolset6": {
            "description": "Text-to-speech, with its arguments. The field being absent from\n[`Toolsets`](super::Toolsets) is Hermes's own default for this\ntoolset; present is the switch thrown on.\n\nThe key is OPTIONAL by the keyless-floor rule: Hermes's default\nvoice is edge-tts, which needs nothing — the empty object is\nspeech on the free voice, coherently. The key is the upgrade,\nnot the existence.",
            "type": "object",
            "properties": {
              "elevenlabs_api_key": {
                "description": "ElevenLabs, applied as `ELEVENLABS_API_KEY` in the\ngateway's process environment; when present the harness\nalso selects the provider (`tts.provider: elevenlabs` in\nthe config it owns). Hermes's other keyed voices are\ndeliberately absent at this pin.",
                "type": [
                  "string",
                  "null"
                ]
              }
            }
          },
          "Toolset7": {
            "description": "Home Assistant control, with its arguments. The field being\nabsent from [`Toolsets`](super::Toolsets) is Hermes's own\ndefault for this toolset; present is the switch thrown on.\n\nBoth arguments are REQUIRED here even though Hermes treats the\nurl as optional: its fallback is `homeassistant.local`, an mDNS\nname that is dead inside a container — an instance the caller\ndoes not name is an instance this run cannot reach.",
            "type": "object",
            "properties": {
              "url": {
                "description": "The instance, applied as `HASS_URL` in the gateway's\nprocess environment.",
                "type": "string"
              },
              "token": {
                "description": "The long-lived access token, applied as `HASS_TOKEN` —\nstatic by Home Assistant's own design, never rotated.",
                "type": "string"
              }
            },
            "required": [
              "url",
              "token"
            ]
          },
          "Toolset8": {
            "description": "Spotify control, with its arguments — back in the vocabulary,\nbecause its only blocker was rotation and rotation is what the\nvault's lock exists for: Hermes refreshes the PKCE state on\neffectively every Spotify tool call and rewrites the store each\ntime, so the login flows in from the vault and its rotated form\nflows back. The field being absent from\n[`Toolsets`](super::Toolsets) is Hermes's own default for this\ntoolset; present is the switch thrown on.\n\nAPPLICATION: the harness sets `HERMES_SPOTIFY_CLIENT_ID` to\n[`client_id`](Self::client_id) in the gateway's process\nenvironment; the login document lives in the vault under\n[`SPOTIFY_OAUTH`](diverge_provider_sdk::shared::containers::vault::keys::SPOTIFY_OAUTH), which the harness locks\nfor the run, reads, and writes as the `providers.spotify` entry\nof `$HERMES_HOME/auth.json` (Hermes keeps this entry inert for\nprovider selection by its own design); at the run's end the\nrotated document is set back under the key, and the key\nunlocked.",
            "type": "object",
            "properties": {
              "client_id": {
                "description": "The caller's own Spotify app (PKCE — there is no secret,\nand Hermes ships no default app). Static, so a plain\nargument.",
                "type": "string"
              }
            },
            "required": [
              "client_id"
            ]
          },
          "Tool": {
            "description": "One tool container the program depends on: everything the caller\nneeds to run it that the image can know, and nothing the caller\nalone can know.\n\nA [`Container`](crate::shared::containers::request::Container) is\nan image, two limits, mounts and arguments. The first four are\nhere, member for member; the mounts are not, because a volume is\nnamed by a `volume_name` in the caller's own listing and a FUSE\nmount is something the caller serves live, and a tool that named\neither would be dictating the caller's storage.",
            "type": "object",
            "properties": {
              "name": {
                "description": "What the program calls it: the caller's handle for the tool\ncontainer, and the label the caller uses if it prefixes the\ntool's tools when it merges lists. Unique in the list. The\nprogram never finds a server by it — it calls tools by whatever\nnames the caller's merged list shows, and knows which image\nserves each by the image under `_meta`, see\n[`shared::mcp`](crate::shared::mcp).",
                "type": "string"
              },
              "image": {
                "description": "The image: a name and a digest, the pair a\n`containers::tools::run` request names.",
                "$ref": "#/$defs/Image"
              },
              "memory": {
                "description": "The memory the tool container needs, in bytes.",
                "type": "integer",
                "format": "uint64",
                "minimum": 0
              },
              "disk": {
                "description": "The bytes the tool container may write, in bytes.",
                "type": "integer",
                "format": "uint64",
                "minimum": 0
              },
              "arguments": {
                "description": "The arguments the tool container is registered with — the\n`arguments` of the run request that makes it, verbatim."
              }
            },
            "required": [
              "name",
              "image",
              "memory",
              "disk",
              "arguments"
            ]
          },
          "Image": {
            "description": "The image, by its repository path and its manifest digest — the\npair [`images::check`](crate::endpoints::images::check::client::request::Frame)\nasks about, so a check that came back available names an image a\nrun can ask for, with nothing to translate between them.\n\n# The digest is the image\n\nA digest cannot be repointed at different content, so an image\nmeans the same bytes every time, wherever they come from. That is\nthe whole of what the wire asserts: the provider runs the image\nthe digest names and no other.\n\n# Who supplies the bytes is the provider's\n\nNot on the wire. A provider may hold the image already, pull it\nfrom a registry it uses, or take it from the caller — asking on\nthe run scope whether the caller holds it and, when it does, for\nits manifest and blobs, see [`oci`](crate::shared::containers::oci).\nWhich of those, in what order, and which registries, is the\nprovider's policy, and a caller learns only whether the run\nhappened. A caller need not hold what it names; one that does\nanswers so when asked.",
            "type": "object",
            "properties": {
              "name": {
                "description": "The repository path — `library/nginx`, `myorg/myimage`. No\nhost: where the bytes come from is not the caller's to say.\n\nIt lands in a reference by concatenation wherever a provider\npulls from, so a provider refuses one that is not a repository\npath and normalizes nothing.",
                "type": "string"
              },
              "digest": {
                "description": "The manifest digest, `<algorithm>:<hex>`.\n\nWhat actually identifies the image: a runtime hashes what it\npulls, so a source serving other bytes under it fails before\nanything runs.",
                "type": "string"
              }
            },
            "required": [
              "name",
              "digest"
            ]
          }
        }
      }
    },
    {
      "key": "openrouter",
      "image_name": "diverge-agentic-loop-openrouter",
      "digest": "unbuilt",
      "schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "title": "Agent",
        "description": "An agent running against OpenRouter.\n\nThe widest parameter set of any upstream, because OpenRouter fronts\nmany providers and exposes what each of them accepts.",
        "type": "object",
        "properties": {
          "model": {
            "description": "The model to route to, in OpenRouter's `vendor/name` form.",
            "type": "string"
          },
          "system_prompt": {
            "description": "The system prompt's text, sent as the conversation's leading\nmessage.",
            "type": [
              "string",
              "null"
            ]
          },
          "top_logprobs": {
            "description": "How many alternatives to report per token. Absent means report\nno log probabilities at all.",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint64",
            "minimum": 0
          },
          "frequency_penalty": {
            "description": "Penalise tokens by how often they have appeared.",
            "type": [
              "number",
              "null"
            ],
            "format": "double"
          },
          "logit_bias": {
            "description": "Per-token additive bias on the logits, keyed by token id.\n\nAn `IndexMap` rather than a `HashMap`: insertion order is\npreserved, so the same bias set serializes identically every\ntime instead of shuffling between runs.",
            "type": [
              "object",
              "null"
            ],
            "additionalProperties": {
              "type": "integer",
              "format": "int64"
            }
          },
          "max_completion_tokens": {
            "description": "Cap on generated tokens, counting reasoning tokens.",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint64",
            "minimum": 0
          },
          "presence_penalty": {
            "description": "Penalise tokens for having appeared at all, regardless of count.",
            "type": [
              "number",
              "null"
            ],
            "format": "double"
          },
          "stop": {
            "description": "Sequences that end generation.",
            "anyOf": [
              {
                "$ref": "#/$defs/Stop"
              },
              {
                "type": "null"
              }
            ]
          },
          "temperature": {
            "description": "Sampling temperature.",
            "type": [
              "number",
              "null"
            ],
            "format": "double"
          },
          "top_p": {
            "description": "Nucleus sampling threshold.",
            "type": [
              "number",
              "null"
            ],
            "format": "double"
          },
          "max_tokens": {
            "description": "Cap on generated tokens, excluding reasoning tokens.",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint64",
            "minimum": 0
          },
          "min_p": {
            "description": "Minimum probability, relative to the most likely token, for a\ntoken to be considered.",
            "type": [
              "number",
              "null"
            ],
            "format": "double"
          },
          "provider": {
            "description": "Which backing providers to route to.",
            "anyOf": [
              {
                "$ref": "#/$defs/Provider"
              },
              {
                "type": "null"
              }
            ]
          },
          "reasoning": {
            "description": "Reasoning configuration.",
            "anyOf": [
              {
                "$ref": "#/$defs/Reasoning"
              },
              {
                "type": "null"
              }
            ]
          },
          "repetition_penalty": {
            "description": "Multiplicative penalty on tokens already produced.",
            "type": [
              "number",
              "null"
            ],
            "format": "double"
          },
          "top_a": {
            "description": "Sample only from tokens above a threshold scaled by the most\nlikely token's probability.",
            "type": [
              "number",
              "null"
            ],
            "format": "double"
          },
          "top_k": {
            "description": "Sample only from the k most likely tokens.",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint64",
            "minimum": 0
          },
          "verbosity": {
            "description": "How detailed responses should be.",
            "anyOf": [
              {
                "$ref": "#/$defs/Verbosity"
              },
              {
                "type": "null"
              }
            ]
          },
          "context_compression": {
            "description": "What to do when the request exceeds the context window.",
            "anyOf": [
              {
                "$ref": "#/$defs/ContextCompression"
              },
              {
                "type": "null"
              }
            ]
          },
          "mcp_tools": {
            "description": "The tool containers this agent depends on, each one the caller\nruns and serves to it as an MCP server, in the form the\nprovider's wire defines. Passed back whole as the registration's\nanswer, which is how the caller learns of them. Absent is none.",
            "type": "array",
            "items": {
              "$ref": "#/$defs/Tool"
            }
          }
        },
        "required": [
          "model"
        ],
        "$defs": {
          "Stop": {
            "description": "Sequences that end generation when produced.\n\nUntagged: one sequence goes on the wire as a bare string and\nseveral as an array, matching what providers accept without a\ncaller having to wrap a single sequence in a list.",
            "anyOf": [
              {
                "description": "One sequence.",
                "type": "string"
              },
              {
                "description": "Several. Providers typically cap this around four.",
                "type": "array",
                "items": {
                  "type": "string"
                }
              }
            ]
          },
          "Provider": {
            "description": "Which backing providers OpenRouter may route to, and in what order.\n\nOpenRouter fronts many providers for one model, and they differ in\nprice, throughput and quantization — so \"which model\" does not\nfully determine what answers.",
            "type": "object",
            "properties": {
              "allow_fallbacks": {
                "description": "Whether to fall back to a provider outside the preferences when\nnone of them can serve the request.",
                "type": [
                  "boolean",
                  "null"
                ]
              },
              "require_parameters": {
                "description": "Only route to providers supporting every parameter sent.\nWithout this, unsupported parameters are silently dropped.",
                "type": [
                  "boolean",
                  "null"
                ]
              },
              "order": {
                "description": "Preferred providers, most preferred first.",
                "type": [
                  "array",
                  "null"
                ],
                "items": {
                  "type": "string"
                }
              },
              "only": {
                "description": "Route to these and no others.",
                "type": [
                  "array",
                  "null"
                ],
                "items": {
                  "type": "string"
                }
              },
              "ignore": {
                "description": "Never route to these.",
                "type": [
                  "array",
                  "null"
                ],
                "items": {
                  "type": "string"
                }
              },
              "quantizations": {
                "description": "Acceptable weight quantizations. A more aggressively quantized\nmodel is cheaper and measurably different.",
                "type": [
                  "array",
                  "null"
                ],
                "items": {
                  "$ref": "#/$defs/ProviderQuantization"
                }
              }
            }
          },
          "ProviderQuantization": {
            "description": "A weight quantization level.",
            "oneOf": [
              {
                "description": "4-bit integer.",
                "type": "string",
                "const": "int4"
              },
              {
                "description": "8-bit integer.",
                "type": "string",
                "const": "int8"
              },
              {
                "description": "4-bit float.",
                "type": "string",
                "const": "fp4"
              },
              {
                "description": "6-bit float.",
                "type": "string",
                "const": "fp6"
              },
              {
                "description": "8-bit float.",
                "type": "string",
                "const": "fp8"
              },
              {
                "description": "16-bit float (half).",
                "type": "string",
                "const": "fp16"
              },
              {
                "description": "16-bit brain float.",
                "type": "string",
                "const": "bf16"
              },
              {
                "description": "32-bit float (full).",
                "type": "string",
                "const": "fp32"
              },
              {
                "description": "Not reported.",
                "type": "string",
                "const": "unknown"
              }
            ]
          },
          "Reasoning": {
            "description": "How much the model should reason before answering, and how much of\nthat reasoning to report.",
            "type": "object",
            "properties": {
              "enabled": {
                "description": "Whether to reason at all.",
                "type": [
                  "boolean",
                  "null"
                ]
              },
              "max_tokens": {
                "description": "A token budget for reasoning. Mutually exclusive with\n[`effort`](Self::effort) on most models — a budget and a level\nare two ways of saying the same thing.",
                "type": [
                  "integer",
                  "null"
                ],
                "format": "uint64",
                "minimum": 0
              },
              "effort": {
                "description": "How hard to reason, as a level rather than a budget.",
                "anyOf": [
                  {
                    "$ref": "#/$defs/ReasoningEffort"
                  },
                  {
                    "type": "null"
                  }
                ]
              },
              "summary_verbosity": {
                "description": "How much of the reasoning to summarise back.",
                "anyOf": [
                  {
                    "$ref": "#/$defs/ReasoningSummaryVerbosity"
                  },
                  {
                    "type": "null"
                  }
                ]
              }
            }
          },
          "ReasoningEffort": {
            "description": "How hard the model should reason.",
            "oneOf": [
              {
                "type": "string",
                "enum": [
                  "minimal",
                  "low",
                  "medium",
                  "high",
                  "xhigh"
                ]
              },
              {
                "description": "Do not reason.",
                "type": "string",
                "const": "none"
              }
            ]
          },
          "ReasoningSummaryVerbosity": {
            "description": "How much of the reasoning to summarise in the response.",
            "oneOf": [
              {
                "description": "Let the model decide.",
                "type": "string",
                "const": "auto"
              },
              {
                "description": "Brief.",
                "type": "string",
                "const": "concise"
              },
              {
                "description": "Thorough.",
                "type": "string",
                "const": "detailed"
              }
            ]
          },
          "Verbosity": {
            "description": "How detailed the model's responses should be. A hint — not every\nmodel honours it.",
            "oneOf": [
              {
                "description": "Concise.",
                "type": "string",
                "const": "low"
              },
              {
                "description": "Balanced.",
                "type": "string",
                "const": "medium"
              },
              {
                "description": "Thorough.",
                "type": "string",
                "const": "high"
              },
              {
                "description": "As detailed as the model will go.",
                "type": "string",
                "const": "max"
              }
            ]
          },
          "ContextCompression": {
            "description": "Which compression engine to enable when a request would otherwise\nexceed the model's context window. Maps onto OpenRouter's\nrequest-body `plugins` array.",
            "oneOf": [
              {
                "description": "Drops content from the middle of the conversation. The only\nengine documented today.",
                "type": "string",
                "const": "middle_out"
              }
            ]
          },
          "Tool": {
            "description": "One tool container the program depends on: everything the caller\nneeds to run it that the image can know, and nothing the caller\nalone can know.\n\nA [`Container`](crate::shared::containers::request::Container) is\nan image, two limits, mounts and arguments. The first four are\nhere, member for member; the mounts are not, because a volume is\nnamed by a `volume_name` in the caller's own listing and a FUSE\nmount is something the caller serves live, and a tool that named\neither would be dictating the caller's storage.",
            "type": "object",
            "properties": {
              "name": {
                "description": "What the program calls it: the caller's handle for the tool\ncontainer, and the label the caller uses if it prefixes the\ntool's tools when it merges lists. Unique in the list. The\nprogram never finds a server by it — it calls tools by whatever\nnames the caller's merged list shows, and knows which image\nserves each by the image under `_meta`, see\n[`shared::mcp`](crate::shared::mcp).",
                "type": "string"
              },
              "image": {
                "description": "The image: a name and a digest, the pair a\n`containers::tools::run` request names.",
                "$ref": "#/$defs/Image"
              },
              "memory": {
                "description": "The memory the tool container needs, in bytes.",
                "type": "integer",
                "format": "uint64",
                "minimum": 0
              },
              "disk": {
                "description": "The bytes the tool container may write, in bytes.",
                "type": "integer",
                "format": "uint64",
                "minimum": 0
              },
              "arguments": {
                "description": "The arguments the tool container is registered with — the\n`arguments` of the run request that makes it, verbatim."
              }
            },
            "required": [
              "name",
              "image",
              "memory",
              "disk",
              "arguments"
            ]
          },
          "Image": {
            "description": "The image, by its repository path and its manifest digest — the\npair [`images::check`](crate::endpoints::images::check::client::request::Frame)\nasks about, so a check that came back available names an image a\nrun can ask for, with nothing to translate between them.\n\n# The digest is the image\n\nA digest cannot be repointed at different content, so an image\nmeans the same bytes every time, wherever they come from. That is\nthe whole of what the wire asserts: the provider runs the image\nthe digest names and no other.\n\n# Who supplies the bytes is the provider's\n\nNot on the wire. A provider may hold the image already, pull it\nfrom a registry it uses, or take it from the caller — asking on\nthe run scope whether the caller holds it and, when it does, for\nits manifest and blobs, see [`oci`](crate::shared::containers::oci).\nWhich of those, in what order, and which registries, is the\nprovider's policy, and a caller learns only whether the run\nhappened. A caller need not hold what it names; one that does\nanswers so when asked.",
            "type": "object",
            "properties": {
              "name": {
                "description": "The repository path — `library/nginx`, `myorg/myimage`. No\nhost: where the bytes come from is not the caller's to say.\n\nIt lands in a reference by concatenation wherever a provider\npulls from, so a provider refuses one that is not a repository\npath and normalizes nothing.",
                "type": "string"
              },
              "digest": {
                "description": "The manifest digest, `<algorithm>:<hex>`.\n\nWhat actually identifies the image: a runtime hashes what it\npulls, so a source serving other bytes under it fails before\nanything runs.",
                "type": "string"
              }
            },
            "required": [
              "name",
              "digest"
            ]
          }
        }
      }
    },
    {
      "key": "eliza",
      "image_name": "diverge-agentic-loop-eliza",
      "digest": "unbuilt",
      "schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "title": "Agent",
        "description": "An agent running against Eliza (elizaOS).\n\nEvery field here is HONORABLE: it names something the harness\nhands the runtime it constructs — a character field Eliza renders\ninto its prompts, a constructor option, a plugin the container\nloads — and nothing else made the cut. Nothing here is a secret:\nevery credential a plugin needs lives in the caller's vault, under\na key the plugin's entry names, and is read per run. The one place\nthis vocabulary bends the protocol's post-transform rule is\n[`character`](Self::character), and the bend is stated there.\n\n# Nothing is pre-wired but the adapter\n\nThe harness itself loads exactly two plugins: `@elizaos/plugin-sql`,\nthe database adapter, pointed at the caller's database; and its own\ndiverge plugin, the caller's MCP tools and resources. Every other\nplugin — the model that speaks, the one that embeds, the tools —\nis the caller's to list here, by package name, with its own\nsettings and secrets. The packages the image carries are\npre-installed and nothing more: listed, they load at the image's\npin; unlisted, they are not touched. An agent that lists no\n[`model_provider_plugins`](Self::model_provider_plugins) has no\nmodel, and its run fails as Eliza's own failure.",
        "type": "object",
        "properties": {
          "character": {
            "description": "Who the agent is: the fields Eliza re-renders into its prompt\nEVERY turn. A parameter of the call, not a transform applied\nupstream — Eliza will not take a finished prompt, so a caller\nwho authored a personality renders it into these fields. See\n[`Character`].",
            "$ref": "#/$defs/Character"
          },
          "memory": {
            "description": "What a turn accumulates beyond the conversation itself —\nreflection, long-term memory, relationships, documents,\nplanning — each a cost per turn and a growth of the\ncontinuation. Absent is everything off. See [`Memory`].",
            "$ref": "#/$defs/Memory",
            "default": {}
          },
          "generate_media": {
            "description": "Whether the model may GENERATE media: core's own\n`GENERATE_MEDIA` action, registered unconditionally and\nunregistered after initialize when this is off. Absent is off.\nThe action's images come from whichever model-provider plugin\nregisters an image-generation tier; none, and the action fails\nas itself. The tiers that READ media — image description,\ntranscription — are not this switch's: a tool's image or audio\nis read to the model by whichever provider registers them, and\ndescribed as bytes otherwise.",
            "type": [
              "boolean",
              "null"
            ]
          },
          "model_provider_plugins": {
            "description": "The plugins that register model handlers — the model that\nspeaks, the one that embeds, the one that describes an image —\nin PRIORITY ORDER: the first listed answers every model type it\nregisters, the next is its failover, and so on. See [`Plugin`],\nand the rule in [the agent module](super): a plugin that\nregisters a model handler belongs here and nowhere else, and\nthe run refuses one listed under [`plugins`](Self::plugins).\nAbsent is no model at all.",
            "type": "array",
            "items": {
              "$ref": "#/$defs/Plugin"
            }
          },
          "plugins": {
            "description": "Every other plugin: actions, providers, services, evaluators —\nany npm package that is an elizaOS plugin and registers no\nmodel handler. The run refuses one that does. Absent is none.",
            "type": "array",
            "items": {
              "$ref": "#/$defs/Plugin"
            }
          },
          "mcp_tools": {
            "description": "The tool containers this agent depends on, each one the caller\nruns and serves to it as an MCP server, in the form the\nprovider's wire defines. Passed back whole as the registration's\nanswer, which is how the caller learns of them. Absent is none.",
            "type": "array",
            "items": {
              "$ref": "#/$defs/Tool"
            }
          }
        },
        "required": [
          "character"
        ],
        "$defs": {
          "Character": {
            "description": "Who the agent is, as Eliza renders it.\n\n[`name`](Self::name) is the one required field — Eliza's own\nminimum, and the prompt's `{{agentName}}`. Everything else is\nan ingredient Eliza samples or blocks into the prompt, empty\nmeaning \"nothing to say\", which is how a caller whose whole\npersonality is the system prompt states that.",
            "type": "object",
            "properties": {
              "name": {
                "description": "The agent's name. The prompt's `{{agentName}}` and\n`{{name}}`; every prompt block is headed by it.",
                "type": "string"
              },
              "system": {
                "description": "The system prompt head. Absent gives Eliza's own: \"You are\n{{name}}, an autonomous AI agent powered by elizaOS.\"",
                "type": [
                  "string",
                  "null"
                ]
              },
              "bio": {
                "description": "Biography lines. Blocked whole into the canonical system\nprompt as \"About {{name}}\", and sampled (up to ten,\ndeterministically) by the character provider.",
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "topics": {
                "description": "Interests. One is rendered as what the agent is \"currently\ninterested in\" and up to five more as \"also interested in\",\npicked deterministically per room.",
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "adjectives": {
                "description": "Traits. One is rendered as \"{{name}} is {{adjective}}\", picked\ndeterministically per room.",
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "style": {
                "description": "Style directions. See [`Style`].",
                "$ref": "#/$defs/Style"
              },
              "message_examples": {
                "description": "Example conversations, each a group of turns in order. Five\ngroups are sampled into chat-room prompts, deterministically\nper room. See [`Example`].",
                "type": "array",
                "items": {
                  "type": "array",
                  "items": {
                    "$ref": "#/$defs/Example"
                  }
                }
              }
            },
            "required": [
              "name"
            ]
          },
          "Style": {
            "description": "How the agent writes, as direction lines.\n\nEliza composes two lists from three: chat style is\n[`all`](Self::all) then [`chat`](Self::chat), post style is\n[`all`](Self::all) then [`post`](Self::post). The loop's one room\nis a chat room, so [`post`](Self::post) never renders here; it is\nkept because the character is one document and a caller may\ncarry it whole.",
            "type": "object",
            "properties": {
              "all": {
                "description": "Directions for everything the agent writes.",
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "chat": {
                "description": "Directions for conversation, after [`all`](Self::all).",
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "post": {
                "description": "Directions for posts, after [`all`](Self::all). Never rendered\nin the loop's room.",
                "type": "array",
                "items": {
                  "type": "string"
                }
              }
            }
          },
          "Example": {
            "description": "One turn in a group of [`message_examples`](super::Character::message_examples).\n\nEliza's `MessageExample` reduced to what renders: who spoke and\nwhat they said. A speaker equal to the character's\n[`name`](super::Character::name) is the agent's own turn; any\nother name is a user's.",
            "type": "object",
            "properties": {
              "name": {
                "description": "Who spoke.",
                "type": "string"
              },
              "text": {
                "description": "What they said.",
                "type": "string"
              }
            },
            "required": [
              "name",
              "text"
            ]
          },
          "Memory": {
            "description": "The memory-shaping switches, every one stated.\n\nEach names a runtime construction fact — a constructor option or\na character flag — that adds model calls to a turn and rows to\nthe continuation; together they are what makes an Eliza agent an\nEliza agent rather than a chat loop, which is why they are the\ncaller's and not the harness's. Each an `Option<bool>` where\nabsent is off — the same as `false` — so a request names only\nwhat it pays for.\n\nNot a switch, because nothing switches it: the per-turn FACTS\nstage. The message handler's first pass extracts facts and\nrelationships as part of its one structured envelope, and they\nare persisted whenever it found any — a cost already inside the\nturn the caller asked for.",
            "type": "object",
            "properties": {
              "advanced_capabilities": {
                "description": "The advanced-capabilities bundle: the post-turn reflection\nevaluators (fact memory, relationships, identities, success),\nexperience, personality and form providers and actions — one\nmerged small-model call after each reply. Constructor option\n`advancedCapabilities`.",
                "type": [
                  "boolean",
                  "null"
                ]
              },
              "advanced_memory": {
                "description": "Long-term memory: distilled episodic, semantic and procedural\nmemories in their own table, written by the advanced-memory\nevaluator. Character flag `advancedMemory`.",
                "type": [
                  "boolean",
                  "null"
                ]
              },
              "relationships": {
                "description": "The relationships feature: the native entity graph, its\nservice and provider. Constructor option `enableRelationships`.",
                "type": [
                  "boolean",
                  "null"
                ]
              },
              "documents": {
                "description": "The documents feature: document and fragment memories,\ningested and searched. Constructor option `enableDocuments`, a\nnative runtime feature like\n[`relationships`](Self::relationships). The plugin that\ningests files is `@elizaos/plugin-documents`, the caller's to\nlist under `plugins` with `DOCUMENTS_PATH` and\n`LOAD_DOCS_ON_STARTUP` as its settings; vectors need a\nmodel-provider plugin that embeds, and without one the search\nis by keyword.",
                "type": [
                  "boolean",
                  "null"
                ]
              },
              "advanced_planning": {
                "description": "Advanced planning: the multi-step planner over the simple\nreply path. Character flag `advancedPlanning`.",
                "type": [
                  "boolean",
                  "null"
                ]
              }
            }
          },
          "Plugin": {
            "description": "An elizaOS plugin the container installs and loads for the run —\nany npm package that exports a `Plugin`, configured by its own\nsetting names, its secrets the vault's.\n\nHOW THE HARNESS APPLIES IT: at the start of the run, every named\npackage the image's project does not already hold is installed\ninto it (`bun add --ignore-scripts`, the command elizaOS's own\ninstaller runs), imported, and handed to the runtime's constructor\nbeside the image's plugins; the version the registry resolved is\nrecorded in the lineage's row, and every later run of the lineage\ninstalls that version, whatever this spec says, so a conversation\nis never resumed against a plugin that changed under it. Each\n[`settings`](Self::settings) entry and each [`secrets`](Self::secrets)\nvalue read from the vault goes in the runtime's constructor\nsettings map and the entry process's environment both — the\ncore's `getSetting` never reads the environment, and some plugins\nread nothing else — the vault's values at the top of Eliza's\nprecedence. A package the registry cannot resolve, one that is\nnot a plugin once imported, or a secret the vault does not hold\nrefuses the run before the runtime starts.\n\nThe plugin runs in the agent's process with the caller's tools in\nreach; the container is the sandbox.",
            "type": "object",
            "properties": {
              "package": {
                "description": "The npm spec: `@elizaos/plugin-discord`,\n`@elizaos/plugin-discord@2.0.4`, `@scope/anything`. A spec\nwithout a version resolves to the registry's latest on the\nlineage's first run, and to that resolved version after.",
                "type": "string"
              },
              "settings": {
                "description": "The plugin's non-secret configuration, by the setting names\nthe plugin reads — its own vocabulary, which the caller who\nchose it knows. JSON values; the harness renders each as the\nstring Eliza's settings carry, and Eliza reads `\"true\"` and\n`\"false\"` back as booleans. Absent is nothing configured.",
                "type": "object",
                "additionalProperties": true
              },
              "secrets": {
                "description": "The settings that are secrets, each a vault key: a bare name\nfor a static secret, an object for one the run rotates and\nwhere the rotated value is left. See [`Secret`]. Absent is no\nsecrets.",
                "type": "array",
                "items": {
                  "$ref": "#/$defs/Secret"
                }
              }
            },
            "required": [
              "package"
            ]
          },
          "Secret": {
            "description": "A secret the plugin reads: a setting name that IS the vault key\nthe value lives under.\n\nUntagged: a bare string is a static secret, read from the vault\nonce per run and set as that setting — the common case, an API\nkey. An object names a secret the run ROTATES — an OAuth login\nwhose refresh token is single-use — and where the plugin leaves\nthe rotated value, so the harness can put it back: the key is\nlocked for the run, read, set as the setting, read back after\nevery turn and at the end from the place [`rotates`](Self::Rotating)\nnames, set into the vault, and unlocked when the run ends. The\nvalue never appears in this document, in the character, or in\nthe schema.\n\n# Credentials never live in the database\n\nThe rotated value's home is the vault, never Eliza's database:\nthe caller's database will be snapshotted and rewound, and a\ncredential rewound is a login burned. Eliza is built on the same\nexpectation — a plugin keeps its credentials in settings, not in\ntables — and this container assumes it: a plugin that persists a\nrotating credential in a table of its own cannot be supported.\nThe three places a plugin may leave a rotated value are its\nsetting, a file, and Eliza's own vault — the encrypted store the\n`@elizaos/vault` library keeps on disk, on by default with a\npassphrase the harness holds — and the harness re-applies the\nvault's copy at the top of Eliza's precedence at every run's\nstart, so a rewound row never wins over the vault.",
            "anyOf": [
              {
                "description": "A static secret: the vault key, read once per run, set as\nthe setting of the same name, never written back.",
                "type": "string"
              },
              {
                "description": "A secret the run rotates.",
                "type": "object",
                "properties": {
                  "key": {
                    "description": "The setting name, and the vault key.",
                    "type": "string"
                  },
                  "rotates": {
                    "description": "Where the plugin leaves the rotated value.",
                    "$ref": "#/$defs/Rotates"
                  }
                },
                "required": [
                  "key",
                  "rotates"
                ]
              }
            ]
          },
          "Rotates": {
            "description": "Where the harness reads a rotated value back from, after every\nturn and at the run's end — the three channels an elizaOS plugin\nhas for a credential it rotates. Untagged.\n\n```json\ntrue                                   // the setting of the same name\n{\"setting\": \"GOOGLE_TOKENS\"}           // another setting\n{\"file\": [\"root\", \".qwen\", \"oauth_creds.json\"]}\n{\"vault\": \"agent.{agent_id}.wallet.evm\"}   // Eliza's own vault\n```\n\nA setting is read through the runtime the harness constructs —\n`getSetting`, which a plugin wrote with `setSetting` — and a\nnon-string value goes to the vault as its JSON; a file goes as\nits bytes; an entry of Eliza's own vault is read through the\n`@elizaos/vault` library with the master key the harness holds,\nand goes as its decrypted value. Nothing else is a channel:\nEliza's cache and its tables are the database, and a credential\ndoes not live there (see [`Secret`](super::Secret)). A read-back that finds nothing\nnew is not an error — the plugin may not have rotated — but a\nread-back that finds nothing at all is a warning at the run's\nend, naming the key, so the caller learns their copy may be stale\nbefore the next login fails.",
            "anyOf": [
              {
                "description": "`true`: the plugin rotates the setting in place, so the\nrotated value is the setting of the same name. `false` is a\nstatic secret said the long way.",
                "type": "boolean"
              },
              {
                "description": "The plugin leaves the rotated value under another setting.",
                "type": "object",
                "properties": {
                  "setting": {
                    "description": "The setting's name.",
                    "type": "string"
                  }
                },
                "required": [
                  "setting"
                ]
              },
              {
                "description": "The plugin leaves the rotated value in a file.",
                "type": "object",
                "properties": {
                  "file": {
                    "description": "The file's path, as components from the container's root\n— the shape every path in this system takes.",
                    "type": "array",
                    "items": {
                      "type": "string"
                    }
                  }
                },
                "required": [
                  "file"
                ]
              },
              {
                "description": "The plugin leaves the rotated value in Eliza's own vault.",
                "type": "object",
                "properties": {
                  "vault": {
                    "description": "The entry's key, as the plugin names it. `{agent_id}` in\nit is replaced with the lineage's agent id, which the\nharness minted and the caller cannot know — the wallet\nplugin's `agent.<agentId>.wallet.<chain>` is the shape.",
                    "type": "string"
                  }
                },
                "required": [
                  "vault"
                ]
              }
            ]
          },
          "Tool": {
            "description": "One tool container the program depends on: everything the caller\nneeds to run it that the image can know, and nothing the caller\nalone can know.\n\nA [`Container`](crate::shared::containers::request::Container) is\nan image, two limits, mounts and arguments. The first four are\nhere, member for member; the mounts are not, because a volume is\nnamed by a `volume_name` in the caller's own listing and a FUSE\nmount is something the caller serves live, and a tool that named\neither would be dictating the caller's storage.",
            "type": "object",
            "properties": {
              "name": {
                "description": "What the program calls it: the caller's handle for the tool\ncontainer, and the label the caller uses if it prefixes the\ntool's tools when it merges lists. Unique in the list. The\nprogram never finds a server by it — it calls tools by whatever\nnames the caller's merged list shows, and knows which image\nserves each by the image under `_meta`, see\n[`shared::mcp`](crate::shared::mcp).",
                "type": "string"
              },
              "image": {
                "description": "The image: a name and a digest, the pair a\n`containers::tools::run` request names.",
                "$ref": "#/$defs/Image"
              },
              "memory": {
                "description": "The memory the tool container needs, in bytes.",
                "type": "integer",
                "format": "uint64",
                "minimum": 0
              },
              "disk": {
                "description": "The bytes the tool container may write, in bytes.",
                "type": "integer",
                "format": "uint64",
                "minimum": 0
              },
              "arguments": {
                "description": "The arguments the tool container is registered with — the\n`arguments` of the run request that makes it, verbatim."
              }
            },
            "required": [
              "name",
              "image",
              "memory",
              "disk",
              "arguments"
            ]
          },
          "Image": {
            "description": "The image, by its repository path and its manifest digest — the\npair [`images::check`](crate::endpoints::images::check::client::request::Frame)\nasks about, so a check that came back available names an image a\nrun can ask for, with nothing to translate between them.\n\n# The digest is the image\n\nA digest cannot be repointed at different content, so an image\nmeans the same bytes every time, wherever they come from. That is\nthe whole of what the wire asserts: the provider runs the image\nthe digest names and no other.\n\n# Who supplies the bytes is the provider's\n\nNot on the wire. A provider may hold the image already, pull it\nfrom a registry it uses, or take it from the caller — asking on\nthe run scope whether the caller holds it and, when it does, for\nits manifest and blobs, see [`oci`](crate::shared::containers::oci).\nWhich of those, in what order, and which registries, is the\nprovider's policy, and a caller learns only whether the run\nhappened. A caller need not hold what it names; one that does\nanswers so when asked.",
            "type": "object",
            "properties": {
              "name": {
                "description": "The repository path — `library/nginx`, `myorg/myimage`. No\nhost: where the bytes come from is not the caller's to say.\n\nIt lands in a reference by concatenation wherever a provider\npulls from, so a provider refuses one that is not a repository\npath and normalizes nothing.",
                "type": "string"
              },
              "digest": {
                "description": "The manifest digest, `<algorithm>:<hex>`.\n\nWhat actually identifies the image: a runtime hashes what it\npulls, so a source serving other bytes under it fails before\nanything runs.",
                "type": "string"
              }
            },
            "required": [
              "name",
              "digest"
            ]
          }
        }
      }
    },
    {
      "key": "python",
      "image_name": "diverge-agentic-loop-python",
      "digest": "unbuilt",
      "schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "title": "Agent",
        "description": "An agent that runs Python instead of calling a model.\n\nNo `model` field, which is the point: nothing is sampled, so there\nis nothing to name — and no sampling parameters either. A Python\nagent occupies the same slot as a model-backed one and answers\ndeterministically, or as deterministically as its source does.",
        "type": "object",
        "properties": {
          "python": {
            "description": "The source, verbatim.\n\nNever normalized — whitespace is significant in Python, so the\ntrimming applied to most string fields would change what the\ncode means. Written to disk as it is at registration, and run\nas it is every turn.",
            "type": "string"
          },
          "requirements": {
            "description": "Third-party packages the source needs: distribution name to\nversion constraint. Installed at registration, once.\n\nPrecision is the author's statement of intent: a loose\nspecifier says \"track updates\", an exact one says \"freeze\nthis\". A map rather than a list of requirement lines, so one\npackage cannot appear twice with constraints that contradict\neach other. Ordered, so the same set always serializes — and\ninstalls — identically instead of shuffling between runs.\n\nOne constraint per package, which is what the shape costs. A\ncompound range (`>= 2, < 3`) and an environment marker\n(`; python_version < \"3.11\"`) are both unrepresentable — the\nfirst needs two constraints for one name, the second needs a\nplace to put the marker. Both are legal in a\n`requirements.txt` and neither survives here.",
            "type": "object",
            "additionalProperties": {
              "$ref": "#/$defs/Version"
            }
          },
          "mcp_tools": {
            "description": "The tool containers this agent depends on, each one the caller\nruns and serves to it as an MCP server, in the form the\nprovider's wire defines. Passed back whole as the registration's\nanswer, which is how the caller learns of them. Absent is none.",
            "type": "array",
            "items": {
              "$ref": "#/$defs/Tool"
            }
          }
        },
        "required": [
          "python"
        ],
        "$defs": {
          "Version": {
            "description": "A constraint on which version of a package satisfies a\nrequirement.\n\nSplit into an operator and a version rather than kept as one\nstring, so the operator is a closed set a consumer matches on\ninstead of a prefix it has to parse off the front. `\">==2.0\"` is\nnot representable.\n\nThere is no \"any version\" — an operator is always required. Say\n`>= 0` and mean it, rather than leaving a consumer to decide what\nan absent constraint implies.",
            "type": "object",
            "properties": {
              "operator": {
                "description": "How to compare.",
                "$ref": "#/$defs/Operator"
              },
              "version": {
                "description": "What to compare against — a PEP 440 version, without the\noperator. `\"2.31.0\"`, not `\">=2.31.0\"`.\n\nLeft a string rather than parsed into components: PEP 440\nversions carry epochs, pre/post/dev segments and local\nidentifiers (`1!2.0.0rc1.post2+ubuntu.1`), and a structure that\nmodelled all of it would be a version parser in a wire type.",
                "type": "string"
              }
            },
            "required": [
              "operator",
              "version"
            ]
          },
          "Operator": {
            "description": "How a [`Version`](super::Version) compares, as PEP 440 defines\nthem.\n\nA closed set, so it is an enum rather than a string: an operator\npip does not understand fails to deserialize instead of failing\nat install time.\n\nSeven of PEP 440's eight. `===`, arbitrary equality, is omitted —\nit exists for versions that are not PEP 440 versions, and PyPI\nenforces PEP 440 on upload, so nothing reachable from the default\nindex can need it.",
            "oneOf": [
              {
                "description": "`==` — this version. The default, because freezing is the\nsafer thing to do by accident.",
                "type": "string",
                "const": "=="
              },
              {
                "description": "`!=` — anything but this version.",
                "type": "string",
                "const": "!="
              },
              {
                "description": "`>=` — this version or newer. The usual way to say \"track\nupdates\".",
                "type": "string",
                "const": ">="
              },
              {
                "description": "`>` — strictly newer.",
                "type": "string",
                "const": ">"
              },
              {
                "description": "`<=` — this version or older.",
                "type": "string",
                "const": "<="
              },
              {
                "description": "`<` — strictly older.",
                "type": "string",
                "const": "<"
              },
              {
                "description": "`~=` — compatible release. Newer, but not past the last\ncomponent: `~= 2.31.0` allows `2.31.4` and refuses `2.32.0`.",
                "type": "string",
                "const": "~="
              }
            ]
          },
          "Tool": {
            "description": "One tool container the program depends on: everything the caller\nneeds to run it that the image can know, and nothing the caller\nalone can know.\n\nA [`Container`](crate::shared::containers::request::Container) is\nan image, two limits, mounts and arguments. The first four are\nhere, member for member; the mounts are not, because a volume is\nnamed by a `volume_name` in the caller's own listing and a FUSE\nmount is something the caller serves live, and a tool that named\neither would be dictating the caller's storage.",
            "type": "object",
            "properties": {
              "name": {
                "description": "What the program calls it: the caller's handle for the tool\ncontainer, and the label the caller uses if it prefixes the\ntool's tools when it merges lists. Unique in the list. The\nprogram never finds a server by it — it calls tools by whatever\nnames the caller's merged list shows, and knows which image\nserves each by the image under `_meta`, see\n[`shared::mcp`](crate::shared::mcp).",
                "type": "string"
              },
              "image": {
                "description": "The image: a name and a digest, the pair a\n`containers::tools::run` request names.",
                "$ref": "#/$defs/Image"
              },
              "memory": {
                "description": "The memory the tool container needs, in bytes.",
                "type": "integer",
                "format": "uint64",
                "minimum": 0
              },
              "disk": {
                "description": "The bytes the tool container may write, in bytes.",
                "type": "integer",
                "format": "uint64",
                "minimum": 0
              },
              "arguments": {
                "description": "The arguments the tool container is registered with — the\n`arguments` of the run request that makes it, verbatim."
              }
            },
            "required": [
              "name",
              "image",
              "memory",
              "disk",
              "arguments"
            ]
          },
          "Image": {
            "description": "The image, by its repository path and its manifest digest — the\npair [`images::check`](crate::endpoints::images::check::client::request::Frame)\nasks about, so a check that came back available names an image a\nrun can ask for, with nothing to translate between them.\n\n# The digest is the image\n\nA digest cannot be repointed at different content, so an image\nmeans the same bytes every time, wherever they come from. That is\nthe whole of what the wire asserts: the provider runs the image\nthe digest names and no other.\n\n# Who supplies the bytes is the provider's\n\nNot on the wire. A provider may hold the image already, pull it\nfrom a registry it uses, or take it from the caller — asking on\nthe run scope whether the caller holds it and, when it does, for\nits manifest and blobs, see [`oci`](crate::shared::containers::oci).\nWhich of those, in what order, and which registries, is the\nprovider's policy, and a caller learns only whether the run\nhappened. A caller need not hold what it names; one that does\nanswers so when asked.",
            "type": "object",
            "properties": {
              "name": {
                "description": "The repository path — `library/nginx`, `myorg/myimage`. No\nhost: where the bytes come from is not the caller's to say.\n\nIt lands in a reference by concatenation wherever a provider\npulls from, so a provider refuses one that is not a repository\npath and normalizes nothing.",
                "type": "string"
              },
              "digest": {
                "description": "The manifest digest, `<algorithm>:<hex>`.\n\nWhat actually identifies the image: a runtime hashes what it\npulls, so a source serving other bytes under it fails before\nanything runs.",
                "type": "string"
              }
            },
            "required": [
              "name",
              "digest"
            ]
          }
        }
      }
    }
  ],
  "agents": [
    {
      "name": "site-fixes",
      "image_name": "diverge-agentic-loop-cc",
      "digest": "unbuilt",
      "created": "2026-09-26T00:08:02.750364+00:00",
      "active": true,
      "last_active": "2026-09-26T00:47:42.750364+00:00",
      "provider": {
        "kind": "outgoing",
        "address": "127.0.0.1:4640"
      },
      "logs_index": 40
    },
    {
      "name": "research-notes",
      "image_name": "diverge-agentic-loop-hermes",
      "digest": "unbuilt",
      "created": "2026-09-24T00:48:02.750364+00:00",
      "active": false,
      "last_active": "2026-09-25T04:48:08.669364+00:00",
      "provider": {
        "kind": "outgoing",
        "address": "127.0.0.1:4640"
      },
      "logs_index": 32
    },
    {
      "name": "broken-loop",
      "image_name": "diverge-agentic-loop-openrouter",
      "digest": "unbuilt",
      "created": "2026-09-25T18:48:02.750364+00:00",
      "active": false,
      "last_active": "2026-09-25T19:48:06.750364+00:00",
      "provider": {
        "kind": "incoming_unbrokered",
        "identity": "studio-pc"
      },
      "logs_index": 5
    }
  ],
  "logs": {
    "broken-loop": [
      {
        "logs_index": 1,
        "created": "2026-09-25T19:48:02.750364+00:00",
        "item": {
          "kind": "user_text",
          "key": "m2",
          "text": "Summarise the reading list"
        }
      },
      {
        "logs_index": 2,
        "created": "2026-09-25T19:48:02.750364+00:00",
        "item": {
          "kind": "active",
          "provider": {
            "kind": "incoming_unbrokered",
            "identity": "studio-pc"
          }
        }
      },
      {
        "logs_index": 3,
        "created": "2026-09-25T19:48:04.750364+00:00",
        "item": {
          "kind": "reasoning",
          "parent": null,
          "text": "Reading the list first."
        }
      },
      {
        "logs_index": 4,
        "created": "2026-09-25T19:48:06.750364+00:00",
        "item": {
          "kind": "error",
          "message": "the upstream refused the request: no API key for it in the vault"
        }
      },
      {
        "logs_index": 5,
        "created": "2026-09-25T19:48:06.750364+00:00",
        "item": {
          "kind": "inactive",
          "provider": {
            "kind": "incoming_unbrokered",
            "identity": "studio-pc"
          }
        }
      }
    ],
    "research-notes": [
      {
        "logs_index": 1,
        "created": "2026-09-25T04:48:02.750364+00:00",
        "item": {
          "kind": "user_text",
          "key": "m1",
          "text": "Pull the open questions out of my notes"
        }
      },
      {
        "logs_index": 2,
        "created": "2026-09-25T04:48:02.750364+00:00",
        "item": {
          "kind": "active",
          "provider": {
            "kind": "outgoing",
            "address": "127.0.0.1:4640"
          }
        }
      },
      {
        "logs_index": 3,
        "created": "2026-09-25T04:48:02.890364+00:00",
        "item": {
          "kind": "reasoning",
          "parent": null,
          "text": "I should look "
        }
      },
      {
        "logs_index": 4,
        "created": "2026-09-25T04:48:02.932364+00:00",
        "item": {
          "kind": "reasoning",
          "parent": null,
          "text": "at what's already "
        }
      },
      {
        "logs_index": 5,
        "created": "2026-09-25T04:48:02.981364+00:00",
        "item": {
          "kind": "reasoning",
          "parent": null,
          "text": "written down before "
        }
      },
      {
        "logs_index": 6,
        "created": "2026-09-25T04:48:03.037364+00:00",
        "item": {
          "kind": "reasoning",
          "parent": null,
          "text": "searching. Ask: Pull "
        }
      },
      {
        "logs_index": 7,
        "created": "2026-09-25T04:48:03.100364+00:00",
        "item": {
          "kind": "reasoning",
          "parent": null,
          "text": "the open questions "
        }
      },
      {
        "logs_index": 8,
        "created": "2026-09-25T04:48:03.170364+00:00",
        "item": {
          "kind": "reasoning",
          "parent": null,
          "text": "out of my "
        }
      },
      {
        "logs_index": 9,
        "created": "2026-09-25T04:48:03.207364+00:00",
        "item": {
          "kind": "reasoning",
          "parent": null,
          "text": "notes"
        }
      },
      {
        "logs_index": 10,
        "created": "2026-09-25T04:48:03.707364+00:00",
        "item": {
          "kind": "tool_call",
          "parent": null,
          "id": "call_26",
          "name": "read_file",
          "arguments": "{\"path\":\"/notes/ideas.md\"}"
        }
      },
      {
        "logs_index": 11,
        "created": "2026-09-25T04:48:04.407364+00:00",
        "item": {
          "kind": "tool_response",
          "parent": null,
          "id": "call_26",
          "is_error": false,
          "text": "# ideas\n\n- A shop on my profile for the poster packs\n- Ask if the studio PC's GPU can be borrowed on weekends\n- What does a bounty look like before money exists?\n- Onboarding without a tour\n"
        }
      },
      {
        "logs_index": 12,
        "created": "2026-09-25T04:48:04.907364+00:00",
        "item": {
          "kind": "tool_call",
          "parent": null,
          "id": "call_27",
          "name": "web_search",
          "arguments": "{\"query\":\"Pull the open questions out of my notes\"}"
        }
      },
      {
        "logs_index": 13,
        "created": "2026-09-25T04:48:06.107364+00:00",
        "item": {
          "kind": "tool_response",
          "parent": null,
          "id": "call_27",
          "is_error": true,
          "text": "The stand-in daemon has no network, so this search returned nothing. A real run would list results here."
        }
      },
      {
        "logs_index": 14,
        "created": "2026-09-25T04:48:06.327364+00:00",
        "item": {
          "kind": "text",
          "parent": null,
          "text": "I couldn't search, "
        }
      },
      {
        "logs_index": 15,
        "created": "2026-09-25T04:48:06.389364+00:00",
        "item": {
          "kind": "text",
          "parent": null,
          "text": "so here's what "
        }
      },
      {
        "logs_index": 16,
        "created": "2026-09-25T04:48:06.458364+00:00",
        "item": {
          "kind": "text",
          "parent": null,
          "text": "your own notes "
        }
      },
      {
        "logs_index": 17,
        "created": "2026-09-25T04:48:06.534364+00:00",
        "item": {
          "kind": "text",
          "parent": null,
          "text": "already say:\n\n1. A "
        }
      },
      {
        "logs_index": 18,
        "created": "2026-09-25T04:48:06.617364+00:00",
        "item": {
          "kind": "text",
          "parent": null,
          "text": "shop on your "
        }
      },
      {
        "logs_index": 19,
        "created": "2026-09-25T04:48:06.707364+00:00",
        "item": {
          "kind": "text",
          "parent": null,
          "text": "profile for the "
        }
      },
      {
        "logs_index": 20,
        "created": "2026-09-25T04:48:06.764364+00:00",
        "item": {
          "kind": "text",
          "parent": null,
          "text": "poster packs\n2. Borrowing "
        }
      },
      {
        "logs_index": 21,
        "created": "2026-09-25T04:48:06.828364+00:00",
        "item": {
          "kind": "text",
          "parent": null,
          "text": "the studio PC's "
        }
      },
      {
        "logs_index": 22,
        "created": "2026-09-25T04:48:06.899364+00:00",
        "item": {
          "kind": "text",
          "parent": null,
          "text": "GPU on weekends\n3. "
        }
      },
      {
        "logs_index": 23,
        "created": "2026-09-25T04:48:06.977364+00:00",
        "item": {
          "kind": "text",
          "parent": null,
          "text": "What a bounty "
        }
      },
      {
        "logs_index": 24,
        "created": "2026-09-25T04:48:07.062364+00:00",
        "item": {
          "kind": "text",
          "parent": null,
          "text": "looks like before "
        }
      },
      {
        "logs_index": 25,
        "created": "2026-09-25T04:48:07.154364+00:00",
        "item": {
          "kind": "text",
          "parent": null,
          "text": "money exists\n4. Onboarding "
        }
      },
      {
        "logs_index": 26,
        "created": "2026-09-25T04:48:07.213364+00:00",
        "item": {
          "kind": "text",
          "parent": null,
          "text": "without a tour\n\nThe "
        }
      },
      {
        "logs_index": 27,
        "created": "2026-09-25T04:48:07.279364+00:00",
        "item": {
          "kind": "text",
          "parent": null,
          "text": "third one is "
        }
      },
      {
        "logs_index": 28,
        "created": "2026-09-25T04:48:07.352364+00:00",
        "item": {
          "kind": "text",
          "parent": null,
          "text": "the open question "
        }
      },
      {
        "logs_index": 29,
        "created": "2026-09-25T04:48:07.432364+00:00",
        "item": {
          "kind": "text",
          "parent": null,
          "text": "the others hang "
        }
      },
      {
        "logs_index": 30,
        "created": "2026-09-25T04:48:07.519364+00:00",
        "item": {
          "kind": "text",
          "parent": null,
          "text": "on."
        }
      },
      {
        "logs_index": 31,
        "created": "2026-09-25T04:48:07.669364+00:00",
        "item": {
          "kind": "usage",
          "prompt_tokens": 483,
          "completion_tokens": 117,
          "total_tokens": 600
        }
      },
      {
        "logs_index": 32,
        "created": "2026-09-25T04:48:08.669364+00:00",
        "item": {
          "kind": "inactive",
          "provider": {
            "kind": "outgoing",
            "address": "127.0.0.1:4640"
          }
        }
      }
    ],
    "site-fixes": [
      {
        "logs_index": 1,
        "created": "2026-09-26T00:47:42.750364+00:00",
        "item": {
          "kind": "user_text",
          "key": "m3",
          "text": "Go through the whole site and tidy it, section by section."
        }
      },
      {
        "logs_index": 2,
        "created": "2026-09-26T00:47:42.750364+00:00",
        "item": {
          "kind": "active",
          "provider": {
            "kind": "outgoing",
            "address": "127.0.0.1:4640"
          }
        }
      },
      {
        "logs_index": 3,
        "created": "2026-09-26T00:48:02.751947+00:00",
        "item": {
          "kind": "reasoning",
          "parent": null,
          "text": "Long job. I'll "
        }
      },
      {
        "logs_index": 4,
        "created": "2026-09-26T00:48:02.751965+00:00",
        "item": {
          "kind": "reasoning",
          "parent": null,
          "text": "go file by "
        }
      },
      {
        "logs_index": 5,
        "created": "2026-09-26T00:48:02.752002+00:00",
        "item": {
          "kind": "reasoning",
          "parent": null,
          "text": "file and report "
        }
      },
      {
        "logs_index": 6,
        "created": "2026-09-26T00:48:02.752049+00:00",
        "item": {
          "kind": "reasoning",
          "parent": null,
          "text": "as I go."
        }
      },
      {
        "logs_index": 7,
        "created": "2026-09-26T00:48:02.752084+00:00",
        "item": {
          "kind": "tool_call",
          "parent": null,
          "id": "call_33",
          "name": "Read",
          "arguments": "{\"file_path\":\"/projects/site/styles.css\"}"
        }
      },
      {
        "logs_index": 8,
        "created": "2026-09-26T00:48:02.752115+00:00",
        "item": {
          "kind": "tool_response",
          "parent": null,
          "id": "call_33",
          "is_error": false,
          "text": "body { font-family: system-ui; margin: 0 auto; max-width: 42rem; }\nheader h1 { font-size: 2rem; }\n"
        }
      },
      {
        "logs_index": 9,
        "created": "2026-09-26T00:48:02.752147+00:00",
        "item": {
          "kind": "reasoning",
          "parent": null,
          "text": "Pass 1: /projects/site/styles.css "
        }
      },
      {
        "logs_index": 10,
        "created": "2026-09-26T00:48:02.752177+00:00",
        "item": {
          "kind": "reasoning",
          "parent": null,
          "text": "looks consistent with "
        }
      },
      {
        "logs_index": 11,
        "created": "2026-09-26T00:48:02.752184+00:00",
        "item": {
          "kind": "reasoning",
          "parent": null,
          "text": "the last pass."
        }
      },
      {
        "logs_index": 12,
        "created": "2026-09-26T00:48:02.752202+00:00",
        "item": {
          "kind": "text",
          "parent": null,
          "text": "Pass 1 of "
        }
      },
      {
        "logs_index": 13,
        "created": "2026-09-26T00:48:02.752233+00:00",
        "item": {
          "kind": "text",
          "parent": null,
          "text": "12 done (/projects/site/styles.css). "
        }
      },
      {
        "logs_index": 14,
        "created": "2026-09-26T00:48:02.752251+00:00",
        "item": {
          "kind": "tool_call",
          "parent": null,
          "id": "call_35",
          "name": "Read",
          "arguments": "{\"file_path\":\"/projects/site/README.md\"}"
        }
      },
      {
        "logs_index": 15,
        "created": "2026-09-26T00:48:02.752281+00:00",
        "item": {
          "kind": "tool_response",
          "parent": null,
          "id": "call_35",
          "is_error": false,
          "text": "# site\n\nA small static site. `index.html` and `styles.css`, nothing else.\n"
        }
      },
      {
        "logs_index": 16,
        "created": "2026-09-26T00:48:02.752299+00:00",
        "item": {
          "kind": "reasoning",
          "parent": null,
          "text": "Pass 2: /projects/site/README.md "
        }
      },
      {
        "logs_index": 17,
        "created": "2026-09-26T00:48:02.752316+00:00",
        "item": {
          "kind": "reasoning",
          "parent": null,
          "text": "looks consistent with "
        }
      },
      {
        "logs_index": 18,
        "created": "2026-09-26T00:48:02.752333+00:00",
        "item": {
          "kind": "reasoning",
          "parent": null,
          "text": "the last pass."
        }
      },
      {
        "logs_index": 19,
        "created": "2026-09-26T00:48:02.752350+00:00",
        "item": {
          "kind": "text",
          "parent": null,
          "text": "Pass 2 of "
        }
      },
      {
        "logs_index": 20,
        "created": "2026-09-26T00:48:02.752379+00:00",
        "item": {
          "kind": "text",
          "parent": null,
          "text": "12 done (/projects/site/README.md). "
        }
      },
      {
        "logs_index": 21,
        "created": "2026-09-26T00:48:02.752396+00:00",
        "item": {
          "kind": "tool_call",
          "parent": null,
          "id": "call_37",
          "name": "Read",
          "arguments": "{\"file_path\":\"/projects/site/index.html\"}"
        }
      },
      {
        "logs_index": 22,
        "created": "2026-09-26T00:48:02.752425+00:00",
        "item": {
          "kind": "tool_response",
          "parent": null,
          "id": "call_37",
          "is_error": false,
          "text": "<!doctype html>\n<html lang=\"en\">\n  <head>\n    <meta charset=\"utf-8\" />\n    <title>Workshop</title>\n    <link rel=\"stylesheet\" href=\"styles.css\" />\n  </head>\n  <body>\n    <header><h1>Workshop</h1></header>\n    <main>\n      <p>Things I'm building, and how to reach me about them.</p>\n      <a href=\"/projects\">Projects</a>\n      <a href=\"/notes\">Notes</a>\n      <a href=\"mailto:hello@example.com\">Email</a>\n      <a href=\"/old-page\">Archive</a>\n    </main>\n  </body>\n</html>\n"
        }
      },
      {
        "logs_index": 23,
        "created": "2026-09-26T00:48:02.752442+00:00",
        "item": {
          "kind": "reasoning",
          "parent": null,
          "text": "Pass 3: /projects/site/index.html "
        }
      },
      {
        "logs_index": 24,
        "created": "2026-09-26T00:48:02.752447+00:00",
        "item": {
          "kind": "reasoning",
          "parent": null,
          "text": "looks consistent with "
        }
      },
      {
        "logs_index": 25,
        "created": "2026-09-26T00:48:02.752463+00:00",
        "item": {
          "kind": "reasoning",
          "parent": null,
          "text": "the last pass."
        }
      },
      {
        "logs_index": 26,
        "created": "2026-09-26T00:48:02.752480+00:00",
        "item": {
          "kind": "text",
          "parent": null,
          "text": "Pass 3 of "
        }
      },
      {
        "logs_index": 27,
        "created": "2026-09-26T00:48:02.752509+00:00",
        "item": {
          "kind": "text",
          "parent": null,
          "text": "12 done (/projects/site/index.html). "
        }
      },
      {
        "logs_index": 28,
        "created": "2026-09-26T00:48:02.752525+00:00",
        "item": {
          "kind": "tool_call",
          "parent": null,
          "id": "call_39",
          "name": "Read",
          "arguments": "{\"file_path\":\"/projects/site/styles.css\"}"
        }
      },
      {
        "logs_index": 29,
        "created": "2026-09-26T00:48:02.752570+00:00",
        "item": {
          "kind": "tool_response",
          "parent": null,
          "id": "call_39",
          "is_error": false,
          "text": "body { font-family: system-ui; margin: 0 auto; max-width: 42rem; }\nheader h1 { font-size: 2rem; }\n"
        }
      },
      {
        "logs_index": 30,
        "created": "2026-09-26T00:48:02.752594+00:00",
        "item": {
          "kind": "reasoning",
          "parent": null,
          "text": "Pass 4: /projects/site/styles.css "
        }
      },
      {
        "logs_index": 31,
        "created": "2026-09-26T00:48:02.752614+00:00",
        "item": {
          "kind": "reasoning",
          "parent": null,
          "text": "looks consistent with "
        }
      },
      {
        "logs_index": 32,
        "created": "2026-09-26T00:48:02.752632+00:00",
        "item": {
          "kind": "reasoning",
          "parent": null,
          "text": "the last pass."
        }
      },
      {
        "logs_index": 33,
        "created": "2026-09-26T00:48:02.752650+00:00",
        "item": {
          "kind": "text",
          "parent": null,
          "text": "Pass 4 of "
        }
      },
      {
        "logs_index": 34,
        "created": "2026-09-26T00:48:02.752683+00:00",
        "item": {
          "kind": "text",
          "parent": null,
          "text": "12 done (/projects/site/styles.css). "
        }
      },
      {
        "logs_index": 35,
        "created": "2026-09-26T00:48:02.752701+00:00",
        "item": {
          "kind": "tool_call",
          "parent": null,
          "id": "call_41",
          "name": "Read",
          "arguments": "{\"file_path\":\"/projects/site/README.md\"}"
        }
      },
      {
        "logs_index": 36,
        "created": "2026-09-26T00:48:02.752718+00:00",
        "item": {
          "kind": "tool_response",
          "parent": null,
          "id": "call_41",
          "is_error": false,
          "text": "# site\n\nA small static site. `index.html` and `styles.css`, nothing else.\n"
        }
      },
      {
        "logs_index": 37,
        "created": "2026-09-26T00:48:02.752736+00:00",
        "item": {
          "kind": "reasoning",
          "parent": null,
          "text": "Pass 5: /projects/site/README.md "
        }
      },
      {
        "logs_index": 38,
        "created": "2026-09-26T00:48:02.752740+00:00",
        "item": {
          "kind": "reasoning",
          "parent": null,
          "text": "looks consistent with "
        }
      },
      {
        "logs_index": 39,
        "created": "2026-09-26T00:48:02.752757+00:00",
        "item": {
          "kind": "reasoning",
          "parent": null,
          "text": "the last pass."
        }
      },
      {
        "logs_index": 40,
        "created": "2026-09-26T00:48:02.752774+00:00",
        "item": {
          "kind": "text",
          "parent": null,
          "text": "Pass 5 of "
        }
      }
    ]
  },
  "machines": [
    {
      "identity": {
        "kind": "outgoing",
        "address": "127.0.0.1:4640"
      },
      "volumes": [
        "workspace",
        "datasets"
      ],
      "added": "2026-09-17T00:48:02.750364+00:00"
    },
    {
      "identity": {
        "kind": "incoming_unbrokered",
        "identity": "studio-pc"
      },
      "volumes": [
        "media"
      ],
      "added": "2026-09-23T00:48:02.750364+00:00"
    }
  ],
  "tree": [
    {
      "kind": "directory",
      "name": "datasets",
      "watched": true,
      "children": [
        {
          "kind": "file",
          "name": "README.md",
          "size": 30,
          "modified_at": 1790383682
        }
      ]
    },
    {
      "kind": "directory",
      "name": "notes",
      "watched": true,
      "children": [
        {
          "kind": "file",
          "name": "ideas.md",
          "size": 190,
          "modified_at": 1790383682
        },
        {
          "kind": "file",
          "name": "reading-list.md",
          "size": 115,
          "modified_at": 1790383682
        }
      ]
    },
    {
      "kind": "directory",
      "name": "projects",
      "watched": true,
      "children": [
        {
          "kind": "directory",
          "name": "site",
          "watched": true,
          "children": [
            {
              "kind": "file",
              "name": "README.md",
              "size": 74,
              "modified_at": 1790383682
            },
            {
              "kind": "file",
              "name": "index.html",
              "size": 473,
              "modified_at": 1790383682
            },
            {
              "kind": "file",
              "name": "styles.css",
              "size": 98,
              "modified_at": 1790383682
            }
          ]
        }
      ]
    }
  ],
  "files": {
    "/datasets/README.md": "# datasets\n\nNothing here yet.\n",
    "/notes/ideas.md": "# ideas\n\n- A shop on my profile for the poster packs\n- Ask if the studio PC's GPU can be borrowed on weekends\n- What does a bounty look like before money exists?\n- Onboarding without a tour\n",
    "/notes/reading-list.md": "# reading list\n\n- The provider protocol, 2.3.0\n- Notes on local-first software\n- Feedback owed on a friend's draft\n",
    "/projects/site/README.md": "# site\n\nA small static site. `index.html` and `styles.css`, nothing else.\n",
    "/projects/site/index.html": "<!doctype html>\n<html lang=\"en\">\n  <head>\n    <meta charset=\"utf-8\" />\n    <title>Workshop</title>\n    <link rel=\"stylesheet\" href=\"styles.css\" />\n  </head>\n  <body>\n    <header><h1>Workshop</h1></header>\n    <main>\n      <p>Things I'm building, and how to reach me about them.</p>\n      <a href=\"/projects\">Projects</a>\n      <a href=\"/notes\">Notes</a>\n      <a href=\"mailto:hello@example.com\">Email</a>\n      <a href=\"/old-page\">Archive</a>\n    </main>\n  </body>\n</html>\n",
    "/projects/site/styles.css": "body { font-family: system-ui; margin: 0 auto; max-width: 42rem; }\nheader h1 { font-size: 2rem; }\n"
  }
}