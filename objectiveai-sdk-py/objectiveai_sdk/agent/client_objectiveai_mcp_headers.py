# THIS FILE IS AUTO-GENERATED. DO NOT EDIT.
# Generated by scripts/install_pydantic.py from objectiveai-json-schema/.
from __future__ import annotations
from pydantic import BaseModel, ConfigDict, Field
from objectiveai_sdk.agent.client_objectiveai_mcp_entry import ClientObjectiveaiMcpEntry


class ClientObjectiveaiMcpHeaders(BaseModel):
    """Snapshot of the three transient `X-OBJECTIVEAI-MCP-*` request
headers an HTTP caller stamps on its initial dial of the
objectiveai-mcp proxy upstream.

Produced by [`ClientObjectiveaiMcp::mcp_headers`] — see that
method's doc for the per-field source rules and sort order."""
    model_config = ConfigDict(title='agent.ClientObjectiveaiMcpHeaders')

    plugins: list[ClientObjectiveaiMcpEntry] = Field(..., description="Plugins the agent expects the client to expose, filtered to\n`executable: true` entries only, projected onto the\n`(owner, name, version)` shape, and sorted by\n`(owner, name, version)`. Becomes the\n`X-OBJECTIVEAI-MCP-PLUGINS` header's JSON payload.")
    root: bool = Field(..., description='Becomes the `X-OBJECTIVEAI-MCP-ROOT` header value verbatim\n(`"true"` / `"false"`).')
    tools: list[ClientObjectiveaiMcpEntry] = Field(..., description="Tools the agent expects the client to expose, sorted by\n`(owner, name, version)`. Becomes the\n`X-OBJECTIVEAI-MCP-TOOLS` header's JSON payload.")

