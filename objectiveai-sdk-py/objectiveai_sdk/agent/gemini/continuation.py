# THIS FILE IS AUTO-GENERATED. DO NOT EDIT.
# Generated by scripts/install_pydantic.py from objectiveai-json-schema/.
from __future__ import annotations
from pydantic import BaseModel, ConfigDict, Field
from objectiveai_sdk.agent.gemini.message import Message
from objectiveai_sdk.agent.gemini.upstream import Upstream


class Continuation(BaseModel):
    model_config = ConfigDict(title='agent.gemini.Continuation')

    agent_instance_hierarchy: str = Field(..., description="Full slash-separated lineage of the agent this continuation\nbelongs to (e.g. `A/B/agtcpl-<uuid>-<created>`). Minted on the\nagent's first spawn and preserved verbatim across every\ncontinuation round so the agent's identity stays stable\nregardless of who resumes the conversation.")
    mcp_sessions: dict[str, str]
    messages: list[Message] = Field(..., description="Full prior conversation in the runner's message shape. The\nGemini runner is stateless (no server-side session resume), so\nthe API persists the entire history here and replays it — prior\nhistory followed by the next turn's messages — on every\ncontinuation round. Empty on a fresh conversation.", json_schema_extra={'omitempty': True})
    session_id: str
    upstream: Upstream

