# THIS FILE IS AUTO-GENERATED. DO NOT EDIT.
# Generated by scripts/install_pydantic.py from objectiveai-json-schema/.
from __future__ import annotations
from typing import Literal, Union
from pydantic import BaseModel, ConfigDict, Field, RootModel
from objectiveai_sdk.client_objectiveai_mcp.mcp_kind import McpKind
from objectiveai_sdk.client_objectiveai_mcp.server_request.initialize_request import InitializeRequest
from objectiveai_sdk.mcp.resource.list_resources_request import ListResourcesRequest
from objectiveai_sdk.mcp.resource.read_resource_request_params import ReadResourceRequestParams
from objectiveai_sdk.mcp.tool.call_tool_request_params import CallToolRequestParams
from objectiveai_sdk.mcp.tool.list_tools_request import ListToolsRequest


class RequestInitialize(InitializeRequest):
    """POST `initialize`. The proxy's `protocolVersion` doesn't ride
across this hop — the API discards it on the way in and
substitutes its own `canonical_initialize_result` on the way
out. The variant carries the plugin arguments the CLI needs at
dial time (parsed by the API off the URL query string)."""
    model_config = ConfigDict(json_schema_extra={'_variant_title': 'Initialize'})

    headers: dict[str, str] = Field(..., description='Verbatim copy of the headers the proxy sent on its HTTP\nrequest to the API. The CLI conduit reads several custom\n`X-OBJECTIVEAI-*` routing headers + `Mcp-Session-Id` off this\nmap; protocol-level headers (Host, Content-Length, …) the API\nalready stripped on its way in.', json_schema_extra={'omitempty': True})
    id: str = Field(..., description='Server-minted correlation id. Echoed by the matching\n[`super::super::server_response::Response`].')
    mcp_kind: McpKind = Field(..., description='Which CLI-hosted MCP server this request targets.')
    type_: Literal['initialize'] = Field(..., alias='type')


class RequestToolsList(ListToolsRequest):
    """POST `tools/list`."""
    model_config = ConfigDict(json_schema_extra={'_variant_title': 'ToolsList'})

    headers: dict[str, str] = Field(..., description='Verbatim copy of the headers the proxy sent on its HTTP\nrequest to the API. The CLI conduit reads several custom\n`X-OBJECTIVEAI-*` routing headers + `Mcp-Session-Id` off this\nmap; protocol-level headers (Host, Content-Length, …) the API\nalready stripped on its way in.', json_schema_extra={'omitempty': True})
    id: str = Field(..., description='Server-minted correlation id. Echoed by the matching\n[`super::super::server_response::Response`].')
    mcp_kind: McpKind = Field(..., description='Which CLI-hosted MCP server this request targets.')
    type_: Literal['tools_list'] = Field(..., alias='type')


class RequestToolsCall(CallToolRequestParams):
    """POST `tools/call`."""
    model_config = ConfigDict(json_schema_extra={'_variant_title': 'ToolsCall'})

    headers: dict[str, str] = Field(..., description='Verbatim copy of the headers the proxy sent on its HTTP\nrequest to the API. The CLI conduit reads several custom\n`X-OBJECTIVEAI-*` routing headers + `Mcp-Session-Id` off this\nmap; protocol-level headers (Host, Content-Length, …) the API\nalready stripped on its way in.', json_schema_extra={'omitempty': True})
    id: str = Field(..., description='Server-minted correlation id. Echoed by the matching\n[`super::super::server_response::Response`].')
    mcp_kind: McpKind = Field(..., description='Which CLI-hosted MCP server this request targets.')
    type_: Literal['tools_call'] = Field(..., alias='type')


class RequestResourcesList(ListResourcesRequest):
    """POST `resources/list`."""
    model_config = ConfigDict(json_schema_extra={'_variant_title': 'ResourcesList'})

    headers: dict[str, str] = Field(..., description='Verbatim copy of the headers the proxy sent on its HTTP\nrequest to the API. The CLI conduit reads several custom\n`X-OBJECTIVEAI-*` routing headers + `Mcp-Session-Id` off this\nmap; protocol-level headers (Host, Content-Length, …) the API\nalready stripped on its way in.', json_schema_extra={'omitempty': True})
    id: str = Field(..., description='Server-minted correlation id. Echoed by the matching\n[`super::super::server_response::Response`].')
    mcp_kind: McpKind = Field(..., description='Which CLI-hosted MCP server this request targets.')
    type_: Literal['resources_list'] = Field(..., alias='type')


class RequestResourcesRead(ReadResourceRequestParams):
    """POST `resources/read`."""
    model_config = ConfigDict(json_schema_extra={'_variant_title': 'ResourcesRead'})

    headers: dict[str, str] = Field(..., description='Verbatim copy of the headers the proxy sent on its HTTP\nrequest to the API. The CLI conduit reads several custom\n`X-OBJECTIVEAI-*` routing headers + `Mcp-Session-Id` off this\nmap; protocol-level headers (Host, Content-Length, …) the API\nalready stripped on its way in.', json_schema_extra={'omitempty': True})
    id: str = Field(..., description='Server-minted correlation id. Echoed by the matching\n[`super::super::server_response::Response`].')
    mcp_kind: McpKind = Field(..., description='Which CLI-hosted MCP server this request targets.')
    type_: Literal['resources_read'] = Field(..., alias='type')


class RequestSessionTerminate(BaseModel):
    """`DELETE` on the routed MCP URL — the proxy closing the
session. No body."""
    model_config = ConfigDict(json_schema_extra={'_variant_title': 'SessionTerminate'})

    headers: dict[str, str] = Field(..., description='Verbatim copy of the headers the proxy sent on its HTTP\nrequest to the API. The CLI conduit reads several custom\n`X-OBJECTIVEAI-*` routing headers + `Mcp-Session-Id` off this\nmap; protocol-level headers (Host, Content-Length, …) the API\nalready stripped on its way in.', json_schema_extra={'omitempty': True})
    id: str = Field(..., description='Server-minted correlation id. Echoed by the matching\n[`super::super::server_response::Response`].')
    mcp_kind: McpKind = Field(..., description='Which CLI-hosted MCP server this request targets.')
    type_: Literal['session_terminate'] = Field(..., alias='type')


class Request(RootModel):
    """One reverse-attach request the API has shipped to the calling
client. The proxy's HTTP method (`POST` for the five JSON-RPC
methods, `DELETE` for session terminate) is implicit in the
[`super::Payload`] variant; the JSON-RPC `{jsonrpc, id, method,
params}` envelope is unwrapped into the typed variant payload.

Which CLI-hosted MCP server the request targets rides as
`mcp_kind` on the envelope. The API parses this off the inbound
URL path (`/objectiveai` → [`super::super::McpKind::ObjectiveAi`];
`/{owner}/{name}/{version}/{mcp}` → [`super::super::McpKind::Other`])
before forwarding.

Wire shape (envelope is `{id, mcp_kind, headers?, type, …variant
fields…}` after the `#[serde(flatten)]` on `payload`):

```json
{
  "id":"…",
  "mcp_kind":{"type":"objective_ai"},
  "headers":{"Mcp-Session-Id":"…"},
  "type":"tools_list",
  "cursor":"…"
}
```"""
    model_config = ConfigDict(title='client_objectiveai_mcp.server_request.Request')

    root: Union[RequestInitialize, RequestToolsList, RequestToolsCall, RequestResourcesList, RequestResourcesRead, RequestSessionTerminate]

