# THIS FILE IS AUTO-GENERATED. DO NOT EDIT.
# Generated by scripts/install_pydantic.py from objectiveai-json-schema/.
from __future__ import annotations
from typing import Literal, Union
from objectiveai_sdk.json_value import JsonValue
from pydantic import BaseModel, ConfigDict, Field, RootModel
from objectiveai_sdk.client_objectiveai_mcp.mcp_kind import McpKind
from objectiveai_sdk.client_objectiveai_mcp.server_response.initialize_reply import InitializeReply
from objectiveai_sdk.mcp.resource.list_resources_result import ListResourcesResult
from objectiveai_sdk.mcp.resource.read_resource_result import ReadResourceResult
from objectiveai_sdk.mcp.tool.call_tool_result import CallToolResult
from objectiveai_sdk.mcp.tool.list_tools_result import ListToolsResult


class ResponseInitializeOk(BaseModel):
    """Method returned a typed result."""
    model_config = ConfigDict(json_schema_extra={'_variant_title': 'Ok'})

    id: str = Field(..., description='Matches the `id` of the\n[`super::super::server_request::Request`] this response is for.')
    kind: Literal['ok']
    mcp_kind: McpKind = Field(..., description="Echoes the request envelope's `mcp_kind` so the API can\nconfirm the CLI dispatched to the right per-MCP handler.")
    result: InitializeReply


class ResponseInitializeErr(BaseModel):
    """Method returned a JSON-RPC error envelope."""
    model_config = ConfigDict(json_schema_extra={'_variant_title': 'Err'})

    code: int = Field(..., ge=-9223372036854775808, le=9223372036854775807)
    data: JsonValue = Field(..., json_schema_extra={'omitempty': True})
    id: str = Field(..., description='Matches the `id` of the\n[`super::super::server_request::Request`] this response is for.')
    kind: Literal['err']
    mcp_kind: McpKind = Field(..., description="Echoes the request envelope's `mcp_kind` so the API can\nconfirm the CLI dispatched to the right per-MCP handler.")
    message: str


class ResponseInitialize(RootModel):
    """Reply to
[`super::super::server_request::Payload::Initialize`]. On
success carries the upstream MCP session id the API stamps
onto its outbound `Mcp-Session-Id` response header so the
proxy adopts it. On failure (dial or aggregate-build error)
carries a JSON-RPC error envelope the API translates into its
own outbound error.

JSON-RPC result/error shape for every typed method. Mirrors
the wire shape upstream MCP servers return (`{result: …}` on
success, `{error: {code, message, data?}}` on failure) but typed
at the SDK level instead of buried inside a `serde_json::Value`."""
    model_config = ConfigDict(json_schema_extra={'_variant_title': 'Initialize'})

    root: Union[ResponseInitializeOk, ResponseInitializeErr]


class ResponseToolsListOk(BaseModel):
    """Method returned a typed result."""
    model_config = ConfigDict(json_schema_extra={'_variant_title': 'Ok'})

    id: str = Field(..., description='Matches the `id` of the\n[`super::super::server_request::Request`] this response is for.')
    kind: Literal['ok']
    mcp_kind: McpKind = Field(..., description="Echoes the request envelope's `mcp_kind` so the API can\nconfirm the CLI dispatched to the right per-MCP handler.")
    result: ListToolsResult


class ResponseToolsListErr(BaseModel):
    """Method returned a JSON-RPC error envelope."""
    model_config = ConfigDict(json_schema_extra={'_variant_title': 'Err'})

    code: int = Field(..., ge=-9223372036854775808, le=9223372036854775807)
    data: JsonValue = Field(..., json_schema_extra={'omitempty': True})
    id: str = Field(..., description='Matches the `id` of the\n[`super::super::server_request::Request`] this response is for.')
    kind: Literal['err']
    mcp_kind: McpKind = Field(..., description="Echoes the request envelope's `mcp_kind` so the API can\nconfirm the CLI dispatched to the right per-MCP handler.")
    message: str


class ResponseToolsList(RootModel):
    """Reply to [`super::super::server_request::Payload::ToolsList`].

JSON-RPC result/error shape for every typed method. Mirrors
the wire shape upstream MCP servers return (`{result: …}` on
success, `{error: {code, message, data?}}` on failure) but typed
at the SDK level instead of buried inside a `serde_json::Value`."""
    model_config = ConfigDict(json_schema_extra={'_variant_title': 'ToolsList'})

    root: Union[ResponseToolsListOk, ResponseToolsListErr]


class ResponseToolsCallOk(BaseModel):
    """Method returned a typed result."""
    model_config = ConfigDict(json_schema_extra={'_variant_title': 'Ok'})

    id: str = Field(..., description='Matches the `id` of the\n[`super::super::server_request::Request`] this response is for.')
    kind: Literal['ok']
    mcp_kind: McpKind = Field(..., description="Echoes the request envelope's `mcp_kind` so the API can\nconfirm the CLI dispatched to the right per-MCP handler.")
    result: CallToolResult


class ResponseToolsCallErr(BaseModel):
    """Method returned a JSON-RPC error envelope."""
    model_config = ConfigDict(json_schema_extra={'_variant_title': 'Err'})

    code: int = Field(..., ge=-9223372036854775808, le=9223372036854775807)
    data: JsonValue = Field(..., json_schema_extra={'omitempty': True})
    id: str = Field(..., description='Matches the `id` of the\n[`super::super::server_request::Request`] this response is for.')
    kind: Literal['err']
    mcp_kind: McpKind = Field(..., description="Echoes the request envelope's `mcp_kind` so the API can\nconfirm the CLI dispatched to the right per-MCP handler.")
    message: str


class ResponseToolsCall(RootModel):
    """Reply to [`super::super::server_request::Payload::ToolsCall`].

JSON-RPC result/error shape for every typed method. Mirrors
the wire shape upstream MCP servers return (`{result: …}` on
success, `{error: {code, message, data?}}` on failure) but typed
at the SDK level instead of buried inside a `serde_json::Value`."""
    model_config = ConfigDict(json_schema_extra={'_variant_title': 'ToolsCall'})

    root: Union[ResponseToolsCallOk, ResponseToolsCallErr]


class ResponseResourcesListOk(BaseModel):
    """Method returned a typed result."""
    model_config = ConfigDict(json_schema_extra={'_variant_title': 'Ok'})

    id: str = Field(..., description='Matches the `id` of the\n[`super::super::server_request::Request`] this response is for.')
    kind: Literal['ok']
    mcp_kind: McpKind = Field(..., description="Echoes the request envelope's `mcp_kind` so the API can\nconfirm the CLI dispatched to the right per-MCP handler.")
    result: ListResourcesResult


class ResponseResourcesListErr(BaseModel):
    """Method returned a JSON-RPC error envelope."""
    model_config = ConfigDict(json_schema_extra={'_variant_title': 'Err'})

    code: int = Field(..., ge=-9223372036854775808, le=9223372036854775807)
    data: JsonValue = Field(..., json_schema_extra={'omitempty': True})
    id: str = Field(..., description='Matches the `id` of the\n[`super::super::server_request::Request`] this response is for.')
    kind: Literal['err']
    mcp_kind: McpKind = Field(..., description="Echoes the request envelope's `mcp_kind` so the API can\nconfirm the CLI dispatched to the right per-MCP handler.")
    message: str


class ResponseResourcesList(RootModel):
    """Reply to
[`super::super::server_request::Payload::ResourcesList`].

JSON-RPC result/error shape for every typed method. Mirrors
the wire shape upstream MCP servers return (`{result: …}` on
success, `{error: {code, message, data?}}` on failure) but typed
at the SDK level instead of buried inside a `serde_json::Value`."""
    model_config = ConfigDict(json_schema_extra={'_variant_title': 'ResourcesList'})

    root: Union[ResponseResourcesListOk, ResponseResourcesListErr]


class ResponseResourcesReadOk(BaseModel):
    """Method returned a typed result."""
    model_config = ConfigDict(json_schema_extra={'_variant_title': 'Ok'})

    id: str = Field(..., description='Matches the `id` of the\n[`super::super::server_request::Request`] this response is for.')
    kind: Literal['ok']
    mcp_kind: McpKind = Field(..., description="Echoes the request envelope's `mcp_kind` so the API can\nconfirm the CLI dispatched to the right per-MCP handler.")
    result: ReadResourceResult


class ResponseResourcesReadErr(BaseModel):
    """Method returned a JSON-RPC error envelope."""
    model_config = ConfigDict(json_schema_extra={'_variant_title': 'Err'})

    code: int = Field(..., ge=-9223372036854775808, le=9223372036854775807)
    data: JsonValue = Field(..., json_schema_extra={'omitempty': True})
    id: str = Field(..., description='Matches the `id` of the\n[`super::super::server_request::Request`] this response is for.')
    kind: Literal['err']
    mcp_kind: McpKind = Field(..., description="Echoes the request envelope's `mcp_kind` so the API can\nconfirm the CLI dispatched to the right per-MCP handler.")
    message: str


class ResponseResourcesRead(RootModel):
    """Reply to
[`super::super::server_request::Payload::ResourcesRead`].

JSON-RPC result/error shape for every typed method. Mirrors
the wire shape upstream MCP servers return (`{result: …}` on
success, `{error: {code, message, data?}}` on failure) but typed
at the SDK level instead of buried inside a `serde_json::Value`."""
    model_config = ConfigDict(json_schema_extra={'_variant_title': 'ResourcesRead'})

    root: Union[ResponseResourcesReadOk, ResponseResourcesReadErr]


class ResponseSessionTerminateOk(BaseModel):
    """Method returned a typed result."""
    model_config = ConfigDict(json_schema_extra={'_variant_title': 'Ok'})

    id: str = Field(..., description='Matches the `id` of the\n[`super::super::server_request::Request`] this response is for.')
    kind: Literal['ok']
    mcp_kind: McpKind = Field(..., description="Echoes the request envelope's `mcp_kind` so the API can\nconfirm the CLI dispatched to the right per-MCP handler.")
    result: None


class ResponseSessionTerminateErr(BaseModel):
    """Method returned a JSON-RPC error envelope."""
    model_config = ConfigDict(json_schema_extra={'_variant_title': 'Err'})

    code: int = Field(..., ge=-9223372036854775808, le=9223372036854775807)
    data: JsonValue = Field(..., json_schema_extra={'omitempty': True})
    id: str = Field(..., description='Matches the `id` of the\n[`super::super::server_request::Request`] this response is for.')
    kind: Literal['err']
    mcp_kind: McpKind = Field(..., description="Echoes the request envelope's `mcp_kind` so the API can\nconfirm the CLI dispatched to the right per-MCP handler.")
    message: str


class ResponseSessionTerminate(RootModel):
    """Acknowledges
[`super::super::server_request::Payload::SessionTerminate`].
On success carries the unit value (no body); on failure
carries the upstream-delete error so the proxy sees a
non-2xx and can retry.

JSON-RPC result/error shape for every typed method. Mirrors
the wire shape upstream MCP servers return (`{result: …}` on
success, `{error: {code, message, data?}}` on failure) but typed
at the SDK level instead of buried inside a `serde_json::Value`."""
    model_config = ConfigDict(json_schema_extra={'_variant_title': 'SessionTerminate'})

    root: Union[ResponseSessionTerminateOk, ResponseSessionTerminateErr]


class Response(RootModel):
    """One reply the calling client's `McpHandler` produced for a
[`super::super::server_request::Request`]. The typed
[`super::Payload`] variant pairs by name with the request side;
every method-specific result or error rides inside it. `mcp_kind`
echoes the request's so the API can sanity-check the routing
without trusting `id` alone.

Wire shape (envelope is `{id, mcp_kind, type, …variant fields…}`
after the `#[serde(flatten)]` on `payload`):

```json
{
  "id":"…",
  "mcp_kind":{"type":"objective_ai"},
  "type":"tools_list",
  "kind":"ok",
  "result":{…}
}
```"""
    model_config = ConfigDict(title='client_objectiveai_mcp.server_response.Response')

    root: Union[ResponseInitialize, ResponseToolsList, ResponseToolsCall, ResponseResourcesList, ResponseResourcesRead, ResponseSessionTerminate]

