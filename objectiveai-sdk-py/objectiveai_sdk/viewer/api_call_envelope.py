# THIS FILE IS AUTO-GENERATED. DO NOT EDIT.
# Generated by scripts/install_pydantic.py from objectiveai-json-schema/.
from __future__ import annotations
from typing import Literal, Union
from objectiveai_sdk.json_value import JsonValue
from pydantic import BaseModel, ConfigDict, Field, RootModel


class ApiCallEnvelopeBegin(BaseModel):
    model_config = ConfigDict(json_schema_extra={'_variant_title': 'Begin'})

    type_: Literal['begin'] = Field(..., alias='type')


class ApiCallEnvelopeChunk(BaseModel):
    model_config = ConfigDict(json_schema_extra={'_variant_title': 'Chunk'})

    chunk: JsonValue
    type_: Literal['chunk'] = Field(..., alias='type')


class ApiCallEnvelopeError(BaseModel):
    model_config = ConfigDict(json_schema_extra={'_variant_title': 'Error'})

    error: JsonValue
    type_: Literal['error'] = Field(..., alias='type')


class ApiCallEnvelopeEnd(BaseModel):
    model_config = ConfigDict(json_schema_extra={'_variant_title': 'End'})

    type_: Literal['end'] = Field(..., alias='type')


class ApiCallEnvelope(RootModel):
    """Wire-format envelope for each value the viewer host emits as
[`Event::ApiCall.value`](super::Event::ApiCall) while servicing one
`api-call-invoke` request.

Mirrors the cli's [`Output<T>`](crate::cli::output::Output)
envelope shape so iframe consumers can apply the same JSONL state
machine to both:

1. `Begin` — exactly one, emitted before any data.
2. `Chunk { chunk }` — one per SSE event for streaming endpoints,
   or one total for unary endpoints (carrying the parsed response
   body).
3. `Error { error }` — only on dispatch failure; replaces any
   further `Chunk`s for that invocation.
4. `End` — exactly one, emitted last; signals the AsyncIterable on
   the JS side to terminate."""
    model_config = ConfigDict(title='viewer.ApiCallEnvelope')

    root: Union[ApiCallEnvelopeBegin, ApiCallEnvelopeChunk, ApiCallEnvelopeError, ApiCallEnvelopeEnd]

