# THIS FILE IS AUTO-GENERATED. DO NOT EDIT.
# Generated by scripts/install_pydantic.py from objectiveai-json-schema/.
from __future__ import annotations
from typing import Annotated, Optional
from pydantic import BaseModel, ConfigDict, Field


class Vote(BaseModel):
    """A single LLM's vote in a vector completion.

Each LLM in the swarm produces a vote indicating which response(s) it
selected. Votes are weighted according to the profile and combined to
produce the final scores.

# Vote Format

The `vote` field is a vector of decimals corresponding to the responses
in the request. Typically one element is 1.0 and the rest are 0.0 (discrete
selection), but when `top_logprobs` is used, votes may be probability
distributions."""
    model_config = ConfigDict(title='vector.completions.response.Vote')

    agent_full_id: str = Field(..., description="WF-level id of the agent that produced this vote — concatenation\nof the primary agent's id with all fallback ids (see\n`InlineAgentWithFallbacks::full_id`). Same for every slot in the\nsame WF request and deterministic across api processes.")
    agent_id: str = Field(..., description="Leaf agent id of the slot that produced this vote (matches the\n`agent_id` on the corresponding\n[`super::super::super::super::agent::completions::response::unary::AgentCompletion`]).\nWhen fallbacks fired, this is the fallback's id rather than the\nprimary's.")
    completion_index: Optional[Annotated[int, Field(ge=0, le=18446744073709551615)]] = Field(None, description='Index correlating this vote with its completion — equals the\n`index` on the corresponding\n[`vector::completions::response::streaming::AgentCompletionChunk`](super::streaming::AgentCompletionChunk)\nwrapper. Populated by the API; `None` only in hand-built values.', json_schema_extra={'omitempty': True})
    flat_swarm_index: int = Field(..., description='Flattened index accounting for agent counts in the swarm.', ge=0, le=18446744073709551615)
    prompt_id: str = Field(..., description='Content hash of the request messages (for caching/deduplication).')
    responses_ids: list[str] = Field(..., description='Content hashes of each response option in the request.')
    swarm_index: int = Field(..., description='Index of the agent configuration within the swarm.', ge=0, le=18446744073709551615)
    vote: list[Annotated[float, Field(ge=-3.4028234663852886e+38, le=3.4028234663852886e+38)]] = Field(..., description='The vote distribution. Each index corresponds to a response from the\nrequest. Typically one element is 1.0 (selected) and the rest are 0.0.')
    weight: float = Field(..., description='The weight applied to this vote when computing final scores.', ge=-3.4028234663852886e+38, le=3.4028234663852886e+38)

