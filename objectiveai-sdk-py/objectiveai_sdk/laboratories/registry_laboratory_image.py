# THIS FILE IS AUTO-GENERATED. DO NOT EDIT.
# Generated by scripts/install_pydantic.py from objectiveai-json-schema/.
from __future__ import annotations
from typing import Union
from pydantic import BaseModel, ConfigDict, Field, RootModel


class RegistryLaboratoryImageTag(BaseModel):
    """`registry/name:tag`."""
    model_config = ConfigDict(json_schema_extra={'_variant_title': 'Tag'})

    name: str = Field(..., description='The repository path — e.g. `library/bash`, `myorg/myimage`.')
    registry: str = Field(..., description='The image host — e.g. `docker.io`, `ghcr.io`,\n`registry.example.com:5000`, `localhost:5000`. Required and\nvalidated to be unambiguously a registry, so short-name\nresolution never happens.')
    tag: str


class RegistryLaboratoryImageDigest(BaseModel):
    """`registry/name@digest` — content-addressed, immutable."""
    model_config = ConfigDict(json_schema_extra={'_variant_title': 'Digest'})

    digest: str
    name: str = Field(..., description='The repository path — e.g. `library/bash`, `myorg/myimage`.')
    registry: str = Field(..., description='The image host — e.g. `docker.io`, `ghcr.io`,\n`registry.example.com:5000`, `localhost:5000`. Required and\nvalidated to be unambiguously a registry, so short-name\nresolution never happens.')


class RegistryLaboratoryImage(RootModel):
    """One registry-hosted laboratory base image, split into its
reference parts."""
    model_config = ConfigDict(title='laboratories.RegistryLaboratoryImage')

    root: Union[RegistryLaboratoryImageTag, RegistryLaboratoryImageDigest]

