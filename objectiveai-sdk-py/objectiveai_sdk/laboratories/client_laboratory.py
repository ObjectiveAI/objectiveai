# THIS FILE IS AUTO-GENERATED. DO NOT EDIT.
# Generated by scripts/install_pydantic.py from objectiveai-json-schema/.
from __future__ import annotations
from typing import Optional
from pydantic import BaseModel, ConfigDict, Field
from objectiveai_sdk.laboratories.client_laboratory_type import ClientLaboratoryType


class ClientLaboratory(BaseModel):
    """A client-resolved laboratory: a client-side MCP server keyed by an
opaque `id`. Wire shape: `{"type":"client","id":"…"}`.

Laboratory ids are only unique per (machine, state) — the same id
can exist on different laboratory hosts. `machine` + `machine_state`
pin THE laboratory this value means, so downstream routing (the
CLI conduit's `/laboratory` forward) is exact rather than
first-match-by-id. Absent pair ⇒ legacy id-only resolution."""
    model_config = ConfigDict(title='laboratories.ClientLaboratory')

    id: str = Field(..., description='The opaque laboratory id.')
    machine: Optional[str] = Field(None, description='The machine id of the laboratory host serving this laboratory.', json_schema_extra={'omitempty': True})
    machine_state: Optional[str] = Field(None, description='The state (on that machine) the laboratory host serves.', json_schema_extra={'omitempty': True})
    type_: ClientLaboratoryType = Field(..., alias='type', description='Discriminator — always `"client"`.')

