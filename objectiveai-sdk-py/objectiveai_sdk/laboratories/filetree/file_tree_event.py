# THIS FILE IS AUTO-GENERATED. DO NOT EDIT.
# Generated by scripts/install_pydantic.py from objectiveai-json-schema/.
from __future__ import annotations
from typing import Literal, Union
from pydantic import BaseModel, ConfigDict, Field, RootModel
from objectiveai_sdk.laboratories.filetree.file_tree_node import FileTreeNode


class FileTreeEventSnapshot(BaseModel):
    """The full tree — the watched root's child nodes — sent once
immediately on connect."""
    model_config = ConfigDict(json_schema_extra={'_variant_title': 'Snapshot'})

    children: list[FileTreeNode] = Field(..., description="The watched root's entries (its own identity is implicit —\nthe caller knows the watched path).")
    type_: Literal['snapshot'] = Field(..., alias='type')


class FileTreeEventUpserted(BaseModel):
    """A single node came into existence or changed. A directory
carries its whole (re-walked) subtree."""
    model_config = ConfigDict(json_schema_extra={'_variant_title': 'Upserted'})

    node: FileTreeNode = Field(..., description='The new/updated node.')
    path: list[str] = Field(..., description="The FULL component path to the node, relative to the\nwatched root (the last element equals `node`'s name).")
    type_: Literal['upserted'] = Field(..., alias='type')


class FileTreeEventRemoved(BaseModel):
    """A single node (and, if a directory, its whole subtree) was
removed."""
    model_config = ConfigDict(json_schema_extra={'_variant_title': 'Removed'})

    path: list[str] = Field(..., description='The FULL component path to the vanished node, relative to\nthe watched root.')
    type_: Literal['removed'] = Field(..., alias='type')


class FileTreeEvent(RootModel):
    """One event on the `/filetree` SSE stream. The first is always a
[`Snapshot`](FileTreeEvent::Snapshot); every later one upserts or
removes a single node as the filesystem changes."""
    model_config = ConfigDict(title='laboratories.filetree.FileTreeEvent')

    root: Union[FileTreeEventSnapshot, FileTreeEventUpserted, FileTreeEventRemoved]

