# THIS FILE IS AUTO-GENERATED. DO NOT EDIT.
# Generated by scripts/install_pydantic.py from objectiveai-json-schema/.
from __future__ import annotations
from typing import Union
from pydantic import ConfigDict, RootModel
from objectiveai_sdk.laboratories.agent_laboratory import AgentLaboratory
from objectiveai_sdk.laboratories.client_laboratory import ClientLaboratory


class LaboratoryClient(RootModel):
    """A client-resolved laboratory, identified by an opaque `id`."""
    model_config = ConfigDict(json_schema_extra={'_variant_title': 'Client', '_outer_object': True})

    root: ClientLaboratory


class LaboratoryAgent(RootModel):
    """An agent-embedded laboratory: the spec rides the marker so the
CLI conduit can materialize the laboratory on demand."""
    model_config = ConfigDict(json_schema_extra={'_variant_title': 'Agent', '_outer_object': True})

    root: AgentLaboratory


class Laboratory(RootModel):
    """A laboratory attached to an agent completion — dialed by the proxy as a
client-side MCP upstream across every agent (and fallback).

Untagged: each variant's payload carries its own `type` discriminator
field, so the wire shape is exactly that of the inner struct (e.g.
`{"type":"client","id":"…"}`)."""
    model_config = ConfigDict(title='laboratories.Laboratory')

    root: Union[LaboratoryClient, LaboratoryAgent]

