# THIS FILE IS AUTO-GENERATED. DO NOT EDIT.
# Generated by scripts/install_pydantic.py from objectiveai-json-schema/.
from __future__ import annotations
from pydantic import BaseModel, ConfigDict, Field


class WhitelistEntry(BaseModel):
    """One whitelist row. Each field is a regex matched **anchored**
against the corresponding install parameter (compiled as
`^(?:pattern)$`). All four fields must match for the row to
allow the install.

The host loads a list of these and accepts a plugin install only
if the (owner, repository, commit_sha_or_HEAD, manifest.version)
quadruple matches at least one row. Use `.*` for catch-all fields."""
    model_config = ConfigDict(title='filesystem.plugins.WhitelistEntry')

    commit_sha: str = Field(..., description='Regex matching the commit sha. When the user omits\n`--commit-sha`, this is matched against the literal string\n`"HEAD"`.')
    owner: str = Field(..., description='Regex matching the GitHub owner (org or user). Case-sensitive.')
    repository: str = Field(..., description='Regex matching the repository name.')
    version: str = Field(..., description="Regex matching the manifest's `version` field.")

