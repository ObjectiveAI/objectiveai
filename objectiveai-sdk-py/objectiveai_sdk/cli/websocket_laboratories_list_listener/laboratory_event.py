# THIS FILE IS AUTO-GENERATED. DO NOT EDIT.
# Generated by scripts/install_pydantic.py from objectiveai-json-schema/.
from __future__ import annotations
from typing import Literal, Optional, Union
from pydantic import BaseModel, ConfigDict, Field, RootModel
from objectiveai_sdk.cli.websocket_laboratories_list_listener.laboratory_status import LaboratoryStatus


class LaboratoryEventSnapshot(BaseModel):
    """The full laboratory set (connected ∪ local scan), sent once
immediately on connect."""
    model_config = ConfigDict(json_schema_extra={'_variant_title': 'Snapshot'})

    laboratories: list[LaboratoryStatus]
    type_: Literal['snapshot'] = Field(..., alias='type')


class LaboratoryEventUpserted(BaseModel):
    """A laboratory appeared or changed — connected, disconnected
(but still locally present), entered the local scan, or had
its identity re-announced. Full-value replace by `id`."""
    model_config = ConfigDict(json_schema_extra={'_variant_title': 'Upserted'})

    laboratory: LaboratoryStatus
    type_: Literal['upserted'] = Field(..., alias='type')


class LaboratoryEventRemoved(BaseModel):
    """A laboratory vanished — deleted, or its serving host
disconnected. Laboratory ids are only unique per (machine,
state), so the pair disambiguates WHICH same-id laboratory
left; an absent pair removes by bare id (legacy daemons)."""
    model_config = ConfigDict(json_schema_extra={'_variant_title': 'Removed'})

    id: str
    machine: Optional[str] = Field(None, json_schema_extra={'omitempty': True})
    machine_state: Optional[str] = Field(None, json_schema_extra={'omitempty': True})
    type_: Literal['removed'] = Field(..., alias='type')


class LaboratoryEvent(RootModel):
    """One event on the `/laboratories/list` stream. The first is always
a [`Snapshot`](LaboratoryEvent::Snapshot); every later one upserts
or removes a single laboratory as the connected set or the local
scan changes."""
    model_config = ConfigDict(title='cli.websocket_laboratories_list_listener.LaboratoryEvent')

    root: Union[LaboratoryEventSnapshot, LaboratoryEventUpserted, LaboratoryEventRemoved]

