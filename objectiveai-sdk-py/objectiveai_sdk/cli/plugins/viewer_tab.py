# THIS FILE IS AUTO-GENERATED. DO NOT EDIT.
# Generated by scripts/install_pydantic.py from objectiveai-json-schema/.
from __future__ import annotations
from typing import Optional, Union
from pydantic import BaseModel, ConfigDict, Field, RootModel


class ViewerTabChannel(BaseModel):
    """A channel handler — opened by the accept flow, never at boot."""
    model_config = ConfigDict(json_schema_extra={'_variant_title': 'Channel'})

    channel_key: str = Field(..., description='The offer key this handler answers (e.g.\n`"browser.login"`) — also the opened tab\'s title.')
    export: Optional[str] = Field(None, description='The export holding the component (`None` = `"default"`).', json_schema_extra={'omitempty': True})
    module: str = Field(..., description="The component's module path, relative to `viewer/`.")


class ViewerTabTab(BaseModel):
    """A regular tab — opened at viewer boot."""
    model_config = ConfigDict(json_schema_extra={'_variant_title': 'Tab'})

    export: Optional[str] = Field(None, description='The export holding the component (`None` = `"default"`).', json_schema_extra={'omitempty': True})
    module: str = Field(..., description="The component's module path, relative to `viewer/`.")
    title: str = Field(..., description="The tab's display title.")


class ViewerTab(RootModel):
    """One declared viewer tab: the component coordinates the viewer's
generic bootstrap dereferences (`import(module)[export]`), plus
EXACTLY ONE of `channel_key` (a channel handler, titled by its
offer key) or `title` (a regular boot tab). Untagged: the present
field decides the variant; an entry carrying both reads as a
handler and its `title` is ignored."""
    model_config = ConfigDict(title='cli.plugins.ViewerTab')

    root: Union[ViewerTabChannel, ViewerTabTab]

