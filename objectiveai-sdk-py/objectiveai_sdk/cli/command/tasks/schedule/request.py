# THIS FILE IS AUTO-GENERATED. DO NOT EDIT.
# Generated by scripts/install_pydantic.py from objectiveai-json-schema/.
from __future__ import annotations
from typing import Annotated, Optional
from pydantic import BaseModel, ConfigDict, Field
from objectiveai_sdk.cli.command.tasks.schedule.path import Path


class Request(BaseModel):
    model_config = ConfigDict(title='cli.command.tasks.schedule.Request')

    command: list[str] = Field(..., description='argv to invoke on each scheduled poll.')
    description: str = Field(..., description='Human-readable label. Required — surfaces on every\n`agents tasks list` row, and the runner uses it in\nobservability output.')
    interval_seconds: Optional[Annotated[int, Field(ge=0, le=18446744073709551615)]] = Field(None, description='Floor on wall-clock seconds between invocations. `None`\nmarks a **oneshot** schedule — the runner fires it once on\nthe next poll and deletes the row. `Some(n)` is a recurring\nschedule with `n` seconds as the minimum gap between\ninvocations.', json_schema_extra={'omitempty': True})
    jq: Optional[str] = Field(None, description='jq filter applied to the JSON output. Ignored when `python`\nis also set — python overrides jq.')
    max_tokens: Optional[Annotated[int, Field(ge=0, le=18446744073709551615)]] = Field(None, description='Response token budget, `>= 1` (`0` is rejected at parse\ntime — omit entirely for unlimited). Forward-compatible\nenvelope data — no leaf enforces it yet.', json_schema_extra={'omitempty': True})
    name: str = Field(..., description='User-facing identifier. Unique per agent instance hierarchy —\na second `schedule` with the same `(name, aih)` fails the\n`schedules` UNIQUE constraint unless `overwrite` is set.\n`agents tasks run` tags every streamed output line with this\nname so the caller can attribute output to its source schedule.')
    overwrite: bool = Field(..., description='Shadow an existing `(name, agent_instance_hierarchy)` schedule\ninstead of erroring on collision: a NEW row is inserted with\n`version = max + 1`. Older versions never list or run again but\nare kept so run history stays per-version; the new version has\nno runs yet, so it fires fresh.')
    path_type: Path
    python: Optional[str] = Field(None, description='Python transform applied to the JSON output. Overrides `jq`\nwhen both are provided.')
    timeout_seconds: Optional[Annotated[int, Field(ge=0, le=18446744073709551615)]] = Field(None, description='Wall-clock execution cap, in whole seconds. Parsed from\n`--timeout` (humantime: `30s`, `5m`, `1h30m`), `> 0`\nenforced at parse time. `db query` threads it to postgres\nwhen set; omit for uncapped.', json_schema_extra={'omitempty': True})

