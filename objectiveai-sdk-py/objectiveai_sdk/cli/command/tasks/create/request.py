# THIS FILE IS AUTO-GENERATED. DO NOT EDIT.
# Generated by scripts/install_pydantic.py from objectiveai-json-schema/.
from __future__ import annotations
from typing import Annotated, Optional
from objectiveai_sdk.json_value import JsonValue
from pydantic import BaseModel, ConfigDict, Field
from objectiveai_sdk.cli.command.tasks.create.path import Path


class Request(BaseModel):
    model_config = ConfigDict(title='cli.command.tasks.create.Request')

    command: JsonValue = Field(..., description="The command to run — the full typed command request.\nSchema-opaque (`serde_json::Value`) ON PURPOSE: embedding the\nroot request enum's schema in a leaf transitively expands the\nwhole command tree, which downstream generated TypeScript\ncannot emit declarations for (TS7056) — the same reason the\nroot aggregate is json_schema_ignore'd. The Rust type is the\nvalidation; the wire shape is unchanged.")
    delay_secs: int = Field(..., description="Seconds until the (first) run; with `repeat`, also the interval\nbetween runs, measured from each run's completion.", ge=0, le=18446744073709551615)
    id: str = Field(..., description="The task's user-chosen name — REQUIRED, laboratory-style.\nUnique per creating plugin (non-plugin creators share one\nnamespace); `tasks delete --id` and re-creation resolve\nagainst it. This is the caller's handle, distinct from the\ndaemon's internal surrogate key.")
    jq: Optional[str] = Field(None, description='jq filter applied to the JSON output. Ignored when `python`\nis also set — python overrides jq.')
    max_tokens: Optional[Annotated[int, Field(ge=0, le=18446744073709551615)]] = Field(None, description='Response token budget, `>= 1` (`0` is rejected at parse\ntime — omit entirely for unlimited). Forward-compatible\nenvelope data — no leaf enforces it yet.', json_schema_extra={'omitempty': True})
    path_type: Path
    python: Optional[str] = Field(None, description='Python transform applied to the JSON output. Overrides `jq`\nwhen both are provided.')
    repeat: bool = Field(False, description='Re-run every `delay_secs` instead of running once.')
    repeat_count: Optional[Annotated[int, Field(ge=0, le=18446744073709551615)]] = Field(None, description='Cap on SUCCESSFUL runs — only valid with `repeat`.', json_schema_extra={'omitempty': True})
    timeout_seconds: Optional[Annotated[int, Field(ge=0, le=18446744073709551615)]] = Field(None, description='Wall-clock execution cap, in whole seconds. Parsed from\n`--timeout` (humantime: `30s`, `5m`, `1h30m`), `> 0`\nenforced at parse time. `db query` threads it to postgres\nwhen set; omit for uncapped.', json_schema_extra={'omitempty': True})

