# THIS FILE IS AUTO-GENERATED. DO NOT EDIT.
# Generated by scripts/install_pydantic.py from objectiveai-json-schema/.
from __future__ import annotations
from typing import Optional
from pydantic import BaseModel, ConfigDict, Field
from objectiveai_sdk.cli.command.tasks.list.plugin import Plugin


class ResponseItem(BaseModel):
    """One schedule row."""
    model_config = ConfigDict(title='cli.command.tasks.list.ResponseItem')

    agent_instance_hierarchy: str
    command: list[str]
    created_at: str = Field(..., description='RFC3339 timestamp this schedule row was created.')
    description: str
    id: int = Field(..., description="The `schedules` row id. Monotonic; pass the highest `id` from a\npage as the next request's `after_id` to paginate forward.", ge=-9223372036854775808, le=9223372036854775807)
    interval: Optional[str] = Field(None, description='`None` for a oneshot; `Some("30s" / "1h" / "1d12h" / …)`\nfor a recurring schedule, formatted as humantime so the\nlist output reads naturally without a unit-conversion\nstep at the consumer. The CLI parser accepts the same\nshape on `agents tasks schedule --interval`.', json_schema_extra={'omitempty': True})
    last_ran_at: Optional[str] = Field(None, description="RFC3339 timestamp of the most recent invocation — this row's\nnewest `tasks_runs` entry. `None` until the runner has fired\nthis version at least once (runs are tracked per-version).", json_schema_extra={'omitempty': True})
    name: str = Field(..., description='The `--name` passed to `agents tasks schedule`. Unique per\n`agent_instance_hierarchy`.')
    plugin: Optional[Plugin] = Field(None, description='The plugin that registered this schedule (its `(owner,\nrepository, version)` coordinate), or `None` when it was not\nscheduled by a plugin.', json_schema_extra={'omitempty': True})
    version: int = Field(..., description="This row's version: `1` for a freshly scheduled task,\n`max + 1` for each `tasks schedule --overwrite` (each version\nis its own row; only the newest lists).", ge=0, le=18446744073709551615)

