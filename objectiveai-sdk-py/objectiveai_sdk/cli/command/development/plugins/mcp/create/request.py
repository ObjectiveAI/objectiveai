# THIS FILE IS AUTO-GENERATED. DO NOT EDIT.
# Generated by scripts/install_pydantic.py from objectiveai-json-schema/.
from __future__ import annotations
from typing import Annotated, Optional
from pydantic import BaseModel, ConfigDict, Field
from objectiveai_sdk.cli.command.development.plugins.mcp.create.path import Path


class Request(BaseModel):
    model_config = ConfigDict(title='cli.command.development.plugins.mcp.create.Request')

    jq: Optional[str] = Field(None, description='jq filter applied to the JSON output. Ignored when `python`\nis also set — python overrides jq.')
    max_tokens: Optional[Annotated[int, Field(ge=0, le=18446744073709551615)]] = Field(None, description='Response token budget, `>= 1` (`0` is rejected at parse\ntime — omit entirely for unlimited). Forward-compatible\nenvelope data — no leaf enforces it yet.', json_schema_extra={'omitempty': True})
    name: str = Field(..., description='Repository segment, likewise lowercased.')
    owner: str = Field(..., description="GitHub `<owner>` segment, lowercased on arrival — the\ndeclaration layer lowercases it too, so the registration and an\nagent's `plugin` entry agree.")
    path: str = Field(..., description="ABSOLUTE path to the plugin's source directory: the one holding\n`objectiveai.json`. Absolute because the laboratory host\nresolves it, and its working directory is not yours.")
    path_type: Path
    python: Optional[str] = Field(None, description='Python transform applied to the JSON output. Overrides `jq`\nwhen both are provided.')
    timeout_seconds: Optional[Annotated[int, Field(ge=0, le=18446744073709551615)]] = Field(None, description='Wall-clock execution cap, in whole seconds. Parsed from\n`--timeout` (humantime: `30s`, `5m`, `1h30m`), `> 0`\nenforced at parse time. `db query` threads it to postgres\nwhen set; omit for uncapped.', json_schema_extra={'omitempty': True})
    version: str = Field(..., description='Plugin version — matched byte-for-byte against what an agent\ndeclares. It is the git tag everywhere else, and stays\ncase-sensitive here even though nothing is fetched.')

