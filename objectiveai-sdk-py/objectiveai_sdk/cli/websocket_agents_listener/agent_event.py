# THIS FILE IS AUTO-GENERATED. DO NOT EDIT.
# Generated by scripts/install_pydantic.py from objectiveai-json-schema/.
from __future__ import annotations
from typing import Literal, Optional, Union
from pydantic import BaseModel, ConfigDict, Field, RootModel
from objectiveai_sdk.cli.websocket_agents_listener.agent_record import AgentRecord


class AgentEventSnapshot(BaseModel):
    """The full set of agents, sent once immediately on connect."""
    model_config = ConfigDict(json_schema_extra={'_variant_title': 'Snapshot'})

    agents: list[AgentRecord]
    type_: Literal['snapshot'] = Field(..., alias='type')


class AgentEventActivated(BaseModel):
    """An agent acquired its per-instance lock (became active)."""
    model_config = ConfigDict(json_schema_extra={'_variant_title': 'Activated'})

    agent: AgentRecord
    type_: Literal['activated'] = Field(..., alias='type')


class AgentEventUpdated(BaseModel):
    """An agent's record changed while it remained present — currently
emitted when its bound tags change (a tag applied, moved, or
removed). Carries the full refreshed record; consumers replace by
`agent_instance_hierarchy`."""
    model_config = ConfigDict(json_schema_extra={'_variant_title': 'Updated'})

    agent: AgentRecord
    type_: Literal['updated'] = Field(..., alias='type')


class AgentEventDeactivated(BaseModel):
    """An agent released its per-instance lock (became inactive) — on
normal stream end OR holder death. `last_active_at` is the release
moment."""
    model_config = ConfigDict(json_schema_extra={'_variant_title': 'Deactivated'})

    agent_instance_hierarchy: str
    last_active_at: Optional[str] = Field(None, json_schema_extra={'omitempty': True})
    type_: Literal['deactivated'] = Field(..., alias='type')


class AgentEvent(RootModel):
    """One event on the `/agents/instances/list` stream. The first is always a
[`Snapshot`](AgentEvent::Snapshot); every later one is an
[`Activated`](AgentEvent::Activated) or
[`Deactivated`](AgentEvent::Deactivated) delta. Consumers key by
`agent_instance_hierarchy` (the snapshot and a delta may overlap by
one item at connect time)."""
    model_config = ConfigDict(title='cli.websocket_agents_listener.AgentEvent')

    root: Union[AgentEventSnapshot, AgentEventActivated, AgentEventUpdated, AgentEventDeactivated]

