# THIS FILE IS AUTO-GENERATED. DO NOT EDIT.
# Generated by scripts/install_pydantic.py from objectiveai-json-schema/.
from __future__ import annotations
from typing import Literal, Union
from pydantic import BaseModel, ConfigDict, Field, RootModel
from objectiveai_sdk.cli.agents_instances_list_listener.agent_status import AgentStatus


class AgentEventSnapshot(BaseModel):
    """The full agent set (every known AIH with its `active` flag),
sent once immediately on connect."""
    model_config = ConfigDict(json_schema_extra={'_variant_title': 'Snapshot'})

    agents: list[AgentStatus]
    type_: Literal['snapshot'] = Field(..., alias='type')


class AgentEventActivated(BaseModel):
    """An agent acquired its per-instance lock (became active)."""
    model_config = ConfigDict(json_schema_extra={'_variant_title': 'Activated'})

    agent_instance_hierarchy: str
    type_: Literal['activated'] = Field(..., alias='type')


class AgentEventDeactivated(BaseModel):
    """An agent released its per-instance lock (became inactive) — on
normal stream end OR holder death. For the release timestamp,
tags, and counters, use `/agents/instances/{*aih}`."""
    model_config = ConfigDict(json_schema_extra={'_variant_title': 'Deactivated'})

    agent_instance_hierarchy: str
    type_: Literal['deactivated'] = Field(..., alias='type')


class AgentEvent(RootModel):
    """One event on the `/agents/instances/list` stream. The first is
always a [`Snapshot`](AgentEvent::Snapshot); every later one flips
one AIH's `active` flag as its instance lock is acquired or
released (an [`Activated`](AgentEvent::Activated) for an unseen AIH
introduces it)."""
    model_config = ConfigDict(title='cli.agents_instances_list_listener.AgentEvent')

    root: Union[AgentEventSnapshot, AgentEventActivated, AgentEventDeactivated]

