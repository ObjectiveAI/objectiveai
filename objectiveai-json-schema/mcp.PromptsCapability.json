{
  "title": "mcp.PromptsCapability",
  "description": "Capabilities for prompt templates.",
  "type": "object",
  "properties": {
    "listChanged": {
      "description": "Whether the server emits notifications when the prompt list changes.",
      "anyOf": [
        {
          "type": "boolean"
        },
        {
          "type": "null"
        }
      ],
      "omitempty": true
    }
  }
}
