{
  "title": "cli.command.agents.logs.token_usage.subscribe.ViewerResponseItem",
  "description": "Viewer-stream mirror of [`ResponseItem`]: the response (nested under\n`value`) plus the broadcast stream `id` and the originating request's\n`path_type`.",
  "type": "object",
  "properties": {
    "id": {
      "type": "string"
    },
    "path_type": {
      "$ref": "cli.command.agents.logs.token_usage.subscribe.Path"
    },
    "value": {
      "$ref": "cli.command.agents.logs.token_usage.subscribe.ResponseItem"
    }
  }
}
