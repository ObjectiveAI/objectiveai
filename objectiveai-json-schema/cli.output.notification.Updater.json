{
  "title": "cli.output.notification.Updater",
  "description": "Wire shapes emitted by `crate::updater::maybe_auto_update`. Every\nnon-cooldown skip path AND every active step emits one of these.\nThe ONLY silent path is the 2-hour marker cooldown — when the marker\nsays we already checked within the time frame, the updater exits\nwithout emission.\n\nErrors are emitted as `super::super::Output::Error` (level=warn,\nfatal=false), not as a variant here.\n\nWire (in combination with `super::super::Output::Notification` +\n`super::Notification`'s `value` wrapper):\n  `{\"type\":\"notification\",\"value\":{\"event\":\"checking\",...}}`.",
  "anyOf": [
    {
      "title": "Skipped",
      "description": "Skipped this run for a non-cooldown reason. (Cooldown is silent.)",
      "type": "object",
      "properties": {
        "event": {
          "type": "string",
          "enum": [
            "skipped"
          ]
        },
        "reason": {
          "$ref": "cli.output.notification.SkipReason"
        }
      }
    },
    {
      "title": "Checking",
      "description": "All gates passed; about to call\n`GET /repos/ObjectiveAI/objectiveai/releases/latest`. The very\nfirst emitted line of any active update run.",
      "type": "object",
      "properties": {
        "asset_name": {
          "type": "string"
        },
        "current_version": {
          "type": "string"
        },
        "event": {
          "type": "string",
          "enum": [
            "checking"
          ]
        }
      }
    },
    {
      "title": "UpToDate",
      "description": "GitHub returned the latest release tag and it's ≤ current.\nTerminal — no more events follow.",
      "type": "object",
      "properties": {
        "current_version": {
          "type": "string"
        },
        "event": {
          "type": "string",
          "enum": [
            "up_to_date"
          ]
        },
        "remote_version": {
          "type": "string"
        }
      }
    },
    {
      "title": "Found",
      "description": "Found a newer release with our asset attached; about to download.",
      "type": "object",
      "properties": {
        "asset_name": {
          "type": "string"
        },
        "current_version": {
          "type": "string"
        },
        "event": {
          "type": "string",
          "enum": [
            "found"
          ]
        },
        "remote_version": {
          "type": "string"
        },
        "url": {
          "type": "string"
        }
      }
    },
    {
      "title": "Installed",
      "description": "Swap complete; the next line of output will come from the\nre-exec'd new binary, not this one. Terminal for the current\nprocess.",
      "type": "object",
      "properties": {
        "current_version": {
          "type": "string"
        },
        "event": {
          "type": "string",
          "enum": [
            "installed"
          ]
        },
        "remote_version": {
          "type": "string"
        }
      }
    }
  ]
}
