{
  "title": "cli.command.agents.instances.list.ResponseItem",
  "description": "One discovered agent instance under a target. Aggregated from the\n`logs.messages`, `message_queue`, and `tags` tiers.",
  "type": "object",
  "properties": {
    "agent": {
      "description": "The agent definition recorded for this AIH — from\n`objectiveai.agent_refs`, with the legacy most-recent-request\nfallback (`lookup_session`). Populated by `agents instances\nget`; `agents instances list` leaves it unset.",
      "anyOf": [
        {
          "$ref": "agent.InlineAgentBaseWithFallbacksOrRemoteCommitOptional"
        },
        {
          "type": "null"
        }
      ],
      "omitempty": true
    },
    "agent_instance_hierarchy": {
      "description": "Full hierarchy of this agent instance.",
      "type": "string"
    },
    "created_at": {
      "description": "RFC3339 timestamp of the first `logs.messages` row for this\nagent. `None` when the agent has no logs yet (queue-only).",
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "omitempty": true
    },
    "last_active_at": {
      "description": "RFC3339 timestamp of the most recent `logs.messages` row for\nthis agent. `None` when the agent has no logs yet (queue-only).",
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "omitempty": true
    },
    "logged": {
      "description": "Total `logs.messages` rows for this agent over all time.",
      "type": "integer",
      "minimum": 0,
      "maximum": 18446744073709551615
    },
    "queued": {
      "description": "Active `message_queue` rows targeting this agent — counting\nboth direct-AIH rows and rows whose tag is bound to this AIH.",
      "type": "integer",
      "minimum": 0,
      "maximum": 18446744073709551615
    },
    "tags": {
      "description": "Tag names currently bound to this AIH, newest-bound first.",
      "type": "array",
      "items": {
        "type": "string"
      }
    }
  }
}
