{
  "title": "cli.command.viewer.config.ViewerRequest",
  "description": "Viewer-stream mirror of [`Request`] — the real command requests only\n(schema-introspection variants are excluded; the viewer streams\nactual command traffic). Untagged: each variant carries the leaf's\n`path_type`, so it stays discriminable.",
  "anyOf": [
    {
      "title": "Get",
      "$ref": "cli.command.viewer.config.get.ViewerRequest"
    },
    {
      "title": "Address",
      "$ref": "cli.command.viewer.config.address.ViewerRequest"
    },
    {
      "title": "Secret",
      "$ref": "cli.command.viewer.config.secret.ViewerRequest"
    },
    {
      "title": "Signature",
      "$ref": "cli.command.viewer.config.signature.ViewerRequest"
    }
  ]
}
