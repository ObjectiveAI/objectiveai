{
  "title": "cli.websocket_agents_instances_listener.AttachedLaboratory",
  "description": "One laboratory attachment on the agent, as carried by\n[`AgentRecord::attached_laboratories`]. Mirrors `agents instances\nget`'s attachment shape (listener-local — this wire is a mirror,\nnot a re-export).",
  "type": "object",
  "properties": {
    "attached_at": {
      "description": "RFC3339 — when it was attached.",
      "type": "string"
    },
    "attached_by": {
      "description": "The AIH that ran the attach. `None` on attachments predating\nattacher tracking.",
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "omitempty": true
    },
    "id": {
      "description": "The attached laboratory's id.",
      "type": "string"
    }
  }
}
