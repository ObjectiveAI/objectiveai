{
  "title": "cli.command.functions.ViewerRequest",
  "description": "Viewer-stream mirror of [`Request`] — the real command requests only\n(schema-introspection variants are excluded; the viewer streams\nactual command traffic). Untagged: each variant carries the leaf's\n`path_type`, so it stays discriminable.",
  "anyOf": [
    {
      "title": "Execute",
      "$ref": "cli.command.functions.execute.ViewerRequest"
    },
    {
      "title": "Get",
      "$ref": "cli.command.functions.get.ViewerRequest"
    },
    {
      "title": "List",
      "$ref": "cli.command.functions.list.ViewerRequest"
    },
    {
      "title": "Profiles",
      "$ref": "cli.command.functions.profiles.ViewerRequest"
    },
    {
      "title": "Publish",
      "$ref": "cli.command.functions.publish.ViewerRequest"
    }
  ]
}
