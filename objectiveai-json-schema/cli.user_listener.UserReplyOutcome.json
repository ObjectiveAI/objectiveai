{
  "title": "cli.user_listener.UserReplyOutcome",
  "description": "The daemon's answer to one `POST /user/{id}/reply`.",
  "anyOf": [
    {
      "title": "Accepted",
      "description": "This reply WON: it was accepted (validator included, when one\nwas set) and unblocks the originating command.",
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "accepted"
          ]
        }
      }
    },
    {
      "title": "Rejected",
      "description": "The originating command's python validator refused this reply\n— the request is STILL PENDING; the same or another connection\nmay reply again.",
      "type": "object",
      "properties": {
        "message": {
          "type": "string"
        },
        "type": {
          "type": "string",
          "enum": [
            "rejected"
          ]
        }
      }
    },
    {
      "title": "Settled",
      "description": "Another reply already won — this one can no longer be\naccepted.",
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "settled"
          ]
        }
      }
    },
    {
      "title": "NotFound",
      "description": "No pending request with that id (never existed, or already\nended).",
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "not_found"
          ]
        }
      }
    }
  ]
}
