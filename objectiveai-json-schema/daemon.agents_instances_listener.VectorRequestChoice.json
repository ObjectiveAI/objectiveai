{
  "title": "daemon.agents_instances_listener.VectorRequestChoice",
  "description": "Mirror of `logs.list.VectorRequestChoice`: this agent's inline\nvoting `key` for the choice plus the choice's content parts in\nrequest order.",
  "type": "object",
  "properties": {
    "key": {
      "description": "The prefix-tree voting key this agent assigned to the choice.",
      "type": "string"
    },
    "parts": {
      "type": "array",
      "items": {
        "$ref": "daemon.agents_instances_listener.VectorRequestChoicePart"
      }
    }
  }
}
