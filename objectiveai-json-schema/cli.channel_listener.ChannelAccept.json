{
  "title": "cli.channel_listener.ChannelAccept",
  "description": "The `POST /channels/{id}/accept` body — the caller's `S_conn`.",
  "type": "object",
  "properties": {
    "conn_secret": {
      "description": "The accepting connection's secret, from the first\n[`ChannelEvent::Connection`] frame.",
      "type": "string"
    }
  }
}
