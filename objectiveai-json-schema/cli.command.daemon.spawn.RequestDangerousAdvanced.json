{
  "title": "cli.command.daemon.spawn.RequestDangerousAdvanced",
  "type": "object",
  "properties": {
    "foreground": {
      "description": "`Some(true)` → THIS process becomes the resident daemon. When\nunset/false, `daemon spawn` re-execs itself with this set and\nreturns once the daemon is up.",
      "anyOf": [
        {
          "type": "boolean"
        },
        {
          "type": "null"
        }
      ],
      "omitempty": true
    }
  }
}
