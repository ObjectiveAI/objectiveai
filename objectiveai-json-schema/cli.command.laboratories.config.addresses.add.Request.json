{
  "title": "cli.command.laboratories.config.addresses.add.Request",
  "type": "object",
  "properties": {
    "jq": {
      "description": "jq filter applied to the JSON output. Ignored when `python`\nis also set — python overrides jq.",
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ]
    },
    "key": {
      "description": "The daemon `ws://` address the laboratory host should connect to.",
      "type": "string"
    },
    "max_tokens": {
      "description": "Response token budget, `>= 1` (`0` is rejected at parse\ntime — omit entirely for unlimited). Forward-compatible\nenvelope data — no leaf enforces it yet.",
      "anyOf": [
        {
          "type": "integer",
          "minimum": 0,
          "maximum": 18446744073709551615
        },
        {
          "type": "null"
        }
      ],
      "omitempty": true
    },
    "path_type": {
      "$ref": "cli.command.laboratories.config.addresses.add.Path"
    },
    "python": {
      "description": "Python transform applied to the JSON output. Overrides `jq`\nwhen both are provided.",
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ]
    },
    "scope": {
      "$ref": "cli.command.SetScope"
    },
    "timeout_seconds": {
      "description": "Wall-clock execution cap, in whole seconds. Parsed from\n`--timeout` (humantime: `30s`, `5m`, `1h30m`), `> 0`\nenforced at parse time. `db query` threads it to postgres\nwhen set; omit for uncapped.",
      "anyOf": [
        {
          "type": "integer",
          "minimum": 0,
          "maximum": 18446744073709551615
        },
        {
          "type": "null"
        }
      ],
      "omitempty": true
    },
    "value": {
      "description": "The signature to present at that address. Empty ⇒ dial\nunauthenticated (the address's daemon must be open). Always\nsent explicitly — NO serde default: empty-string schema\ndefaults are banned (the json-schema builder asserts it), and\nthe CLI fills `\"\"` itself when `--value` is omitted.",
      "type": "string"
    }
  }
}
