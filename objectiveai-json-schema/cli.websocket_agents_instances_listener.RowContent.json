{
  "title": "cli.websocket_agents_instances_listener.RowContent",
  "description": "One row's full current content. Content shapes mirror\n`agents logs open`'s payloads (`Text`/`Image`/`Audio`/`Video`/`File`),\nplus the kinds that are inlined here rather than opened by id.",
  "anyOf": [
    {
      "title": "Text",
      "type": "object",
      "properties": {
        "text": {
          "type": "string"
        },
        "type": {
          "type": "string",
          "enum": [
            "text"
          ]
        }
      }
    },
    {
      "title": "Image",
      "type": "object",
      "$ref": "agent.completions.message.ImageUrl",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "image"
          ]
        }
      }
    },
    {
      "title": "Audio",
      "type": "object",
      "$ref": "agent.completions.message.InputAudio",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "audio"
          ]
        }
      }
    },
    {
      "title": "Video",
      "type": "object",
      "$ref": "agent.completions.message.VideoUrl",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "video"
          ]
        }
      }
    },
    {
      "title": "File",
      "type": "object",
      "$ref": "agent.completions.message.File",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "file"
          ]
        }
      }
    },
    {
      "title": "Refusal",
      "type": "object",
      "properties": {
        "text": {
          "type": "string"
        },
        "type": {
          "type": "string",
          "enum": [
            "refusal"
          ]
        }
      }
    },
    {
      "title": "Reasoning",
      "type": "object",
      "properties": {
        "text": {
          "type": "string"
        },
        "type": {
          "type": "string",
          "enum": [
            "reasoning"
          ]
        }
      }
    },
    {
      "title": "ToolCall",
      "description": "A tool call's full current state — `arguments` is the complete\naccumulated string so far (full-value, not a fragment).",
      "type": "object",
      "properties": {
        "arguments": {
          "type": "string"
        },
        "function_name": {
          "type": "string"
        },
        "tool_call_id": {
          "type": "string"
        },
        "type": {
          "type": "string",
          "enum": [
            "tool_call"
          ]
        }
      }
    },
    {
      "title": "Vote",
      "description": "The per-agent vote: this agent's score for each choice, in\nchoice order.",
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "vote"
          ]
        },
        "vote": {
          "type": "array",
          "items": {
            "type": "number",
            "minimum": -3.4028234663852886e+38,
            "maximum": 3.4028234663852886e+38
          }
        }
      }
    },
    {
      "title": "Head",
      "description": "A HEAD row ([`RowTableKind::ToolResponse`] /\n[`RowTableKind::RequestMessageTool`] /\n[`RowTableKind::RequestVectorChoice`]): its payload rides the\nrow's metadata fields (`tool_call_id` / `choice_key`), not the\ncontent.",
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "head"
          ]
        }
      }
    }
  ]
}
