{
  "title": "cli.command.laboratories.list.ResponseItem",
  "description": "One laboratory container, reconstructed from its `objectiveai.laboratory`\nlabel. Mirrors the `create` echo: `{ id, image, mounts, env, cwd }`.",
  "type": "object",
  "properties": {
    "cwd": {
      "type": "string"
    },
    "env": {
      "type": "array",
      "items": {
        "$ref": "cli.command.laboratories.create.EnvVar"
      }
    },
    "id": {
      "type": "string"
    },
    "image": {
      "type": "string"
    },
    "mounts": {
      "type": "array",
      "items": {
        "$ref": "cli.command.laboratories.create.Mount"
      }
    },
    "source": {
      "description": "Where this laboratory lives relative to this machine + state.",
      "$ref": "cli.command.laboratories.list.Source"
    }
  }
}
