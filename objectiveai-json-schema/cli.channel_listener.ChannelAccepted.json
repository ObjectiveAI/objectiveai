{
  "title": "cli.channel_listener.ChannelAccepted",
  "description": "The `POST /channels/{id}/accept` success body: the owner secret\n(`S_owner`) — the per-channel capability for `channels logs\nreply|list|open|subscribe` and `channels close`.",
  "type": "object",
  "properties": {
    "secret": {
      "type": "string"
    }
  }
}
