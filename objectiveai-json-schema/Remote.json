{
  "title": "Remote",
  "description": "The remote source where a function, profile, or agent is hosted.",
  "anyOf": [
    {
      "title": "Github",
      "description": "GitHub repository.",
      "type": "string",
      "enum": [
        "github"
      ]
    },
    {
      "title": "Client",
      "description": "The connected client (resolved over the websocket reverse-channel).",
      "type": "string",
      "enum": [
        "client"
      ]
    },
    {
      "title": "Mock",
      "description": "Mock (for testing).",
      "type": "string",
      "enum": [
        "mock"
      ]
    }
  ]
}
