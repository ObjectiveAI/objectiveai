{
  "title": "cli.command.channels.logs.list.ChannelLogEntry",
  "description": "One channel-log entry ENVELOPE — no content (open reveals that).\nIdentity is INLINE and daemon-authored (unspoofable). The two\ndirections are ASYMMETRIC in identity, so the entry is an enum\ntagged by `kind`:\n\n- `request` (publisher→owner): the publisher is a PLUGIN — the\n  plugin trio is REQUIRED.\n- `reply` (owner→publisher): the replier is never a plugin — no\n  plugin trio at all.",
  "anyOf": [
    {
      "title": "Request",
      "description": "A publisher→owner message. The publisher is a plugin, so the\noriginating plugin trio is always present.",
      "type": "object",
      "properties": {
        "id": {
          "description": "The entry's `channel_messages.id` — the cursor for\n`--after-id` and the `--entry-id` for `channels logs open`.",
          "type": "integer",
          "minimum": -9223372036854775808,
          "maximum": 9223372036854775807
        },
        "kind": {
          "type": "string",
          "enum": [
            "request"
          ]
        },
        "plugin_name": {
          "type": "string"
        },
        "plugin_owner": {
          "description": "The originating plugin (owner/repository/version) — always\npresent: a channel's requester is a plugin.",
          "type": "string"
        },
        "plugin_version": {
          "type": "string"
        },
        "sender_agent_instance_hierarchy": {
          "description": "The AIH of the agent that sent the entry (always present —\nthe daemon defaults it).",
          "type": "string"
        },
        "timestamp": {
          "description": "RFC3339 delivery time.",
          "type": "string"
        }
      }
    },
    {
      "title": "Reply",
      "description": "An owner→publisher message. The replier is typically not a\nplugin, so the plugin trio is OPTIONAL — present only when a\nplugin happened to send the reply.",
      "type": "object",
      "properties": {
        "id": {
          "description": "The entry's `channel_messages.id`.",
          "type": "integer",
          "minimum": -9223372036854775808,
          "maximum": 9223372036854775807
        },
        "kind": {
          "type": "string",
          "enum": [
            "reply"
          ]
        },
        "plugin_name": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "omitempty": true
        },
        "plugin_owner": {
          "description": "The originating plugin (owner/repository/version) — present\nonly when a plugin sent the reply.",
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "omitempty": true
        },
        "plugin_version": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "omitempty": true
        },
        "sender_agent_instance_hierarchy": {
          "description": "The AIH of the agent that sent the entry (always present —\nthe daemon defaults it).",
          "type": "string"
        },
        "timestamp": {
          "description": "RFC3339 delivery time.",
          "type": "string"
        }
      }
    }
  ]
}
