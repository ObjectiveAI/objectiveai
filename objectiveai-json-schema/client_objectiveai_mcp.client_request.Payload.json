{
  "title": "client_objectiveai_mcp.client_request.Payload",
  "description": "Tagged union of request shapes the API can push down the\nreverse-attach channel to the client-app layer.",
  "type": "object",
  "$ref": "agent.completions.request.AgentCompletionNotifyParams",
  "properties": {
    "type": {
      "type": "string",
      "enum": [
        "agent_completion_notify"
      ]
    }
  }
}
