{
  "title": "cli.command.RootViewerRequest",
  "description": "Viewer-stream mirror of [`Request`] — the real command tiers only\n(schema-introspection variants excluded). Each variant wraps a tier's\n`ViewerRequest`; every leaf underneath carries its `path_type`, so the\nuntagged enum stays discriminable.\n\nNamed `RootViewerRequest` (not `ViewerRequest`): the flat codegen name\nwould collapse `cli.command.ViewerRequest` and `cli.command.viewer.Request`\nto the same PascalCase identifier, so the root wrapper carries the `Root`\nprefix to stay distinct and shippable.",
  "anyOf": [
    {
      "title": "Agents",
      "$ref": "cli.command.agents.ViewerRequest"
    },
    {
      "title": "Api",
      "$ref": "cli.command.api.ViewerRequest"
    },
    {
      "title": "Daemon",
      "$ref": "cli.command.daemon.ViewerRequest"
    },
    {
      "title": "Db",
      "$ref": "cli.command.db.ViewerRequest"
    },
    {
      "title": "Functions",
      "$ref": "cli.command.functions.ViewerRequest"
    },
    {
      "title": "KillAll",
      "$ref": "cli.command.kill_all.ViewerRequest"
    },
    {
      "title": "Laboratories",
      "$ref": "cli.command.laboratories.ViewerRequest"
    },
    {
      "title": "Mcp",
      "$ref": "cli.command.mcp.ViewerRequest"
    },
    {
      "title": "Plugins",
      "$ref": "cli.command.plugins.ViewerRequest"
    },
    {
      "title": "Python",
      "$ref": "cli.command.python.ViewerRequest"
    },
    {
      "title": "Swarms",
      "$ref": "cli.command.swarms.ViewerRequest"
    },
    {
      "title": "Tools",
      "$ref": "cli.command.tools.ViewerRequest"
    },
    {
      "title": "Update",
      "$ref": "cli.command.update.ViewerRequest"
    },
    {
      "title": "Viewer",
      "$ref": "cli.command.viewer.ViewerRequest"
    }
  ]
}
