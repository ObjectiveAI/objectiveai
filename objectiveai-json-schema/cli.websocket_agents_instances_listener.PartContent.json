{
  "title": "cli.websocket_agents_instances_listener.PartContent",
  "description": "One part's opened content — what `agents logs read id` would return\nfor the part's `id`, inlined. The mirror of the list part `type`\ntags (`text` / `image` / `audio` / `video` / `file`), each carrying\nits payload.",
  "anyOf": [
    {
      "title": "Text",
      "type": "object",
      "properties": {
        "text": {
          "type": "string"
        },
        "type": {
          "type": "string",
          "enum": [
            "text"
          ]
        }
      }
    },
    {
      "title": "Image",
      "type": "object",
      "$ref": "agent.completions.message.ImageUrl",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "image"
          ]
        }
      }
    },
    {
      "title": "Audio",
      "type": "object",
      "$ref": "agent.completions.message.InputAudio",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "audio"
          ]
        }
      }
    },
    {
      "title": "Video",
      "type": "object",
      "$ref": "agent.completions.message.VideoUrl",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "video"
          ]
        }
      }
    },
    {
      "title": "File",
      "type": "object",
      "$ref": "agent.completions.message.File",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "file"
          ]
        }
      }
    }
  ]
}
