{
  "title": "cli.command.tasks.create.Request",
  "type": "object",
  "properties": {
    "command": {
      "description": "The command to run — the full typed command request.\nSchema-opaque (`serde_json::Value`) ON PURPOSE: embedding the\nroot request enum's schema in a leaf transitively expands the\nwhole command tree, which downstream generated TypeScript\ncannot emit declarations for (TS7056) — the same reason the\nroot aggregate is json_schema_ignore'd. The Rust type is the\nvalidation; the wire shape is unchanged."
    },
    "delay_secs": {
      "description": "Seconds until the (first) run; with `repeat`, also the interval\nbetween runs, measured from each run's completion.",
      "type": "integer",
      "minimum": 0,
      "maximum": 18446744073709551615
    },
    "id": {
      "description": "The task's user-chosen name — REQUIRED, laboratory-style.\nUnique per creating plugin (non-plugin creators share one\nnamespace); `tasks delete --id` and re-creation resolve\nagainst it. This is the caller's handle, distinct from the\ndaemon's internal surrogate key.",
      "type": "string"
    },
    "jq": {
      "description": "jq filter applied to the JSON output. Ignored when `python`\nis also set — python overrides jq.",
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ]
    },
    "max_tokens": {
      "description": "Response token budget, `>= 1` (`0` is rejected at parse\ntime — omit entirely for unlimited). Forward-compatible\nenvelope data — no leaf enforces it yet.",
      "anyOf": [
        {
          "type": "integer",
          "minimum": 0,
          "maximum": 18446744073709551615
        },
        {
          "type": "null"
        }
      ],
      "omitempty": true
    },
    "path_type": {
      "$ref": "cli.command.tasks.create.Path"
    },
    "python": {
      "description": "Python transform applied to the JSON output. Overrides `jq`\nwhen both are provided.",
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ]
    },
    "repeat": {
      "description": "Re-run every `delay_secs` instead of running once.",
      "type": "boolean",
      "default": false
    },
    "repeat_count": {
      "description": "Cap on SUCCESSFUL runs — only valid with `repeat`.",
      "anyOf": [
        {
          "type": "integer",
          "minimum": 0,
          "maximum": 18446744073709551615
        },
        {
          "type": "null"
        }
      ],
      "omitempty": true
    },
    "timeout_seconds": {
      "description": "Wall-clock execution cap, in whole seconds. Parsed from\n`--timeout` (humantime: `30s`, `5m`, `1h30m`), `> 0`\nenforced at parse time. `db query` threads it to postgres\nwhen set; omit for uncapped.",
      "anyOf": [
        {
          "type": "integer",
          "minimum": 0,
          "maximum": 18446744073709551615
        },
        {
          "type": "null"
        }
      ],
      "omitempty": true
    }
  }
}
