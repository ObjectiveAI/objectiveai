{
  "title": "cli.command.agents.tags.remove.Response",
  "description": "The removed tag's shape — mirrors `lookup`'s two live states (a\nmissing tag is an error, not a state).",
  "anyOf": [
    {
      "title": "Bound",
      "description": "The tag was BOUND to this hierarchy.",
      "type": "object",
      "properties": {
        "agent_instance_hierarchy": {
          "type": "string"
        },
        "detached_laboratories": {
          "description": "Laboratory attachments targeting this tag that were detached\nwith it.",
          "type": "integer",
          "minimum": 0,
          "maximum": 18446744073709551615
        },
        "name": {
          "description": "The removed tag's name, echoed.",
          "type": "string"
        },
        "state": {
          "type": "string",
          "enum": [
            "bound"
          ]
        }
      }
    },
    {
      "title": "Grouped",
      "description": "The tag was GROUPED. `tag_group_deleted` reports whether this\nremoval emptied — and so garbage-collected — the group.",
      "type": "object",
      "properties": {
        "detached_laboratories": {
          "description": "Laboratory attachments targeting this tag that were detached\nwith it.",
          "type": "integer",
          "minimum": 0,
          "maximum": 18446744073709551615
        },
        "name": {
          "description": "The removed tag's name, echoed.",
          "type": "string"
        },
        "state": {
          "type": "string",
          "enum": [
            "grouped"
          ]
        },
        "tag_group_deleted": {
          "type": "boolean"
        }
      }
    }
  ]
}
