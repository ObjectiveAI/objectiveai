{
  "title": "cli.plugins.Viewer",
  "description": "A plugin's viewer surface: the identity icon and the tabs it\ncontributes. Every path is relative to the plugin's `viewer/`\nfolder (authored as if the CWD were inside it).",
  "type": "object",
  "properties": {
    "icon": {
      "description": "The identity icon, shown beside the identity in the tab strip.",
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "omitempty": true
    },
    "tabs": {
      "description": "The plugin's tabs, by NAME — manifest order is strip order.\nEvery declared tab opens at viewer boot.",
      "anyOf": [
        {
          "type": "object",
          "additionalProperties": {
            "$ref": "cli.plugins.ViewerTab"
          }
        },
        {
          "type": "null"
        }
      ],
      "omitempty": true
    }
  }
}
