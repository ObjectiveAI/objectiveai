{
  "title": "cli.command.agents.message.ResponseItem",
  "description": "Streamed response (stream=true). The cli yields a sequence of\nthese. Same `Delivered` / `Enqueued` / `Id` first-item\nsemantics as [`Response`]; the spawn-take-over branch adds\nstreaming `Chunk` items after the initial `Id`.",
  "anyOf": [
    {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "delivered"
          ]
        }
      }
    },
    {
      "type": "object",
      "properties": {
        "agent_instance_hierarchy": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "omitempty": true
        },
        "agent_tag": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "omitempty": true
        },
        "id": {
          "type": "integer",
          "minimum": -9223372036854775808,
          "maximum": 9223372036854775807
        },
        "type": {
          "type": "string",
          "enum": [
            "enqueued"
          ]
        }
      }
    },
    {
      "type": "object",
      "properties": {
        "agent_instance_hierarchy": {
          "type": "string"
        },
        "type": {
          "type": "string",
          "enum": [
            "id"
          ]
        }
      }
    },
    {
      "description": "Newtype-of-struct under an internally-tagged enum: the\nchunk's own fields land at the top level of the JSON, with\n`\"type\":\"chunk\"` injected. Wire shape equivalent to spawn's\n`ResponseItem::Chunk(AgentCompletionChunk)` plus the `type`\ndiscriminator.",
      "type": "object",
      "$ref": "agent.completions.response.streaming.AgentCompletionChunk",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "chunk"
          ]
        }
      }
    }
  ]
}
