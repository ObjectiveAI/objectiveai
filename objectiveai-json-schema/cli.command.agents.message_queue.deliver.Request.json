{
  "title": "cli.command.agents.message_queue.deliver.Request",
  "type": "object",
  "properties": {
    "jq": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ]
    },
    "parent_agent_instance_hierarchy": {
      "description": "Subtree root to deliver under. Defaults to the cli's own\n`Config.agent_instance_hierarchy` when omitted.",
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "omitempty": true
    },
    "path_type": {
      "$ref": "cli.command.agents.message_queue.deliver.Path"
    }
  }
}
