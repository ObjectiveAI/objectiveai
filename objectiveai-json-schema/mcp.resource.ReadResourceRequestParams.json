{
  "title": "mcp.resource.ReadResourceRequestParams",
  "description": "Parameters for a `resources/read` request.",
  "type": "object",
  "properties": {
    "uri": {
      "description": "The URI of the resource to read.",
      "type": "string"
    }
  }
}
