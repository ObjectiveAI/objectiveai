{
  "title": "daemon.laboratories_listener.LaboratoryRecord",
  "description": "One laboratory's full record. Identity fields are present when a\nconnected laboratory HOST serves the laboratory; a laboratory known\nONLY through attachment rows (or not at all) zero-fills them —\n`machine: None` marks \"not served anywhere\", mirroring the agents\n`get_exact` zero-fill convention. There is no local-vs-remote\nsplit — machine identity is the only provenance.",
  "type": "object",
  "properties": {
    "agent_full_id": {
      "description": "For agent laboratories: the full id of the agent the\nlaboratory derives from. `None` for user-created laboratories.",
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "omitempty": true
    },
    "attachments": {
      "description": "Every attachment row targeting this laboratory, oldest first.",
      "type": "array",
      "items": {
        "$ref": "daemon.laboratories_listener.LaboratoryAttachment"
      },
      "default": []
    },
    "connected": {
      "description": "Whether the serving host's `/laboratory` connection is live\nright now.",
      "type": "boolean"
    },
    "created_at": {
      "description": "Unix seconds when the laboratory container was created, from\npodman's container record. `None` when the identity source\ndidn't report it (or the laboratory is known only through\nattachment rows).",
      "anyOf": [
        {
          "type": "integer",
          "minimum": -9223372036854775808,
          "maximum": 9223372036854775807
        },
        {
          "type": "null"
        }
      ],
      "omitempty": true
    },
    "cwd": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "omitempty": true
    },
    "env": {
      "type": "array",
      "items": {
        "$ref": "laboratories.EnvVar"
      },
      "default": []
    },
    "id": {
      "description": "Raw (state-agnostic) laboratory id.",
      "type": "string"
    },
    "image": {
      "anyOf": [
        {
          "$ref": "laboratories.LaboratoryImage"
        },
        {
          "type": "null"
        }
      ],
      "omitempty": true
    },
    "machine": {
      "description": "The machine whose laboratory host serves this laboratory —\n`None` when no connected host serves it.",
      "anyOf": [
        {
          "$ref": "machine.MachineIdentity"
        },
        {
          "type": "null"
        }
      ],
      "omitempty": true
    },
    "machine_state": {
      "description": "The state (on that machine) the serving host serves —\nlaboratory ids are only unique per (machine, state).",
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "omitempty": true
    },
    "mounts": {
      "type": "array",
      "items": {
        "$ref": "laboratories.Mount"
      },
      "default": []
    }
  }
}
