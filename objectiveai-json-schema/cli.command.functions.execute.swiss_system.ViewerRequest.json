{
  "title": "cli.command.functions.execute.swiss_system.ViewerRequest",
  "description": "Viewer-stream mirror of [`Request`]: the request (nested under\n`value`, `path_type` and all) plus the broadcast stream `id`.",
  "type": "object",
  "properties": {
    "id": {
      "type": "string"
    },
    "value": {
      "$ref": "cli.command.functions.execute.swiss_system.Request"
    }
  }
}
