{
  "title": "vector.completions.response.streaming.AgentCompletionChunk",
  "description": "A streaming agent completion chunk from a single agent within a vector completion.\n\nThe `index` field is used to correlate chunks belonging to the same\nunderlying completion when accumulating via [`push`](Self::push).",
  "type": "object",
  "properties": {
    "agent_full_id": {
      "description": "WF-level id: concatenation of the primary agent's id with all\nfallback ids (see `InlineAgentWithFallbacks::full_id`). Same\nfor every slot in the same WF request.",
      "type": "string"
    },
    "agent_id": {
      "description": "Leaf agent id of the slot that produced this chunk. For the\nprimary attempt this is the primary agent's id; on fallback it\nis the fallback agent's id. Same on every chunk of a slot.",
      "type": "string"
    },
    "agent_inline": {
      "description": "The resolved inline WF definition for this agent. Populated\nONLY on the FIRST chunk of the completion; [`push`](Self::push)\nkeeps the first value.",
      "anyOf": [
        {
          "$ref": "agent.InlineAgentWithFallbacks"
        },
        {
          "type": "null"
        }
      ],
      "omitempty": true
    },
    "agent_instance_hierarchy": {
      "description": "Full agent instance hierarchy for this completion's slot —\n`{ctx lineage}/{agent_full_id}-{response_id}`, or the fixed\ncontinuation value on resume. Same on every chunk of a slot.",
      "type": "string"
    },
    "agent_remote": {
      "description": "`RemotePath` the WF was fetched from. `None` when the WF was\nsupplied inline. Same for every slot in the same WF request.",
      "anyOf": [
        {
          "$ref": "RemotePath"
        },
        {
          "type": "null"
        }
      ],
      "omitempty": true
    },
    "continuation": {
      "description": "Continuation state for multi-turn conversations (only present in the final chunk).",
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "omitempty": true
    },
    "created": {
      "type": "integer",
      "minimum": 0,
      "maximum": 18446744073709551615
    },
    "error": {
      "description": "Error details if this completion failed.",
      "anyOf": [
        {
          "$ref": "error.ResponseError"
        },
        {
          "type": "null"
        }
      ],
      "omitempty": true
    },
    "id": {
      "type": "string"
    },
    "index": {
      "description": "Index used to correlate chunks from the same completion.",
      "type": "integer",
      "minimum": 0,
      "maximum": 18446744073709551615
    },
    "messages": {
      "type": "array",
      "items": {
        "$ref": "agent.completions.response.streaming.MessageChunk"
      }
    },
    "messages_queued": {
      "description": "`true` when the MCP proxy holds queued messages that were not\ndelivered to the agent via a tool response on this turn. Only\nset when `continuation` is also set — the caller acts on it by\nissuing the continuation. Absent when nothing is queued, when\nthere is no continuation to act on, or when the peek failed\n(the failure is surfaced via `error`).",
      "anyOf": [
        {
          "type": "boolean"
        },
        {
          "type": "null"
        }
      ],
      "omitempty": true
    },
    "object": {
      "description": "The object type (always \"agent.completion.chunk\").",
      "$ref": "agent.completions.response.streaming.Object"
    },
    "request_choice_keys": {
      "description": "This agent's prefix-tree voting key for each response choice, in\nthe SAME order as the request's `responses`\n(`request_choice_keys[i]` is this agent's key for choice `i`).\nKeys are per-agent (randomized), so they differ between agents.\nPopulated ONLY on the FIRST chunk of the completion;\n[`push`](Self::push) keeps the first value.",
      "anyOf": [
        {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        {
          "type": "null"
        }
      ],
      "omitempty": true
    },
    "upstream": {
      "description": "Upstream provider",
      "$ref": "agent.Upstream"
    },
    "usage": {
      "description": "Token usage (only present in the final chunk).",
      "anyOf": [
        {
          "$ref": "agent.completions.response.Usage"
        },
        {
          "type": "null"
        }
      ],
      "omitempty": true
    }
  }
}
