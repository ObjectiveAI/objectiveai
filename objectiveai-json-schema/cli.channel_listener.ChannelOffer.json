{
  "title": "cli.channel_listener.ChannelOffer",
  "description": "One channel OFFER, as broadcast to every connected channel stream.\nCarries no secret — the publisher's `S_pub` is returned to the\npublisher's command, and the owner's `S_owner` is delivered only\nover the accepting connection's stream.",
  "type": "object",
  "properties": {
    "agent_arguments": {
      "description": "The originating caller's agent identity, from its scope.",
      "$ref": "cli.command.AgentArguments"
    },
    "channel_id": {
      "description": "The daemon-minted channel id — the accept + log routing key.",
      "type": "string"
    },
    "details": {
      "description": "Arbitrary offer payload, opaque to the daemon."
    },
    "key": {
      "description": "Caller-chosen discriminator (e.g. `\"browser.login\"`) — how a\nuser surface decides whether/how to accept the offer.",
      "type": "string"
    },
    "message": {
      "description": "Human-readable offer message, opaque to the daemon.",
      "type": "string"
    },
    "plugin_name": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "omitempty": true
    },
    "plugin_owner": {
      "description": "The PLUGIN that originated the offer — daemon-authored\n(unspoofable; stamped by `plugins run`), absent when the\ncaller wasn't a plugin.",
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "omitempty": true
    },
    "plugin_version": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "omitempty": true
    }
  }
}
