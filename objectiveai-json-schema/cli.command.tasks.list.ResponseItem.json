{
  "title": "cli.command.tasks.list.ResponseItem",
  "description": "One schedule row.",
  "type": "object",
  "properties": {
    "agent_instance_hierarchy": {
      "type": "string"
    },
    "command": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "created_at": {
      "description": "RFC3339 timestamp this schedule row was created.",
      "type": "string"
    },
    "description": {
      "type": "string"
    },
    "id": {
      "description": "The `schedules` row id. Monotonic; pass the highest `id` from a\npage as the next request's `after_id` to paginate forward.",
      "type": "integer",
      "minimum": -9223372036854775808,
      "maximum": 9223372036854775807
    },
    "interval": {
      "description": "`None` for a oneshot; `Some(\"30s\" / \"1h\" / \"1d12h\" / …)`\nfor a recurring schedule, formatted as humantime so the\nlist output reads naturally without a unit-conversion\nstep at the consumer. The CLI parser accepts the same\nshape on `agents tasks schedule --interval`.",
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "omitempty": true
    },
    "last_ran_at": {
      "description": "RFC3339 timestamp of the most recent invocation — this row's\nnewest `tasks_runs` entry. `None` until the runner has fired\nthis version at least once (runs are tracked per-version).",
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "omitempty": true
    },
    "name": {
      "description": "The `--name` passed to `agents tasks schedule`. Unique per\n`agent_instance_hierarchy`.",
      "type": "string"
    },
    "plugin": {
      "description": "The plugin that registered this schedule (its `(owner,\nrepository, version)` coordinate), or `None` when it was not\nscheduled by a plugin.",
      "anyOf": [
        {
          "$ref": "cli.command.tasks.list.Plugin"
        },
        {
          "type": "null"
        }
      ],
      "omitempty": true
    },
    "version": {
      "description": "This row's version: `1` for a freshly scheduled task,\n`max + 1` for each `tasks schedule --overwrite` (each version\nis its own row; only the newest lists).",
      "type": "integer",
      "minimum": 0,
      "maximum": 18446744073709551615
    }
  }
}
