{
  "title": "mcp.shared.IconTheme",
  "description": "Theme preference for an icon.",
  "anyOf": [
    {
      "title": "Light",
      "type": "string",
      "enum": [
        "light"
      ]
    },
    {
      "title": "Dark",
      "type": "string",
      "enum": [
        "dark"
      ]
    }
  ]
}
