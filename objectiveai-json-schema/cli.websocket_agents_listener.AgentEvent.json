{
  "title": "cli.websocket_agents_listener.AgentEvent",
  "description": "One event on the `/agents/instances/list` stream. The first is always a\n[`Snapshot`](AgentEvent::Snapshot); every later one is an\n[`Activated`](AgentEvent::Activated) or\n[`Deactivated`](AgentEvent::Deactivated) delta. Consumers key by\n`agent_instance_hierarchy` (the snapshot and a delta may overlap by\none item at connect time).",
  "anyOf": [
    {
      "title": "Snapshot",
      "description": "The full set of agents, sent once immediately on connect.",
      "type": "object",
      "properties": {
        "agents": {
          "type": "array",
          "items": {
            "$ref": "cli.websocket_agents_listener.AgentRecord"
          }
        },
        "type": {
          "type": "string",
          "enum": [
            "snapshot"
          ]
        }
      }
    },
    {
      "title": "Activated",
      "description": "An agent acquired its per-instance lock (became active).",
      "type": "object",
      "properties": {
        "agent": {
          "$ref": "cli.websocket_agents_listener.AgentRecord"
        },
        "type": {
          "type": "string",
          "enum": [
            "activated"
          ]
        }
      }
    },
    {
      "title": "Updated",
      "description": "An agent's record changed while it remained present — currently\nemitted when its bound tags change (a tag applied, moved, or\nremoved). Carries the full refreshed record; consumers replace by\n`agent_instance_hierarchy`.",
      "type": "object",
      "properties": {
        "agent": {
          "$ref": "cli.websocket_agents_listener.AgentRecord"
        },
        "type": {
          "type": "string",
          "enum": [
            "updated"
          ]
        }
      }
    },
    {
      "title": "Deactivated",
      "description": "An agent released its per-instance lock (became inactive) — on\nnormal stream end OR holder death. `last_active_at` is the release\nmoment.",
      "type": "object",
      "properties": {
        "agent_instance_hierarchy": {
          "type": "string"
        },
        "last_active_at": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "omitempty": true
        },
        "type": {
          "type": "string",
          "enum": [
            "deactivated"
          ]
        }
      }
    }
  ]
}
