{
  "title": "cli.broadcast_listener.ListenerEnd",
  "description": "A stream terminator on the `/listen` broadcast — see the module\ndocs.",
  "type": "object",
  "properties": {
    "end": {
      "description": "Always `true` — the terminator marker.",
      "type": "boolean"
    },
    "id": {
      "description": "The broadcast stream id this terminator closes.",
      "type": "string"
    }
  }
}
