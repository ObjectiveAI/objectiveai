{
  "title": "cli.plugins.TypedOutput",
  "description": "The three explicitly-typed plugin output variants. Internally\ntagged on `type`.",
  "anyOf": [
    {
      "title": "Command",
      "type": "object",
      "properties": {
        "command": {
          "type": "string"
        },
        "id": {
          "description": "Plugin-minted correlation id for this command. The host\nstreams every emission from the command back into the\nplugin's stdin; plugins demultiplex concurrent in-flight\ncommands by matching against the echoed id on each response\nline.",
          "type": "string"
        },
        "type": {
          "type": "string",
          "enum": [
            "command"
          ]
        }
      }
    },
    {
      "title": "Mcp",
      "type": "object",
      "$ref": "cli.plugins.Mcp",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "mcp"
          ]
        }
      }
    },
    {
      "title": "Error",
      "type": "object",
      "$ref": "cli.output.Error",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "error"
          ]
        }
      }
    }
  ]
}
