{
  "title": "LogReference",
  "description": "Plain on-disk pointer (`type` + `path` only).",
  "type": "object",
  "properties": {
    "path": {
      "description": "Relative on-disk path of the referenced file (under\n`${config_base_dir}/logs/`). Skipped when empty — the no-data\nsentinel case used by some wrappers when the inner chunk has\nno content to log.",
      "type": "string",
      "omitempty": true
    },
    "type": {
      "$ref": "LogReferenceTag"
    }
  }
}
