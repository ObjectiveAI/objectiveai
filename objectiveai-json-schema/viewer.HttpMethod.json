{
  "title": "viewer.HttpMethod",
  "description": "HTTP method an [`ApiCallSubType`] maps to. Mirrors the methods\n`objectiveai-api`'s router uses (`POST`, `GET`, `DELETE`).",
  "type": "string",
  "enum": [
    "GET",
    "POST",
    "DELETE"
  ]
}
