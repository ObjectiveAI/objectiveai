{
  "title": "cli.command.agents.tags.add.Response",
  "type": "object",
  "properties": {
    "agent_full_id": {
      "type": "string"
    },
    "name": {
      "type": "string"
    },
    "parent_agent_instance_hierarchy": {
      "description": "Resolved parent scope — filled in with the cli's own\n`Config.agent_instance_hierarchy` when the caller did not\nsupply one.",
      "type": "string"
    }
  }
}
