{
  "title": "vector.completions.response.Vote",
  "description": "A single LLM's vote in a vector completion.\n\nEach LLM in the swarm produces a vote indicating which response(s) it\nselected. Votes are weighted according to the profile and combined to\nproduce the final scores.\n\n# Vote Format\n\nThe `vote` field is a vector of decimals corresponding to the responses\nin the request. Typically one element is 1.0 and the rest are 0.0 (discrete\nselection), but when `top_logprobs` is used, votes may be probability\ndistributions.",
  "type": "object",
  "properties": {
    "agent_full_id": {
      "description": "WF-level id of the agent that produced this vote — concatenation\nof the primary agent's id with all fallback ids (see\n`InlineAgentWithFallbacks::full_id`). Same for every slot in the\nsame WF request and deterministic across api processes.",
      "type": "string"
    },
    "agent_id": {
      "description": "Leaf agent id of the slot that produced this vote (matches the\n`agent_id` on the corresponding\n[`super::super::super::super::agent::completions::response::unary::AgentCompletion`]).\nWhen fallbacks fired, this is the fallback's id rather than the\nprimary's.",
      "type": "string"
    },
    "completion_index": {
      "description": "Index correlating this vote with its completion — equals the\n`index` on the corresponding\n[`vector::completions::response::streaming::AgentCompletionChunk`](super::streaming::AgentCompletionChunk)\nwrapper. Populated by the API; `None` only in hand-built values.",
      "anyOf": [
        {
          "type": "integer",
          "minimum": 0,
          "maximum": 18446744073709551615
        },
        {
          "type": "null"
        }
      ],
      "omitempty": true
    },
    "flat_swarm_index": {
      "description": "Flattened index accounting for agent counts in the swarm.",
      "type": "integer",
      "minimum": 0,
      "maximum": 18446744073709551615
    },
    "prompt_id": {
      "description": "Content hash of the request messages (for caching/deduplication).",
      "type": "string"
    },
    "responses_ids": {
      "description": "Content hashes of each response option in the request.",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "swarm_index": {
      "description": "Index of the agent configuration within the swarm.",
      "type": "integer",
      "minimum": 0,
      "maximum": 18446744073709551615
    },
    "vote": {
      "description": "The vote distribution. Each index corresponds to a response from the\nrequest. Typically one element is 1.0 (selected) and the rest are 0.0.",
      "type": "array",
      "items": {
        "type": "number",
        "minimum": -3.4028234663852886e+38,
        "maximum": 3.4028234663852886e+38
      }
    },
    "weight": {
      "description": "The weight applied to this vote when computing final scores.",
      "type": "number",
      "minimum": -3.4028234663852886e+38,
      "maximum": 3.4028234663852886e+38
    }
  }
}
