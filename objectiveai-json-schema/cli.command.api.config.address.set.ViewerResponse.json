{
  "title": "cli.command.api.config.address.set.ViewerResponse",
  "description": "Viewer-stream mirror of [`Response`]: the response (nested under\n`value`) plus the broadcast stream `id` and the originating request's\n`path_type`.",
  "type": "object",
  "properties": {
    "id": {
      "type": "string"
    },
    "path_type": {
      "$ref": "cli.command.api.config.address.set.Path"
    },
    "value": {
      "$ref": "cli.command.Ok"
    }
  }
}
