{
  "title": "cli.command.tasks.list.Request",
  "type": "object",
  "properties": {
    "after_id": {
      "description": "Skip rows with `schedules.id <= after_id`. Use the highest\n`id` from a previous page to paginate forward.",
      "anyOf": [
        {
          "type": "integer",
          "minimum": -9223372036854775808,
          "maximum": 9223372036854775807
        },
        {
          "type": "null"
        }
      ],
      "omitempty": true
    },
    "count": {
      "description": "Per-target row cap — each target's query returns at most this\nmany rows (ascending id, after `after_id`). `None` = unlimited.",
      "anyOf": [
        {
          "type": "integer",
          "minimum": 0,
          "maximum": 18446744073709551615
        },
        {
          "type": "null"
        }
      ],
      "omitempty": true
    },
    "exhausted": {
      "description": "Show only schedules NOT currently runnable — fired\noneshots (visible briefly before the runner deletes\nthem) and interval rows that are cooling down. Mutually\nexclusive with `pending`.",
      "type": "boolean"
    },
    "interval": {
      "description": "Filter to recurring rows only. Mutually exclusive with `oneshot`.",
      "type": "boolean"
    },
    "jq": {
      "description": "jq filter applied to the JSON output. Ignored when `python`\nis also set — python overrides jq.",
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ]
    },
    "oneshot": {
      "description": "Filter to oneshot rows only. Mutually exclusive with `interval`.",
      "type": "boolean"
    },
    "path_type": {
      "$ref": "cli.command.tasks.list.Path"
    },
    "pending": {
      "description": "Show only schedules currently runnable — oneshots that\nhave never fired, and interval rows whose interval has\nelapsed. Mutually exclusive with `exhausted`.",
      "type": "boolean"
    },
    "python": {
      "description": "Python transform applied to the JSON output. Overrides `jq`\nwhen both are provided.",
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ]
    },
    "targets": {
      "description": "One or more targets to list schedules for. Each resolves to a\nsingle AIH (`me` → the cli's own; `instance=L[,parent=P]`;\n`tag=T` BOUND-only — PENDING / ABSENT error), and rows whose\n`agent_instance_hierarchy` equals any resolved AIH are returned.",
      "type": "array",
      "items": {
        "$ref": "cli.command.agents.logs.read.all.Target"
      }
    }
  }
}
