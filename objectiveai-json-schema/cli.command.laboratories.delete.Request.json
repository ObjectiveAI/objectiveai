{
  "title": "cli.command.laboratories.delete.Request",
  "type": "object",
  "properties": {
    "id": {
      "type": "string"
    },
    "jq": {
      "description": "jq filter applied to the JSON output. Ignored when `python`\nis also set — python overrides jq.",
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ]
    },
    "kind": {
      "$ref": "cli.command.laboratories.delete.Kind"
    },
    "machine": {
      "description": "The EXACT machine id whose laboratory host owns the container.\nProvided together with `machine_state` or not at all; neither ⇒\nthe current machine + the daemon's own state.",
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "omitempty": true
    },
    "machine_state": {
      "description": "The state (on `machine`) whose laboratory host owns the\ncontainer. Paired with `machine` — both or neither.",
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "omitempty": true
    },
    "max_tokens": {
      "description": "Response token budget, `>= 1` (`0` is rejected at parse\ntime — omit entirely for unlimited). Forward-compatible\nenvelope data — no leaf enforces it yet.",
      "anyOf": [
        {
          "type": "integer",
          "minimum": 0,
          "maximum": 18446744073709551615
        },
        {
          "type": "null"
        }
      ],
      "omitempty": true
    },
    "path_type": {
      "$ref": "cli.command.laboratories.delete.Path"
    },
    "python": {
      "description": "Python transform applied to the JSON output. Overrides `jq`\nwhen both are provided.",
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ]
    },
    "timeout_seconds": {
      "description": "Wall-clock execution cap, in whole seconds. Parsed from\n`--timeout` (humantime: `30s`, `5m`, `1h30m`), `> 0`\nenforced at parse time. `db query` threads it to postgres\nwhen set; omit for uncapped.",
      "anyOf": [
        {
          "type": "integer",
          "minimum": 0,
          "maximum": 18446744073709551615
        },
        {
          "type": "null"
        }
      ],
      "omitempty": true
    }
  }
}
