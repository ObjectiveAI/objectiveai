{
  "title": "cli.command.tools.ViewerRequest",
  "description": "Viewer-stream mirror of [`Request`] — the real command requests only\n(schema-introspection variants are excluded; the viewer streams\nactual command traffic). Untagged: each variant carries the leaf's\n`path_type`, so it stays discriminable.",
  "anyOf": [
    {
      "title": "Get",
      "$ref": "cli.command.tools.get.ViewerRequest"
    },
    {
      "title": "Install",
      "$ref": "cli.command.tools.install.ViewerRequest"
    },
    {
      "title": "List",
      "$ref": "cli.command.tools.list.ViewerRequest"
    },
    {
      "title": "Run",
      "$ref": "cli.command.tools.run.ViewerRequest"
    }
  ]
}
