{
  "title": "cli.command.agents.read.subscribe.RequestMessageKind",
  "description": "The six values stored in the `messages.kind` TEXT column. Owning\nthis enum in the SDK lets bare-naked callers reason about message\nkinds without depending on the CLI's filesystem layer.",
  "type": "string",
  "enum": [
    "agent_completion_request",
    "function_execution_request",
    "function_invention_recursive_request",
    "agent_completion_notification",
    "assistant_response",
    "tool_response"
  ]
}
