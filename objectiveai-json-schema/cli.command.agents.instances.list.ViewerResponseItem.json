{
  "title": "cli.command.agents.instances.list.ViewerResponseItem",
  "description": "Viewer-stream mirror of [`ResponseItem`]: the response (nested under\n`value`) plus the broadcast stream `id` and the originating request's\n`path_type`.",
  "type": "object",
  "properties": {
    "id": {
      "type": "string"
    },
    "path_type": {
      "$ref": "cli.command.agents.instances.list.Path"
    },
    "value": {
      "$ref": "cli.command.agents.instances.list.ResponseItem"
    }
  }
}
