{
  "title": "cli.command.agents.queue.deliver.Request",
  "type": "object",
  "properties": {
    "dangerous_advanced": {
      "anyOf": [
        {
          "$ref": "cli.command.agents.queue.deliver.RequestDangerousAdvanced"
        },
        {
          "type": "null"
        }
      ]
    },
    "jq": {
      "description": "jq filter applied to the JSON output. Ignored when `python`\nis also set — python overrides jq.",
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ]
    },
    "path_type": {
      "$ref": "cli.command.agents.queue.deliver.Path"
    },
    "python": {
      "description": "Python transform applied to the JSON output. Overrides `jq`\nwhen both are provided.",
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ]
    }
  }
}
