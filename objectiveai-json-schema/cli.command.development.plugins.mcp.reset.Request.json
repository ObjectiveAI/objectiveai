{
  "title": "cli.command.development.plugins.mcp.reset.Request",
  "type": "object",
  "properties": {
    "caches": {
      "description": "Also delete the plugin's build CACHE directories.\n\nOff by default, and that default is the point: this command\nruns after every edit, and the caches are the only reason the\nrebuild it triggers is fast. Ask for this when a cache is\ncorrupt or a toolchain changed, not routinely.",
      "type": "boolean",
      "default": false
    },
    "jq": {
      "description": "jq filter applied to the JSON output. Ignored when `python`\nis also set — python overrides jq.",
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ]
    },
    "max_tokens": {
      "description": "Response token budget, `>= 1` (`0` is rejected at parse\ntime — omit entirely for unlimited). Forward-compatible\nenvelope data — no leaf enforces it yet.",
      "anyOf": [
        {
          "type": "integer",
          "minimum": 0,
          "maximum": 18446744073709551615
        },
        {
          "type": "null"
        }
      ],
      "omitempty": true
    },
    "name": {
      "description": "Repository segment, likewise lowercased.",
      "type": "string"
    },
    "owner": {
      "description": "GitHub `<owner>` segment, lowercased on arrival.",
      "type": "string"
    },
    "path_type": {
      "$ref": "cli.command.development.plugins.mcp.reset.Path"
    },
    "python": {
      "description": "Python transform applied to the JSON output. Overrides `jq`\nwhen both are provided.",
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ]
    },
    "timeout_seconds": {
      "description": "Wall-clock execution cap, in whole seconds. Parsed from\n`--timeout` (humantime: `30s`, `5m`, `1h30m`), `> 0`\nenforced at parse time. `db query` threads it to postgres\nwhen set; omit for uncapped.",
      "anyOf": [
        {
          "type": "integer",
          "minimum": 0,
          "maximum": 18446744073709551615
        },
        {
          "type": "null"
        }
      ],
      "omitempty": true
    },
    "version": {
      "description": "Plugin version — with owner and name, it derives the image tag.",
      "type": "string"
    }
  }
}
