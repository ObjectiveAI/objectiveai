{
  "title": "daemon.channel_listener.ChannelOffer",
  "description": "One channel OFFER, as broadcast to every connected channel stream.\nCarries no secret — the publisher's `S_pub` is returned to the\npublisher's command, and the owner's `S_owner` is returned by the\naccept POST.",
  "type": "object",
  "properties": {
    "agent_full_id": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "omitempty": true
    },
    "agent_id": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "omitempty": true
    },
    "agent_instance_hierarchy": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "omitempty": true
    },
    "agent_remote": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "omitempty": true
    },
    "channel_id": {
      "description": "The daemon-minted channel id — the accept + log routing key.",
      "type": "string"
    },
    "details": {
      "description": "Arbitrary offer payload, opaque to the daemon."
    },
    "key": {
      "description": "Caller-chosen discriminator (e.g. `\"browser.login\"`) — how a\nuser surface decides whether/how to accept the offer.",
      "type": "string"
    },
    "message": {
      "description": "Human-readable offer message, opaque to the daemon.",
      "type": "string"
    },
    "plugin_name": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "omitempty": true
    },
    "plugin_owner": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "omitempty": true
    },
    "plugin_version": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "omitempty": true
    },
    "response_id": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "omitempty": true
    },
    "response_ids": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "omitempty": true
    },
    "task": {
      "description": "Fired by the task scheduler (daemon-authored; see the struct\ndocs). Always present on the wire; `default` only tolerates\nframes from older producers.",
      "type": "boolean",
      "default": false
    }
  }
}
