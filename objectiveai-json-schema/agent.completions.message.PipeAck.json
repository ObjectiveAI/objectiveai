{
  "title": "agent.completions.message.PipeAck",
  "anyOf": [
    {
      "title": "Ok",
      "description": "The `RichContent` line parsed, queued in the writer task, and\ndispatched to the API server successfully.",
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "ok"
          ]
        }
      }
    },
    {
      "title": "Error",
      "description": "Something failed — either the line wasn't valid\n`RichContent` JSON or the API-side notify dispatch returned\nan error. `message` is human-readable and not load-bearing.",
      "type": "object",
      "properties": {
        "message": {
          "type": "string"
        },
        "type": {
          "type": "string",
          "enum": [
            "error"
          ]
        }
      }
    }
  ]
}
