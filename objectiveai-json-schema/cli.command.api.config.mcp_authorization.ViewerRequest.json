{
  "title": "cli.command.api.config.mcp_authorization.ViewerRequest",
  "description": "Viewer-stream mirror of [`Request`] — the real command requests only\n(schema-introspection variants are excluded; the viewer streams\nactual command traffic). Untagged: each variant carries the leaf's\n`path_type`, so it stays discriminable.",
  "anyOf": [
    {
      "title": "Add",
      "$ref": "cli.command.api.config.mcp_authorization.add.ViewerRequest"
    },
    {
      "title": "Del",
      "$ref": "cli.command.api.config.mcp_authorization.del.ViewerRequest"
    },
    {
      "title": "Get",
      "$ref": "cli.command.api.config.mcp_authorization.get.ViewerRequest"
    }
  ]
}
