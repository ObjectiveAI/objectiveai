{
  "title": "daemon.agents_instances_listener.AgentRecord",
  "description": "One agent's record: identity, spawn / last-active timestamps, tag\nbindings, counters, and whether its per-instance lock is currently\nheld. Mirrors `agents instances list`'s `ResponseItem` plus the\nlive `active` flag. Carried by [`AgentInstanceEvent::Agent`] — the\n`/agents/instances/list` stream itself is a flat list of AIH\nstrings and carries no records.",
  "type": "object",
  "properties": {
    "active": {
      "description": "Whether the agent's per-instance lock is currently held — i.e. a\nlive process owns this agent right now.",
      "type": "boolean"
    },
    "active_laboratories": {
      "description": "The agent's ACTIVE laboratories — the laboratory ids actually\nsent with the MOST RECENT spawn request (what the latest pass\ndialed), in resolve order. Most-recent-value semantics: the set\nsurvives deactivation and is replaced on the next pass. Fully\nseparate from `attached_laboratories`.",
      "type": "array",
      "items": {
        "type": "string"
      },
      "default": []
    },
    "agent_instance_hierarchy": {
      "description": "Full hierarchy of this agent instance.",
      "type": "string"
    },
    "attached_laboratories": {
      "description": "The agent's ATTACHED laboratories — the effective set the next\nspawn pass dials (the AIH's own attachments UNION its bound\ntags'), oldest-attached first. Live-tracked: attach/detach may\nhappen at any time, active agents included.",
      "type": "array",
      "items": {
        "$ref": "daemon.agents_instances_listener.AttachedLaboratory"
      },
      "default": []
    },
    "last_active_at": {
      "description": "RFC3339 timestamp the agent was last active. Meaningful only when\n`active` is `false` — a live agent's last-active is implicitly\n\"now\", so it is left `None` while active and stamped at the moment\nthe lock releases.",
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "omitempty": true
    },
    "logged": {
      "description": "Total `objectiveai.messages` rows for this agent over all time.",
      "type": "integer",
      "minimum": 0,
      "maximum": 18446744073709551615
    },
    "queued": {
      "description": "Active `message_queue` rows targeting this agent.",
      "type": "integer",
      "minimum": 0,
      "maximum": 18446744073709551615
    },
    "spawned_at": {
      "description": "RFC3339 timestamp of the first `objectiveai.messages` row for this\nagent (spawn time). `None` for an agent with no logs yet.",
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "omitempty": true
    },
    "tags": {
      "description": "Tag names currently bound to this AIH, newest-bound first.",
      "type": "array",
      "items": {
        "type": "string"
      }
    }
  }
}
