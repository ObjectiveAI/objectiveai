{
  "title": "cli.command.tasks.run.Request",
  "type": "object",
  "properties": {
    "jq": {
      "description": "jq filter applied to the JSON output. Ignored when `python`\nis also set — python overrides jq.",
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ]
    },
    "path_type": {
      "$ref": "cli.command.tasks.run.Path"
    },
    "python": {
      "description": "Python transform applied to the JSON output. Overrides `jq`\nwhen both are provided.",
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ]
    },
    "stream_all": {
      "description": "Stream every item every fired task emits (each a\n[`ValueResponseItem`]). When false — the default — each task\nyields exactly one [`SuccessResponseItem`] summary instead; the\nfull output still lands in `tasks_logs` either way.",
      "type": "boolean"
    }
  }
}
