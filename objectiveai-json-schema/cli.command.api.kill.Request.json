{
  "title": "cli.command.api.kill.Request",
  "type": "object",
  "properties": {
    "jq": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ]
    },
    "path_type": {
      "$ref": "cli.command.api.kill.Path"
    },
    "scope": {
      "description": "Always Global — api has a single machine-wide lock.",
      "$ref": "cli.command.SetScope"
    }
  }
}
