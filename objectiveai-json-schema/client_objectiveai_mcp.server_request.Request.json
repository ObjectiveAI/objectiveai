{
  "title": "client_objectiveai_mcp.server_request.Request",
  "description": "One reverse-attach request the API has shipped to the calling\nclient. The proxy's HTTP method (`POST` for the five JSON-RPC\nmethods, `DELETE` for session terminate) is implicit in the\n[`super::Payload`] variant; the JSON-RPC `{jsonrpc, id, method,\nparams}` envelope is unwrapped into the typed variant payload.\n\nWhich CLI-hosted MCP server the request targets rides as\n`mcp_kind` on the envelope. The API parses this off the inbound\nURL path (`/objectiveai` → [`super::super::McpKind::ObjectiveAi`];\n`/{owner}/{name}/{version}/{mcp}` → [`super::super::McpKind::Other`])\nbefore forwarding.\n\nWire shape (envelope is `{id, mcp_kind, headers?, type, …variant\nfields…}` after the `#[serde(flatten)]` on `payload`):\n\n```json\n{\n  \"id\":\"…\",\n  \"mcp_kind\":{\"type\":\"objective_ai\"},\n  \"headers\":{\"Mcp-Session-Id\":\"…\"},\n  \"type\":\"tools_list\",\n  \"cursor\":\"…\"\n}\n```",
  "anyOf": [
    {
      "title": "Initialize",
      "description": "POST `initialize`. The proxy's `protocolVersion` doesn't ride\nacross this hop — the API discards it on the way in and\nsubstitutes its own `canonical_initialize_result` on the way\nout. The variant carries the plugin arguments the CLI needs at\ndial time (parsed by the API off the URL query string).",
      "type": "object",
      "$ref": "client_objectiveai_mcp.server_request.InitializeRequest",
      "properties": {
        "headers": {
          "description": "Verbatim copy of the headers the proxy sent on its HTTP\nrequest to the API. The CLI conduit reads several custom\n`X-OBJECTIVEAI-*` routing headers + `Mcp-Session-Id` off this\nmap; protocol-level headers (Host, Content-Length, …) the API\nalready stripped on its way in.",
          "type": "object",
          "additionalProperties": {
            "type": "string"
          },
          "omitempty": true
        },
        "id": {
          "description": "Server-minted correlation id. Echoed by the matching\n[`super::super::server_response::Response`].",
          "type": "string"
        },
        "mcp_kind": {
          "description": "Which CLI-hosted MCP server this request targets.",
          "$ref": "client_objectiveai_mcp.McpKind"
        },
        "type": {
          "type": "string",
          "enum": [
            "initialize"
          ]
        }
      }
    },
    {
      "title": "ToolsList",
      "description": "POST `tools/list`.",
      "type": "object",
      "$ref": "mcp.tool.ListToolsRequest",
      "properties": {
        "headers": {
          "description": "Verbatim copy of the headers the proxy sent on its HTTP\nrequest to the API. The CLI conduit reads several custom\n`X-OBJECTIVEAI-*` routing headers + `Mcp-Session-Id` off this\nmap; protocol-level headers (Host, Content-Length, …) the API\nalready stripped on its way in.",
          "type": "object",
          "additionalProperties": {
            "type": "string"
          },
          "omitempty": true
        },
        "id": {
          "description": "Server-minted correlation id. Echoed by the matching\n[`super::super::server_response::Response`].",
          "type": "string"
        },
        "mcp_kind": {
          "description": "Which CLI-hosted MCP server this request targets.",
          "$ref": "client_objectiveai_mcp.McpKind"
        },
        "type": {
          "type": "string",
          "enum": [
            "tools_list"
          ]
        }
      }
    },
    {
      "title": "ToolsCall",
      "description": "POST `tools/call`.",
      "type": "object",
      "$ref": "mcp.tool.CallToolRequestParams",
      "properties": {
        "headers": {
          "description": "Verbatim copy of the headers the proxy sent on its HTTP\nrequest to the API. The CLI conduit reads several custom\n`X-OBJECTIVEAI-*` routing headers + `Mcp-Session-Id` off this\nmap; protocol-level headers (Host, Content-Length, …) the API\nalready stripped on its way in.",
          "type": "object",
          "additionalProperties": {
            "type": "string"
          },
          "omitempty": true
        },
        "id": {
          "description": "Server-minted correlation id. Echoed by the matching\n[`super::super::server_response::Response`].",
          "type": "string"
        },
        "mcp_kind": {
          "description": "Which CLI-hosted MCP server this request targets.",
          "$ref": "client_objectiveai_mcp.McpKind"
        },
        "type": {
          "type": "string",
          "enum": [
            "tools_call"
          ]
        }
      }
    },
    {
      "title": "ResourcesList",
      "description": "POST `resources/list`.",
      "type": "object",
      "$ref": "mcp.resource.ListResourcesRequest",
      "properties": {
        "headers": {
          "description": "Verbatim copy of the headers the proxy sent on its HTTP\nrequest to the API. The CLI conduit reads several custom\n`X-OBJECTIVEAI-*` routing headers + `Mcp-Session-Id` off this\nmap; protocol-level headers (Host, Content-Length, …) the API\nalready stripped on its way in.",
          "type": "object",
          "additionalProperties": {
            "type": "string"
          },
          "omitempty": true
        },
        "id": {
          "description": "Server-minted correlation id. Echoed by the matching\n[`super::super::server_response::Response`].",
          "type": "string"
        },
        "mcp_kind": {
          "description": "Which CLI-hosted MCP server this request targets.",
          "$ref": "client_objectiveai_mcp.McpKind"
        },
        "type": {
          "type": "string",
          "enum": [
            "resources_list"
          ]
        }
      }
    },
    {
      "title": "ResourcesRead",
      "description": "POST `resources/read`.",
      "type": "object",
      "$ref": "mcp.resource.ReadResourceRequestParams",
      "properties": {
        "headers": {
          "description": "Verbatim copy of the headers the proxy sent on its HTTP\nrequest to the API. The CLI conduit reads several custom\n`X-OBJECTIVEAI-*` routing headers + `Mcp-Session-Id` off this\nmap; protocol-level headers (Host, Content-Length, …) the API\nalready stripped on its way in.",
          "type": "object",
          "additionalProperties": {
            "type": "string"
          },
          "omitempty": true
        },
        "id": {
          "description": "Server-minted correlation id. Echoed by the matching\n[`super::super::server_response::Response`].",
          "type": "string"
        },
        "mcp_kind": {
          "description": "Which CLI-hosted MCP server this request targets.",
          "$ref": "client_objectiveai_mcp.McpKind"
        },
        "type": {
          "type": "string",
          "enum": [
            "resources_read"
          ]
        }
      }
    },
    {
      "title": "SessionTerminate",
      "description": "`DELETE` on the routed MCP URL — the proxy closing the\nsession. No body.",
      "type": "object",
      "properties": {
        "headers": {
          "description": "Verbatim copy of the headers the proxy sent on its HTTP\nrequest to the API. The CLI conduit reads several custom\n`X-OBJECTIVEAI-*` routing headers + `Mcp-Session-Id` off this\nmap; protocol-level headers (Host, Content-Length, …) the API\nalready stripped on its way in.",
          "type": "object",
          "additionalProperties": {
            "type": "string"
          },
          "omitempty": true
        },
        "id": {
          "description": "Server-minted correlation id. Echoed by the matching\n[`super::super::server_response::Response`].",
          "type": "string"
        },
        "mcp_kind": {
          "description": "Which CLI-hosted MCP server this request targets.",
          "$ref": "client_objectiveai_mcp.McpKind"
        },
        "type": {
          "type": "string",
          "enum": [
            "session_terminate"
          ]
        }
      }
    }
  ]
}
