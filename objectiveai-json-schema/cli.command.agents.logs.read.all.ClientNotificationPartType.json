{
  "title": "cli.command.agents.logs.read.all.ClientNotificationPartType",
  "description": "Type tag for one `ClientNotification` part — the table-kind of\nthe underlying `message_queue_*` content row.",
  "type": "string",
  "enum": [
    "text",
    "image",
    "audio",
    "video",
    "file"
  ]
}
