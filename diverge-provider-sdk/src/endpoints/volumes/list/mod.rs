//! Listing the directories a provider offers.
//!
//! One question with no parameters, and one answer. Split by who
//! SENDS, as everywhere else: the question is in [`client`], the
//! answer in [`server`].

pub mod client;
pub mod server;
