[workspace]
resolver = "3"
members = [
    "objectiveai-api",
    "objectiveai-db",
    "objectiveai-laboratory",
    "objectiveai-mcp-laboratory",
    "objectiveai-mcp-proxy",
    "objectiveai-mcp-plugin-framework-rs",
    "objectiveai-mcp-plugin-scaffold-rs",
    "objectiveai-sdk-rs",
    "objectiveai-json-schema/builder",
    "objectiveai-sdk-rs-wasm-js",
    "objectiveai-sdk-rs-macros",
    "objectiveai-sdk-rs-pyo3",
    "objectiveai-sdk-rs-cffi", "objectiveai-daemon", "objectiveai-cli",
    # Integration-test fixture crates — plugin stubs the cli
    # integration tests build and exec, grouped under tests/plugins/.
    "tests/plugins/test-mcp-plugin-named",
    "tests/plugins/hello-plugin",
    "tests/plugins/test-mcp-plugin",
    "tests/plugins/test-mcp-plugin-self-call",
    "tests/plugins/test-mcp-plugin-failing",
    "tests/plugins/daemon-echo-plugin",
    "tests/plugins/stderr-plugin",
    "objectiveai-viewer/src-tauri",
    "tests/objectiveai-tests",
    "tests/objectiveai-api-tests",
    "tests/objectiveai-cli-tests",
]

# Built only for wasm32-wasip1 by objectiveai-daemon/build.rs — excluded so the
# host-target workspace build never tries to compile its wasm-only host import.
exclude = ["objectiveai-rustpython-wasm"]

# objectiveai-mcp-plugin-scaffold-rs is a template that gets COPIED OUT and
# built on its own, so its manifest must be exactly what a standalone plugin
# needs: a plain `objectiveai-mcp-plugin-framework = "x.y.z"`, no path to a
# checkout that will not be there. Building it here would then want the
# framework from crates.io at the version this branch is developing, which by
# definition is not published yet. Redirecting the registry to the local
# framework keeps the scaffold compiling on every workspace build — so it
# cannot rot against a framework change — while leaving the copied folder
# untouched, since nothing here travels with it.
[patch.crates-io]
objectiveai-mcp-plugin-framework = { path = "objectiveai-mcp-plugin-framework-rs" }

[workspace.metadata.tools]
wasm-pack = "0.14.0"
maturin = "1.12.6"
