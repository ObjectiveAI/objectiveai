// THIS FILE IS AUTO-GENERATED. DO NOT EDIT.
// Generated by scripts/install_go.go from objectiveai-json-schema/.
package objectiveai

import (
	"encoding/json"
	"fmt"
)

// One output item from one fired schedule's in-process stream.
// `id` is the source schedule's stable identifier, formatted
// `"{name}-{db_id}"` so it's both human-readable (the user-
// supplied `--name` from `schedule`) and globally unique (the
// row id from `schedules`). `value` is the typed root
// [`crate::cli::command::ResponseItem`] emitted by the scheduled
// cli leaf — boxed because the root union transitively contains
// *this* variant (`agents → tasks → run`), and boxing is what
// makes the recursion sized.
//
// Exempt from json-schema coverage because the boxed root union
// is the same TS7056 blowup the root and tier aggregates already
// dodge — see the marker on `cli.command.ResponseItem` itself.
type CliCommandAgentsTasksRunResponseItem struct {
	ID string `json:"id"`
	Value CliCommandResponseItem `json:"value"`
}

func (CliCommandAgentsTasksRunResponseItem) SchemaTitle() string { return "cli.command.agents.tasks.run.ResponseItem" }
func (v CliCommandAgentsTasksRunResponseItem) Validate() error {
	return variantValidator.Struct(v)
}

func (v *CliCommandAgentsTasksRunResponseItem) UnmarshalJSON(data []byte) error {
	var raw map[string]json.RawMessage
	if err := json.Unmarshal(data, &raw); err != nil {
		return err
	}
	for _, key := range []string{"id", "value"} {
		if _, ok := raw[key]; !ok {
			return fmt.Errorf("CliCommandAgentsTasksRunResponseItem: missing required field %q", key)
		}
	}
	type Alias CliCommandAgentsTasksRunResponseItem
	var alias Alias
	if err := json.Unmarshal(data, &alias); err != nil {
		return err
	}
	*v = CliCommandAgentsTasksRunResponseItem(alias)
	return nil
}
