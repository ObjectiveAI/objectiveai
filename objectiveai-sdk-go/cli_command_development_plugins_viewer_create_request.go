// THIS FILE IS AUTO-GENERATED. DO NOT EDIT.
// Generated by scripts/install_go.go from objectiveai-json-schema/.
package objectiveai

import (
	"encoding/json"
	"fmt"
)

type CliCommandDevelopmentPluginsViewerCreateRequest struct {
	// jq filter applied to the JSON output. Ignored when `python`
	// is also set — python overrides jq.
	Jq *string `json:"jq"`
	// Response token budget, `>= 1` (`0` is rejected at parse
	// time — omit entirely for unlimited). Forward-compatible
	// envelope data — no leaf enforces it yet.
	MaxTokens *uint64 `json:"max_tokens,omitempty" validate:"omitempty,min=0,max=18446744073709551615"`
	// Repository segment, likewise lowercased.
	Name string `json:"name"`
	// GitHub `<owner>` segment, lowercased on arrival — the
	// declaration layer lowercases it too, so the registration and an
	// agent's `plugin` entry agree.
	Owner string `json:"owner"`
	// ABSOLUTE path to the plugin's source directory: the one holding
	// `objectiveai.json`. Absolute because the VIEWER resolves it,
	// and its working directory is not yours.
	Path string `json:"path"`
	PathType CliCommandDevelopmentPluginsViewerCreatePath `json:"path_type"`
	// Python transform applied to the JSON output. Overrides `jq`
	// when both are provided.
	Python *string `json:"python"`
	// Wall-clock execution cap, in whole seconds. Parsed from
	// `--timeout` (humantime: `30s`, `5m`, `1h30m`), `> 0`
	// enforced at parse time. `db query` threads it to postgres
	// when set; omit for uncapped.
	TimeoutSeconds *uint64 `json:"timeout_seconds,omitempty" validate:"omitempty,min=0,max=18446744073709551615"`
	// Plugin version — matched byte-for-byte against what an agent
	// declares. It stays case-sensitive even though nothing is
	// fetched — it is part of every `plugin://` URL.
	Version string `json:"version"`
}

func (CliCommandDevelopmentPluginsViewerCreateRequest) SchemaTitle() string { return "cli.command.development.plugins.viewer.create.Request" }
func (v CliCommandDevelopmentPluginsViewerCreateRequest) Validate() error {
	return variantValidator.Struct(v)
}

func (v *CliCommandDevelopmentPluginsViewerCreateRequest) UnmarshalJSON(data []byte) error {
	var raw map[string]json.RawMessage
	if err := json.Unmarshal(data, &raw); err != nil {
		return err
	}
	for _, key := range []string{"name", "owner", "path", "path_type", "version"} {
		if _, ok := raw[key]; !ok {
			return fmt.Errorf("CliCommandDevelopmentPluginsViewerCreateRequest: missing required field %q", key)
		}
	}
	type Alias CliCommandDevelopmentPluginsViewerCreateRequest
	var alias Alias
	if err := json.Unmarshal(data, &alias); err != nil {
		return err
	}
	*v = CliCommandDevelopmentPluginsViewerCreateRequest(alias)
	return nil
}
