// THIS FILE IS AUTO-GENERATED. DO NOT EDIT.
// Generated by scripts/install_go.go from objectiveai-json-schema/.
package objectiveai

import (
	"encoding/json"
	"fmt"
)

type AgentGeminiContinuation struct {
	// Full slash-separated lineage of the agent this continuation
	// belongs to (e.g. `A/B/agtcpl-<uuid>-<created>`). Minted on the
	// agent's first spawn and preserved verbatim across every
	// continuation round so the agent's identity stays stable
	// regardless of who resumes the conversation.
	AgentInstanceHierarchy string `json:"agent_instance_hierarchy"`
	MCPSessions OrderedMap[string, string] `json:"mcp_sessions"`
	// Full prior conversation in the runner's message shape. The
	// Gemini runner is stateless (no server-side session resume), so
	// the API persists the entire history here and replays it — prior
	// history followed by the next turn's messages — on every
	// continuation round. Empty on a fresh conversation.
	Messages []AgentGeminiMessage `json:"messages,omitempty"`
	SessionID string `json:"session_id"`
	Upstream AgentGeminiUpstream `json:"upstream"`
}

func (AgentGeminiContinuation) SchemaTitle() string { return "agent.gemini.Continuation" }
func (v AgentGeminiContinuation) Validate() error {
	return variantValidator.Struct(v)
}

func (v *AgentGeminiContinuation) UnmarshalJSON(data []byte) error {
	var raw map[string]json.RawMessage
	if err := json.Unmarshal(data, &raw); err != nil {
		return err
	}
	for _, key := range []string{"agent_instance_hierarchy", "mcp_sessions", "messages", "session_id", "upstream"} {
		if _, ok := raw[key]; !ok {
			return fmt.Errorf("AgentGeminiContinuation: missing required field %q", key)
		}
	}
	type Alias AgentGeminiContinuation
	var alias Alias
	if err := json.Unmarshal(data, &alias); err != nil {
		return err
	}
	*v = AgentGeminiContinuation(alias)
	return nil
}
