// THIS FILE IS AUTO-GENERATED. DO NOT EDIT.
// Generated by scripts/install_go.go from objectiveai-json-schema/.
package objectiveai

import (
	"encoding/json"
	"fmt"
)

// One whitelist row. Each field is a regex matched **anchored**
// against the corresponding install parameter (compiled as
// `^(?:pattern)$`). All four fields must match for the row to
// allow the install.
//
// The host loads a list of these and accepts a plugin install only
// if the (owner, repository, commit_sha_or_HEAD, manifest.version)
// quadruple matches at least one row. Use `.*` for catch-all fields.
type FilesystemPluginsWhitelistEntry struct {
	// Regex matching the commit sha. When the user omits
	// `--commit-sha`, this is matched against the literal string
	// `"HEAD"`.
	CommitSha string `json:"commit_sha"`
	// Regex matching the GitHub owner (org or user). Case-sensitive.
	Owner string `json:"owner"`
	// Regex matching the repository name.
	Repository string `json:"repository"`
	// Regex matching the manifest's `version` field.
	Version string `json:"version"`
}

func (FilesystemPluginsWhitelistEntry) SchemaTitle() string { return "filesystem.plugins.WhitelistEntry" }
func (v FilesystemPluginsWhitelistEntry) Validate() error {
	return variantValidator.Struct(v)
}

func (v *FilesystemPluginsWhitelistEntry) UnmarshalJSON(data []byte) error {
	var raw map[string]json.RawMessage
	if err := json.Unmarshal(data, &raw); err != nil {
		return err
	}
	for _, key := range []string{"commit_sha", "owner", "repository", "version"} {
		if _, ok := raw[key]; !ok {
			return fmt.Errorf("FilesystemPluginsWhitelistEntry: missing required field %q", key)
		}
	}
	type Alias FilesystemPluginsWhitelistEntry
	var alias Alias
	if err := json.Unmarshal(data, &alias); err != nil {
		return err
	}
	*v = FilesystemPluginsWhitelistEntry(alias)
	return nil
}
