// THIS FILE IS AUTO-GENERATED. DO NOT EDIT.
// Generated by scripts/install_go.go from objectiveai-json-schema/.
package objectiveai

import (
	"encoding/json"
	"fmt"
)

// Expression for the input to pass to the placeholder function.
// Receives: `input`, `map` (if mapped).
//
// A value that can be either a literal or an expression.
//
// This allows Function definitions to mix static values with dynamic
// expressions. During compilation, expressions are evaluated while
// literal values pass through unchanged.
//
// # Example
//
// Literal value:
// ```json
// "hello world"
// ```
//
// JMESPath expression:
// ```json
// {"$jmespath": "input.greeting"}
// ```
//
// Starlark expression:
// ```json
// {"$starlark": "input['greeting']"}
// ```
type FunctionsPlaceholderVectorFunctionTaskExpressionInput struct {
	// An expression (JMESPath or Starlark) to evaluate.
	Expression *FunctionsExpressionExpression 
	// A literal value.
	Value *FunctionsExpressionInputValueExpression 
}

func (v FunctionsPlaceholderVectorFunctionTaskExpressionInput) MarshalJSON() ([]byte, error) {
	if v.Expression != nil {
		return json.Marshal(v.Expression)
	}
	if v.Value != nil {
		return json.Marshal(v.Value)
	}
	return []byte("null"), nil
}

func (v *FunctionsPlaceholderVectorFunctionTaskExpressionInput) UnmarshalJSON(data []byte) error {
	{
		var try FunctionsExpressionExpression
		if err := json.Unmarshal(data, &try); err == nil {
			candidate := FunctionsPlaceholderVectorFunctionTaskExpressionInput{}
			candidate.Expression = &try
			if candidate.Validate() == nil {
				*v = candidate
				return nil
			}
		}
	}
	{
		var try FunctionsExpressionInputValueExpression
		if err := json.Unmarshal(data, &try); err == nil {
			candidate := FunctionsPlaceholderVectorFunctionTaskExpressionInput{}
			candidate.Value = &try
			if candidate.Validate() == nil {
				*v = candidate
				return nil
			}
		}
	}
	return fmt.Errorf("data did not match any variant of FunctionsPlaceholderVectorFunctionTaskExpressionInput")
}

func (v FunctionsPlaceholderVectorFunctionTaskExpressionInput) Validate() error {
	count := 0
	if v.Expression != nil { count++ }
	if v.Value != nil { count++ }
	if count != 1 {
		return fmt.Errorf("FunctionsPlaceholderVectorFunctionTaskExpressionInput: exactly one variant must be set, got %d", count)
	}
	return variantValidator.Struct(v)
}
// Expression for a placeholder vector function task (pre-compilation).
//
// Like [`VectorFunctionTaskExpression`] but without owner/repository/commit.
// Always produces an equalized vector of length `output_length`.
type FunctionsPlaceholderVectorFunctionTaskExpression struct {
	// Expression for the input to pass to the placeholder function.
	// Receives: `input`, `map` (if mapped).
	//
	// A value that can be either a literal or an expression.
	//
	// This allows Function definitions to mix static values with dynamic
	// expressions. During compilation, expressions are evaluated while
	// literal values pass through unchanged.
	//
	// # Example
	//
	// Literal value:
	// ```json
	// "hello world"
	// ```
	//
	// JMESPath expression:
	// ```json
	// {"$jmespath": "input.greeting"}
	// ```
	//
	// Starlark expression:
	// ```json
	// {"$starlark": "input['greeting']"}
	// ```
	Input FunctionsPlaceholderVectorFunctionTaskExpressionInput `json:"input"`
	// Expression merging sub-inputs back into one input.
	// Receives: `input` (as an array).
	InputMerge FunctionsExpressionExpression `json:"input_merge"`
	// JSON Schema defining the expected input structure.
	InputSchema FunctionsExpressionInputSchema `json:"input_schema"`
	// Expression transforming input into sub-inputs for swiss system.
	// Receives: `input`.
	InputSplit FunctionsExpressionExpression `json:"input_split"`
	// Expression that evaluates to the number of mapped task instances.
	// Each instance receives `map` as an integer index (0-based).
	Map *FunctionsExpressionExpression `json:"map,omitempty"`
	// Expression to transform the equalized vector output.
	// Receives: `input`, `output` as `Vector(equalized)`.
	Output FunctionsExpressionExpression `json:"output"`
	// Expression computing the expected output vector length.
	// Receives: `input`.
	OutputLength FunctionsExpressionExpression `json:"output_length"`
	// If this expression evaluates to true, skip the task. Receives: `input`.
	Skip *FunctionsExpressionExpression `json:"skip,omitempty"`
}

func (FunctionsPlaceholderVectorFunctionTaskExpression) SchemaTitle() string { return "functions.PlaceholderVectorFunctionTaskExpression" }
func (v FunctionsPlaceholderVectorFunctionTaskExpression) Validate() error {
	return variantValidator.Struct(v)
}

func (v *FunctionsPlaceholderVectorFunctionTaskExpression) UnmarshalJSON(data []byte) error {
	var raw map[string]json.RawMessage
	if err := json.Unmarshal(data, &raw); err != nil {
		return err
	}
	for _, key := range []string{"input", "input_merge", "input_schema", "input_split", "output", "output_length"} {
		if _, ok := raw[key]; !ok {
			return fmt.Errorf("FunctionsPlaceholderVectorFunctionTaskExpression: missing required field %q", key)
		}
	}
	type Alias FunctionsPlaceholderVectorFunctionTaskExpression
	var alias Alias
	if err := json.Unmarshal(data, &alias); err != nil {
		return err
	}
	*v = FunctionsPlaceholderVectorFunctionTaskExpression(alias)
	return nil
}
