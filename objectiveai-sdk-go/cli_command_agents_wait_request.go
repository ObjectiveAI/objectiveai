// THIS FILE IS AUTO-GENERATED. DO NOT EDIT.
// Generated by scripts/install_go.go from objectiveai-json-schema/.
package objectiveai

import (
	"encoding/json"
	"fmt"
)

type CliCommandAgentsWaitRequest struct {
	// The status to wait FOR: `true` resolves when the agent is
	// ACTIVE (up — its instance lock held), `false` when it is
	// INACTIVE (done — its instance lock free). Defaults to `false`
	// on the wire, so a request predating this field keeps its
	// original wait-until-done meaning.
	Active bool `json:"active" default:"false"`
	// Who to wait on — an instance hierarchy or a tag. A plain ref
	// has no live identity and errors.
	Agent CliCommandAgentsAgentSelector `json:"agent"`
	// jq filter applied to the JSON output. Ignored when `python`
	// is also set — python overrides jq.
	Jq *string `json:"jq"`
	// Response token budget, `>= 1` (`0` is rejected at parse
	// time — omit entirely for unlimited). Forward-compatible
	// envelope data — no leaf enforces it yet.
	MaxTokens *uint64 `json:"max_tokens,omitempty" validate:"omitempty,min=0,max=18446744073709551615"`
	PathType CliCommandAgentsWaitPath `json:"path_type"`
	// Python transform applied to the JSON output. Overrides `jq`
	// when both are provided.
	Python *string `json:"python"`
	// Wall-clock execution cap, in whole seconds. Parsed from
	// `--timeout` (humantime: `30s`, `5m`, `1h30m`), `> 0`
	// enforced at parse time. `db query` threads it to postgres
	// when set; omit for uncapped.
	TimeoutSeconds *uint64 `json:"timeout_seconds,omitempty" validate:"omitempty,min=0,max=18446744073709551615"`
}

func (CliCommandAgentsWaitRequest) SchemaTitle() string { return "cli.command.agents.wait.Request" }
func (v CliCommandAgentsWaitRequest) Validate() error {
	return variantValidator.Struct(v)
}

func (v *CliCommandAgentsWaitRequest) UnmarshalJSON(data []byte) error {
	var raw map[string]json.RawMessage
	if err := json.Unmarshal(data, &raw); err != nil {
		return err
	}
	for _, key := range []string{"active", "agent", "path_type"} {
		if _, ok := raw[key]; !ok {
			return fmt.Errorf("CliCommandAgentsWaitRequest: missing required field %q", key)
		}
	}
	type Alias CliCommandAgentsWaitRequest
	var alias Alias
	if err := json.Unmarshal(data, &alias); err != nil {
		return err
	}
	*v = CliCommandAgentsWaitRequest(alias)
	return nil
}
