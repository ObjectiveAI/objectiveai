// THIS FILE IS AUTO-GENERATED. DO NOT EDIT.
// Generated by scripts/install_go.go from objectiveai-json-schema/.
package objectiveai

import (
	"encoding/json"
	"fmt"
)

// One laboratory on the `/laboratories/list` stream: its spec, the
// machine whose host serves it, and whether that host is connected
// right now. There is no local-vs-remote split — machine identity is
// the only provenance.
type DaemonLaboratoriesListListenerLaboratoryStatus struct {
	// For agent laboratories: the full id of the agent the
	// laboratory derives from. `None` for user-created laboratories.
	AgentFullID *string `json:"agent_full_id,omitempty"`
	// Whether the serving host's `/laboratory` connection is live
	// right now.
	Connected bool `json:"connected"`
	// Unix seconds when the laboratory container was created, from
	// podman's container record. `None` when the host didn't report
	// it.
	CreatedAt *int64 `json:"created_at,omitempty" validate:"omitempty,min=-9223372036854775808,max=9223372036854775807"`
	Cwd string `json:"cwd"`
	Env []LaboratoriesEnvVar `json:"env"`
	// Raw (state-agnostic) laboratory id.
	ID string `json:"id"`
	Image LaboratoriesLaboratoryImage `json:"image"`
	// The machine whose laboratory host serves this laboratory.
	Machine *MachineMachineIdentity `json:"machine,omitempty"`
	// The state (on that machine) the serving host serves —
	// laboratory ids are only unique per (machine, state), so the
	// stream may legitimately carry several same-id items that
	// differ here.
	MachineState *string `json:"machine_state,omitempty"`
	Mounts []LaboratoriesMount `json:"mounts"`
	// Whether the laboratory's CONTAINER is running right now. The
	// lifecycle starts and stops containers on demand (MCP
	// connections and filetree watchers are the demand), and every
	// transition streams as an upsert — subscribers hold live state.
	// Defaulted so frames predating this field parse (as
	// not-running).
	Running bool `json:"running" default:"false"`
}

func (DaemonLaboratoriesListListenerLaboratoryStatus) SchemaTitle() string { return "daemon.laboratories_list_listener.LaboratoryStatus" }
func (v DaemonLaboratoriesListListenerLaboratoryStatus) Validate() error {
	return variantValidator.Struct(v)
}

func (v *DaemonLaboratoriesListListenerLaboratoryStatus) UnmarshalJSON(data []byte) error {
	var raw map[string]json.RawMessage
	if err := json.Unmarshal(data, &raw); err != nil {
		return err
	}
	for _, key := range []string{"connected", "cwd", "env", "id", "image", "mounts", "running"} {
		if _, ok := raw[key]; !ok {
			return fmt.Errorf("DaemonLaboratoriesListListenerLaboratoryStatus: missing required field %q", key)
		}
	}
	type Alias DaemonLaboratoriesListListenerLaboratoryStatus
	var alias Alias
	if err := json.Unmarshal(data, &alias); err != nil {
		return err
	}
	*v = DaemonLaboratoriesListListenerLaboratoryStatus(alias)
	return nil
}
