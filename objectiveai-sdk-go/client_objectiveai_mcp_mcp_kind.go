// THIS FILE IS AUTO-GENERATED. DO NOT EDIT.
// Generated by scripts/install_go.go from objectiveai-json-schema/.
package objectiveai

import (
	"encoding/json"
	"fmt"
)

// The CLI's primary `objectiveai-mcp` upstream — the local MCP
// server bound at the CLI process's `--mcp-address`.
type ClientObjectiveaiMcpMcpKindObjectiveAI struct {
	Type string `json:"type" validate:"oneof=objective_ai"`
}

func (v *ClientObjectiveaiMcpMcpKindObjectiveAI) UnmarshalJSON(data []byte) error {
	var raw map[string]json.RawMessage
	if err := json.Unmarshal(data, &raw); err != nil {
		return err
	}
	for _, key := range []string{"type"} {
		if _, ok := raw[key]; !ok {
			return fmt.Errorf("ClientObjectiveaiMcpMcpKindObjectiveAI: missing required field %q", key)
		}
	}
	type Alias ClientObjectiveaiMcpMcpKindObjectiveAI
	var alias Alias
	if err := json.Unmarshal(data, &alias); err != nil {
		return err
	}
	*v = ClientObjectiveaiMcpMcpKindObjectiveAI(alias)
	return nil
}
func (ClientObjectiveaiMcpMcpKindObjectiveAI) SchemaVariantTitle() string { return "ObjectiveAI" }

// A plugin-hosted MCP server. Identified by the plugin's
// `(owner, name, version)` tuple plus the `mcp` server name
// within that plugin's manifest. Mirrors the API URL
// `/{owner}/{name}/{version}/{mcp}` and the agent declaration's
// `plugins[i].{owner,name,version}.mcp_servers[j].name`.
type ClientObjectiveaiMcpMcpKindOther struct {
	MCP string `json:"mcp"`
	Name string `json:"name"`
	Owner string `json:"owner"`
	Type string `json:"type" validate:"oneof=other"`
	Version string `json:"version"`
}

func (v *ClientObjectiveaiMcpMcpKindOther) UnmarshalJSON(data []byte) error {
	var raw map[string]json.RawMessage
	if err := json.Unmarshal(data, &raw); err != nil {
		return err
	}
	for _, key := range []string{"mcp", "name", "owner", "type", "version"} {
		if _, ok := raw[key]; !ok {
			return fmt.Errorf("ClientObjectiveaiMcpMcpKindOther: missing required field %q", key)
		}
	}
	type Alias ClientObjectiveaiMcpMcpKindOther
	var alias Alias
	if err := json.Unmarshal(data, &alias); err != nil {
		return err
	}
	*v = ClientObjectiveaiMcpMcpKindOther(alias)
	return nil
}
func (ClientObjectiveaiMcpMcpKindOther) SchemaVariantTitle() string { return "Other" }

// Which CLI-hosted MCP server a frame belongs to. Stamped on every
// [`super::server_request::Request`] / [`super::server_response::Response`]
// envelope and on every [`super::client_request::McpListChanged`]
// payload so the CLI's per-MCP dispatch table can route by enum
// rather than by parsing the proxy's URL path on every hop.
//
// Wire shape: `{"type":"objective_ai"}` or
// `{"type":"other","owner":"…","name":"…","version":"…","mcp":"…"}`.
//
// The four discriminator fields on `Other` mirror the API's
// `/{owner}/{name}/{version}/{mcp}` URL path that the proxy dials
// for plugin-hosted MCP servers.
type ClientObjectiveaiMcpMcpKind struct {
	// The CLI's primary `objectiveai-mcp` upstream — the local MCP
	// server bound at the CLI process's `--mcp-address`.
	ObjectiveAI *ClientObjectiveaiMcpMcpKindObjectiveAI `outerObject:"true"`
	// A plugin-hosted MCP server. Identified by the plugin's
	// `(owner, name, version)` tuple plus the `mcp` server name
	// within that plugin's manifest. Mirrors the API URL
	// `/{owner}/{name}/{version}/{mcp}` and the agent declaration's
	// `plugins[i].{owner,name,version}.mcp_servers[j].name`.
	Other *ClientObjectiveaiMcpMcpKindOther `outerObject:"true"`
}

func (v ClientObjectiveaiMcpMcpKind) MarshalJSON() ([]byte, error) {
	if v.ObjectiveAI != nil {
		return json.Marshal(v.ObjectiveAI)
	}
	if v.Other != nil {
		return json.Marshal(v.Other)
	}
	return []byte("null"), nil
}

func (v *ClientObjectiveaiMcpMcpKind) UnmarshalJSON(data []byte) error {
	{
		var try ClientObjectiveaiMcpMcpKindObjectiveAI
		if err := json.Unmarshal(data, &try); err == nil {
			candidate := ClientObjectiveaiMcpMcpKind{}
			candidate.ObjectiveAI = &try
			if candidate.Validate() == nil {
				*v = candidate
				return nil
			}
		}
	}
	{
		var try ClientObjectiveaiMcpMcpKindOther
		if err := json.Unmarshal(data, &try); err == nil {
			candidate := ClientObjectiveaiMcpMcpKind{}
			candidate.Other = &try
			if candidate.Validate() == nil {
				*v = candidate
				return nil
			}
		}
	}
	return fmt.Errorf("data did not match any variant of ClientObjectiveaiMcpMcpKind")
}

func (v ClientObjectiveaiMcpMcpKind) Validate() error {
	count := 0
	if v.ObjectiveAI != nil { count++ }
	if v.Other != nil { count++ }
	if count != 1 {
		return fmt.Errorf("ClientObjectiveaiMcpMcpKind: exactly one variant must be set, got %d", count)
	}
	return variantValidator.Struct(v)
}
func (ClientObjectiveaiMcpMcpKind) SchemaTitle() string { return "client_objectiveai_mcp.McpKind" }

