// THIS FILE IS AUTO-GENERATED. DO NOT EDIT.
// Generated by scripts/install_go.go from objectiveai-json-schema/.
package objectiveai

import (
	"encoding/json"
	"fmt"
)

// Terminal marker for a plugin-dispatched command. Emitted by the
// host on the plugin's stdin after the command's `run()` task has
// resolved, wrapped in the same
// [`crate::cli::plugins::PluginCommandResponse`] envelope every
// preceding response line for the same command rode through — the
// correlation id (when the originating `TypedPluginOutput::Command`
// had one) is on the envelope's `id` field, not on this struct, so
// the plugin's read loop demuxes uniformly.
//
// Wire (inside the envelope, with id):
// `{"id":"<id>","value":{"type":"notification","type":"command_complete","exit_code":<n>}}`.
type CliOutputNotificationCommandComplete struct {
	// Exit code the cli command returned. `0` = success, non-zero
	// = failure (same shape `crate::run::run` returns).
	ExitCode int32 `json:"exit_code" validate:"min=-2147483648,max=2147483647"`
}

func (CliOutputNotificationCommandComplete) SchemaTitle() string { return "cli.output.notification.CommandComplete" }
func (v CliOutputNotificationCommandComplete) Validate() error {
	return variantValidator.Struct(v)
}

func (v *CliOutputNotificationCommandComplete) UnmarshalJSON(data []byte) error {
	var raw map[string]json.RawMessage
	if err := json.Unmarshal(data, &raw); err != nil {
		return err
	}
	for _, key := range []string{"exit_code"} {
		if _, ok := raw[key]; !ok {
			return fmt.Errorf("CliOutputNotificationCommandComplete: missing required field %q", key)
		}
	}
	type Alias CliOutputNotificationCommandComplete
	var alias Alias
	if err := json.Unmarshal(data, &alias); err != nil {
		return err
	}
	*v = CliOutputNotificationCommandComplete(alias)
	return nil
}
