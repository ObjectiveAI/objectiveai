// THIS FILE IS AUTO-GENERATED. DO NOT EDIT.
// Generated by scripts/install_go.go from objectiveai-json-schema/.
package objectiveai

import (
	"encoding/json"
	"fmt"
)

type CliCommandTasksCreateRequest struct {
	// The command to run — the full typed command request.
	// Schema-opaque (`serde_json::Value`) ON PURPOSE: embedding the
	// root request enum's schema in a leaf transitively expands the
	// whole command tree, which downstream generated TypeScript
	// cannot emit declarations for (TS7056) — the same reason the
	// root aggregate is json_schema_ignore'd. The Rust type is the
	// validation; the wire shape is unchanged.
	Command JsonValue `json:"command"`
	// Seconds until the (first) run; with `repeat`, also the interval
	// between runs, measured from each run's completion.
	DelaySecs uint64 `json:"delay_secs" validate:"min=0,max=18446744073709551615"`
	// The task's user-chosen name — REQUIRED, laboratory-style.
	// Unique per creating plugin (non-plugin creators share one
	// namespace); `tasks delete --id` and re-creation resolve
	// against it. This is the caller's handle, distinct from the
	// daemon's internal surrogate key.
	ID string `json:"id"`
	// jq filter applied to the JSON output. Ignored when `python`
	// is also set — python overrides jq.
	Jq *string `json:"jq"`
	// Response token budget, `>= 1` (`0` is rejected at parse
	// time — omit entirely for unlimited). Forward-compatible
	// envelope data — no leaf enforces it yet.
	MaxTokens *uint64 `json:"max_tokens,omitempty" validate:"omitempty,min=0,max=18446744073709551615"`
	PathType CliCommandTasksCreatePath `json:"path_type"`
	// Python transform applied to the JSON output. Overrides `jq`
	// when both are provided.
	Python *string `json:"python"`
	// Re-run every `delay_secs` instead of running once.
	Repeat bool `json:"repeat" default:"false"`
	// Cap on SUCCESSFUL runs — only valid with `repeat`.
	RepeatCount *uint64 `json:"repeat_count,omitempty" validate:"omitempty,min=0,max=18446744073709551615"`
	// Wall-clock execution cap, in whole seconds. Parsed from
	// `--timeout` (humantime: `30s`, `5m`, `1h30m`), `> 0`
	// enforced at parse time. `db query` threads it to postgres
	// when set; omit for uncapped.
	TimeoutSeconds *uint64 `json:"timeout_seconds,omitempty" validate:"omitempty,min=0,max=18446744073709551615"`
}

func (CliCommandTasksCreateRequest) SchemaTitle() string { return "cli.command.tasks.create.Request" }
func (v CliCommandTasksCreateRequest) Validate() error {
	return variantValidator.Struct(v)
}

func (v *CliCommandTasksCreateRequest) UnmarshalJSON(data []byte) error {
	var raw map[string]json.RawMessage
	if err := json.Unmarshal(data, &raw); err != nil {
		return err
	}
	for _, key := range []string{"command", "delay_secs", "id", "path_type", "repeat"} {
		if _, ok := raw[key]; !ok {
			return fmt.Errorf("CliCommandTasksCreateRequest: missing required field %q", key)
		}
	}
	type Alias CliCommandTasksCreateRequest
	var alias Alias
	if err := json.Unmarshal(data, &alias); err != nil {
		return err
	}
	*v = CliCommandTasksCreateRequest(alias)
	return nil
}
