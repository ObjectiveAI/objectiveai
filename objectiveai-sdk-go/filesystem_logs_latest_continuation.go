// THIS FILE IS AUTO-GENERATED. DO NOT EDIT.
// Generated by scripts/install_go.go from objectiveai-json-schema/.
package objectiveai

import (
	"encoding/json"
	"fmt"
)

// Everything needed to POST a continuation that re-enters an agent's
// most recent completed conversation.
//
// Field-set mirrors the inline-on-disk shape of
// [`crate::agent::completions::request::AgentCompletionCreateParamsLog`]:
// `agent`, `provider`, `response_format`, `seed` stay inline in the
// request log; the caller composes them with their own new `messages`
// + the `continuation` string.
type FilesystemLogsLatestContinuation struct {
	// The agent definition from the original request log — reused
	// verbatim so the conversation continues against the same agent.
	Agent AgentInlineAgentBaseWithFallbacksOrRemoteCommitOptional `json:"agent"`
	// Continuation token to set as `continuation: Some(_)` on the
	// fresh `AgentCompletionCreateParams`. This is the **response**
	// continuation — the value the server emitted in the final
	// streaming chunk, persisted by
	// [`crate::agent::completions::response::streaming::AgentCompletionChunk::produce_files`]
	// as a JSON-quoted string at
	// `logs/agents/completions/response/continuation/<response_id>.json`.
	// Resuming a conversation requires this value, **not** the
	// previous turn's request-side continuation (which would replay
	// from the earlier turn's end).
	Continuation string `json:"continuation"`
	// Original provider routing preferences, if any.
	Provider *AgentCompletionsRequestProvider `json:"provider"`
	// Original response format constraint, if any.
	ResponseFormat *AgentCompletionsRequestResponseFormatParam `json:"response_format"`
	// The response_id of the original (about-to-be-continued)
	// agent-completion turn. **Not necessarily the newest** request
	// for the agent — `read_latest_continuation` walks back from
	// newest to oldest and returns the first one whose continuation
	// file exists.
	ResponseID string `json:"response_id"`
	// Original seed, if any. The caller decides whether to override
	// with their own (e.g. cli `--seed`).
	Seed *int64 `json:"seed" validate:"omitempty,min=-9223372036854775808,max=9223372036854775807"`
}

func (FilesystemLogsLatestContinuation) SchemaTitle() string { return "filesystem.logs.LatestContinuation" }
func (v FilesystemLogsLatestContinuation) Validate() error {
	return variantValidator.Struct(v)
}

func (v *FilesystemLogsLatestContinuation) UnmarshalJSON(data []byte) error {
	var raw map[string]json.RawMessage
	if err := json.Unmarshal(data, &raw); err != nil {
		return err
	}
	for _, key := range []string{"agent", "continuation", "response_id"} {
		if _, ok := raw[key]; !ok {
			return fmt.Errorf("FilesystemLogsLatestContinuation: missing required field %q", key)
		}
	}
	type Alias FilesystemLogsLatestContinuation
	var alias Alias
	if err := json.Unmarshal(data, &alias); err != nil {
		return err
	}
	*v = FilesystemLogsLatestContinuation(alias)
	return nil
}
