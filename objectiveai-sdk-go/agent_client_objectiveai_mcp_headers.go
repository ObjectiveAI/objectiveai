// THIS FILE IS AUTO-GENERATED. DO NOT EDIT.
// Generated by scripts/install_go.go from objectiveai-json-schema/.
package objectiveai

import (
	"encoding/json"
	"fmt"
)

// Snapshot of the three transient `X-OBJECTIVEAI-MCP-*` request
// headers an HTTP caller stamps on its initial dial of the
// objectiveai-mcp proxy upstream.
//
// Produced by [`ClientObjectiveaiMcp::mcp_headers`] — see that
// method's doc for the per-field source rules and sort order.
type AgentClientObjectiveaiMcpHeaders struct {
	// Plugins the agent expects the client to expose, filtered to
	// `executable: true` entries only, projected onto the
	// `(owner, name, version)` shape, and sorted by
	// `(owner, name, version)`. Becomes the
	// `X-OBJECTIVEAI-MCP-PLUGINS` header's JSON payload.
	Plugins []AgentClientObjectiveaiMcpEntry `json:"plugins"`
	// Becomes the `X-OBJECTIVEAI-MCP-ROOT` header value verbatim
	// (`"true"` / `"false"`).
	Root bool `json:"root"`
	// Tools the agent expects the client to expose, sorted by
	// `(owner, name, version)`. Becomes the
	// `X-OBJECTIVEAI-MCP-TOOLS` header's JSON payload.
	Tools []AgentClientObjectiveaiMcpEntry `json:"tools"`
}

func (AgentClientObjectiveaiMcpHeaders) SchemaTitle() string { return "agent.ClientObjectiveaiMcpHeaders" }
func (v AgentClientObjectiveaiMcpHeaders) Validate() error {
	return variantValidator.Struct(v)
}

func (v *AgentClientObjectiveaiMcpHeaders) UnmarshalJSON(data []byte) error {
	var raw map[string]json.RawMessage
	if err := json.Unmarshal(data, &raw); err != nil {
		return err
	}
	for _, key := range []string{"plugins", "root", "tools"} {
		if _, ok := raw[key]; !ok {
			return fmt.Errorf("AgentClientObjectiveaiMcpHeaders: missing required field %q", key)
		}
	}
	type Alias AgentClientObjectiveaiMcpHeaders
	var alias Alias
	if err := json.Unmarshal(data, &alias); err != nil {
		return err
	}
	*v = AgentClientObjectiveaiMcpHeaders(alias)
	return nil
}
