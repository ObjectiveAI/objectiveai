// THIS FILE IS AUTO-GENERATED. DO NOT EDIT.
// Generated by scripts/install_go.go from objectiveai-json-schema/.
package objectiveai

import (
	"encoding/json"
	"fmt"
)

type CliCommandAgentsTasksScheduleRequest struct {
	// argv to invoke on each scheduled poll.
	Command []string `json:"command"`
	// Human-readable label. Required — surfaces on every
	// `agents tasks list` row, and the runner uses it in
	// observability output.
	Description string `json:"description"`
	// Floor on wall-clock seconds between invocations. `None`
	// marks a **oneshot** schedule — the runner fires it once on
	// the next poll and deletes the row. `Some(n)` is a recurring
	// schedule with `n` seconds as the minimum gap between
	// invocations.
	IntervalSeconds *uint64 `json:"interval_seconds,omitempty" validate:"omitempty,min=0,max=18446744073709551615"`
	Jq *string `json:"jq"`
	// User-facing identifier. Globally unique — a second
	// `schedule` with the same name fails the
	// `schedules.name` UNIQUE constraint. `agents tasks run`
	// tags every streamed output line with this name so the
	// caller can attribute output to its source schedule.
	Name string `json:"name"`
	PathType CliCommandAgentsTasksSchedulePath `json:"path_type"`
}

func (CliCommandAgentsTasksScheduleRequest) SchemaTitle() string { return "cli.command.agents.tasks.schedule.Request" }
func (v CliCommandAgentsTasksScheduleRequest) Validate() error {
	return variantValidator.Struct(v)
}

func (v *CliCommandAgentsTasksScheduleRequest) UnmarshalJSON(data []byte) error {
	var raw map[string]json.RawMessage
	if err := json.Unmarshal(data, &raw); err != nil {
		return err
	}
	for _, key := range []string{"command", "description", "name", "path_type"} {
		if _, ok := raw[key]; !ok {
			return fmt.Errorf("CliCommandAgentsTasksScheduleRequest: missing required field %q", key)
		}
	}
	type Alias CliCommandAgentsTasksScheduleRequest
	var alias Alias
	if err := json.Unmarshal(data, &alias); err != nil {
		return err
	}
	*v = CliCommandAgentsTasksScheduleRequest(alias)
	return nil
}
