// THIS FILE IS AUTO-GENERATED. DO NOT EDIT.
// Generated by scripts/install_go.go from objectiveai-json-schema/.
package objectiveai

import (
	"encoding/json"
	"fmt"
)

// One agent's record: identity, spawn / last-active timestamps, tag
// bindings, counters, and whether its per-instance lock is currently
// held. Mirrors `agents instances list`'s `ResponseItem` plus the
// live `active` flag. Carried by [`AgentInstanceEvent::Agent`] — the
// `/agents/instances/list` stream itself is a flat list of AIH
// strings and carries no records.
type CliAgentsInstancesListenerAgentRecord struct {
	// Whether the agent's per-instance lock is currently held — i.e. a
	// live process owns this agent right now.
	Active bool `json:"active"`
	// The agent's ACTIVE laboratories — the laboratory ids actually
	// sent with the MOST RECENT spawn request (what the latest pass
	// dialed), in resolve order. Most-recent-value semantics: the set
	// survives deactivation and is replaced on the next pass. Fully
	// separate from `attached_laboratories`.
	ActiveLaboratories []string `json:"active_laboratories" default:"[]"`
	// Full hierarchy of this agent instance.
	AgentInstanceHierarchy string `json:"agent_instance_hierarchy"`
	// The agent's ATTACHED laboratories — the effective set the next
	// spawn pass dials (the AIH's own attachments UNION its bound
	// tags'), oldest-attached first. Live-tracked: attach/detach may
	// happen at any time, active agents included.
	AttachedLaboratories []CliAgentsInstancesListenerAttachedLaboratory `json:"attached_laboratories" default:"[]"`
	// RFC3339 timestamp the agent was last active. Meaningful only when
	// `active` is `false` — a live agent's last-active is implicitly
	// "now", so it is left `None` while active and stamped at the moment
	// the lock releases.
	LastActiveAt *string `json:"last_active_at,omitempty"`
	// Total `objectiveai.messages` rows for this agent over all time.
	Logged uint64 `json:"logged" validate:"min=0,max=18446744073709551615"`
	// Active `message_queue` rows targeting this agent.
	Queued uint64 `json:"queued" validate:"min=0,max=18446744073709551615"`
	// RFC3339 timestamp of the first `objectiveai.messages` row for this
	// agent (spawn time). `None` for an agent with no logs yet.
	SpawnedAt *string `json:"spawned_at,omitempty"`
	// Tag names currently bound to this AIH, newest-bound first.
	Tags []string `json:"tags"`
}

func (CliAgentsInstancesListenerAgentRecord) SchemaTitle() string { return "cli.agents_instances_listener.AgentRecord" }
func (v CliAgentsInstancesListenerAgentRecord) Validate() error {
	return variantValidator.Struct(v)
}

func (v *CliAgentsInstancesListenerAgentRecord) UnmarshalJSON(data []byte) error {
	var raw map[string]json.RawMessage
	if err := json.Unmarshal(data, &raw); err != nil {
		return err
	}
	for _, key := range []string{"active", "active_laboratories", "agent_instance_hierarchy", "attached_laboratories", "logged", "queued", "tags"} {
		if _, ok := raw[key]; !ok {
			return fmt.Errorf("CliAgentsInstancesListenerAgentRecord: missing required field %q", key)
		}
	}
	type Alias CliAgentsInstancesListenerAgentRecord
	var alias Alias
	if err := json.Unmarshal(data, &alias); err != nil {
		return err
	}
	*v = CliAgentsInstancesListenerAgentRecord(alias)
	return nil
}
