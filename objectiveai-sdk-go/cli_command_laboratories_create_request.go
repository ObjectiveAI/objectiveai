// THIS FILE IS AUTO-GENERATED. DO NOT EDIT.
// Generated by scripts/install_go.go from objectiveai-json-schema/.
package objectiveai

import (
	"encoding/json"
	"fmt"
)

type CliCommandLaboratoriesCreateRequest struct {
	// Default working directory new agents start in; defaults to `/`.
	Cwd string `json:"cwd" default:"/"`
	Env []CliCommandLaboratoriesCreateEnvVar `json:"env"`
	ID string `json:"id"`
	// The base image, split (`registry` + `name` + tag XOR digest) —
	// a joined reference string is never accepted, so unqualified
	// short names are unrepresentable.
	Image LaboratoriesLaboratoryImage `json:"image"`
	// jq filter applied to the JSON output. Ignored when `python`
	// is also set — python overrides jq.
	Jq *string `json:"jq"`
	Kind CliCommandLaboratoriesCreateKind `json:"kind"`
	// The EXACT machine id (as `laboratories list` reports it) whose
	// laboratory host should own the container. Provided together
	// with `machine_state` or not at all; neither ⇒ the current
	// machine + the daemon's own state.
	Machine *string `json:"machine,omitempty"`
	// The state (on `machine`) whose laboratory host should own the
	// container. Paired with `machine` — both or neither.
	MachineState *string `json:"machine_state,omitempty"`
	// Response token budget, `>= 1` (`0` is rejected at parse
	// time — omit entirely for unlimited). Forward-compatible
	// envelope data — no leaf enforces it yet.
	MaxTokens *uint64 `json:"max_tokens,omitempty" validate:"omitempty,min=0,max=18446744073709551615"`
	Mounts []CliCommandLaboratoriesCreateMount `json:"mounts"`
	PathType CliCommandLaboratoriesCreatePath `json:"path_type"`
	// Python transform applied to the JSON output. Overrides `jq`
	// when both are provided.
	Python *string `json:"python"`
	// Wall-clock execution cap, in whole seconds. Parsed from
	// `--timeout` (humantime: `30s`, `5m`, `1h30m`), `> 0`
	// enforced at parse time. `db query` threads it to postgres
	// when set; omit for uncapped.
	TimeoutSeconds *uint64 `json:"timeout_seconds,omitempty" validate:"omitempty,min=0,max=18446744073709551615"`
}

func (CliCommandLaboratoriesCreateRequest) SchemaTitle() string { return "cli.command.laboratories.create.Request" }
func (v CliCommandLaboratoriesCreateRequest) Validate() error {
	return variantValidator.Struct(v)
}

func (v *CliCommandLaboratoriesCreateRequest) UnmarshalJSON(data []byte) error {
	var raw map[string]json.RawMessage
	if err := json.Unmarshal(data, &raw); err != nil {
		return err
	}
	for _, key := range []string{"cwd", "env", "id", "image", "kind", "mounts", "path_type"} {
		if _, ok := raw[key]; !ok {
			return fmt.Errorf("CliCommandLaboratoriesCreateRequest: missing required field %q", key)
		}
	}
	type Alias CliCommandLaboratoriesCreateRequest
	var alias Alias
	if err := json.Unmarshal(data, &alias); err != nil {
		return err
	}
	*v = CliCommandLaboratoriesCreateRequest(alias)
	return nil
}
