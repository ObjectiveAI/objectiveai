// THIS FILE IS AUTO-GENERATED. DO NOT EDIT.
// Generated by scripts/install_go.go from objectiveai-json-schema/.
package objectiveai

import (
	"encoding/json"
	"fmt"
)

// One conversation row, content inlined. The logs-list part shape
// (`{id, delivered_at, type}`) with the DB-born `id` replaced by the
// writer-assigned identity `(table, response_id, row_index,
// row_sub_index)` and the actual content.
type CliWebsocketAgentsInstancesListenerConversationRow struct {
	AgentInstanceHierarchy string `json:"agent_instance_hierarchy"`
	// This agent's voting key for the choice — on the
	// `request_vector_choice` HEAD (live) and on choice content rows
	// in snapshots (joined).
	ChoiceKey *string `json:"choice_key,omitempty"`
	Content CliWebsocketAgentsInstancesListenerRowContent `json:"content"`
	// RFC3339. Live rows: when the writer shipped this value.
	// Snapshot rows: the row's logged timestamp.
	DeliveredAt string `json:"delivered_at"`
	// `message_queue.id` of the consumed parent queue row —
	// notification rows only (the ClientNotification block boundary).
	MessageQueueID *int64 `json:"message_queue_id,omitempty" validate:"omitempty,min=-9223372036854775808,max=9223372036854775807"`
	// Idempotency key of the consumed queue row, if one was set —
	// notification rows only.
	MessageQueueKey *string `json:"message_queue_key,omitempty"`
	// RFC3339 `message_queue.enqueued_at` — notification rows only.
	QueuedAt *string `json:"queued_at,omitempty"`
	// The enclosing agent completion's id — same semantics as the
	// logs-list blocks' `response_id`.
	ResponseID string `json:"response_id"`
	// `objectiveai.messages.row_index` semantics: the message's index
	// within `messages[]`, the choice index for vector-choice rows,
	// the `message_queue_contents.id` for notification rows, and `0`
	// for the (index-less) vote row.
	RowIndex int64 `json:"row_index" validate:"min=-9223372036854775808,max=9223372036854775807"`
	// `part_index` for content-part rows, `tool_call_index` for
	// tool-call rows; `None` where the shape has no sub-index.
	RowSubIndex *int64 `json:"row_sub_index,omitempty" validate:"omitempty,min=-9223372036854775808,max=9223372036854775807"`
	// Enqueuer AIH — notification (`message_queue_*`) rows only.
	SenderAgentInstanceHierarchy *string `json:"sender_agent_instance_hierarchy,omitempty"`
	Table CliWebsocketAgentsInstancesListenerRowTableKind `json:"table"`
	// The tool call this row answers — on the two tool HEAD kinds
	// (live) and on tool-response / request-tool content rows in
	// snapshots (joined). Block assembly accepts either source.
	ToolCallID *string `json:"tool_call_id,omitempty"`
}

func (CliWebsocketAgentsInstancesListenerConversationRow) SchemaTitle() string { return "cli.websocket_agents_instances_listener.ConversationRow" }
func (v CliWebsocketAgentsInstancesListenerConversationRow) Validate() error {
	return variantValidator.Struct(v)
}

func (v *CliWebsocketAgentsInstancesListenerConversationRow) UnmarshalJSON(data []byte) error {
	var raw map[string]json.RawMessage
	if err := json.Unmarshal(data, &raw); err != nil {
		return err
	}
	for _, key := range []string{"agent_instance_hierarchy", "content", "delivered_at", "response_id", "row_index", "table"} {
		if _, ok := raw[key]; !ok {
			return fmt.Errorf("CliWebsocketAgentsInstancesListenerConversationRow: missing required field %q", key)
		}
	}
	type Alias CliWebsocketAgentsInstancesListenerConversationRow
	var alias Alias
	if err := json.Unmarshal(data, &alias); err != nil {
		return err
	}
	*v = CliWebsocketAgentsInstancesListenerConversationRow(alias)
	return nil
}
