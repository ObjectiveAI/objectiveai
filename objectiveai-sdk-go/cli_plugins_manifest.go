// THIS FILE IS AUTO-GENERATED. DO NOT EDIT.
// Generated by scripts/install_go.go from objectiveai-json-schema/.
package objectiveai

import (
	"encoding/json"
	"fmt"
)

// A plugin's `objectiveai.json`, whole — ONE schema for the ONE
// file. The container half (`containerfile`, `port`) is what the
// laboratory host builds and runs; the viewer half (`viewer`,
// `icon`, `tabs`) is what the viewer surfaces. `icon` and tab
// `module` paths are relative to the declared `viewer` root
// (authored as if the CWD were inside it).
type CliPluginsManifest struct {
	// Repo-relative path (forward slashes) to the Containerfile /
	// Dockerfile the plugin's image builds from — the repo checkout
	// is the build context.
	Containerfile string `json:"containerfile"`
	Description *string `json:"description,omitempty"`
	// The identity icon, shown beside the identity in the tab strip.
	Icon *string `json:"icon,omitempty"`
	// The port the plugin's MCP server listens on inside the
	// container — published to a random loopback host port at
	// create. Never 0.
	Port uint32 `json:"port" validate:"min=0,max=65535"`
	// The plugin's tabs, in declaration order (strip order). A
	// [`ViewerTab::Channel`] entry (`channel_key`) is a CHANNEL
	// HANDLER: it never opens at boot — it opens when an offer with
	// that key is accepted, with the full offer as its arguments,
	// titled by the offer key. A [`ViewerTab::Tab`] entry (`title`)
	// is a regular tab, opened at viewer boot. Duplicates (same
	// `module` among regular tabs, same `channel_key` among
	// handlers) — entries after the first are ignored.
	Tabs *[]CliPluginsViewerTab `json:"tabs,omitempty"`
	// Repo-relative path (forward slashes) to the viewer extension's
	// JS ROOT — the folder holding its `package.json`; `icon` and
	// tab `module` paths resolve against it. ABSENT = the plugin
	// has no viewer extension (`icon`/`tabs` are ignored).
	Viewer *string `json:"viewer,omitempty"`
}

func (CliPluginsManifest) SchemaTitle() string { return "cli.plugins.Manifest" }
func (v CliPluginsManifest) Validate() error {
	return variantValidator.Struct(v)
}

func (v *CliPluginsManifest) UnmarshalJSON(data []byte) error {
	var raw map[string]json.RawMessage
	if err := json.Unmarshal(data, &raw); err != nil {
		return err
	}
	for _, key := range []string{"containerfile", "port"} {
		if _, ok := raw[key]; !ok {
			return fmt.Errorf("CliPluginsManifest: missing required field %q", key)
		}
	}
	type Alias CliPluginsManifest
	var alias Alias
	if err := json.Unmarshal(data, &alias); err != nil {
		return err
	}
	*v = CliPluginsManifest(alias)
	return nil
}
