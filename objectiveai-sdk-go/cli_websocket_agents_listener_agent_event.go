// THIS FILE IS AUTO-GENERATED. DO NOT EDIT.
// Generated by scripts/install_go.go from objectiveai-json-schema/.
package objectiveai

import (
	"encoding/json"
	"fmt"
)

// The full set of agents, sent once immediately on connect.
type CliWebsocketAgentsListenerAgentEventSnapshot struct {
	Agents []CliWebsocketAgentsListenerAgentRecord `json:"agents"`
	Type string `json:"type" validate:"oneof=snapshot"`
}

func (v *CliWebsocketAgentsListenerAgentEventSnapshot) UnmarshalJSON(data []byte) error {
	var raw map[string]json.RawMessage
	if err := json.Unmarshal(data, &raw); err != nil {
		return err
	}
	for _, key := range []string{"agents", "type"} {
		if _, ok := raw[key]; !ok {
			return fmt.Errorf("CliWebsocketAgentsListenerAgentEventSnapshot: missing required field %q", key)
		}
	}
	type Alias CliWebsocketAgentsListenerAgentEventSnapshot
	var alias Alias
	if err := json.Unmarshal(data, &alias); err != nil {
		return err
	}
	*v = CliWebsocketAgentsListenerAgentEventSnapshot(alias)
	return nil
}
func (CliWebsocketAgentsListenerAgentEventSnapshot) SchemaVariantTitle() string { return "Snapshot" }

// An agent acquired its per-instance lock (became active).
type CliWebsocketAgentsListenerAgentEventActivated struct {
	Agent CliWebsocketAgentsListenerAgentRecord `json:"agent"`
	Type string `json:"type" validate:"oneof=activated"`
}

func (v *CliWebsocketAgentsListenerAgentEventActivated) UnmarshalJSON(data []byte) error {
	var raw map[string]json.RawMessage
	if err := json.Unmarshal(data, &raw); err != nil {
		return err
	}
	for _, key := range []string{"agent", "type"} {
		if _, ok := raw[key]; !ok {
			return fmt.Errorf("CliWebsocketAgentsListenerAgentEventActivated: missing required field %q", key)
		}
	}
	type Alias CliWebsocketAgentsListenerAgentEventActivated
	var alias Alias
	if err := json.Unmarshal(data, &alias); err != nil {
		return err
	}
	*v = CliWebsocketAgentsListenerAgentEventActivated(alias)
	return nil
}
func (CliWebsocketAgentsListenerAgentEventActivated) SchemaVariantTitle() string { return "Activated" }

// An agent's record changed while it remained present — currently
// emitted when its bound tags change (a tag applied, moved, or
// removed). Carries the full refreshed record; consumers replace by
// `agent_instance_hierarchy`.
type CliWebsocketAgentsListenerAgentEventUpdated struct {
	Agent CliWebsocketAgentsListenerAgentRecord `json:"agent"`
	Type string `json:"type" validate:"oneof=updated"`
}

func (v *CliWebsocketAgentsListenerAgentEventUpdated) UnmarshalJSON(data []byte) error {
	var raw map[string]json.RawMessage
	if err := json.Unmarshal(data, &raw); err != nil {
		return err
	}
	for _, key := range []string{"agent", "type"} {
		if _, ok := raw[key]; !ok {
			return fmt.Errorf("CliWebsocketAgentsListenerAgentEventUpdated: missing required field %q", key)
		}
	}
	type Alias CliWebsocketAgentsListenerAgentEventUpdated
	var alias Alias
	if err := json.Unmarshal(data, &alias); err != nil {
		return err
	}
	*v = CliWebsocketAgentsListenerAgentEventUpdated(alias)
	return nil
}
func (CliWebsocketAgentsListenerAgentEventUpdated) SchemaVariantTitle() string { return "Updated" }

// An agent released its per-instance lock (became inactive) — on
// normal stream end OR holder death. `last_active_at` is the release
// moment.
type CliWebsocketAgentsListenerAgentEventDeactivated struct {
	AgentInstanceHierarchy string `json:"agent_instance_hierarchy"`
	LastActiveAt *string `json:"last_active_at,omitempty"`
	Type string `json:"type" validate:"oneof=deactivated"`
}

func (v *CliWebsocketAgentsListenerAgentEventDeactivated) UnmarshalJSON(data []byte) error {
	var raw map[string]json.RawMessage
	if err := json.Unmarshal(data, &raw); err != nil {
		return err
	}
	for _, key := range []string{"agent_instance_hierarchy", "type"} {
		if _, ok := raw[key]; !ok {
			return fmt.Errorf("CliWebsocketAgentsListenerAgentEventDeactivated: missing required field %q", key)
		}
	}
	type Alias CliWebsocketAgentsListenerAgentEventDeactivated
	var alias Alias
	if err := json.Unmarshal(data, &alias); err != nil {
		return err
	}
	*v = CliWebsocketAgentsListenerAgentEventDeactivated(alias)
	return nil
}
func (CliWebsocketAgentsListenerAgentEventDeactivated) SchemaVariantTitle() string { return "Deactivated" }

// One event on the `/agents` stream. The first is always a
// [`Snapshot`](AgentEvent::Snapshot); every later one is an
// [`Activated`](AgentEvent::Activated) or
// [`Deactivated`](AgentEvent::Deactivated) delta. Consumers key by
// `agent_instance_hierarchy` (the snapshot and a delta may overlap by
// one item at connect time).
type CliWebsocketAgentsListenerAgentEvent struct {
	// The full set of agents, sent once immediately on connect.
	Snapshot *CliWebsocketAgentsListenerAgentEventSnapshot `outerObject:"true"`
	// An agent acquired its per-instance lock (became active).
	Activated *CliWebsocketAgentsListenerAgentEventActivated `outerObject:"true"`
	// An agent's record changed while it remained present — currently
	// emitted when its bound tags change (a tag applied, moved, or
	// removed). Carries the full refreshed record; consumers replace by
	// `agent_instance_hierarchy`.
	Updated *CliWebsocketAgentsListenerAgentEventUpdated `outerObject:"true"`
	// An agent released its per-instance lock (became inactive) — on
	// normal stream end OR holder death. `last_active_at` is the release
	// moment.
	Deactivated *CliWebsocketAgentsListenerAgentEventDeactivated `outerObject:"true"`
}

func (v CliWebsocketAgentsListenerAgentEvent) MarshalJSON() ([]byte, error) {
	if v.Snapshot != nil {
		return json.Marshal(v.Snapshot)
	}
	if v.Activated != nil {
		return json.Marshal(v.Activated)
	}
	if v.Updated != nil {
		return json.Marshal(v.Updated)
	}
	if v.Deactivated != nil {
		return json.Marshal(v.Deactivated)
	}
	return []byte("null"), nil
}

func (v *CliWebsocketAgentsListenerAgentEvent) UnmarshalJSON(data []byte) error {
	{
		var try CliWebsocketAgentsListenerAgentEventSnapshot
		if err := json.Unmarshal(data, &try); err == nil {
			candidate := CliWebsocketAgentsListenerAgentEvent{}
			candidate.Snapshot = &try
			if candidate.Validate() == nil {
				*v = candidate
				return nil
			}
		}
	}
	{
		var try CliWebsocketAgentsListenerAgentEventActivated
		if err := json.Unmarshal(data, &try); err == nil {
			candidate := CliWebsocketAgentsListenerAgentEvent{}
			candidate.Activated = &try
			if candidate.Validate() == nil {
				*v = candidate
				return nil
			}
		}
	}
	{
		var try CliWebsocketAgentsListenerAgentEventUpdated
		if err := json.Unmarshal(data, &try); err == nil {
			candidate := CliWebsocketAgentsListenerAgentEvent{}
			candidate.Updated = &try
			if candidate.Validate() == nil {
				*v = candidate
				return nil
			}
		}
	}
	{
		var try CliWebsocketAgentsListenerAgentEventDeactivated
		if err := json.Unmarshal(data, &try); err == nil {
			candidate := CliWebsocketAgentsListenerAgentEvent{}
			candidate.Deactivated = &try
			if candidate.Validate() == nil {
				*v = candidate
				return nil
			}
		}
	}
	return fmt.Errorf("data did not match any variant of CliWebsocketAgentsListenerAgentEvent")
}

func (v CliWebsocketAgentsListenerAgentEvent) Validate() error {
	count := 0
	if v.Snapshot != nil { count++ }
	if v.Activated != nil { count++ }
	if v.Updated != nil { count++ }
	if v.Deactivated != nil { count++ }
	if count != 1 {
		return fmt.Errorf("CliWebsocketAgentsListenerAgentEvent: exactly one variant must be set, got %d", count)
	}
	return variantValidator.Struct(v)
}
func (CliWebsocketAgentsListenerAgentEvent) SchemaTitle() string { return "cli.websocket_agents_listener.AgentEvent" }

