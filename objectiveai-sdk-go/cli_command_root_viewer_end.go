// THIS FILE IS AUTO-GENERATED. DO NOT EDIT.
// Generated by scripts/install_go.go from objectiveai-json-schema/.
package objectiveai

import (
	"encoding/json"
	"fmt"
)

// Viewer-stream terminator — the daemon broadcasts exactly one per
// stream `id` when the producer's feed closes, marking that run's
// frame stream complete. Named to match [`RootViewerRequest`].
//
// Consumer discrimination across the three broadcast frame shapes: a
// request frame has no top-level `path_type`; a response frame has
// `path_type` + `value`; the terminator has `path_type` and
// `end: true` (and no `value`).
type CliCommandRootViewerEnd struct {
	// Always `true` — the terminator marker.
	End bool `json:"end"`
	// The broadcast stream id this terminator closes.
	ID string `json:"id"`
	// The originating request's `path_type`, mirroring the id's
	// response frames.
	PathType string `json:"path_type"`
}

func (CliCommandRootViewerEnd) SchemaTitle() string { return "cli.command.RootViewerEnd" }
func (v CliCommandRootViewerEnd) Validate() error {
	return variantValidator.Struct(v)
}

func (v *CliCommandRootViewerEnd) UnmarshalJSON(data []byte) error {
	var raw map[string]json.RawMessage
	if err := json.Unmarshal(data, &raw); err != nil {
		return err
	}
	for _, key := range []string{"end", "id", "path_type"} {
		if _, ok := raw[key]; !ok {
			return fmt.Errorf("CliCommandRootViewerEnd: missing required field %q", key)
		}
	}
	type Alias CliCommandRootViewerEnd
	var alias Alias
	if err := json.Unmarshal(data, &alias); err != nil {
		return err
	}
	*v = CliCommandRootViewerEnd(alias)
	return nil
}
