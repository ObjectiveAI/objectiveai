{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window and detached tab shell windows",
  "windows": ["main", "shell-*"],
  "permissions": [
    "core:default"
  ]
}
