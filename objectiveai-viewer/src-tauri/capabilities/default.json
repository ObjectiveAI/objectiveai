{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for every shell window's webviews: the chrome (tab strip + status bar) and the per-tab content webviews. A `windows` glob authorizes ALL webviews of a matching window; `webviews` lists the labels explicitly as documentation/hardening. All windows are shell-N — none is special.",
  "windows": ["shell-*"],
  "webviews": ["chrome-*", "tab-*"],
  "permissions": [
    "core:default"
  ]
}
