{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window and agent windows",
  "windows": ["main", "agent-*"],
  "permissions": [
    "core:default"
  ]
}
