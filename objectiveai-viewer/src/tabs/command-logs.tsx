import { CommandLogsPane } from "../components/CommandLogsPane";

/** The command-logs home tab — the /listen broadcast's request list. */
export default function CommandLogsTab() {
  return <CommandLogsPane />;
}
