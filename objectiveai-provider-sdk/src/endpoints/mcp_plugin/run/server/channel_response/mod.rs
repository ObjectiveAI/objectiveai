//! The answers a provider sends on the channels a caller opened.
//!
//! [`mcp`] is what the plugin's MCP server said, and it is the only
//! one — a caller has exactly one thing to ask a running plugin.

pub mod mcp;
