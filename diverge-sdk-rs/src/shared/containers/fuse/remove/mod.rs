//! An entry of a mounted directory, removed: the mount and the path
//! of a file or an empty directory, answered with one message — ok,
//! or error — then the close.

pub mod request;
pub mod response;
