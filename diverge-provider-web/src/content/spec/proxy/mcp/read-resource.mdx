---
title: '/mcp/read-resource/{channel}'
summary: 'Kind 3: One resource of the caller’s servers, read. The ask is the params as JSON; the answer is one message, a result or an error, then the close.'
order: 4
draft: false
---

The ask is kind `3` on [`/requests`](/proxy/requests/); its
payload is the JSON of the params below, running to the end of the
message. The server answers on `/mcp/read-resource/{channel}` with exactly
one message — the response frame below, its first byte `0` for a
result or `1` for an error, the rest that variant's JSON — and then
the close. A path closed cleanly with no message is the exchange
not served. An abrupt end, or a `/requests` connection that ended
before the path was opened, is an exchange the container asks again
— see [MCP Exchanges](/proxy/mcp/).

The params are `diverge-provider-sdk/src/shared/mcp/read_resource/request/request.rs`:

```rust include=diverge-provider-sdk/src/shared/mcp/read_resource/request/request.rs
```

The answer is `diverge-provider-sdk/src/shared/mcp/read_resource/response/frame.rs`:

```rust include=diverge-provider-sdk/src/shared/mcp/read_resource/response/frame.rs
```
