---
title: 'OpenRouter'
summary: 'Deployment of the "openrouter" agent kind: its image, 512 MiB of memory, and 256 MiB of disk.'
order: 1
draft: false
---

The container deployed for an agent whose `upstream` is
`"openrouter"` — the kind defined in
[OpenRouter](/endpoints/agentic-loop-run/request/openrouter/).


| Property | Value |
|----------|-------|
| Image | `TODO` |
| Memory | 536,870,912 bytes (512 MiB) |
| Disk | 268,435,456 bytes (256 MiB) |

The memory ceiling is the container's whole allowance, interpreter
and runtime included. The disk ceiling is what the container may
write over its image; the image's own layers are not counted.
