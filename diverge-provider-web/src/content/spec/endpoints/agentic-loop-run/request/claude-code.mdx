---
title: 'ClaudeCode'
summary: 'Agent kind "claude_code": run Claude Code — model, thinking, and effort.'
order: 2
draft: false
---

The agent kind whose `upstream` member is `"claude_code"`, read under the layer's [notation](/endpoints/#notation).

```rust
pub struct Agent {
    /// The discriminator. Always "claude_code".
    pub upstream: Upstream,
    /// The model to run.
    pub model: String,
    /// Whether to think before answering.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub thinking: Option<bool>,
    /// How much effort to spend thinking. Meaningful only with
    /// thinking on.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub effort: Option<Effort>,
}

#[serde(rename_all = "snake_case")]
pub enum Upstream {
    ClaudeCode,
}

#[serde(rename_all = "snake_case")]
pub enum Effort {
    Low,
    Medium,
    High,
    Xhigh,
    Max,
}
```
