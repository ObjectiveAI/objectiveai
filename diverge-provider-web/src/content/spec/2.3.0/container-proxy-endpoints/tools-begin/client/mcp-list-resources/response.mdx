---
title: 'Response'
summary: 'The sequence of channel responses that answers the channel, what ends it, and what a finish with nothing before it means.'
order: 2
draft: false
---

```text
[0][result JSON …]    the result of the method
[1][error JSON …]     the MCP error
```

- **The sequence.** Exactly one channel response, the result or the
  error, followed by the channel response finish. A channel response
  finish that no channel response precedes states that the server did
  not serve the exchange.

The channel response is defined by `diverge-provider-sdk-rs/src/container_proxy_endpoints/tools/begin/server/channel_response/mcp_list_resources/frame.rs`:

```rust include=diverge-provider-sdk-rs/src/container_proxy_endpoints/tools/begin/server/channel_response/mcp_list_resources/frame.rs
```

and by `diverge-provider-sdk-rs/src/shared/mcp/list_resources/response/frame.rs`:

```rust include=diverge-provider-sdk-rs/src/shared/mcp/list_resources/response/frame.rs
```
