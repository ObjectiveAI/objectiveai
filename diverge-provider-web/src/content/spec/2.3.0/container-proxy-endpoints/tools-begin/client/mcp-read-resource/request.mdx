---
title: 'Request'
summary: 'The payload of the channel request: the tag byte 5 and the request of the channel.'
order: 1
draft: false
---

```text
[5][params JSON …]
```

- **The params.** The params of the MCP method `resources/read`, as JSON.
- **Malformed.** A channel request whose payload does not decode as
  stated is answered by a channel response finish that no channel
  response precedes, and the server does nothing else for it.

The channel request is defined by `diverge-sdk-rs/src/container_proxy/outside/tools/begin/client/channel_request/frame.rs`:

```rust include=diverge-sdk-rs/src/container_proxy/outside/tools/begin/client/channel_request/frame.rs
```

and by `diverge-sdk-rs/src/shared/mcp/read_resource/request/request.rs`:

```rust include=diverge-sdk-rs/src/shared/mcp/read_resource/request/request.rs
```
