---
title: 'Request'
summary: 'The payload of the channel request: the tag byte 1 and the request of the channel.'
order: 1
draft: false
---

```text
[1][command …]
```

- **The command.** Bytes, verbatim, running to the end of the
  payload.

The channel request is defined by `diverge-sdk-rs/src/container_proxy/outside/tools/begin/server/channel_request/frame.rs`:

```rust include=diverge-sdk-rs/src/container_proxy/outside/tools/begin/server/channel_request/frame.rs
```

and by `diverge-sdk-rs/src/shared/containers/command/request/request.rs`:

```rust include=diverge-sdk-rs/src/shared/containers/command/request/request.rs
```
