---
title: 'Response'
summary: 'The sequence of channel responses that answers the channel, what ends it, and what a finish with nothing before it means.'
order: 2
draft: false
---

```text
[0]             ok
[1][message …]  an error, UTF-8
```

- **The sequence.** Exactly one channel response, ok or an error, followed
  by the channel response finish.
- **A finish with nothing.** A channel response finish that no
  channel response precedes states that the client did not serve the
  ask. The server does not ask again.

The channel response is defined by `diverge-sdk-rs/src/container_proxy/outside/tools/begin/client/channel_response/vault_delete/frame.rs`:

```rust include=diverge-sdk-rs/src/container_proxy/outside/tools/begin/client/channel_response/vault_delete/frame.rs
```

and by `diverge-sdk-rs/src/shared/containers/vault/response/frame.rs`:

```rust include=diverge-sdk-rs/src/shared/containers/vault/response/frame.rs
```
