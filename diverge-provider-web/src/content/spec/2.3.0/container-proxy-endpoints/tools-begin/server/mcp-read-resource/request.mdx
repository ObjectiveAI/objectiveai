---
title: 'Request'
summary: 'The payload of the channel request: the tag byte 10 and the request of the channel.'
order: 1
draft: false
---

```text
[10][params JSON …]
```

- **The params.** The params of the MCP method `resources/read`, as JSON,
  verbatim.

The channel request is defined by `diverge-provider-sdk-rs/src/container_proxy_endpoints/tools/begin/server/channel_request/frame.rs`:

```rust include=diverge-provider-sdk-rs/src/container_proxy_endpoints/tools/begin/server/channel_request/frame.rs
```

and by `diverge-provider-sdk-rs/src/shared/mcp/read_resource/request/request.rs`:

```rust include=diverge-provider-sdk-rs/src/shared/mcp/read_resource/request/request.rs
```
