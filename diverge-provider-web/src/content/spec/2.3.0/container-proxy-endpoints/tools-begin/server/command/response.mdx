---
title: 'Response'
summary: 'The sequence of channel responses that answers the channel, what ends it, and what a finish with nothing before it means.'
order: 2
draft: false
---

```text
[0][item …]          zero or more, one per item
[1][error JSON …]    last, the command did not finish
```

- **The sequence.** Zero or more channel responses whose first byte
  is `0`, each one item verbatim, in order. An error, when the client
  sends one, is the last channel response. The channel response
  finish follows the last channel response.
- **A finish with nothing.** A channel response finish that no
  channel response precedes states that the client did not serve the
  ask. The server does not ask again.

The channel response is defined by `diverge-sdk-rs/src/container_proxy/outside/tools/begin/client/channel_response/command/frame.rs`:

```rust include=diverge-sdk-rs/src/container_proxy/outside/tools/begin/client/channel_response/command/frame.rs
```

and by `diverge-sdk-rs/src/shared/containers/command/response/frame.rs`:

```rust include=diverge-sdk-rs/src/shared/containers/command/response/frame.rs
```
