---
title: 'Request'
summary: 'The payload of the channel request: the tag byte 3 and the request of the channel.'
order: 1
draft: false
---

```text
[3][request JSON …]
```

- **`key`.** A string: the `key` of the enqueues whose messages are
  withdrawn.
- **Malformed.** A channel request whose payload does not decode as
  stated is answered by a channel response finish that no channel
  response precedes, and the server does nothing else for it.

The channel request is defined by `diverge-sdk-rs/src/container_proxy/outside/agents/begin/client/channel_request/frame.rs`:

```rust include=diverge-sdk-rs/src/container_proxy/outside/agents/begin/client/channel_request/frame.rs
```

and by `diverge-sdk-rs/src/shared/containers/dequeue/request/request.rs`:

```rust include=diverge-sdk-rs/src/shared/containers/dequeue/request/request.rs
```
