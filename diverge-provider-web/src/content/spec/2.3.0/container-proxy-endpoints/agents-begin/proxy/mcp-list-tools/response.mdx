---
title: 'Response'
summary: 'The sequence of channel responses that answers the channel, what ends it, and what a finish with nothing before it means.'
order: 2
draft: false
---

```text
[0][result JSON …]    the result of the method
[1][error JSON …]     the MCP error
```

- **The sequence.** Exactly one channel response, the result or the
  error, followed by the channel response finish.
- **A finish with nothing.** A channel response finish that no
  channel response precedes states that the server did not serve the
  ask. The proxy does not ask again.

The channel response is defined by `diverge-provider-sdk-rs/src/container_proxy_endpoints/agents/begin/client/channel_response/mcp_list_tools/frame.rs`:

```rust include=diverge-provider-sdk-rs/src/container_proxy_endpoints/agents/begin/client/channel_response/mcp_list_tools/frame.rs
```

and by `diverge-provider-sdk-rs/src/shared/mcp/list_tools/response/frame.rs`:

```rust include=diverge-provider-sdk-rs/src/shared/mcp/list_tools/response/frame.rs
```
