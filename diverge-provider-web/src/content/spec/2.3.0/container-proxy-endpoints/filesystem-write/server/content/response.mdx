---
title: 'Response'
summary: 'The sequence of channel responses that answers the channel, what ends it, and what a finish with nothing before it means.'
order: 2
draft: false
---

```text
[0][bytes …]         a piece of the file
[1][error JSON …]    the content was not supplied
```

- **The sequence.** Zero or more channel responses whose first byte
  is `0`, each a piece of the file in order, followed by the channel
  response finish; or the channel responses sent so far, exactly one
  channel response whose first byte is `1`, and the channel response
  finish.
- **The error.** A channel that ends in the error is a write
  abandoned: the server discards the temporary and answers the scope
  with the error.
- **A finish with nothing.** A channel response finish that no
  channel response precedes is a file of zero bytes.

The channel response is defined by `diverge-provider-sdk-rs/src/container_proxy_endpoints/filesystem/write/client/channel_response/content/frame.rs`:

```rust include=diverge-provider-sdk-rs/src/container_proxy_endpoints/filesystem/write/client/channel_response/content/frame.rs
```

and by `diverge-provider-sdk-rs/src/shared/containers/write_bytes/response/frame.rs`:

```rust include=diverge-provider-sdk-rs/src/shared/containers/write_bytes/response/frame.rs
```
