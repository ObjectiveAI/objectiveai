---
title: 'Request'
summary: 'The payload is the tag byte 5 followed by the path of the file as JSON: an array of components from the root of the container.'
order: 1
draft: false
---

```text
[5][request JSON …]
```

- **`path`.** An array of strings: the components of the destination
  from the root of the container. Never empty; no component empty,
  `.` or `..`, or containing `/` or NUL.
- **No content.** The request carries no content and no length: the
  content arrives on the channel the proxy opens.
- **Malformed.** A payload that does not decode as stated is answered
  by a response finish that no response precedes.

The request is defined by `diverge-provider-sdk-rs/src/container_proxy_endpoints/filesystem/write/client/request/frame.rs`:

```rust include=diverge-provider-sdk-rs/src/container_proxy_endpoints/filesystem/write/client/request/frame.rs
```
