---
title: 'Stop'
summary: 'The server ends the tree by opening one channel on the scope; the proxy sends nothing on the channel and answers with the response finish of the scope.'
order: 0
draft: false
---

The server ends the tree by opening one channel on the scope, as
[Channels](/frames/channels/) provides. The channel request is the
stop. The proxy sends no frame on the channel; the response finish of
the scope is its answer.

- **The request.** The payload of the channel request is empty. Every
  channel request the server sends on this scope is the stop,
  whatever its payload. [Request](/container-proxy-endpoints/filesystem-tree/server/stop/request/) states
  its form.
- **The response.** The proxy sends no channel response and no
  channel response finish on the channel. It sends no response on
  the scope after it receives the stop, and it sends the response
  finish of the scope. [Response](/container-proxy-endpoints/filesystem-tree/server/stop/response/) states
  the sequence.
- **One channel.** A second channel request on the scope has the
  effect of the first and no other effect.
- **The filesystem.** The stop does not alter the filesystem of the
  container, or any other tree on the connection.
