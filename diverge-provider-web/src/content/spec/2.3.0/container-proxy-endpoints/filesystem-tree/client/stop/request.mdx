---
title: 'Request'
summary: 'The payload of the channel request is empty; the channel carries no tag.'
order: 1
draft: false
---

```text
[]
```

- **No parameter.** The channel request carries nothing. The frame
  itself, on a channel of this scope, is the whole of the stop.
- **Bytes in the payload.** The server ignores every byte of a payload
  that is not empty. A channel request with a payload is not
  malformed, and it is the stop.

The channel request is defined by `diverge-provider-sdk-rs/src/container_proxy_endpoints/filesystem/tree/client/channel_request/frame.rs`:

```rust include=diverge-provider-sdk-rs/src/container_proxy_endpoints/filesystem/tree/client/channel_request/frame.rs
```
