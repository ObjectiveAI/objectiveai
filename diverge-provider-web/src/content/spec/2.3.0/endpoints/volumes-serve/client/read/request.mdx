---
title: 'Request'
summary: 'The payload of the channel request: the tag byte 0 and the request of the channel.'
order: 1
draft: false
---

```text
[0][path_len: u16, big-endian][path …][offset: u64, big-endian][length: u32, big-endian]
```

- **The form.** The bytes after the tag are exactly the bytes after
  the tag of the [FUSE read](/container-proxy-endpoints/fuse-mount/server/read/request/) ask on a
  mount scope, and mean the same; that page states every field. The
  path is UTF-8, `/`-separated, with no leading `/`; empty is the
  root of the volume.
- **Malformed.** A channel request whose payload does not decode as
  stated is answered by a channel response finish that no channel
  response precedes, and the server does nothing else for it.

The channel request is defined by `diverge-sdk-rs/src/provider/endpoints/volumes/serve/client/channel_request/frame.rs`:

```rust include=diverge-sdk-rs/src/provider/endpoints/volumes/serve/client/channel_request/frame.rs
```

and by `diverge-sdk-rs/src/container_proxy/outside/fuse/mount/server/channel_request/read.rs`:

```rust include=diverge-sdk-rs/src/container_proxy/outside/fuse/mount/server/channel_request/read.rs
```
