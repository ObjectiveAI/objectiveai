---
title: 'Request'
summary: 'The payload of the channel request the server sends: the tag byte 3 and the ask.'
order: 1
draft: false
---

```text
[3][write_id: u32, big-endian]
```

- **`write_id`.** The write id of the client's write channel request,
  as four bytes, big-endian.

The channel request is defined by `diverge-provider-sdk/src/endpoints/containers/tools/run/server/channel_request/frame.rs`:

```rust include=diverge-provider-sdk/src/endpoints/containers/tools/run/server/channel_request/frame.rs
```

and by `diverge-provider-sdk/src/shared/containers/write_bytes/request/request.rs`:

```rust include=diverge-provider-sdk/src/shared/containers/write_bytes/request/request.rs
```
