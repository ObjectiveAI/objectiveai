---
title: 'Request'
summary: 'The payload of the channel request the server sends: the tag byte 1 and the ask.'
order: 1
draft: false
---

```text
[1][request JSON …]
```

- **`digest`.** The blob digest, `<algorithm>:<hex>`.

The channel request is defined by `diverge-sdk-rs/src/provider/endpoints/containers/tools/run/server/channel_request/frame.rs`:

```rust include=diverge-sdk-rs/src/provider/endpoints/containers/tools/run/server/channel_request/frame.rs
```

and by `diverge-sdk-rs/src/shared/containers/oci/blob/request/request.rs`:

```rust include=diverge-sdk-rs/src/shared/containers/oci/blob/request/request.rs
```
