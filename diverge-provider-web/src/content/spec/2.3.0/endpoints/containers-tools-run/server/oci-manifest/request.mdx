---
title: 'Request'
summary: 'The payload of the channel request the server sends: the tag byte 0 and the ask.'
order: 1
draft: false
---

```text
[0][request JSON …]
```

- **`digest`.** The manifest digest, `<algorithm>:<hex>`.

The channel request is defined by `diverge-provider-sdk/src/endpoints/containers/tools/run/server/channel_request/frame.rs`:

```rust include=diverge-provider-sdk/src/endpoints/containers/tools/run/server/channel_request/frame.rs
```

and by `diverge-provider-sdk/src/shared/containers/oci/manifest/request/request.rs`:

```rust include=diverge-provider-sdk/src/shared/containers/oci/manifest/request/request.rs
```
