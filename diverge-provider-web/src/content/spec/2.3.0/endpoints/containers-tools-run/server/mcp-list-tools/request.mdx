---
title: 'Request'
summary: 'The payload of the channel request the server sends: the tag byte 12 and the ask.'
order: 1
draft: false
---

```text
[12][params JSON …]
```

- **The params.** The params of the exchange as JSON, relayed
  verbatim.

The channel request is defined by `diverge-provider-sdk-rs/src/endpoints/containers/tools/run/server/channel_request/frame.rs`:

```rust include=diverge-provider-sdk-rs/src/endpoints/containers/tools/run/server/channel_request/frame.rs
```

and by `diverge-provider-sdk-rs/src/shared/mcp/list_tools/request/request.rs`:

```rust include=diverge-provider-sdk-rs/src/shared/mcp/list_tools/request/request.rs
```
