---
title: 'Request'
summary: 'The payload of the channel request the server sends: the tag byte 9 and the ask.'
order: 1
draft: false
---

```text
[9][key …]
```

- **The ask.** The bytes after the tag are the ask as the
  [Container Proxy Endpoints](/container-proxy-endpoints/) layer
  defines it, relayed verbatim.

The channel request is defined by `diverge-provider-sdk-rs/src/endpoints/containers/tools/run/server/channel_request/frame.rs`:

```rust include=diverge-provider-sdk-rs/src/endpoints/containers/tools/run/server/channel_request/frame.rs
```

and by `diverge-provider-sdk-rs/src/shared/containers/vault/delete/request/request.rs`:

```rust include=diverge-provider-sdk-rs/src/shared/containers/vault/delete/request/request.rs
```
