---
title: 'Request'
summary: 'The payload of the channel request the server sends: the tag byte 11 and the ask.'
order: 1
draft: false
---

```text
[11][ttl: u32, big-endian][key …]
```

- **The ask.** The bytes after the tag are the ask as the proxy's
  [`/vault/lock/{channel}`](/proxy/vault/lock/) page defines them,
  relayed verbatim.

The channel request is defined by `diverge-provider-sdk-rs/src/endpoints/containers/tools/run/server/channel_request/frame.rs`:

```rust include=diverge-provider-sdk-rs/src/endpoints/containers/tools/run/server/channel_request/frame.rs
```

and by `diverge-provider-sdk-rs/src/shared/containers/vault/lock/request/request.rs`:

```rust include=diverge-provider-sdk-rs/src/shared/containers/vault/lock/request/request.rs
```
