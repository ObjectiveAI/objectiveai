---
title: 'Request'
summary: 'The payload of the channel request the server sends: the tag byte 8 and the ask.'
order: 1
draft: false
---

```text
[8][key …]
```

- **The ask.** The bytes after the tag are the ask as the
  [Container Proxy Endpoints](/container-proxy-endpoints/) layer
  defines it, relayed verbatim.

The channel request is defined by `diverge-sdk-rs/src/provider/endpoints/containers/tools/run/server/channel_request/frame.rs`:

```rust include=diverge-sdk-rs/src/provider/endpoints/containers/tools/run/server/channel_request/frame.rs
```

and by `diverge-sdk-rs/src/shared/containers/vault/get/request/request.rs`:

```rust include=diverge-sdk-rs/src/shared/containers/vault/get/request/request.rs
```
