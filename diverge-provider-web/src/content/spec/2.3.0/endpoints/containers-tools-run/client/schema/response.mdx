---
title: 'Response'
summary: 'The sequence of channel responses the server sends, what ends it, and what a finish with nothing before it means.'
order: 2
draft: false
---

```text
[0][schema JSON …]    the JSON Schema of the arguments
[1][error JSON …]     the proxy could not say
```

- **The sequence.** Exactly one channel response, relayed as the
  proxy sent it, followed by the channel response finish. A channel
  response finish that no channel response precedes states that the
  proxy did not serve the question.

The channel response is defined by `diverge-sdk-rs/src/shared/containers/schema/response/frame.rs`:

```rust include=diverge-sdk-rs/src/shared/containers/schema/response/frame.rs
```
