---
title: 'Request'
summary: 'The payload of the channel request: the tag byte 4 and the request of the channel.'
order: 1
draft: false
---

```text
[4][connection_id: u32, big-endian]
```

- **`connection_id`.** The id the server announced on its own
  postgres channel, as four bytes, big-endian.
- **Malformed.** A channel request whose payload does not decode as
  stated is answered by a channel response finish that no channel
  response precedes, and the server does nothing else for it.

The channel request is defined by `diverge-provider-sdk/src/endpoints/containers/tools/run/client/channel_request/frame.rs`:

```rust include=diverge-provider-sdk/src/endpoints/containers/tools/run/client/channel_request/frame.rs
```

and by `diverge-provider-sdk/src/shared/containers/postgres/request/postgres.rs`:

```rust include=diverge-provider-sdk/src/shared/containers/postgres/request/postgres.rs
```
