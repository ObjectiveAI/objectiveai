---
title: 'Response'
summary: 'The sequence of channel responses the server sends, what ends it, and what a finish with nothing before it means.'
order: 2
draft: false
---

```text
[0]                  the file is at the destination
[1][error JSON …]    it is not
```

- **The sequence.** Exactly one channel response, followed by the
  channel response finish. The server sends it after the read has
  ended and the proxy of the container named has answered. A channel
  response finish that no channel response precedes states that the
  proxy of the container of the scope did not serve the read.
- **Transferred.** The byte `0` and nothing after it: the proxy of the
  container named answered that the file is at the destination,
  whole.
- **The error.** After the byte `1`, exactly one JSON value in the form defined on the [volumes::list response](/endpoints/volumes-list/response/) page: the refusal of
  either proxy, the rule of the channel not met, or the server's own
  error. The destination is as it was before the request.

The channel response is defined by `diverge-provider-sdk-rs/src/endpoints/containers/tools/connect/server/channel_response/transfer/frame.rs`:

```rust include=diverge-provider-sdk-rs/src/endpoints/containers/tools/connect/server/channel_response/transfer/frame.rs
```

and by `diverge-provider-sdk-rs/src/shared/containers/transfer/response/frame.rs`:

```rust include=diverge-provider-sdk-rs/src/shared/containers/transfer/response/frame.rs
```
