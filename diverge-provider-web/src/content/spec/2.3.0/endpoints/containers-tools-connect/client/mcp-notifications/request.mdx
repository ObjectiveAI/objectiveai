---
title: 'Request'
summary: 'The payload of the channel request: the tag byte 9 and the request of the channel.'
order: 1
draft: false
---

```text
[9]
```

- **No parameter.** The channel request carries nothing after the
  tag. A server ignores every byte that follows the tag.

The channel request is defined by `diverge-provider-sdk-rs/src/endpoints/containers/tools/connect/client/channel_request/frame.rs`:

```rust include=diverge-provider-sdk-rs/src/endpoints/containers/tools/connect/client/channel_request/frame.rs
```

and by `diverge-provider-sdk-rs/src/shared/mcp/notifications/request/request.rs`:

```rust include=diverge-provider-sdk-rs/src/shared/mcp/notifications/request/request.rs
```
