---
title: 'Request'
summary: 'The payload of the channel request the server sends: the tag byte 21 and the ask.'
order: 1
draft: false
---

```text
[21][id_len: u16, big-endian][id …][from_len: u16, big-endian][from …][to …]
```

- **The ask.** The bytes after the tag are the id of the mount, which
  the server adds, and the ask as the
  [Container Proxy Endpoints](/container-proxy-endpoints/) layer
  defines it, relayed verbatim: the path of the entry relative to the
  root of the mount, and what the ask carries.

The channel request is defined by `diverge-provider-sdk-rs/src/endpoints/containers/agents/run/server/channel_request/frame.rs`:

```rust include=diverge-provider-sdk-rs/src/endpoints/containers/agents/run/server/channel_request/frame.rs
```

and by `diverge-provider-sdk-rs/src/shared/containers/fuse/rename/request/request.rs`:

```rust include=diverge-provider-sdk-rs/src/shared/containers/fuse/rename/request/request.rs
```
