---
title: 'Request'
summary: 'The payload of the channel request the server sends: the tag byte 14 and the ask.'
order: 1
draft: false
---

```text
[14][params JSON …]
```

- **The params.** The params of the exchange as JSON, relayed
  verbatim.

The channel request is defined by `diverge-sdk-rs/src/provider/endpoints/containers/agents/run/server/channel_request/frame.rs`:

```rust include=diverge-sdk-rs/src/provider/endpoints/containers/agents/run/server/channel_request/frame.rs
```

and by `diverge-sdk-rs/src/shared/mcp/list_resources/request/request.rs`:

```rust include=diverge-sdk-rs/src/shared/mcp/list_resources/request/request.rs
```
