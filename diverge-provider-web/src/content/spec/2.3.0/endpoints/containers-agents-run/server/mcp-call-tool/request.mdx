---
title: 'Request'
summary: 'The payload of the channel request the server sends: the tag byte 15 and the ask.'
order: 1
draft: false
---

```text
[15][params JSON …]
```

- **The params.** The params of the exchange as JSON, relayed
  verbatim.

The channel request is defined by `diverge-provider-sdk/src/endpoints/containers/agents/run/server/channel_request/frame.rs`:

```rust include=diverge-provider-sdk/src/endpoints/containers/agents/run/server/channel_request/frame.rs
```

and by `diverge-provider-sdk/src/shared/mcp/call_tool/request/request.rs`:

```rust include=diverge-provider-sdk/src/shared/mcp/call_tool/request/request.rs
```
