---
title: 'Request'
summary: 'The payload of the channel request the server sends: the tag byte 19 and the ask.'
order: 1
draft: false
---

```text
[19][id_len: u16, big-endian][id …][path_len: u16, big-endian][path …][bytes …]
```

- **The ask.** The bytes after the tag are the ask as the proxy's
  [`/fuse/write/{channel}`](/proxy/fuse/write/) page defines them,
  relayed verbatim: the id of the mount, and the path of the entry
  relative to the root of the mount.

The channel request is defined by `diverge-provider-sdk-rs/src/endpoints/containers/agents/run/server/channel_request/frame.rs`:

```rust include=diverge-provider-sdk-rs/src/endpoints/containers/agents/run/server/channel_request/frame.rs
```

and by `diverge-provider-sdk-rs/src/shared/containers/fuse/write/request/request.rs`:

```rust include=diverge-provider-sdk-rs/src/shared/containers/fuse/write/request/request.rs
```
