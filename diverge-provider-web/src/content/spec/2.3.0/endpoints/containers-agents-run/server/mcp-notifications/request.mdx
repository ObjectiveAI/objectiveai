---
title: 'Request'
summary: 'The payload of the channel request the server sends: the tag byte 16 and the ask.'
order: 1
draft: false
---

```text
[16]
```

- **No parameter.** The channel request carries nothing after the
  tag.

The channel request is defined by `diverge-provider-sdk-rs/src/endpoints/containers/agents/run/server/channel_request/frame.rs`:

```rust include=diverge-provider-sdk-rs/src/endpoints/containers/agents/run/server/channel_request/frame.rs
```

and by `diverge-provider-sdk-rs/src/shared/mcp/notifications/request/request.rs`:

```rust include=diverge-provider-sdk-rs/src/shared/mcp/notifications/request/request.rs
```
