---
title: 'Request'
summary: 'The payload of the channel request the server sends: the tag byte 5 and the ask.'
order: 1
draft: false
---

```text
[5][request JSON …]
```

- **`identity`.** The identity of the mount, `<size>:<base64url sha256>`.

The channel request is defined by `diverge-provider-sdk/src/endpoints/containers/agents/run/server/channel_request/frame.rs`:

```rust include=diverge-provider-sdk/src/endpoints/containers/agents/run/server/channel_request/frame.rs
```

and by `diverge-provider-sdk/src/shared/containers/fetch_directory/request/request.rs`:

```rust include=diverge-provider-sdk/src/shared/containers/fetch_directory/request/request.rs
```
