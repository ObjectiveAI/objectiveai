---
title: 'Request'
summary: 'The payload of the channel request the server sends: the tag byte 5 and the ask.'
order: 1
draft: false
---

```text
[5][request JSON …]
```

- **`identity`.** The identity of the mount, `<size>:<hash>`, the hash the
  directory's `dirhash` as the [volumes::stat response](/endpoints/volumes-stat/response/)
  defines it.

The channel request is defined by `diverge-provider-sdk-rs/src/endpoints/containers/agents/run/server/channel_request/frame.rs`:

```rust include=diverge-provider-sdk-rs/src/endpoints/containers/agents/run/server/channel_request/frame.rs
```

and by `diverge-provider-sdk-rs/src/shared/containers/fetch_directory/request/request.rs`:

```rust include=diverge-provider-sdk-rs/src/shared/containers/fetch_directory/request/request.rs
```
