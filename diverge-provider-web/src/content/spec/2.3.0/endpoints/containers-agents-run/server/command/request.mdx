---
title: 'Request'
summary: 'The payload of the channel request the server sends: the tag byte 7 and the ask.'
order: 1
draft: false
---

```text
[7][command …]
```

- **The command.** Bytes, verbatim, running to the end of the
  payload. The server does not read them.

The channel request is defined by `diverge-sdk-rs/src/provider/endpoints/containers/agents/run/server/channel_request/frame.rs`:

```rust include=diverge-sdk-rs/src/provider/endpoints/containers/agents/run/server/channel_request/frame.rs
```

and by `diverge-sdk-rs/src/shared/containers/command/request/request.rs`:

```rust include=diverge-sdk-rs/src/shared/containers/command/request/request.rs
```
