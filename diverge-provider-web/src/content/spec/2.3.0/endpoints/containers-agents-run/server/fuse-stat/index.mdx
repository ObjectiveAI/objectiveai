---
title: 'FUSE stat'
summary: 'Tag 24: the server relays an ask of the container’s proxy that asks what an entry of a mount is the client serves live, by the mount’s id and the entry’s path; the client answers with exactly one channel response, which the server relays to the proxy.'
order: 24
draft: false
---

The server opens this channel when the container's proxy sends the
FUSE ask on `/requests` that asks what an entry of a mount is the client serves live. The ask
is relayed verbatim; the client's one answer is relayed back.

- **The request.** The tag byte `24` followed by the ask in the form
  the proxy's [`/fuse/stat/{channel}`](/proxy/fuse/stat/) page
  defines. [Request](/endpoints/containers-agents-run/server/fuse-stat/request/) states its
  form.
- **The response.** Exactly one channel response and the channel
  response finish. [Response](/endpoints/containers-agents-run/server/fuse-stat/response/)
  states the sequence.
- **The conduit.** The server relays the one channel response to the
  proxy's `/fuse/stat/{channel}` as one message and closes that
  connection. The server does not read the ask or the answer, and
  does not send the ask twice.

- **A file mount.** For a mount of `fuse_file_mounts`, the path of the ask is empty.
