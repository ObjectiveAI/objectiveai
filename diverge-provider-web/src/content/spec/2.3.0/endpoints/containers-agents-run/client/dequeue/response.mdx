---
title: 'Response'
summary: 'The sequence of channel responses the server sends, what ends it, and what a finish with nothing before it means.'
order: 2
draft: false
---

```text
[0]                  the queue held one or more messages
[1]                  the queue was empty
[2][error JSON …]    the state of the queue could not be determined
```

- **The sequence.** Exactly one channel response, relayed as the
  proxy sent it, followed by the channel response finish. A channel
  response finish that no channel response precedes states that the
  proxy did not serve the request.

The channel response is defined by `diverge-provider-sdk/src/shared/containers/dequeue/response/frame.rs`:

```rust include=diverge-provider-sdk/src/shared/containers/dequeue/response/frame.rs
```
