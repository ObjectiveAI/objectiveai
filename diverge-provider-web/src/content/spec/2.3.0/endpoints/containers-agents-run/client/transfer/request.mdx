---
title: 'Request'
summary: 'The payload of the channel request: the tag byte 4 and the request of the channel.'
order: 1
draft: false
---

The payload of the channel request is the tag byte followed by the
request as JSON, read as [Notation](/endpoints/#notation) provides:

```text
[4][request JSON …]
```

- **`path`.** The file, as components from the root of the container
  of the scope.
- **`id`.** The id of the container the file is written into, as its
  run answered it.
- **`destination`.** The destination, as components from the root of
  the container named by `id`.
- **Malformed.** A channel request whose payload does not decode as
  stated is answered by a channel response finish that no channel
  response precedes, and the server does nothing else for it.

The channel request is defined by `diverge-provider-sdk-rs/src/endpoints/containers/agents/run/client/channel_request/frame.rs`:

```rust include=diverge-provider-sdk-rs/src/endpoints/containers/agents/run/client/channel_request/frame.rs
```

and by `diverge-provider-sdk-rs/src/shared/containers/transfer/request/request.rs`:

```rust include=diverge-provider-sdk-rs/src/shared/containers/transfer/request/request.rs
```
