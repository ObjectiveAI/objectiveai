---
title: '/agent/register'
summary: 'The agent, sent exactly once for the life of the container; the proxy sends exactly one answer, registered or an error, and closes the connection.'
order: 1
draft: false
---

The server opens `/agent/register` and sends exactly one message, the
request defined below as JSON. The proxy sends exactly one message
and closes the connection. In the message, a first byte of `0` states
that the agent is registered; a first byte of `1` is followed by an
error as JSON.

- **Once.** The agent is fixed for the life of the container. A
  registration after a successful registration is answered with an
  error. A loop asked for before a registration is answered with an
  error on [`/agent/run`](/proxy/agent/run/).
- **No repetition.** A registration whose path ended abruptly has an
  answer the server did not receive. No party retries it.

The request is defined by
`diverge-provider-sdk-rs/src/container_proxy/agent/register/request/request.rs`:

```rust include=diverge-provider-sdk-rs/src/container_proxy/agent/register/request/request.rs
```

The answer is defined by
`diverge-provider-sdk-rs/src/container_proxy/agent/register/response/frame.rs`:

```rust include=diverge-provider-sdk-rs/src/container_proxy/agent/register/response/frame.rs
```
