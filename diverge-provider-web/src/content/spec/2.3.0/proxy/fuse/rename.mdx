---
title: '/fuse/rename/{channel}'
summary: 'Kind 16: an entry moved within its mount; the server sends exactly one message and closes the connection.'
order: 6
draft: false
---

The ask is kind `16` on [`/requests`](/proxy/requests/):

```text
[16][id_len: u16, big-endian][id …][from_len: u16, big-endian][from …][to …]
```

The server answers on `/fuse/rename/{channel}` by sending exactly one
message and closing the connection. In the message, a first byte of `0` states that the entry was moved; a first byte of `1` is followed by a message stating why it was not.
A path that the proxy or the server closes cleanly with no message
before the close states that the ask was not served. An abrupt end of the path leaves the operation failed, and
the proxy does not ask it again, as [FUSE](/proxy/fuse/) provides.

- **Within one mount.** Neither path is empty. A file at `to` is
  replaced. The caller refuses a directory at `to` with `1` and a
  message. The proxy asks no rename across mounts.

The ask is defined by `diverge-provider-sdk/src/shared/containers/fuse/rename/request/request.rs`:

```rust include=diverge-provider-sdk/src/shared/containers/fuse/rename/request/request.rs
```

The answer is the ack defined on [FUSE](/proxy/fuse/).
