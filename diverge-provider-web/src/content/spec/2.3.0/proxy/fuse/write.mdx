---
title: '/fuse/write/{channel}'
summary: 'Kind 13: a file, stored whole; the server sends exactly one message and closes the connection.'
order: 3
draft: false
---

The ask is kind `13` on [`/requests`](/proxy/requests/):

```text
[13][id_len: u16, big-endian][id …][path_len: u16, big-endian][path …][bytes …]
```

The server answers on `/fuse/write/{channel}` by sending exactly one
message and closing the connection. In the message, a first byte of `0` states that the file was stored; a first byte of `1` is followed by a message stating why it was not.
A path that the proxy or the server closes cleanly with no message
before the close states that the ask was not served. An abrupt end of the path leaves the operation failed, and
the proxy does not ask it again, as [FUSE](/proxy/fuse/) provides.

- **Whole.** The bytes replace the file entire. A file not yet held
  is created by its first write.

The ask is defined by `diverge-provider-sdk/src/shared/containers/fuse/write/request/request.rs`:

```rust include=diverge-provider-sdk/src/shared/containers/fuse/write/request/request.rs
```

The answer is the ack defined on [FUSE](/proxy/fuse/).
