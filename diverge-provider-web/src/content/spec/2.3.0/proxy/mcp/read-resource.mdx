---
title: '/mcp/read-resource/{channel}'
summary: 'Kind 3: One resource of the caller’s servers, read. The ask carries the params as JSON; the server sends exactly one message, a result or an error, and closes the connection.'
order: 4
draft: false
---

The ask is kind `3` on [`/requests`](/proxy/requests/). Its
payload is the JSON of the params defined below, running to the end
of the message. The server answers on `/mcp/read-resource/{channel}` by
sending exactly one message and closing the connection. The message
is the response frame defined below: its first byte is `0`, followed
by the result as JSON, or `1`, followed by the error as JSON.
A path that the proxy or the server closes cleanly with no message
before the close states that the ask was not served. An abrupt end of the path, and a `/requests` connection
that ends before the server opens the path, leave the exchange
unanswered, and the proxy asks it again, as
[MCP Exchanges](/proxy/mcp/) provides.

The params are defined by `diverge-provider-sdk/src/shared/mcp/read_resource/request/request.rs`:

```rust include=diverge-provider-sdk/src/shared/mcp/read_resource/request/request.rs
```

The answer is defined by `diverge-provider-sdk/src/shared/mcp/read_resource/response/frame.rs`:

```rust include=diverge-provider-sdk/src/shared/mcp/read_resource/response/frame.rs
```
