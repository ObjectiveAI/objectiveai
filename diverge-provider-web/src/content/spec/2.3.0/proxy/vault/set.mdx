---
title: '/vault/set/{channel}'
summary: 'Kind 6: a key written. Ok, or an error, in one message, then the close.'
order: 2
draft: false
---

The ask is kind `6` on [`/requests`](/proxy/requests/):

```text
[6][key_len: u16, big-endian][key …][value …]
```

The server answers on `/vault/set/{channel}` with exactly one
message, then the close. A path closed cleanly with no message is
the operation not served; an abrupt end is the operation failed, and
it is not asked again — see [Vault](/proxy/vault/).

The ask is `diverge-provider-sdk/src/shared/containers/vault/set/request/request.rs`:

```rust include=diverge-provider-sdk/src/shared/containers/vault/set/request/request.rs
```

The answer is the frame of [Vault](/proxy/vault/): `0` ok, or `1` and a message.
